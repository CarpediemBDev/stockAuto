# 👑 StockAuto AI & Human Master Directive (AGENTS.md)

본 문서는 **StockAuto** 프로젝트를 진행하는 모든 AI 에이전트와 인간 개발자가 준수해야 하는 **프로젝트 전용 절대 수칙이자 단일 백업 가이드 원장**입니다.

> 이 파일은 루트 `CLAUDE.md`의 `@AGENTS.md` import로 **매 세션 자동 주입**됩니다. 따로 열어 읽을 필요가 없습니다.

공통 AI 작업 원칙은 프로젝트 내부 **`skills/common-ai-workflow/SKILL.md`**에 보관합니다. 이 문서는 그 공통 원칙을 StockAuto의 도메인, 하네스, 문서, 브로커, 프론트/백엔드 구조에 적용한 프로젝트 전용 레이어입니다.

---

## ⚡ 시작 전 필독: 콜드 스타트 읽기 순서 (Reading Order)

새로 투입된 AI/개발자는 추측하지 말고 **아래 순서로 읽은 뒤** 작업을 시작합니다:

1. **`docs/README.md`** — 문서 지도(SSOT 인덱스). 어떤 주제가 어느 문서 소유인지 먼저 파악합니다.
2. **최신 `docs/tasks/YYYY-MM-DD.md`** — 오늘/직전 현황판. 진행 중(`[/]`)·승인대기(`[R]`) 항목과 인수인계, 미해결 위험을 확인합니다.
3. **대상 코드·계약 문서 정독** — 변경할 소스와 관련 계약 문서(`SCHEMA.md`, `API_STANDARD.md` 등)를 직접 열어 실제 구조를 눈으로 확인합니다.

> 작업 시작 전 오늘 현황판이 없으면 **`python scripts/new_task.py`**로 표준 양식(변경 영향 기록 9필드 포함)을 자동 생성한 뒤 채웁니다. 계약 검사 형식은 `- 필드: 내용`(하이픈, 별표/볼드 금지)만 인정됩니다.

---

## 🧩 0. 문서 레이어와 책임 분리 (Common vs StockAuto)

*   **공통 원칙 원장:** `skills/common-ai-workflow/SKILL.md`
    *   무추측 개발, 분석 전용 요청 시 코드 수정 금지, 작업 선등록, 변경 영향 지도, 검증 후 보고, 인수인계, 반복 오류의 하네스화 같은 모든 프로젝트에 재사용할 AI 작업 방식을 관리합니다.
*   **StockAuto 전용 원장:** `AGENTS.md`, `docs/AI_WORKFLOW.md`, `docs/tasks/YYYY-MM-DD.md`
    *   StockAuto의 실제 경로, 명령어, 도메인 구조, 거래 모드, 브로커, 금융 계산, 스캐너, 릴리즈 하네스, 문서 반영 규칙을 관리합니다.
*   **도메인 감사 스킬:** `skills/stockauto-release-risk-auditor/SKILL.md`
    *   `skills/common-ai-workflow/SKILL.md` 위에서 동작하는 StockAuto 전용 감사 지침입니다. 거래 프로세스, 계산 불변식, API/프론트 계약, 릴리즈 위험, SSOT 중복 구현을 점검합니다.
*   **새 프로젝트 적용 기준:**
    *   새 프로젝트에는 `skills/common-ai-workflow`를 복사해 먼저 적용하고, 이 파일의 StockAuto 전용 경로·명령·도메인 규칙은 그대로 복사하지 않습니다.
    *   새 프로젝트의 `AGENTS.md`에는 해당 프로젝트의 구조, 명령어, 검증 하네스, 도메인 불변식만 추가합니다.

---

## 🧭 1. AI 핵심 기동 및 검증 프로토콜 (Elite Start & Check)

*   **무추측 개발 원칙 (Zero-Speculation Rule):** 
    *   에이전트는 절대 기존 소스코드의 함수명, 클래스 인터페이스, 데이터베이스 테이블 구조를 '추측'하거나 임의로 가정해서 코드를 작성해서는 안 됩니다.
    *   반드시 변경 대상 소스파일을 직접 읽어 실제 임포트 구조와 타입 정의를 눈으로 확인한 뒤 코딩을 시작해야 합니다.
*   **하드 하네스 사전 검증 (Pre-flight Verification):** 
    *   구현이 끝난 후 유저에게 완료 보고를 하기 전에, **반드시 아래 검증 단계를 수행하여 오류가 0건**임을 입증해야 합니다.
    *   **백엔드**: `python -m py_compile [수정한 파일 경로]` 실행 ➔ 문법적 무결성 검증
    *   **프론트엔드**: `npm run lint` 및 `npx tsc --noEmit` 실행 ➔ TypeScript 및 ESLint 에러 0건 검증
    *   **120% 에이전트 보강 검증**: `python scripts/check_chaos_fuzzing.py` (다이나믹 카오스 퍼징) 및 `python scripts/auto_rollback_guard.py` (자율 롤백 안전 검증) 통과 입증
    *   **마이그레이션 안전성 검사**: `python scripts/check_migration_safety.py` — 변경된 Alembic 마이그레이션의 f-string SQL 조립(R1), 무조건 `DELETE FROM`(R2), 무조건 `UPDATE`(R3), 되돌릴 컬럼값을 확인하지 않는 `SET col = NULL` 롤백(R4)을 정적 검출한다. 하네스 Step 2에 자동 편입되어 있으며, `--all` 인자로 전체 마이그레이션 전수 감사가 가능하다.
    *   **현황판 자가 승격 차단**: 하네스는 변경된 `docs/tasks/YYYY-MM-DD.md`를 HEAD(로컬)/`CHANGE_BASE_SHA`(CI)와 대조하여, 사용자 승인 없이 `[R]`/`[/]`/`[ ]` → `[x]`로 자가 승격하거나 신규 항목을 처음부터 `[x]`로 등록하는 것을 자동 차단한다. 승인이 확인된 항목만 `- [x] 제목 <!-- APPROVED: 날짜/사유 -->` 마커로 통과시킨다. (SMART SKIP 문서 전용 커밋에서도 항상 가동)

*   **완성형 한글 표준화 (NFC Encoding):**
    *   윈도우 환경 및 다양한 OS 간의 한글 깨짐(자소 분리 현상)을 방지하기 위해, 모든 마크다운과 파이썬 소스코드 내 한글 텍스트는 무조건 **완성형(NFC)** 표준으로 디스크에 저장 및 인코딩되어야 합니다.
*   **단순 질문 시 계획 선제시 원칙 (Query-Only / Plan-First Protocol):**
    *   유저가 코딩을 명시적으로 지시하지 않고 질문이나 의견을 가볍게 물어본 단계(단순 질문, 아키텍처 토론 등)에서는 **절대로 임의로 소스코드를 수정해서는 안 됩니다.**
    *   이때는 오직 현상 분석 결과와 변경 계획(또는 선택 가능한 대안 설계)만을 서술하고 유저의 추가 승인이나 명시적인 지시가 있을 때까지 코드 수정을 전면 보류해야 합니다.
*   **Zero-Complacency 코드 검수 (Critical Auditor):**
    *   코드 검수·로직 분석 요청에는 "깔끔하다", "괜찮다" 식 통과를 금지합니다. 컴파일 에러가 없어도 **엣지 케이스, 소수점/금융 연산 오차, 비동기 레이스 컨디션, 예외 누락을 최소 3개** 찾아 비판적으로 보고합니다.
*   **사실 기반 보고 (Fact-Driven Verification):**
    *   "100% 완료", "누락 0건" 같은 단정은 **전수 조사를 마친 뒤에만** 씁니다. 수치는 반드시 **분모(전체 대상)와 분자(실제 처리·유효 대상)**를 함께 제시합니다.
    *   하네스를 통과시키려고 문구만 끼워맞추는 눈속임(`영향 없음 - ...` 꼼수 표기)을 금지합니다. 검증을 못 했으면 "미검증"으로 사유와 함께 보고합니다.
*   **비가역 데이터 마이그레이션(Alembic) 금지 (Reversible Migration Invariant):**
    *   DB 마이그레이션 작성 시 `downgrade()` 함수는 `upgrade()`에서 변경된 행만 국소적으로 복원해야 하며, 기존에 존재하던 정상 데이터까지 일괄 삭제(`SET col = NULL`)하거나 파괴하는 비가역 마이그레이션을 엄격히 금지합니다.
    *   raw SQL 작성 시 f-string 수동 이스케이프에 의존하지 말고 안전한 파라미터 바인딩 및 롤백 안전성을 사전 검증해야 합니다.
*   **유령 커밋(Ghost Commit) 방지:**
    *   로컬 `main`에서 `git reset` 등으로 버려진 커밋이 작업 브랜치에 fast-forward로 엉켜 남는 현상을 예방합니다. 주 원인은 **이미 병합된 브랜치 위에 새 작업을 얹는 것**이므로, 새 작업은 항상 최신 `main`에서 분기합니다(§2-1).
    *   Git 트리거별 정지선은 **§2에만** 정의합니다(중복 금지).


---

## 🚫 2. Git 명령어 동결 및 시점 통제 (Absolute Git Freeze)

*   **자율 실행 금지:** Git 상태 변경 명령(`add`/`commit`/`push`/`checkout`/`branch`/`gh pr`)은 유저 트리거 없이 실행하지 않습니다. 조회용 `status`, `log`, `diff`는 자유입니다.
*   **트리거별 정지선 (이 문서의 SSOT — 다른 절에서 재정의 금지):**

    | 유저 트리거 | 도달 지점 | 정지 후 행동 |
    | :--- | :--- | :--- |
    | `"로컬 커밋만 해줘"` | 2단계 (브랜치 생성 + 원자적 커밋) | 커밋 목록 보고 |
    | `"커밋하자"` / `"깃에 올리자"` | 4단계 (PR 상정 + CI 확인) | **병합하지 않고 정지.** PR 링크와 CI 결과 보고 |
    | `"머지까지 진행해"` / `"머지까지 완료해"` | 6단계 완결 (병합 + 브랜치 정리) | 병합 결과 보고 |
    | 단독 `"진행해"` | 현재 단계 계속만 | **병합·브랜치 삭제 금지** |

    > 상위 트리거 없이 정지선을 넘지 않습니다. **비가역 단계 — 원격 Push(3), PR 병합(5), 원격 브랜치 삭제(6) — 직전에는 대상 브랜치·커밋 목록·영향 범위를 먼저 보고**하고 진행합니다.
*   **한글 커밋 메시지 표준:** 모든 커밋 메시지는 유저가 변경 사항을 직관적으로 검증할 수 있도록 구체적인 한글(Korean) 표준을 준수하여 상세히 기술합니다. (예: `feat: KIS API 잔고 환율 연산 정밀화 및 이중 임포트 제거`)
*   **페어 프로그래밍 서명 (Co-authored-by):** 커밋 메시지 본문 최하단에 빈 줄 하나를 두고 공동 저자 태그를 답니다. 서명은 고정 문자열이 아니라 **그 커밋을 실제로 만든 세션**을 기록하는 값이므로, 다른 에이전트의 서명을 대신 붙이지 않습니다.
    *   Claude Code 세션 ➔ `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
    *   그 외 에이전트 ➔ 해당 에이전트의 공식 표기.
    *   ⚠️ **한 커밋에 여러 세션의 작업이 섞이면 서명이 사실과 달라집니다.** 서명으로 해결할 문제가 아니라 §2-1의 "1 브랜치 = 1 목적"과 원자적 분리 커밋으로 예방할 문제입니다. **커밋 전 `git status`로 내 슬라이스만 스테이징**하고, 다른 세션이 워킹트리에 남긴 변경을 함께 커밋하지 않습니다.

---

## 🌿 2-1. 브랜치 전략 및 원격 반영 6단계 전체 라이프사이클 (Full Git Lifecycle)

*   **모델 — 트렁크 기반:** `main`은 항상 CI(`verify_harness`) 녹색·배포가능 상태를 유지합니다. `main` 직접 push/commit은 절대 금지되며, 모든 작업은 `main`에서 짧게 분기해 병합하고 사라지는 **단기 브랜치**로 진행합니다.
*   **1 브랜치 = 1 목적 = 1 PR:** 한 브랜치는 리뷰 가능한 결과물 하나에 대응합니다. 그 결과물이 PR로 넘어가는 순간, 다음 작업은 최신 `main`에서 새 브랜치로 시작합니다.
*   **네이밍 표준:** `<type>/<kebab-요약>` 형식. type ∈ {`feat`, `fix`, `refactor`, `chore`, `docs`, `test`}. 예: `feat/auth-security-hardening`, `fix/start-dev-kill-restart`.
*   **분기 시점 (When to Branch):** 아래 중 하나라도 해당하면 **최신 `main`에서 새 브랜치를 생성**합니다.
    *   새 기능/버그수정/리팩터를 착수할 때
    *   직전 브랜치가 이미 PR 상정·병합됐을 때 (⚠️ **병합된 브랜치 위에 새 작업을 얹지 않습니다** — 유령 커밋의 주 원인)
    *   현재 작업과 목적·긴급도·리스크가 다른 일이 끼어들 때
    *   반대로 같은 목적의 후속 커밋(리뷰 반영·직접 관련 잔버그)이나 곁다리 소규모 수정은 분기하지 않고 **커밋만 목적별로 원자적 분리**(`git add -p`)합니다.

### 🔄 원격 반영 6단계 라이프사이클 (트리거별 정지선 준수)

각 단계는 §2의 트리거가 허용한 정지선까지만 집행합니다. 정지선을 넘는 실행은 금지입니다.

1. **단기 브랜치 생성 (Branching):** 최신 `main` 기반으로 단기 브랜치를 생성하고(`git checkout -b <type>/<kebab-summary>`), **브랜치명을 당일 현황판(§3)에 기록**해 다른 세션과 상태를 공유합니다.
2. **원자적 분리 커밋 (Atomic Commit):** 변경을 논리적 단위별로 분리 커밋합니다 (구체적 한글 메시지 + `Co-authored-by` 서명, 당일 현황판 마감 동반). 목적이 다른 변경을 한 커밋에 섞지 않습니다.
3. **원격 Push (Remote Push):** `git push origin <branch>`. **비가역 — 직전 보고.**
4. **PR 상정 및 CI 확인 (PR & CI):** `gh pr create`로 PR을 열고 `gh pr checks --watch`로 harness CI 결과를 확인합니다.
5. **CI 녹색 확인 후 병합 (Merge Commit):** CI 전체 통과를 확인한 뒤 `gh pr merge --merge`로 병합해 이력을 보존합니다. **비가역 — 직전 보고.**
6. **로컬 동기화 및 브랜치 정리 (Sync & Clean):** `git checkout main`, `git pull origin main`으로 최신화 후 병합된 브랜치를 삭제합니다 (`git branch -d <branch>`, `git push origin --delete <branch>`). **비가역 — 직전 보고.**

> ⚠️ **저장소 설정 제약 (실측 기준):** 이 저장소는 `allow_auto_merge: false`, `delete_branch_on_merge: false` 입니다.
> 따라서 `gh pr merge --auto`는 **동작하지 않습니다.** 반드시 `gh pr checks --watch`로 CI를 기다린 뒤 수동 병합하고, 브랜치 삭제도 6단계에서 명시적으로 수행해야 합니다.

---

## 📋 3. 일자별 현황판 관리 (Daily Task SSOT)

*   **일자별 현황판 기동 (`YYYY-MM-DD.md`):** 
    *   모든 개발 목표와 실시간 작업 목록은 프로젝트 내 **`docs/tasks/`** 폴더 하위에 **당일 날짜 기준의 마크다운 파일(예: `2026-05-31.md`)로 신규 생성 및 분할 적재**되어야 합니다.
    *   절대 프로젝트 루트나 `docs/` 바로 밑에 단일 마스터 `task.md`를 덮어쓰거나 무단 생성하지 않습니다.
*   **작업 선등록 및 승인 프로세스:**
    *   유저와 신규 작업 범위가 합의되는 즉시, 코딩을 시작하기 전에 **당일 기준 일자별 `YYYY-MM-DD.md` 파일에 미완료 `[ ]` 상태로 태스크를 선등록**해야 합니다. 선등록 없는 코딩은 금지됩니다.
    *   상태는 `[ ]`(대기), `[/]`(진행 중), `[R]`(구현·검증 완료/승인 대기), `[x]`(사용자 승인 완료), `[-]`(취소·중복·후속 작업으로 대체)만 사용합니다.
    *   구현과 검증이 끝나면 `[R]`로 전환하며, **유저의 최종 승인(Accept) 사인이 떨어졌을 때만 `[x]`로 갱신**합니다. 리젝 시에는 `[/]` 또는 `[ ]`로 롤백하고, 더 이상 수행하지 않는 항목만 사유와 함께 `[-]`로 표시합니다.
*   **상태 변경과 커밋의 동반 규칙 (Status-Commit Coupling):**
    *   현황판 상태 변경은 별도 커밋으로 흩뜨리지 않고 **관련 코드 커밋에 함께 실려 올라갑니다.** 특히 유저 승인으로 `[x]` 마감 처리할 때는, 그 마감을 반영한 현황판 수정을 **다음 커밋에 포함**해 함께 올립니다. 이미 병합이 끝나 함께 실을 커밋이 없다면, 별도의 짧은 브랜치로 현황판 마감만 반영합니다(`main` 직접 커밋 금지).
    *   **되돌림 시 상태 원복:** 어떤 작업의 커밋이 롤백되거나 병합이 revert되면, 해당 현황판 항목의 상태도 **같이 되돌립니다**(예: `[x]`/`[R]` → `[R]` 또는 `[/]`). 코드는 되돌아갔는데 현황판만 완료로 남는 불일치를 금지합니다. 되돌린 사유와 다음 시작 지점을 인수인계에 남깁니다.

---

## 🔗 4. GitHub 이슈 운영 (Issue Tracking)

> 작업 원장은 `docs/tasks/YYYY-MM-DD.md` **하나**입니다(§3). GitHub Issue는 버그 전용 보조 원장이며, 별도의 외부 백로그 도구는 운영하지 않습니다.

*   **GitHub 이슈 운영 규칙:**
    *   GitHub Issue는 버그, 오류, 장애, 회귀, CI 실패의 증상·재현·원인·수정·회귀 테스트를 추적하는 기술 원장입니다.
    *   일반 기능 개선, UI 문구 수정, 운영 설계 보강, 문서 정리는 기본적으로 `docs/tasks/YYYY-MM-DD.md`에만 기록하고 GitHub Issue를 만들지 않습니다.
    *   기능 개선이라도 유저가 명시적으로 "깃허브 이슈로 만들자", "이슈 등록해"라고 지시한 경우에만 GitHub Issue를 생성합니다.
    *   GitHub Issue와 연결된 버그 수정 작업은 일자별 현황판 항목에 이슈 번호를 함께 기록합니다.
    *   구현 및 검증이 끝나면 연결 현황판을 `[R]`로 바꾸되, 유저 최종 승인(Accept) 전에는 GitHub Issue를 닫지 않습니다.
    *   유저가 "승인", "승인할게", "완료 처리" 등 명시적 승인 신호를 주면 `docs/tasks/YYYY-MM-DD.md`의 해당 항목을 `[x]`로 변경하고, 연결된 GitHub Issue가 있으면 `completed` 사유로 close합니다.
    *   유저가 리젝하거나 보류하면 일자별 현황판 상태를 롤백하고 GitHub Issue를 닫지 않습니다.

---

## 🔄 5. 변경 영향 계약 및 AI 인수인계 (Change Contract & Handoff)

*   **변경 전 영향 지도 작성:**
    *   아키텍처, 데이터 흐름, API 계약, DB 스키마, 인증·권한, 캐시, 스케줄러 또는 외부 연동을 변경할 때는 구현 전에 생산자(Producer), 모든 소비자(Consumers), 저장소·캐시, 사용자 데이터 경계를 실제 코드 검색으로 확인합니다.
    *   생산자만 수정하고 API 라우터, 백그라운드 작업, 프론트엔드, 테스트, 운영 문서 등 소비 경로를 누락하는 부분 수정은 금지합니다.
*   **신규 기능 SSOT 우선 설계:**
    *   신규 기능을 구현하거나 기존 기능을 확장할 때는 먼저 `rg`로 동일·유사 기능의 라우터, 서비스, 훅, 컴포넌트, 스케줄러, 계산식, 테스트를 전수 검색합니다.
    *   동일한 검증, DB 변경, API 호출, 캐시 갱신, 토스트 처리, 수수료·손익·수량 계산을 파라미터만 바꿔 여러 파일에 재구현하는 것을 금지합니다.
    *   백엔드 비즈니스 변경은 `backend/app/<domain>/services.py` 또는 도메인 전용 모듈, 순수 계산은 명시적 계산 모듈, 프론트 공통 액션은 `frontend/hooks/use<Feature>Actions.ts`, API 엔드포인트 래퍼는 `frontend/lib/api.ts`에 모아 한 곳에서 호출하게 합니다.
    *   FastAPI 라우터는 요청 파싱·의존성 주입·순수 데이터 반환에 집중하고, 컴포넌트는 렌더링과 훅 호출에 집중합니다. 라우터나 컴포넌트가 동일 업무 규칙의 원장이 되지 않도록 합니다.
    *   공통화 후에는 생산자 단위 테스트만으로 완료 처리하지 말고, 실제 소비자인 API·스케줄러·프론트 화면·통합 흐름 중 최소 하나를 포함해 회귀 검증합니다.
*   **일자별 변경 영향 기록:**
    *   계약 영향 변경은 당일 `docs/tasks/YYYY-MM-DD.md`에 `변경 분류`, `생산자`, `소비자`, `데이터 경계`, `API 계약`, `문서 반영`, `회귀 테스트`, `미해결 위험`, `인수인계` 항목을 빠짐없이 기록합니다.
    *   기록 형식과 AI 시작·종료 절차는 **`docs/AI_WORKFLOW.md`**를 단일 기준으로 사용합니다.
*   **장기 문서 동기화:**
    *   구현과 함께 관련 `docs/` 기술 문서를 같은 작업 범위에서 갱신합니다. 문서 영향이 없으면 일자별 변경 영향 기록에 구체적인 사유를 남깁니다.
    *   사용자별 데이터 분리·재결합, 공용 캐시와 개인 데이터 경계처럼 도메인 계약이 있는 경우 해당 계약 문서를 먼저 확인하고 실제 코드와 동시에 수정합니다.
*   **소비자 중심 회귀 검증:**
    *   단위 함수 테스트만으로 완료 처리하지 않습니다. 변경된 생산자를 사용하는 API, 스케줄러, 프론트엔드 또는 통합 흐름 중 최소 하나를 포함하고, 멀티테넌시 변경은 최소 두 사용자의 격리·재결합 시나리오를 검증합니다.
*   **기계적 누락 차단:**
    *   `scripts/verify_harness.py`는 계약 영향 파일이 변경되었는데 일자별 영향 기록이나 장기 기술 문서 반영이 없으면 검증을 실패시킵니다. 우회하거나 검사 대상을 임의로 축소하지 않습니다.
*   **다음 AI 인수인계:**
    *   작업 종료 시 현황판에 마지막 검증 결과, 미해결 위험, 연결된 GitHub Issue 번호, 다음 시작 지점을 기록합니다. 다음 AI는 추측하지 않고 `AGENTS.md` → 최신 현황판 → 연결 이슈 → 관련 계약 문서 → 실제 코드·테스트 순으로 읽습니다.

---

## 🏛 6. 백엔드 아키텍처 및 상세 표준 (Backend Architecture)

*   **도메인 격리 및 단일 책임 원칙 (SRP):**
    *   모든 비즈니스 로직은 `backend/app/` 아래 도메인 폴더로 격리 관리합니다:
        *   `app/watchlist/` (관심종목 및 필터)
        *   `app/scanner/` (마켓 스캐너 및 AI 뉴스 분석)
        *   `app/bot/` (자동 매매 스케줄러 및 멀티 전략 관리)
        *   `app/trades/` (체결 확인 및 계좌/주문 API)
        *   `app/translations/` (다국어 사전 캐시)
*   **SSOT 코어 모듈 집중:** 
    *   데이터베이스 모델(`models.py`), 연결 세션(`database.py`), 전역 예외(`exceptions.py`), 환경 설정(`config.py`)은 무조건 **`app/core/` 하위 폴더**로 단일화합니다.
    *   순환 참조(Circular Imports)를 차단하기 위해 임포트 시 절대 경로 (`from app.core...`)로 통일하고 필요시 지연 임포트(Lazy Import)를 사용합니다.
*   **전역 응답 자동 포장 준수 (Global Response Wrapper):**
    *   FastAPI의 모든 API는 `app.core.response`에 정의된 `SuccessResponseRoute`에 의해 자동 가로채기 되어 `success_response` 규격(`{ code, message, data }`)으로 자동 포장됩니다.
    *   따라서 백엔드 코딩 시 라우터(`router.py`)에서 `success_response`를 직접 호출하지 말고, 순수 데이터 객체(dict, Pydantic 모델 등)만 반환해야 합니다. (택배 포장을 쌩으로 뜯거나 함부로 수동 포장하지 마세요).
*   **Alembic 마이그레이션 필수 준수:**
    *   DB 스키마 컬럼 변경 시, 소스코드 내에 임시 ALTER TABLE SQL을 주입하는 꼼수를 엄격히 금지합니다. 무조건 `models.py` 수정 후 `alembic`을 통해 마이그레이션 스크립트를 작성하며, 앱 구동 시 `migrator.py`가 자동으로 이를 감지하여 안전하게 실행(기존 DB는 Stamp, 신규 DB는 Upgrade)하도록 관리합니다.
*   **전략 1:1 대응 표준 (Strategy 1:1 Identity Rule):**
    *   모든 매매 알고리즘은 **소스코드의 물리 파이썬 파일(`.py`) 1개당 DB `strategies` 테이블의 행 1개로 정확히 1:1 일치**하여 관리되어야 합니다.
    *   **동형 변종 등록 금지**: 동일한 소스코드 파일에서 파라미터 튜닝값만 다르게 칠한 가상 분기(예: `complex_ep`, `leveraged_regime_3x` 등)를 DB의 `strategies` 테이블에 별도의 독립 행으로 추가 등록하는 행위를 엄격히 금지합니다.
    *   **자본 격리/슬롯 매니저의 전략 배제**: 자본 분할 격리를 담당하는 오케스트레이터 모듈(예: `core_satellite`, `multi_slot` 등)은 개별 매매 전략이 아니므로 DB `strategies` 테이블에 등록해서는 안 됩니다.
    *   **조립 블록의 독립성 존중**: 복합 전략 `S`를 구축하는 과정에서 단독 기법인 `A`와 `B`가 포함되거나 재사용되더라도, `A.py`와 `B.py`가 별개의 소스코드 파일로 존재한다면 DB 상에서도 `A`, `B`, `S`는 각각 1개씩의 고유 전략으로 독립 카운트 및 등록되어야 합니다.


---

## 💻 7. 프론트엔드 및 고성능 UI 표준 (Frontend & UI)

*   **기술 스택:** Next.js 16 (App Router), React 19, Tailwind CSS v4 (`@import "tailwindcss"` + HSL 커스텀 프로퍼티 테마), Lucide React, Premium HSL 다크 모드 테마.
*   **프레임워크 표준 엄격 준수 (No Hacks):**
    *   린트 에러를 임시방편으로 모면하기 위한 우회 기술(예: `any` 캐스팅, `setTimeout` 난사, 무작정 eslint-disable 적용)을 절대 금지합니다. React 19의 렌더링 최적화, 비동기 데이터 패칭 가이드라인 등 프레임워크가 권장하는 **'정석(Standard)' 패턴**으로 정밀 구현합니다.
*   **Vue 마스터용 React 트러블슈팅 문서화:**
    *   개발 중 신규 React Hook을 채택하거나 컴포넌트 렌더링 최적화 트러블슈팅이 완료되면, 반드시 그 즉시 **`docs/REACT_GUIDE.md`** 가이드 문서를 직관적인 비유와 함께 현행화하여 기록을 보존해야 합니다.

---

## ⚙️ 8. 런타임 퀵 레퍼런스 (CLI Quick Commands)

### 0. 백엔드 가상환경 표준
```bash
# Official backend virtual environment directory
backend/venv
```

### 1. 백엔드 가동 모드 선택
```bash
# In backend/ folder
python run.py local  # 가상 simulated 투자 환경 기동
python run.py dev    # 모의투자(MOCK) 금융 서버 연동 기동
python run.py prod   # 실전투자(REAL) 라이브 정규 트레이딩 기동
```

### 2. Alembic 마이그레이션 도구
```bash
alembic revision --autogenerate -m "스키마설명"
alembic upgrade head
```

### 3. 프론트엔드 빌드 및 분석
```bash
# In frontend/ folder
npm run dev     # 개발 로컬 서버 구동
npm run build   # 프로덕션 빌드 무결성 확인
npm run lint    # ESLint 정적 분석 통과 검증
```

> **🚨 dist 삭제 금지선(`.next` / `.next-e2e`)**: Next 서버가 살아 있는 동안 그 dist 디렉터리를 지우면 안 됩니다.
> Turbopack 색인(`.meta`)이 사라진 조각(`.sst`)을 계속 참조해 `Unable to open static sorted file … (os error 3)`로 앱 전체가 깨집니다(2026-09-08 사고).
> 반드시 **서버 종료 → dist 삭제 → 재기동** 순서로 하고, 프로세스는 `taskkill /F /T /PID <PID>`로 정리합니다.
> 하네스(`verify_harness.py`)는 `.next`를 건드리지 않으므로(E2E는 `.next-e2e` 사용) **하네스 전 캐시 청소는 불필요**합니다.
> `scripts/guard_next_dist.py`(PreToolUse 훅)가 이를 보조합니다 — 서버가 살아 있는 상태의 dist 삭제를 감지하면 점유 PID와 종료 명령을 보여주고 **승인을 요구(ask)**합니다. 차단(deny)이 아닌 이유는, 2026-09-08 사고가 승인 절차의 부재가 아니라 승인하는 쪽이 서버 가동 사실을 몰라서 났기 때문입니다.
> 이 훅은 **전역 `~/.claude/settings.json`에 `$CLAUDE_PROJECT_DIR` 기준으로 등록**되어 있고 `test -f`로 감싸져 있으므로, **워크트리·새 클론에서도 자동 적용**되며 스크립트가 없는 다른 프로젝트에서는 무해하게 통과합니다. 프로젝트별 재연결은 필요 없습니다.

---

## 👥 9. 작업 분담 원칙 (Work Delegation)

*   **기본값은 단일 책임(Single-Hand Default)**: 이 저장소는 SSOT·계약 규율이 강하므로, 하나의 작업은 **설계→구현→검증까지 한 세션이 일관되게 끌고 가는 것을 기본**으로 합니다. 분량이 많다는 이유만으로 코어 로직을 나눠 짜지 않습니다. 나눠 짜면 스타일·전제·중복이 어긋나 조율·재검증 비용이 절감분을 초과합니다.
*   **코어는 위임 금지**: 손익·수수료·수량·주문 등 금융 계산, 스키마/API 계약 변경, 레이스 컨디션·소수점 오차가 걸린 로직, SSOT 중복 차단·아키텍처 결정은 결합도가 높으므로 분할 위임하지 않습니다.
*   **예외적 분업 기준(결합도 기반)**: 계약 결합도가 낮고 분량만 큰 작업(테스트 픽스처/더미데이터 대량 생성, 대량 주석 정리, 서로 얽히지 않는 독립 모듈 스캐폴딩, 반복 보일러플레이트)에 한해 별도 세션/서브에이전트에 위임할 수 있습니다.
*   **서치·감사·검증은 위임이 아니라 본인 책임**: 변경 영향도 조사(SSOT 중복 검색), 비판적 코드 감사(§1), 하네스 검증(`verify_harness.py`)은 **작업을 맡은 세션이 직접 수행**합니다. 컨텍스트가 비어 있는 별도 에이전트에 넘기면 이 저장소의 계약·불변식을 모른 채 판단하게 되어 오히려 규율이 깨집니다.
*   **위임 시 작업 지시서 필수 항목**: ①대상 파일·함수·라인과 건드리면 안 되는 경계, ②입출력 계약(`docs/SCHEMA.md`·`docs/API_STANDARD.md` 기준), ③재사용할 기존 SSOT 지점, ④통과 기준(`python scripts/verify_harness.py` + 엣지케이스 N개), ⑤프로젝트 불문율(자율 git 금지·계약 9필드 형식 등). 받는 쪽은 판단 부담 없이 이 지시서 범위 내에서만 구현합니다.
*   **세션 간 협업은 비동기 릴레이**: 동시에 도는 세션끼리 서로를 실시간 호출할 채널은 없습니다. 협업은 공유 원장(`docs/tasks/YYYY-MM-DD.md`)에 작업 지시서·검증 결과·인수인계를 남기고 다른 세션이 읽어 이어받는 **비동기 바통 터치**로만 이뤄집니다. 임의로 "위임 완료"라고 보고하지 않습니다.

### 9-1. 작업 격리: 세션별 worktree 필수 (Session Isolation)

> 상세 규칙·세팅 체크리스트는 **[`docs/MULTI_SESSION_WORKTREE.md`](docs/MULTI_SESSION_WORKTREE.md)** 참조.

*   **핵심 규칙**: worktree는 "AI 종류"당 1개가 아니라 **"동시에 파일을 수정하는 세션"당 1개**입니다. 세션 여러 개끼리도, 사람이 IDE로 편집하는 것도 **같은 워킹트리를 공유하면 동일하게 충돌**합니다.
*   **쓰기 세션은 자기 전용 폴더에서만** 작업합니다. 기본 폴더는 `D:\dev\workspace\stockAuto\`이며, 두 번째 쓰기 세션이 필요해지면 그때 `git worktree add`로 전용 폴더를 만들고 **현재 배치를 당일 현황판에 기록**합니다. 읽기 전용(분석·리뷰) 세션은 worktree가 필요 없습니다.
*   **남의 변경을 쓸어 담지 않기**: 작업 전 `git worktree list` · `git branch --show-current` · `git status --short`로 자가 점검하고, **내가 만들지 않은 미커밋 변경이 보이면 `git add -A`/`commit -a` 금지**, 자기 파일만 명시적으로 스테이징합니다. 브랜치가 예상과 다르면 즉시 멈추고 보고하며, 임의 `checkout`/`reset --hard`로 남의 작업을 지우지 않습니다.
*   **공유 자원 직렬화**: worktree는 파일·브랜치만 분리합니다. 백엔드 포트(8000, `run.py` 하드코딩)·Redis 락(6379)은 여전히 공유되므로 **앱(백엔드) 구동과 `verify_harness` 실행은 한 번에 한 폴더만** 수행합니다(동시 실행 시 포트·DB·락 경합으로 서로의 검증이 깨집니다).
*   **하네스는 한 번에 하나만 (2026-09-13 확인)**: 같은 체크아웃을 공유하는 세션이 둘이면 `verify_harness`를 **동시에 돌릴 수 없습니다.** `frontend/.next-e2e`의 next build 락 때문에 나중에 진입한 쪽이 `Another next build process is already running`으로 8단계 E2E 웹서버 기동에 실패합니다(1~7단계는 통과하므로 원인이 늦게 드러납니다). **커밋·푸시 훅도 전체 하네스를 돌리므로 커밋만 해도 같은 충돌이 납니다.** 하네스 실행과 커밋은 **착수 전에 다른 세션에 알려 직렬화**합니다.
    *   **판별 기준**: `[BLOCKED]`인데 **테스트 단언 실패가 0건이고 webServer 기동 단계에서 깨졌다면** 코드 결함이 아니라 락 경합입니다. 코드를 고치려 들지 말고 다른 실행이 끝나기를 기다렸다가 재실행해 확인합니다. (2026-09-13 실측: 동일 트리·동일 코드로 락이 빈 뒤 재실행하자 8/8 통과. 변수는 동시 실행 하나뿐이었습니다.)

