# Frontend environment variables for development/staging builds
NEXT_PUBLIC_API_BASE=/api/v1
BACKEND_API_ORIGIN=http://127.0.0.1:8000
NEXT_PUBLIC_APP_ENV=dev
# 실시간 전송. 기본 on. 폴링으로 되돌리려면 "false"로 설정(Redis 필요).
NEXT_PUBLIC_SSE_ENABLED=true

