# Frontend environment variables for production builds
NEXT_PUBLIC_API_BASE=/api/v1
BACKEND_API_ORIGIN=https://stockauto-backend.example.com
NEXT_PUBLIC_APP_ENV=prod
# 실시간 전송. 기본 on. 폴링으로 되돌리려면 "false"로 설정(Redis 필요).
NEXT_PUBLIC_SSE_ENABLED=true

