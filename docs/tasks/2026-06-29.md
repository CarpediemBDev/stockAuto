# 2026-06-29 작업 현황판

## 1. 신규 기능 SSOT 구현 가이드 스킬 등록
- **상태:** `[R]`
- **작업 설명:** 관심종목/손익 계산 SSOT 정리 사례를 바탕으로, 신규 기능 개발 시 동일 기능을 여러 파일에 분산 구현하지 않도록 기존 StockAuto 규칙 원장과 기존 스킬에 통합한다. 잘못 생성한 별도 개인 스킬은 제거한다.
- **변경 분류:** AI 작업 절차/개발 가이드 규칙 보강
- **생산자:** `AGENTS.md`, `skills/stockauto-release-risk-auditor/SKILL.md`
- **소비자:** StockAuto 작업을 수행하는 Codex/AI 에이전트
- **데이터 경계:** 애플리케이션 런타임 데이터 변경 없음
- **API 계약:** 변경 없음
- **문서 반영:** 신규 별도 원장을 만들지 않고 기존 `AGENTS.md`와 기존 StockAuto 스킬에 반영
- **회귀 테스트:** `quick_validate.py skills/stockauto-release-risk-auditor` 통과, `AGENTS.md`·`docs/AI_WORKFLOW.md`·기존 스킬 파일 UTF-8/NFC 확인 완료
- **미해결 위험:** 실제 신규 개발 작업에서 기존 스킬이 자동 트리거되는지 추후 사용 사례로 확인 필요
- **인수인계:** 잘못 생성한 별도 개인 스킬은 삭제 완료. 신규 기능 구축 또는 중복 구현 점검 시 기존 StockAuto 규칙과 `skills/stockauto-release-risk-auditor`의 SSOT 절차를 먼저 적용한다.

## 2. 스윙 예측 볼린저 밴드폭 필드명 정정 커밋
- **상태:** `[R]`
- **작업 설명:** 스윙 예측 후보의 `squeeze_pct` 필드명을 계산 의미가 명확한 `bollinger_band_width_percentile`로 정정하고, 백엔드 생산자·프론트 소비자·E2E mock을 동기화한 변경을 커밋한다.
- **변경 분류:** [API 계약] 스윙 예측 지표 필드명 정정
- **생산자:** `backend/app/scanner/swing_predictor.py`
- **소비자:** `frontend/components/SwingPredictorCard.tsx`, `frontend/e2e/scanner-complex.spec.ts`
- **데이터 경계:** 기존 persisted snapshot의 `squeeze_pct` 호환 변환은 기존 커밋의 `swing_prediction_cache.py`에서 유지. 이번 커밋은 현재 남은 추적 변경분 정리
- **API 계약:** 신규 응답 필드는 `bollinger_band_width_percentile`
- **문서 반영:** 본 현황판에 커밋 사유와 검증 결과 기록
- **회귀 테스트:** py_compile, frontend lint/typecheck, commit hook 전체 verify_harness 통과
- **미해결 위험:** 없음
- **인수인계:** 다음 AI는 `rg "squeeze_pct|bollinger_band_width_percentile"`로 레거시 호환 외 잔여 옛 필드 사용 여부를 확인

### 변경 영향 기록
- 변경 분류: [API 계약] 스윙 예측 지표 필드명 정정
- 생산자: `backend/app/scanner/swing_predictor.py`, `backend/app/scanner/data_provider.py`
- 소비자: `frontend/components/SwingPredictorCard.tsx`, `frontend/e2e/scanner-complex.spec.ts`
- 데이터 경계: 기존 persisted snapshot의 `squeeze_pct` 호환 변환은 기존 커밋의 `swing_prediction_cache.py`에서 유지. 사용자별 데이터 변경 없음
- API 계약: 스윙 예측 후보 신규 응답 필드는 `bollinger_band_width_percentile`
- 문서 반영: `docs/API_STANDARD.md`에 스윙 예측 후보의 `bollinger_band_width_percentile` 응답 계약과 레거시 `squeeze_pct` 정규화 방침 기록
- 회귀 테스트: py_compile, frontend lint/typecheck, commit hook 전체 verify_harness
- 미해결 위험: 없음
- 인수인계: 다음 AI는 `rg "squeeze_pct|bollinger_band_width_percentile"`로 레거시 호환 외 잔여 옛 필드 사용 여부를 확인

## 3. 시크릿 키 이중 봉투 복호화 및 자동 로테이션 시스템 구축
- **상태:** `[R]`
- **작업 설명:** MultiFernet 기반 이중 열쇠 복호화 지원 및 DB 재암호화 + .env 자동 갱신 원클릭 로테이션 스크립트 구축
- **변경 분류:** [보안/인프라] 시크릿 키 이중 암호화 및 자동 로테이션
- **생산자:** `backend/app/core/credentials.py`, `scripts/rotate_master_key.py`
- **소비자:** 백엔드 전체 브로커 자격증명 모듈, 시스템 관리자
- **데이터 경계:** `broker_credentials` 테이블 데이터 재암호화
- **API 계약:** 변경 없음
- **문서 반영:** `docs/PRODUCTION_DEPLOYMENT_PLAN.md` 반영 완료
- **회귀 테스트:** py_compile, pytest(test_master_key_rotation.py) 100% 통과
- **미해결 위험:** 없음
- **인수인계:** `python scripts/rotate_master_key.py`로 키 로테이션 수행 가능

## 4. 계산·프로세스 리스크 감사 하네스 등록
- **상태:** `[R]`
- **작업 설명:** 자주 요청하는 프로세스 오류, 금융 계산 오류, 계약 드리프트 점검을 반복 가능한 하네스 검사로 등록해 `scripts/verify_harness.py` 실행 시 자동 검증되게 한다.
- **변경 분류:** [하네스/운영 절차] 릴리즈 리스크 감사 자동화
- **생산자:** `scripts/verify_harness.py`, `scripts/check_numeric_invariants.py`, `scripts/check_process_invariants.py`
- **소비자:** 로컬 pre-flight 검증, CI/수동 하네스 실행자, StockAuto 작업을 수행하는 AI 에이전트
- **데이터 경계:** 런타임 DB·사용자 데이터 변경 없음. 테스트/하네스가 소스와 테스트 파일의 정적 계약만 읽음
- **API 계약:** API 응답 형식 변경 없음. 하네스가 API·스케줄러·거래 프로세스 변경 시 필요한 회귀 테스트 누락을 차단
- **문서 반영:** `docs/AI_WORKFLOW.md`에 계산·프로세스 리스크 감사 하네스 실행 기준 반영 완료
- **회귀 테스트:** `py_compile` 통과, `check_numeric_invariants.py` 통과, `check_process_invariants.py` 통과, `backend/tests/test_change_contract_harness.py` 6개 통과, `backend/tests/test_master_key_rotation.py` 1개 통과, 전체 `scripts/verify_harness.py` 통과, `npm run build` 통과
- **미해결 위험:** 없음. 전체 pytest에서 기존 Pydantic `Field(example=...)` deprecation 및 미등록 `real_rate_limiter` mark 경고 9건은 남아 있으나 실패는 아님
- **인수인계:** 다음 AI는 `scripts/check_numeric_invariants.py`와 `scripts/check_process_invariants.py`를 먼저 열어 하네스가 요구하는 골든 테스트/프로세스 테스트 목록을 확인. 계산식 변경은 명시 숫자 기반 골든 테스트를 추가하고, 거래 프로세스 변경은 주문 생명주기·락·모드 격리·테넌트 격리 테스트가 유지되는지 신규 스크립트로 확인

### 변경 영향 기록
- 변경 분류: `하네스/운영 절차`, `계산 불변식`, `거래 프로세스`
- 생산자: `scripts/verify_harness.py`의 하네스 단계와 `scripts/check_numeric_invariants.py`, `scripts/check_process_invariants.py`
- 소비자: `backend\venv\Scripts\python.exe scripts\verify_harness.py`, 로컬 pre-flight 검증, CI 검증 흐름, AI 작업 종료 절차
- 데이터 경계: 사용자 DB와 외부 브로커에는 접근하지 않고 소스·테스트·문서 파일만 읽어 정적 계약과 골든 케이스 존재 여부를 확인
- API 계약: API 필드를 직접 변경하지 않으며, API·스케줄러·거래 계산 변경 시 소비 경로 회귀 테스트가 하네스에서 요구되도록 보강
- 문서 반영: `docs/AI_WORKFLOW.md`
- 회귀 테스트: `py_compile`, 신규 검사 스크립트 직접 실행, `backend/tests/test_change_contract_harness.py`, `backend/tests/test_master_key_rotation.py`, 전체 `scripts/verify_harness.py`, `npm run build`
- 미해결 위험: 없음. 기존 pytest 경고 9건은 후속 정리 후보
- 인수인계: 계산식 변경은 명시 숫자 기반 골든 테스트를 추가하고, 거래 프로세스 변경은 주문 생명주기·락·모드 격리·테넌트 격리 테스트가 유지되는지 신규 스크립트로 확인

## 5. 공통 AI 워크플로우와 StockAuto 전용 문서 레이어 분리
- **상태:** `[-]`
- **작업 설명:** 새 프로젝트에서도 재사용할 수 있는 공통 AI 작업 원칙을 개인 스킬 `common-ai-workflow`로 분리하려던 시도. 사용자 지적에 따라 개인 사용자 폴더 원본 방식은 폐기하고, 아래 6번 항목의 프로젝트 로컬 `skills/` 원본 방식으로 대체한다.
- **변경 분류:** [AI 작업 절차/문서 구조] 공통 규칙과 프로젝트 전용 규칙 분리
- **생산자:** 개인 스킬 시도분, `AGENTS.md`, `docs/AI_WORKFLOW.md`
- **소비자:** 새 프로젝트를 시작하는 Codex/AI 에이전트, StockAuto 작업을 이어받는 AI 에이전트와 인간 개발자
- **데이터 경계:** 애플리케이션 런타임 DB·캐시·브로커 데이터 변경 없음. 개인 Codex 스킬과 문서 파일만 변경
- **API 계약:** 변경 없음
- **문서 반영:** 6번 항목에서 프로젝트 로컬 `skills/` 원본으로 대체
- **회귀 테스트:** 6번 항목에서 재검증
- **미해결 위험:** 개인 사용자 폴더 원본 방식은 폐기
- **인수인계:** 6번 항목의 `skills/common-ai-workflow`와 `skills/stockauto-release-risk-auditor`를 기준으로 한다

### 변경 영향 기록
- 변경 분류: `AI 작업 절차`, `문서 구조`, `개인 Codex 스킬`
- 생산자: 개인 스킬 시도분, `AGENTS.md`, `docs/AI_WORKFLOW.md`
- 소비자: 새 프로젝트에서 공통 작업 규칙을 적용하는 Codex/AI 에이전트, StockAuto 프로젝트 전용 규칙을 따르는 Codex/AI 에이전트
- 데이터 경계: 런타임 사용자 데이터·시장 데이터·브로커 상태에는 접근하지 않고 문서와 개인 스킬 파일만 변경
- API 계약: API 경로·요청·응답·인증 계약 변경 없음
- 문서 반영: 6번 항목으로 대체
- 회귀 테스트: 6번 항목에서 재검증
- 미해결 위험: 개인 사용자 폴더 원본 방식은 폐기
- 인수인계: 공통 원칙 변경은 `skills/common-ai-workflow`, StockAuto 도메인 감사 변경은 `skills/stockauto-release-risk-auditor`에서 먼저 한다

## 6. 프로젝트 로컬 스킬 원본 전환
- **상태:** `[R]`
- **작업 설명:** 개인 사용자 폴더 `C:\Users\Im\.codex\skills`를 원본으로 삼지 않고, StockAuto 프로젝트 루트의 `skills/` 아래에 공통 AI 워크플로우와 StockAuto 감사 스킬 원본을 두도록 정리한다.
- **변경 분류:** [AI 작업 절차/문서 구조] 프로젝트 로컬 스킬 원본화
- **생산자:** `skills/common-ai-workflow/SKILL.md`, `skills/common-ai-workflow/references/*.md`, `skills/stockauto-release-risk-auditor/SKILL.md`, `AGENTS.md`, `docs/AI_WORKFLOW.md`
- **소비자:** StockAuto 작업을 이어받는 Codex/AI 에이전트, 새 프로젝트에 규칙을 복사·적용하는 인간 개발자
- **데이터 경계:** 런타임 DB·캐시·브로커 데이터 변경 없음. 프로젝트 문서와 스킬 원본 파일만 변경
- **API 계약:** 변경 없음
- **문서 반영:** `AGENTS.md`와 `docs/AI_WORKFLOW.md`가 프로젝트 내부 `skills/` 경로를 보도록 수정 완료
- **회귀 테스트:** `quick_validate.py skills/common-ai-workflow` 통과, `quick_validate.py skills/stockauto-release-risk-auditor` 통과, 프로젝트 로컬 스킬 스크립트 py_compile 통과, 프로젝트 로컬 감사 스크립트 3종 직접 실행 통과, 문서·스킬 NFC 확인 통과, `scripts/verify_harness.py` 통과, `npm run build` 통과
- **미해결 위험:** 없음. 개인 사용자 폴더의 `common-ai-workflow`, `stockauto-release-risk-auditor` 설치본은 제거 완료
- **인수인계:** 다음 AI는 공통 원칙과 StockAuto 감사 지침을 `D:\dev\workspace\stockAuto\skills\...`에서 먼저 확인. 개인 `C:\Users\Im\.codex\skills` 아래 설치본을 원본으로 읽지 않는다

### 변경 영향 기록
- 변경 분류: `AI 작업 절차`, `문서 구조`, `프로젝트 로컬 스킬`
- 생산자: `skills/common-ai-workflow/SKILL.md`, `skills/common-ai-workflow/references/*.md`, `skills/stockauto-release-risk-auditor/SKILL.md`, `AGENTS.md`, `docs/AI_WORKFLOW.md`
- 소비자: StockAuto 프로젝트 작업을 수행하는 Codex/AI 에이전트와 인간 개발자
- 데이터 경계: 사용자 데이터·시장 데이터·브로커 상태에는 접근하지 않고 프로젝트 문서와 스킬 원본만 변경
- API 계약: API 경로·요청·응답·인증 계약 변경 없음
- 문서 반영: `AGENTS.md`, `docs/AI_WORKFLOW.md`, `docs/tasks/2026-06-29.md`
- 회귀 테스트: `quick_validate.py skills/common-ai-workflow`, `quick_validate.py skills/stockauto-release-risk-auditor`, 프로젝트 로컬 스킬 스크립트 py_compile, 프로젝트 로컬 감사 스크립트 3종 직접 실행, 문서·스킬 NFC 확인, `scripts/verify_harness.py`, `npm run build`
- 미해결 위험: 없음. 개인 Codex 설치본은 제거했고 문서상 원본은 프로젝트 `skills/`로 고정
- 인수인계: 공통 원칙 변경은 `skills/common-ai-workflow`, StockAuto 도메인 감사 변경은 `skills/stockauto-release-risk-auditor`를 먼저 수정한다

## 7. README AI 워크플로우 요약 반영 및 커밋
- **상태:** `[R]`
- **작업 설명:** README의 오래된 AI 협업 안내를 프로젝트 내부 `skills/` 원본 구조에 맞춘 짧은 요약으로 교체하고, 이번 AI 워크플로우/하네스/스킬 정리 범위만 선별 커밋한다.
- **변경 분류:** [문서/인수인계] README 진입점 정리
- **생산자:** `README.md`, `AGENTS.md`, `docs/AI_WORKFLOW.md`, `skills/common-ai-workflow`, `skills/stockauto-release-risk-auditor`, `scripts/verify_harness.py`
- **소비자:** 저장소를 처음 여는 인간 개발자와 StockAuto 작업을 시작하는 Codex/AI 에이전트
- **데이터 경계:** 런타임 DB·캐시·브로커 데이터 변경 없음. README와 AI 작업 원본 문서만 변경
- **API 계약:** 변경 없음
- **문서 반영:** README의 `AI 협업 가이드` 섹션을 짧은 경로 안내로 교체 완료
- **회귀 테스트:** 프로젝트 로컬 스킬 2종 `quick_validate.py` 통과, backend 전체 pytest 직접 실행 181개 통과, `scripts/verify_harness.py` 통과, 사용자 `.codex` 개인 스킬 제거 확인
- **미해결 위험:** 기존 워크트리에 이번 범위 밖 미완료 변경이 있어 커밋 시 선별 스테이징 필요. backend 전체 pytest는 통과했고 환경 부하 시 기존 120초 하네스 제한에 걸릴 수 있어 `scripts/verify_harness.py` pytest timeout을 240초로 조정
- **인수인계:** README는 입구 요약만 두고, 실제 규칙은 `AGENTS.md`, `docs/AI_WORKFLOW.md`, `skills/`를 기준으로 한다. 사용자 `.codex/skills`에는 시스템 스킬 `.system`만 남아 있음

### 변경 영향 기록
- 변경 분류: `문서`, `AI 작업 절차`, `프로젝트 로컬 스킬`
- 생산자: `README.md`, `AGENTS.md`, `docs/AI_WORKFLOW.md`, `skills/common-ai-workflow`, `skills/stockauto-release-risk-auditor`
- 소비자: 신규 개발자, Codex/AI 에이전트, 커밋 전 하네스 실행자
- 데이터 경계: 런타임 사용자 데이터·시장 데이터·브로커 상태에는 접근하지 않고 문서와 하네스 원본만 변경
- API 계약: API 경로·요청·응답·인증 계약 변경 없음
- 문서 반영: `README.md`, `docs/tasks/2026-06-29.md`
- 회귀 테스트: 프로젝트 로컬 스킬 2종 `quick_validate.py`, backend 전체 pytest 직접 실행, `scripts/verify_harness.py`, 사용자 `.codex` 개인 스킬 제거 확인
- 미해결 위험: 기존 워크트리의 다른 미완료 변경은 이번 커밋에서 제외. backend 전체 pytest는 직접 실행 기준 통과했고 하네스 timeout은 240초로 조정
- 인수인계: README는 요약 링크 역할만 하며, 세부 규칙 변경은 `AGENTS.md`, `docs/AI_WORKFLOW.md`, `skills/`에서 수행한다
