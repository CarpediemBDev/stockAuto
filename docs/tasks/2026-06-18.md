# 2026-06-18 일자별 현황판

## [Refactor] 잔여 하위 컴포넌트 전역 SWR 캐싱 도입 및 15초 폴링 통일

### 목표
- 기존 `usePolling` 타이머를 사용하는 잔여 컴포넌트들을 모두 SWR로 마이그레이션.
- timeout 방지 목적으로 늘려두었던 30초~60초 주기를 15초 기본값으로 일괄 통일.
- `setInterval` / `setTimeout` 을 이용한 API 호출 코드를 프론트엔드에서 전부 제거.

### 작업 목록 (Linear: 보조 백로그 등록 필요)
- `[x]` `frontend/components/BotSignals.tsx` SWR 도입 (기존 30초 -> 15초)
- `[x]` `frontend/components/ManualWatchList.tsx` SWR 도입 (기존 30초 -> 15초)
- `[x]` `frontend/components/OverseasScanner.tsx` SWR 도입 (기존 30초 -> 15초)
- `[x]` `frontend/components/SwingPredictorCard.tsx` SWR 도입 (기존 60초 -> 15초)
- `[x]` `frontend/components/admin/SystemHealth.tsx` SWR 도입 (기존 5초 -> 15초)
- `[x]` 모든 프론트엔드 파일 린트 및 빌드 검증 (`npm run lint; npx tsc --noEmit`)

## [BugFix] BacktestTournament.tsx 인코딩 및 구문 오류 수정

### 목표
- `BacktestTournament.tsx`에서 발생한 "Parsing error: File appears to be binary" 컴파일 오류 및 코드 블록 중복/손상 버그 수정.

### 작업 목록
- `[x]` `BacktestTournament.tsx` 손상된 인코딩 복구 및 `handleRunSimulation` 중복 구문 제거
- `[x]` 프론트엔드 `npm run lint` 및 `npx tsc --noEmit` 무결성 사전 검증 통과 (유저 승인 완료)

## [UI] 실시간 아레나 리그 자산 성장 추이 차트 제거

### 목표
- 참여 유저/전략이 많아짐에 따라 차트의 실효성이 떨어지고 공간만 차지한다는 피드백에 따라 해당 라인 차트를 대시보드에서 완전히 제거.

### 작업 목록
- `[x]` `UserManagement.tsx` 내부 Recharts 의존성 및 LineChart 관련 컴포넌트 JSX 제거
- `[x]` 차트 렌더링용 병합 데이터 생성 로직(`getChartData` 등) 일괄 삭제
- `[x]` 프론트엔드 정적 분석 무결성 검증 통과 (에러 0건)

## [Feature] 네이버 증권 스크래퍼 단타 지표 최적화 개편

### 목표
- 시가총액 위주의 종목 수집을 중단하고, 유동성과 변동성 위주의 단타 특화 지표(거래대금, 거래량, 상승, 하락, 인기토론)로 수집 대상을 개편한다.

### 작업 목록
- `[x]` `naver_scraper.js` 5개 탭 다중 수회 크롤링 및 JSON 매핑 로직 적용
- `[x]` `naver_scraper_runner.py` 반환 타입 `dict[str, list[str]]` 적용 및 파싱
- `[x]` `discovery.py` 네이버 수집 결과 병합 구조 수정
- `[x]` `python -m py_compile` 백엔드 구문 오류 0건 검증

## [Feature] Admin 시스템 헬스 크롤링 모니터링 (대시보드)

### 목표
- Admin 화면에서 `discovery.py`의 최신 실행 상태(증권사별 스크래핑 성공/실패 여부 및 수집 개수)를 실시간으로 확인할 수 있는 프리미엄 통계 카드 UI 제공.

### 작업 목록
- `[x]` `discovery.py` 전역 상태 변수(`LATEST_DISCOVERY_STATS`) 캐싱 구현
- `[x]` `admin/router.py` 신규 API(`/discovery-stats`) 엔드포인트 추가
- `[x]` `SystemHealth.tsx` SWR 폴링 및 크롤러 상태 카드 UI 구현
- `[x]` 백/프론트 린트 및 빌드 검증 (`npm run lint; npx tsc --noEmit`)
