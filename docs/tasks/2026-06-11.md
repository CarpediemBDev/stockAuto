
- [x] 매수/매도 로직 내 페니 주식(AKAN 등) 하드코딩 제거 및 스캐너 동적 연동 완료
- [x] 프론트엔드 대시보드 API 하드코딩 제거
- [x] 동적 티커 매수 검증용 방어 테스트 추가
- [x] 스윙 스캐너 업데이트 시각 텍스트 가독성 개선 (text-zinc-600 -> text-zinc-400)
- [x] 매수/매도 로직 내 페니 주식(AKAN 등) 하드코딩 제거 및 스캐너 동적 연동 완료
- [x] 프론트엔드 대시보드 API 하드코딩 제거
- [x] 동적 티커 매수 검증용 방어 테스트 추가
- [x] 스윙 스캐너 업데이트 시각 텍스트 가독성 개선 (text-zinc-600 -> text-zinc-400)
- [x] 스윙 스캐너 상태 표시 점(Status Dot) 색상 동적화 (fresh/stale/failed 등)
- [x] 스캐너 컴포넌트 간 UI 스타일 일관성 개선 (카드 투명도, 패딩, 둥글기 통일)
- [x] 11개 계정(admin ~ admin11)의 자동 매매 전략 및 종목 미매수 원인 정밀 분석
- [x] Uvicorn 자동 재기동(Auto-Reload) 방지용 `logs/*` 제외 패턴 추가 (`backend/run.py`)
- [x] 목표 종목 및 관심 종목의 재무 필터링(적자 기업 퇴출) 바이패스 적용 (`backend/app/scanner/scanner.py`)
- [x] MultiStrategyManager 내 TARGET_TICKERS 속성 누락으로 인한 AttributeError 해결 및 검증
- [x] 핵심 로직 내 하드코딩 TARGET_TICKERS 제거 및 스캐너(scan_market_expert) 우회 티커 외부 주입 방식 리팩토링
- [x] 추가 발굴된 backtest_runner.py, BotSignals.tsx, BacktestTournament.tsx 내 하드코딩 티커 목록 제거 및 동적 연동
- [x] 백테스트 토너먼트 시 매수/매도/관심종목 합집합 중복제거 종목 동적 추출 연동
- [x] Holding 및 TradeLog 모델에 strategy_type 필드 추가 및 DB 마이그레이션 적용
- [x] MultiStrategyManager 및 백엔드 코어(scheduler.py, simulated_broker.py, order_reconciler.py, backtest_runner.py) 내 prefix/언더바 파싱 로직 완전 삭제 및 strategy_type 기반 리팩토링
- [x] 테스트 케이스(test_*.py)에서 prefix 로직 제거 및 신규 모델 정의에 맞춰 재작성
- [x] pytest를 통한 전체 테스트 구동 및 검증 통과

- [x] 로그인·인증·인가 구현 완성도 및 보안 위험 점검
- [x] Refresh Token HttpOnly 쿠키 전용화 및 로컬·운영 SameSite/CORS 세션 복구 안정화
  - E2E 빌드 산출물을 `.next-e2e`로 분리하여 개발 서버의 `.next` 덮어쓰기 방지
  - 로컬 Next.js API 프록시 대상을 `127.0.0.1:8000`으로 고정하여 IPv6 `localhost` 해석 대기 제거
  - Next.js 개발 서버 `allowedDevOrigins`에 `127.0.0.1` 추가
  - 인증 초기화와 401 재시도의 refresh 요청을 단일 Promise로 합쳐 토큰 회전 경쟁 방지
  - `localhost:3000` 및 `127.0.0.1:3000` 로그인 후 새로고침 세션 유지 실브라우저 검증 통과
  - ESLint, TypeScript, Playwright E2E 5건 통과
- [x] 다중 전략 동일 종목 보유 시 잔고 동기화(Self-Healing) 수량 왜곡 버그 수정 (scheduler.py)
- [x] 수동 일괄 청산 로직 내 불필요한 prefix split 코드 정리 (router_account.py)
