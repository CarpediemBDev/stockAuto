# 2026-07-08 Task Board

## 신규 작업
- [x] 대시보드 잔고 API 타임아웃 근본 해결 — 유저 대면 `/account/balance`에서 외부 네트워크 호출 전면 제거(스냅샷 읽기 전환), Holding.last_price 도입, 체결 직후 스냅샷 즉시 갱신, telegram 스냅샷 trade_mode 필터 버그 동반 수정

### 변경 영향 기록
- 변경 분류: 기능 개선 (백엔드 API 재설계 + DB 스키마 확장 + 프론트 신선도 배지)
- 생산자: scheduler 봇 사이클(Holding.last_price 기록), admin_balance_cache_sync(AccountEquitySnapshot 기록 + 낡은 last_price 벌크 갱신), 체결 직후 강제 스냅샷 트리거
- 소비자: GET /account/balance(스냅샷 읽기 전용), simulated_broker.get_account_balance/get_holdings(last_price 평가), telegram 상태 보고, 프론트 AccountBalance
- 데이터 경계: holdings.last_price/last_price_updated_at 신설(USD 기준), account_equity_snapshots.profit_loss 신설 — Alembic 마이그레이션 필요
- API 계약: GET /account/balance 응답 필드 유지 + captured_at 추가(비파괴적)
- 문서 반영: docs/SCHEMA.md에 holdings.last_price/last_price_updated_at 및 신규 ⑨ account_equity_snapshots 섹션 반영 완료
- 회귀 테스트: verify_harness.py 전체 통과 (pytest 219 passed, tsc/ESLint 통과, Playwright E2E 8 passed, 카오스/계약/NFC/Alembic OK). simulated broker 잔고 테스트는 last_price 계약으로 갱신 + 평단가 폴백 회귀 테스트 신규 추가
- 미해결 위험: ① 봇 미가동 유저 last_price 신선도는 admin_balance_cache_sync의 10분 벌크 갱신에 의존 — 해당 잡 주기가 길면 지연 확대. ② 스냅샷 없는 유저의 최초 1회 폴백 경로는 여전히 브로커 직접 호출(threadpool 격리됨). ③ 로컬 Redis(Memurai)를 서비스가 아닌 사용자 프로세스로 기동한 상태 — 재부팅 시 수동 재기동 필요
- 인수인계: 구현·검증 완료 후 사용자 승인으로 [x] 확정(2026-07-09). 사용자 지시로 커밋·main 머지·GitHub 이슈 등록/클로즈 진행. Alembic 마이그레이션 7a1b2c3d4e5f 로컬 적용됨 — 배포 환경에는 alembic upgrade head 필요
- [/] 신전략 발굴을 위한 멀티 에이전트(3개 분야) 리서치 수행 및 전략 후보 선정
- [/] 13명의 서치 에이전트 파견을 통한 13개 신규 전략(100개 목표) 정밀 리서치 수행

### 변경 영향 기록
- 변경 분류: 분석 전용 (신규 트레이딩 전략 리서치 및 후보 선정)
- 생산자: 세션 내 3개 서치 에이전트 및 메인 에이전트의 종합 분석 보고서
- 소비자: 신규 전략 채택 및 구현 계획 수립 의사결정
- 데이터 경계: 없음
- API 계약: 변경 없음
- 문서 반영: 없음 (전략 선정 후 구현 시 반영)
- 회귀 테스트: 없음 (코드 변경 없음)
- 미해결 위험: 백테스팅을 위한 데이터 확보 가능 여부 및 실시간 실거래 엔진 배선 복잡도
- 인수인계: 리서치 에이전트들의 제안을 종합 정리하여 보고한 뒤, 사용자의 전략 선택 및 승인 대기

