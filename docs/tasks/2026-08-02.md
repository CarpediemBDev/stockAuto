# 2026-08-02 Task Board

## institutional_follow 정정 노트 + 백테스트 하네스 QQQ 알파 비교 컬럼 배선
- [x] institutional_follow 백테스트 결과 정직성 정정 및 QQQ 알파 측정 배선 (사용자 지시 "작업판에 정정 노트 추가하고 QQQ 알파 비교 컬럼 배선해"): 2026-08-01 작업판의 institutional_follow 항목이 "평균 전략 수익률 +7.44% / 미해결 위험: 없음"으로 성공처럼 기록됐으나, 동일 3년 유니버스에서 QQQ 단순보유는 +88.95%로 전략이 QQQ 대비 약 −81%p 언더퍼폼한다는 핵심 사실이 누락됨(raw수익 vs 알파 혼동 함정). 이를 (1) 2026-08-01 작업판에 정정 노트로 명시하고, (2) scripts/run_institutional_backtest.py가 종목별 자기 단순보유(bh_return)와만 비교하던 것을 QQQ 총수익 벤치마크 대비 알파(alpha_vs_qqq) 컬럼·평균·정직한 판정문으로 확장해 동일 함정이 재발하지 않도록 배선. <!-- APPROVED: 2026-08-30/유저승인("12건 전부 승인") -->

### 변경 영향 기록
- 변경 분류: 검증 하네스 확장 + 문서 정정 (스크래치 아닌 저장소 백테스트 스크립트 1개 수정, 제품 런타임/계약/스키마 무변경)
- 생산자: scripts/run_institutional_backtest.py — QQQ 3년 단순보유를 공통 벤치마크로 1회 산출(fetch_qqq_benchmark), run_backtest_for_symbol 결과에 alpha_vs_qqq 추가, main()의 종목별 출력·유니버스 평균에 QQQ 대비 알파 컬럼과 "QQQ 초과 여부" 판정문 추가
- 소비자: docs/tasks/2026-08-01.md(institutional_follow 항목에 정정 노트 append), 백테스트를 돌려보는 개발자/사용자(콘솔 출력에서 QQQ 알파를 직접 확인)
- 데이터 경계: 변경 없음 (yfinance 일봉 읽기 전용, DB·테이블·스키마·마이그레이션 무관)
- API 계약: 변경 없음 (엔드포인트·응답 스키마 무변동, 스크립트 콘솔 출력만 확장)
- 문서 반영: docs/tasks/2026-08-01.md에 institutional_follow 결과 정정 노트 추가, docs/SCHEMA.md·docs/API_STANDARD.md는 계약 변경 없어 해당 없음
- 회귀 테스트: run_institutional_backtest.py py_compile OK, 스크립트 실전 실행으로 QQQ 알파 컬럼·평균·판정문 정상 출력 확인. 제품 코드(app/strategies/*) 무수정이라 pytest 회귀 대상 없음
- 미해결 위험: 벤치마크는 yfinance 조정종가 기반이라 실체결가와 미세 괴리 가능. 유니버스가 NVDA/AMD 등 승자 편향 소수 종목이라 절대수익은 여전히 과대평가 소지 — alpha_vs_qqq가 음수임을 보이는 것이 이 배선의 목적이므로 편향은 결론(QQQ 미달)을 오히려 보수적으로 만듦
- 인수인계: 배선 후 institutional_follow는 QQQ 대비 음의 알파임이 콘솔에 상시 노출됨. QQQ 총수익 초과가 목표라면 신규 타이밍 전략이 아니라 기존 leveraged_regime(레버리지=베타) 경로가 정답임을 재확인

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 전략 카탈로그 읽기 로직 공용 훅 추출 (중복 제거 후속)
- [x] 전략 카탈로그/현재 전략 조회 로직 공용 훅화 (사용자 지시 "훅으로 만들어서 진행하자"): 앞선 SSOT 통합 작업에서 StrategyCatalog.tsx와 CurrentStrategyBadge.tsx가 각각 interface Strategy 선언 + useSWR('/strategies/catalog')를 중복 보유하고, 배지는 useSWR('/admin') + "기본값 regime_switching" 조인 로직까지 중복함. 이를 frontend/hooks/useStrategyCatalog.ts 공용 훅(useStrategyCatalog: 카탈로그 조회, useCurrentStrategy: /admin strategy_type + 카탈로그 조인)으로 추출해 타입·fetch·기본값 규칙을 단일화한다. SWR 키 캐시로 네트워크 요청 수는 불변(순수 유지보수성 개선). 설정 페이지의 useSWR('/admin')은 커스텀 fetcher + onSuccess 폼 하이드레이션으로 성격이 달라 범위에서 제외한다. <!-- APPROVED: 2026-08-30/유저승인("12건 전부 승인") -->

### 변경 영향 기록
- 변경 분류: 프론트엔드 리팩터링 (읽기 전용 SWR 조회 로직 공용 훅 추출, 백엔드/계약/스키마/마이그레이션·런타임 동작 무변경)
- 생산자: frontend/hooks/useStrategyCatalog.ts (신규, Strategy 타입 + useStrategyCatalog + useCurrentStrategy export)
- 소비자: frontend/components/StrategyCatalog.tsx (자체 interface Strategy·useSWR 제거 → useStrategyCatalog 사용), frontend/components/CurrentStrategyBadge.tsx (자체 interface Strategy·2개 useSWR·조인 로직 제거 → useCurrentStrategy 사용)
- 데이터 경계: 변경 없음 (기존 /strategies/catalog·/admin GET 재사용, 신규 엔드포인트·DB 접근 없음)
- API 계약: 변경 없음 (엔드포인트·응답 스키마 무변동)
- 문서 반영: 본 작업판에 기록. docs/SCHEMA.md·docs/API_STANDARD.md는 계약 무변경으로 해당 없음
- 회귀 테스트: verify_harness 전체 통과 (backend pytest 386 passed, frontend tsc/ESLint 통과, Playwright E2E 8 passed, SUCCESS). 로컬 SIMULATED admin 계정으로 대시보드 배지·설정 카드 셀렉터 렌더 및 콘솔 에러 0을 브라우저 실측 확인(리팩터 전후 동작 동일)
- 미해결 위험: 훅 반환 형태 변경 시 두 소비자가 tsc에서 즉시 검출됨. 런타임 동작·렌더 결과는 앞선 [R] 작업과 동일해야 하며 브라우저 실측으로 재확인 필요
- 인수인계: 설정 페이지 useSWR('/admin')은 의도적으로 훅에 흡수하지 않음(폼 하이드레이션 결합도 상승 방지). 읽기 전용 소비자만 훅화

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 전략 선택 UI를 설정 화면으로 단일화(SSOT) + 대시보드 현재전략 배지
- [x] 전략 선택 SSOT 통합 (사용자 지시 "시작해", 앞선 대안 B + 축소 C 합의): 대시보드에 인라인된 StrategyCatalog 카드 그리드(카드 클릭=즉시 라이브 전환)를 제거하고, 전략 선택의 단일 진입점을 개인 설정 화면(/admin/settings Trading 탭)으로 통합한다. 설정 화면의 빈약한 select를 카탈로그 카드 UI로 승격하되 저장 버튼 방식(즉시 전환 아님)으로 통일해 오조작·가동중 전환 위험을 제거한다. 대시보드에는 읽기 전용 "현재 실행 전략" 배지 + 설정 링크만 남긴다. 시장 국면(regime) 배지는 이미 존재하는 MarketHeader(/market/overview 실신호)를 재사용하므로 신규 구현하지 않는다. <!-- APPROVED: 2026-08-30/유저승인("12건 전부 승인") -->

### 변경 영향 기록
- 변경 분류: 프론트엔드 UI 리팩터링 (전략 선택 컴포넌트 재배치·중복 제거, 백엔드/계약/스키마/마이그레이션 무변경)
- 생산자: frontend/components/StrategyCatalog.tsx(controlled 셀렉터로 전환: value/onSelect props, 자체 /admin 저장·POST 제거), frontend/components/CurrentStrategyBadge.tsx(신규, 읽기 전용 현재 전략 배지 + 설정 링크), frontend/app/admin/settings/page.tsx(select를 StrategyCatalog로 교체, 저장은 기존 Save 버튼이 strategy_type 포함 페이로드로 처리)
- 소비자: frontend/components/Dashboard.tsx(StrategyCatalog 그리드 제거 → CurrentStrategyBadge로 교체), locales/ko.json·en.json(대시보드 현재전략 배지 신규 문구), 최종 사용자(전략 변경 동선이 대시보드 즉시전환 → 설정 저장으로 이동)
- 데이터 경계: 변경 없음 (기존 /admin GET·POST, /strategies/catalog, /market/overview 재사용, 신규 엔드포인트·DB 접근 없음)
- API 계약: 변경 없음 (엔드포인트·요청/응답 스키마 무변동, strategy_type은 기존 /admin POST 페이로드 필드 그대로 사용)
- 문서 반영: 본 작업판에 통합 설계 기록. docs/SCHEMA.md·docs/API_STANDARD.md는 계약 무변경으로 해당 없음
- 회귀 테스트: verify_harness 전체 통과 (backend pytest 386 passed, frontend tsc/ESLint 통과, Playwright E2E 8 passed, SUCCESS). 추가로 로컬 SIMULATED admin 계정으로 대시보드 배지·설정 카드 셀렉터 렌더 및 controlled 선택 이동(즉시저장 아님)을 브라우저 실측 확인
- 미해결 위험: StrategyCatalog의 public props 변경으로 유일 소비자(대시보드→설정)가 바뀌므로 참조 누락 시 tsc에서 즉시 검출. i18n 신규 키는 ko/en 동시 추가로 parity 유지 필요
- 인수인계: 국면→전략 1클릭 자동전환은 알파 부재(strategy-catalog-no-alpha) 및 휩쏘 과매매 위험으로 의도적으로 채택하지 않음. 전략 전환은 설정 저장 단일 경로로만 발생. 국면 배지는 MarketHeader 재사용

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
