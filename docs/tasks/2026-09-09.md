# 2026-09-09 Task Board

## Next dist(.next) 삭제 가드 훅
- [x] 살아있는 Next 서버 밑의 dist(`.next` / `.next-e2e`) 삭제를 PreToolUse 훅으로 차단 <!-- APPROVED: 2026-09-10 사용자 승인 -->
- 브랜치: `fix/2026-09-09-dev-guards`

### 사고 경위 (2026-09-08)
- 다른 세션이 하네스 실행 전 청소 목적으로 `cd frontend && rm -rf .next` 실행. 그 시점에 `npm run local`(next dev) 서버가 살아 있었음.
- 1차 rm 실패: `cannot remove '.next/dev/cache/turbopack/ee6e79b1': Directory not empty` (서버가 핸들을 쥔 채 캐시 재생성 중).
- 2차 rm이 `2>/dev/null`로 에러를 무시하고 삭제 완료. 서버는 종료되지 않음.
- 결과: 메모리의 Turbopack 색인이 사라진 `00000010.sst`를 계속 참조 → `Unable to open static sorted file … (os error 3)`로 프론트 전체 다운.
- 복구: 좀비 서버(PID 4824) 종료 → `.next` 삭제 → 재기동(`Ready in 5.2s`) → 로그인 페이지 정상 렌더 확인.

### 변경 영향 기록
- 변경 분류: 개발 환경 가드(툴링). 런타임/비즈니스 로직 변경 없음.
- 생산자: `scripts/guard_next_dist.py` (PreToolUse 훅 스크립트), `.claude/settings.json` (훅 배선, `.claude/`는 gitignore라 체크아웃 로컬).
- 소비자: Claude/Antigravity 등 이 저장소에서 Bash·PowerShell을 실행하는 모든 에이전트 세션.
- 데이터 경계: 훅 stdin JSON의 `tool_input.command` 문자열만 읽는다. 파일·DB·네트워크 접근 없음. 프로세스 조회는 `Get-CimInstance Win32_Process` 읽기 전용.
- API 계약: Claude Code PreToolUse 훅 규약(`hookSpecificOutput.permissionDecision`). 백엔드/프론트 API 계약 변경 없음.
- 문서 반영: `AGENTS.md` §8-3에 dist 삭제 금지선과 복구 순서 추가.
- 회귀 테스트: `scratchpad/test_guard.py` 11케이스 전부 통과(deny 6: dev dist 삭제·하위 캐시 삭제·Remove-Item·git clean -xfd·shutil.rmtree·커밋 메시지 뒤에 실제 삭제를 체이닝한 우회 / allow 5: 사고 경위를 적은 커밋 메시지·e2e dist 삭제(해당 서버 없음)·조회·서버 기동·무관 경로 삭제). 실제 세션에서도 훅이 자신의 테스트 명령을 deny하는 것으로 배선 동작 확인. `python scripts/verify_harness.py` 8/8 통과(3회차, pytest 574건·tsc·eslint·Playwright 8건). 1·2회차는 아래 미해결 위험의 DB 가드 레이스로 BLOCKED.
- 미해결 위험: (하네스 별건) 라이브 백엔드(`run.py local`, PID 18752)가 가동 중이면 `tests/conftest.py:229` 의 `guard_dev_database_untouched` 세션 teardown이 오탐한다. 2회차 전체 하네스에서 `stock_translations` 2417→2418 변동으로 `[FAIL] Backend pytest failed`가 났으나, 실제 삽입 주체는 스캐너가 넣은 신규 종목명(id 2419 `MDU`)이고 테스트는 전량 통과(teardown 단계 ERROR 1건)했다. 이 가드는 다른 프로세스의 DB 쓰기를 구분하지 못한다. pytest 단독 재실행(574건 통과, teardown ERROR 없음)과 3회차 하네스 통과로 레이스임이 확정됐다 — 봇을 켜둔 채 하네스를 돌리면 언제든 재발한다. `.claude/`가 gitignore 대상이라 훅 배선은 워크트리·신규 클론에 자동 전파되지 않는다(스크립트는 `scripts/`에 있어 전파됨). 또한 삭제 동사와 dist 이름이 함께 등장하기만 하면 실제 삭제가 아닌 명령도 서버 가동 중에는 deny된다. 이 오탐은 실제로 발생해(사고 경위를 적은 커밋 메시지가 거부됨) `git commit`·`git tag`·`gh pr` 등 메시지를 싣는 단일 명령은 예외로 좁혔다 — 체이닝 연산자가 하나라도 있으면 예외가 풀리므로 `git commit ... && rm -rf .next` 우회는 그대로 걸린다. 그 밖의 문장형 오탐(테스트 문자열 등)은 안전 측 오탐으로 수용.
- 인수인계: 프론트 dev 서버는 :3000에서 재기동된 상태. dist 청소가 필요하면 서버 종료 → 삭제 → 재기동 순서를 지킬 것. 하네스는 `.next`를 사용하지 않으므로 하네스 전 청소는 하지 말 것.

## 수확 모드 임계값 화면 표시

- [R] 무장 임계·트레일링 폭을 잔고 화면에 노출 — ATR 파생값이라 유저 대면 경로에서 계산할 수 없으므로 스케줄러가 산출한 값을 DB에 영속화하고 API가 읽어 내린다. EXTERNAL 보유분 전체에 대해 계산하여 스위치를 켜기 전에도 기준을 볼 수 있게 한다

### 변경 영향 기록
- 변경 분류: 기능 추가 (화면 표시) + 스키마 변경 + API 계약 추가
- 생산자: backend/app/core/models.py(Holding.harvest_arm_pct·harvest_trailing_pct 신설), backend/alembic/versions/d4e1c8b7f206_add_harvest_threshold_snapshot.py(신규 마이그레이션), backend/app/bot/scheduler.py(_persist_harvest_thresholds 신설 및 EXTERNAL 분기 진입부에서 호출), backend/app/trades/router_account.py(GET /holdings·PATCH management 응답에 두 필드 노출), frontend/components/PortfolioView.tsx(Holding 타입 확장·수확 카드에 임계 표시), locales/ko.json·locales/en.json(portfolio.harvest_arm_threshold·harvest_trailing_width 키 신설), backend/tests/test_harvest_mode.py(신규 4건)
- 소비자: GET /api/v1/account/holdings, PATCH /api/v1/account/holdings/{ticker}/management, 프론트 위임 모달의 수확 카드, 모바일 앱(stock-auto-mobile, 통지 필요)
- 데이터 경계: holdings 테이블에 FLOAT NULL 2컬럼 추가. 기존 행은 NULL이며 다음 사이클부터 채워진다. 값은 판정의 입력이 아니라 판정 결과의 기록이다 - _evaluate_harvest는 종전대로 자기 사이클에서 다시 계산해 쓰므로 이 컬럼이 낡거나 NULL이어도 매매 동작은 달라지지 않는다. management=EXTERNAL인 행에만 채우며 harvest_enabled와는 무관하다. 스위치를 켜기 전에 기준을 볼 수 있어야 켤지 말지 판단할 수 있기 때문이고, 계산은 어차피 사이클마다 돌아 비용 증가가 없다. 봇 소유분은 수확 대상이 아니므로 NULL로 남는다. 쓰기는 소수점 2자리로 끊어 비교한 뒤 값이 바뀔 때만 수행해 무의미한 UPDATE가 매 사이클 쌓이지 않게 했다
- API 계약: GET /api/v1/account/holdings와 PATCH /api/v1/account/holdings/{ticker}/management 응답에 harvest_arm_pct·harvest_trailing_pct가 추가된다. 서버는 응답 시점에 계산하지 않고 스케줄러가 영속화한 값을 그대로 내려준다 - 응답 시 계산하려면 ATR을 얻으려 외부 시세를 호출해야 해서 유저 대면 경로 외부 호출 0건 원칙에 어긋난다. 아직 관측 전이면 null이며 클라이언트는 값을 감춰야 한다. 추정치로 대신 채우면 실제 판정과 어긋난 숫자가 기준으로 보인다
- 문서 반영: docs/SCHEMA.md(holdings ERD 및 필드 설명에 2컬럼 추가), docs/API_STANDARD.md(6.2절 GET /holdings 추가 필드 항목 확장)
- 회귀 테스트: backend/tests/test_harvest_mode.py 4건 신규. 스위치가 꺼져 있어도 기록될 것, ATR 변화가 다음 사이클에 반영될 것(하한 15%·상한 40% 양쪽 경계 포함), 봇 소유분에는 기록되지 않을 것, version_id를 올리지 않을 것. 네거티브 컨트롤로 _persist_harvest_thresholds 호출을 pass로 바꾸자 앞의 2건이 실패했고(harvest_arm_pct가 None) 되돌리니 통과했다. python scripts/verify_harness.py 8/8 통과 - pytest 574건, tsc, eslint, Playwright 8건
- 미해결 위험: 첫째, 실행 중인 백엔드는 이 변경 이전(2026-09-09 01:46 기동) 코드라 재기동 전까지 컬럼이 NULL로 남고 화면에도 표시되지 않는다. run.py의 reload는 하드코딩 False다. 라이브 A/B 계정이 매매 중이므로 재기동 시점은 사용자 판단이 필요하다. 둘째, 표시값은 스냅샷이라 마지막 관측 시점의 ATR을 반영한다. 사이클 간격이 실측 9~18분이므로 화면의 임계와 다음 판정에 쓰일 임계가 미세하게 다를 수 있다. 셋째, 무장 도달가는 클라이언트가 observed_base_price와 arm_pct로 계산하는데 observed_base_price가 아직 NULL이면 current_price로 폴백하므로, 스위치를 켠 직후 한 사이클 동안 실제 앵커와 다른 값이 보일 수 있다
- 인수인계: 임계값 두 컬럼은 표시 전용이라는 계약을 지켜야 한다. 판정 코드가 이 값을 읽기 시작하면 낡은 스냅샷으로 매매하게 되고, 사이클 간격이 흔들리는 현 상태에서 그 낡음은 수십 분에 이를 수 있다. 새 소비자를 추가할 때 읽기 전용인지 확인할 것. 쓰기가 version_id를 올리지 않는 것도 계약이다 - 올리면 같은 사이클 Part B의 db.merge가 StaleDataError로 터져 사이클이 통째로 중단되며, 이는 last_price·highest_price가 Core UPDATE를 쓰는 것과 같은 이유다. 회귀 테스트가 이 두 가지를 고정하고 있다. 이 현황판은 2026-09-09 세션 중 다른 세션의 덮어쓰기로 본 블록이 한 번 유실되어 재작성했다. 다음 시작 지점은 (a) 백엔드 재기동 후 실제 화면 확인, (b) 모바일 세션에 계약 통지(GET /holdings 필드 2개 추가). 모바일은 아직 Holdings 화면 자체가 없어 이 필드의 소비자가 되려면 그 화면이 먼저 필요하다

## 개발 DB 오염 가드 오탐 수정

- [x] 라이브 봇의 정상 append를 "테스트가 개발 DB를 오염시켰다"로 오탐하는 세션 teardown 가드 교정 <!-- APPROVED: 2026-09-10 사용자 승인 -->
- 브랜치: `fix/2026-09-09-dev-guards`

### 변경 영향 기록
- 변경 분류: 테스트 하네스 판정 로직 수정 (버그 수정). 런타임/비즈니스 로직 변경 없음.
- 생산자: backend/tests/conftest.py(STRICT_WATCHED_TABLES·APPEND_TOLERANT_WATCHED_TABLES 분리, detect_dev_db_pollution 신설, guard_dev_database_untouched가 baseline id 구간으로 비교), backend/tests/test_dev_db_guard.py(신규 4건)
- 소비자: pytest 세션 전체(session-scoped autouse 픽스처), scripts/verify_harness.py의 6/8 단계.
- 데이터 경계: 개발 SQLite를 read-only(mode=ro)로만 연다. 쓰기 없음. users·strategies는 종전대로 행 수 동일성, stock_translations는 스냅샷 시점의 max(id) 이하 구간 행 수만 비교한다.
- API 계약: 없음. 애플리케이션 API 표면 변경 없음.
- 문서 반영: 영향 없음 - 테스트 세션 내부의 판정 로직이라 docs/SCHEMA.md·docs/API_STANDARD.md 같은 장기 계약 문서의 서술을 바꾸지 않는다. 판정 근거와 실측 증거(스캐너 append 경로, id 2419 MDU)는 conftest.py 주석에 남겼다.
- 회귀 테스트: backend/tests/test_dev_db_guard.py 4건 — 봇 append는 통과, 기존 행 유실은 실패, users 행 변동은 종전대로 실패, 스냅샷 부재 시 미개입. 4건 실행 시 실제 세션 픽스처가 그대로 돌아 스냅샷 자료구조 변경의 배선까지 검증된다. python scripts/verify_harness.py 8/8 통과 - pytest 578건, tsc, eslint, Playwright 8건.
- 미해결 위험: stock_translations에 대해 신규 행 추가는 더 이상 오염으로 잡히지 않는다. 테스트가 이 테이블에 새 행만 넣는 경로가 생기면 놓친다 - 기존 행 유실·변조와 users·strategies는 그대로 잡히므로 2026-08-24 사고 양상(유저 데이터 유실)의 방어선은 유지된다. 또한 Translator.load_cache의 레거시 STRATEGY 접두 행 삭제가 테스트 세션 중 백엔드 재기동과 겹치면 기존 행 감소로 잡힐 수 있으나, 해당 레거시 행은 이미 정리되어 평시 삭제 건수는 0이다.
- 인수인계: 이 가드는 봇이 평시에 건드리지 않는 테이블만 감시한다는 원칙 위에 서 있다. 감시 테이블을 추가할 때는 봇 경로(스캐너·스케줄러·번역기)가 그 테이블에 쓰는지 먼저 확인할 것 - stock_translations가 정확히 그 확인을 건너뛰어 들어왔던 케이스다.

## 포트폴리오 종목별 매수 이력 조회

- [/] 실시간 포트폴리오 카드에서 종목을 클릭하면 해당 티커의 매수·매도 이력(시각·체결가·수량·전략·실현손익)을 모달로 보여준다. `GET /api/v1/trades`에 `ticker` 필터를 추가하고, 프론트는 슬라이스(전략)별로 현재 포지션 보유 시작일을 계산해 표시한다.

### 변경 영향 기록
- 변경 분류: (작업 중 - 완료 시 작성)
