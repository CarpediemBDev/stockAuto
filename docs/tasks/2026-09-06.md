# 2026-09-06 Task Board

## 보유 종목 관리 모드(격리·수확·방어) 설계 확정
- [x] 봇이 매수하지 않은 보유 종목의 관리 모드 설계 확정 및 문서화 — docs/plans/holding_management_modes.md 신규 작성, docs/README.md 인덱스 갱신 <!-- APPROVED: 2026-09-06 사용자 승인 -->
- [x] 1단계 격리 구현 — Holding.management 컬럼 추가 + 마이그레이션, 가드 6곳(sync_broker_holdings 기본값 반전·수량 차분 배분 순서·exit 판정 분기·매수 동일티커 차단·슬롯 자본 제외·force-liquidate 확인), 잔고 UI 뱃지 <!-- APPROVED: 2026-09-06 사용자 승인 -->
- [x] 2단계 개별 종목 수동 매도 엔드포인트 신설 — POST /account/holdings/{ticker}/sell. confirm 기본 false 프리뷰 게이트, 슬라이스 특정, 사용자·심볼 이중 락, 잔고 화면 매도 버튼과 확인 모달 <!-- APPROVED: 2026-09-06 사용자 승인 -->
- [x] 3단계 수확 모드 — observed_base_price 앵커, ATR 기반 급등 무장(min(40%, max(15%, ATR x4))), 트레일링(max(5%, ATR x2)) 추적, 관측 시작가 하한 가드, 무장 텔레그램 알림, 종목별 opt-in 토글 <!-- APPROVED: 2026-09-06 사용자 승인 -->
- [R] 4단계 방어 경보 — 전이(추가 악화 + 신저가 갱신) 기반 판정, 경보만 전송하고 주문 없음
- [R] 5단계 방어 조치 — guard_action 3모드(ALERT_ONLY 기본 / SHADOW 기록만 / LIQUIDATE 부분청산), 연속 10사이클·일일 1회 캡, 섀도 로그를 측정 원천으로 사용
- [ ] 6단계 위임 모드 — risk_basis_price 분리, 재진입 심사 1회
- [x] 2026-09-02 현황판 승인 마커 소급 부착 <!-- APPROVED: 2026-09-06 사용자 승인 --> — 하네스 자가승격 차단 검사가 [x] 3건을 반려하고 있었다. 해당 현황판 인수인계에 "2026-09-03 사용자 승인 완료"가 기록되어 있어 그 근거를 명시한 APPROVED 마커를 달았다. 승인 사실 자체는 그 세션의 기록에 의존했으므로 사용자에게 직접 확인을 구했고, 2026-09-06 세션에서 "2026-09-03에 승인한 것이 맞다"는 확인을 받았다. 마커는 유효하다
- [R] 보유 종목 응답의 매도 대상 키 안정화 — holdings.id가 브로커마다 불안정(KIS idx+1000, Toss 누락)해 slices 배열을 신설하고 (ticker, strategy_type)을 매도 키로 고정
- [R] 개별 매도 중복 요청 가드 수정과 계약 명문화 2건 — 상태 기반 가드가 같은 틱의 연타를 통과시켜 수량 지정 시 이중 매도가 가능했다. useRef 래치로 교체
- [R] 위임 스위치 UI를 카드 푸터에서 전용 모달로 분리 — 버튼 줄바꿈 붕괴 해소, 3모드 선택기와 청산 비율(25/50/75/100%) 선택 추가
- [R] 다중 슬라이스 티커의 수량 유실 수정 — sync_broker_holdings가 티커당 브로커 행 1개를 가정해 40주가 15주로 깎이고 EXTERNAL 행이 파괴됐다. 브로커 행을 티커 단위로 먼저 합산하도록 교정
- [R] 방어 알림 문구 평이화와 SHADOW 모드 재명명 — 사이클·시그널 용어를 걷어내고 "기록만"을 "매도 시점 알림"으로 변경
- [R] 보유 종목 삭제 감사 로깅 가드 — core/holding_audit.delete_holding 단일 창구 신설, 앱 내 삭제 8곳을 경유시켜 소거법 진단이 가능하도록 함. DB 직접 조작은 여전히 추적 불가
- [R] 방어 조치 게이트를 벽시계 기준으로 교정 — 사이클 주기 실측 중앙값 124초(104~846초)라 10사이클이 20분~2시간으로 흔들렸다. guard_streak_started_at + GUARD_SUSTAIN_MINUTES 병행 게이트로 하한 고정
- [R] 벽시계 게이트의 연속 시각 리셋 누락 수정 — 모드 전환 4개 분기 중 3개가 시각을 리셋하지 않았고 guard_enabled=True는 아무것도 리셋하지 않았다. _restart_streak()로 일원화
- [R] 수확 노이즈 버퍼를 벽시계 기준으로 교정 — 마지막 남은 사이클 기반 버퍼. harvest_breach_started_at 신설, 재기동이 대기를 지우던 인메모리 의존 제거
- [ ] 백로그: 고점 판단 방법론 재검토 — 이동평균 이탈·스윙 하이·도치안·ZigZag·피벗·SAR·Chandelier Exit·거래량 프로파일 비교. 관측 최고가 트레일링 대비 개선폭을 백테스트로 먼저 실증

### 변경 영향 기록
- 변경 분류: 문서 (설계 정본 신규 + 인덱스 갱신). 애플리케이션 코드 변경 없음
- 생산자: docs/plans/holding_management_modes.md 신규 작성. docs/README.md 계획 문서 묶음 표에 1행 추가
- 소비자: 후속 구현 세션(백엔드·프론트), stock-auto-mobile 세션. 코드 소비자는 아직 없음
- 데이터 경계: 스키마 변경 없음. 문서에 제안된 Holding 신규 컬럼(management, observed_base_price, harvest_enabled, harvest_armed, guard_enabled, guard_baseline_score, guard_baseline_low, risk_basis_price)은 설계안일 뿐 아직 마이그레이션하지 않았다. risk_basis_price를 avg_price와 분리하는 근거(실현손익 계산과 손절 앵커의 용도 충돌)를 문서 4.1에 명시했다
- API 계약: 변경 없음. 문서에 예고한 /balance·/holdings 응답 필드 추가와 개별 종목 매도 엔드포인트 신설은 1~2단계 착수 시 docs/API_STANDARD.md에 반영한다
- 문서 반영: docs/plans/holding_management_modes.md 신규. docs/README.md 인덱스 갱신. SIGNAL.md·SCHEMA.md·API_STANDARD.md는 해당 단계 구현 시 갱신 예정
- 회귀 테스트: 코드 변경이 없어 기능 테스트 대상 없음. python scripts/verify_harness.py 8/8 통과. 최초 실행은 자가승격 차단 검사가 docs/tasks/2026-09-02.md의 [x] 3건을 반려해 BLOCKED였고(나머지 8단계는 전부 통과), 해당 항목에 승인 마커를 부착한 뒤 재실행해 통과시켰다
- 미해결 위험: 현재 sync_broker_holdings는 DB에 없는 브로커 보유분을 ctx.first_slot_key 아래 Holding으로 자동 생성한다. 즉 실증권 API를 연동하면 사용자가 봇 도입 이전에 직접 매수한 종목 전체가 자동으로 봇 관리 대상이 되어 손절·시그널붕괴 청산에 노출된다. 1단계 격리가 구현되기 전까지 실계좌 연동을 진행해서는 안 된다. 또한 앱 안에 개별 종목을 파는 경로가 없어(force-liquidate는 전량, MCP SELL은 501) 격리 계약의 "사용자가 직접 관리"가 현재는 증권사 앱 직접 조작에만 의존한다
- 인수인계: 워킹트리에 다른 세션의 미커밋 슬라이스가 함께 있다. backend/app/mcp/router.py와 신설 테스트는 2026-09-01 현황판 신규 작업 7의 슬라이스이고, backend/app/core/telegram.py·backend/app/bot/scheduler.py와 test_telegram_alert_delivery.py는 2026-09-02 현황판 슬라이스다. 커밋 시 서명이 사실과 맞도록 슬라이스별로 나눠 스테이징해야 한다. 하네스 1단계가 남기는 생산자 경고(scheduler.py·mcp/router.py 미참조)는 이 다중 세션 공유 트리 때문이며, 해당 파일들의 생산자 기록은 각자의 현황판이 소유한다. 설계는 확정됐고 구현은 미착수다. 다음 시작 지점은 1단계 격리이며, 막아야 할 지점 6곳이 문서 5.1에 파일·함수 단위로 정리되어 있다. 2번(exit 판정)은 루프 상단 continue가 아니라 판정부 분기여야 한다는 점이 3단계 수확 모드의 전제이므로 반드시 지켜야 한다. 고점 판단 방법론은 컨텍스트 오염을 피하기 위해 이번 세션에서 다루지 않고 문서 6절 백로그로만 남겼다. stock-auto-mobile 세션에는 설계 공유 메시지를 보냈으며, API 계약 확정 전까지 모바일 구현 착수는 보류하도록 요청했다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 1단계 격리 구현

### 변경 영향 기록
- 변경 분류: 기능 추가 (봇 관할권 분리) + API 계약 변경 + 스키마 변경
- 생산자: backend/app/core/models.py(MANAGEMENT_BOT_OWNED·MANAGEMENT_EXTERNAL·EXTERNAL_STRATEGY_TYPE 상수 신설, Holding.management 컬럼 추가), backend/alembic/versions/a7c1e93b5d20_add_holding_management.py(신규 마이그레이션), backend/app/bot/scheduler.py(sync_broker_holdings 기본값 반전·수량 차분 배분 순서·process_exit_signals 관측/판정 분기·매수 경로 동일티커 차단), backend/app/bot/order_reconciler.py(has_unresolved_orders_for_ticker 신설), backend/app/bot/multi_strategy_manager.py(calculate_slots_allocation에서 EXTERNAL 제외), backend/app/trades/router_account.py(get_holdings management 노출·force_liquidate include_external 게이트), backend/app/brokers/simulated_broker.py(get_holdings management 전달), frontend/components/PortfolioView.tsx(봇 관리 안 함 뱃지), locales/ko.json·locales/en.json(portfolio.external_badge·external_badge_tip 키 신설), backend/tests/test_holding_management_isolation.py(신규 10건), backend/tests/test_trading_flow_scenarios.py(sync guard 기대값을 신규 계약에 맞게 갱신)
- 소비자: 스케줄러 매도 판정 루프와 매수 루프, 슬롯 자본 배분, GET /api/v1/account/holdings, POST /api/v1/account/force-liquidate, 프론트 PortfolioView, 모바일 앱(stock-auto-mobile, 계약 통지 완료)
- 데이터 경계: holdings 테이블에 management VARCHAR NOT NULL DEFAULT 'BOT_OWNED' 1개 컬럼 추가. 기존 행은 전부 BOT_OWNED로 채운다 - 과거 유령 보유 복원분을 EXTERNAL로 소급 판정할 근거가 DB에 없어 소급 분류는 하지 않았고 이후 동기화부터 새 기본값이 적용된다. EXTERNAL 행의 strategy_type은 'external'로 고정하며 어떤 전략 슬롯 키와도 겹치지 않는다. 복합 유니크 제약(user_id, ticker, strategy_type) 덕에 같은 티커를 봇 슬롯과 EXTERNAL로 동시에 보유할 수 있다. 브로커 계좌에서는 두 슬라이스가 한 덩어리이므로 이것은 회계 격리이지 세무 격리가 아니다. 동기화 동작이 하나 바뀐다 - 브로커 수량이 DB보다 많고 해당 티커에 미해결 봇 주문이 없으면, 증가분을 기존 봇 슬라이스에 가산하지 않고 EXTERNAL 슬라이스를 새로 만들어 담는다. 총량은 브로커와 일치하지만 행이 둘로 나뉜다. 이 방향을 택한 이유는 오판의 비대칭이다 - EXTERNAL로 잘못 넣으면 봇이 그 수량을 관리하지 않을 뿐이지만, 봇 슬라이스로 잘못 넣으면 사용자가 직접 산 물량이 다음 사이클에 손절로 청산된다. 기존 test_trading_flow_scenarios의 sync guard 기대값(단일 행 8주)이 이 계약을 반영해 갱신됐다
- API 계약: 두 가지가 바뀐다. (1) GET /api/v1/account/holdings 응답 각 항목에 management 필드가 추가되고, EXTERNAL 항목의 strategy_name은 전략 번역기를 타지 않고 '봇 관리 안 함'으로 고정된다. (2) POST /api/v1/account/force-liquidate에 include_external 쿼리 파라미터(기본 false)가 추가되어 기본값에서 EXTERNAL 보유분이 청산 대상에서 빠지고, 응답에 excluded_external_count와 liquidated_tickers가 추가된다
- 문서 반영: docs/SCHEMA.md(holdings 테이블에 strategy_type·management 필드 설명 추가, 스테일했던 유니크 제약 표기를 실제 3컬럼 제약으로 정정), docs/API_STANDARD.md(6절 보유 종목 관할권 계약 신설), docs/plans/holding_management_modes.md(1단계 구현 완료 반영)
- 회귀 테스트: backend/tests/test_holding_management_isolation.py 10건 신규 통과. 네거티브 컨트롤로 가드의 실효성을 실증했다 - process_exit_signals의 관할권 분기를 무력화하자 test_external_holding_is_not_sold_but_is_still_observed가 실패하며 EXTERNAL 보유분이 시그널 붕괴로 실제 매도됐고(로그에 EXIT SIGNAL과 SUCCESS sold 기록), 되돌리니 통과했다. 첫 작성본은 EXTERNAL의 strategy_type을 'external'로 두어 가드를 껐는데도 통과했다 - 슬롯 조회에서 이미 걸러졌기 때문이며, 관할권 판정의 권한이 management에 있음을 실제로 검증하도록 strategy_type을 살아 있는 슬롯 키로 바꿨다. 첫 하네스 실행에서 기존 테스트 2건이 깨졌고 둘 다 실제 회귀였다. (1) test_i18n_messages의 하드코딩 한글 금지 검사 - 뱃지 문구를 tsx에 직접 박았던 것을 locales 키로 옮겨 해소했다. (2) test_trading_flow_scenarios의 sync guard - 수량 증가분이 봇 슬라이스로 가지 않고 EXTERNAL로 분리되면서 단일 행 8주 기대가 깨졌다. 이것은 의도한 계약 변경이므로 테스트를 신규 계약(봇 5주 + EXTERNAL 3주, 총량 8주)으로 갱신했다. 이후 python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 브로커 물량은 물리적으로 하나라서 같은 티커를 EXTERNAL과 봇 슬롯으로 나눠 들면 실현손익과 세금은 증권사 원가 규칙을 따르고 DB 슬라이스 회계와 어긋날 수 있다. 둘째, 앱 안에 개별 종목을 파는 경로가 여전히 없어(force-liquidate는 전량, MCP SELL은 501) 격리 계약의 '사용자가 직접 관리'가 증권사 앱 직접 조작에만 의존한다 - 2단계가 이것을 해소한다. 셋째, 수량 차분 배분은 티커 단위 미해결 주문 유무로 원인을 가르는 휴리스틱이다. 봇 체결이 원장에 반영되기 전 사이클에 사용자가 같은 티커를 앱 밖에서 거래하면 배분이 어긋날 수 있고, 다음 사이클 동기화로 자가 교정되지만 그 사이 슬라이스 수량이 일시적으로 틀린다. 넷째, 기존 행을 전부 BOT_OWNED로 채웠으므로 이미 유령 보유로 복원된 외부 종목이 있다면 여전히 봇 관할이다 - 실계좌 연동 전에 수동 확인이 필요하다
- 인수인계: 관할권 판정의 단일 권한은 Holding.management이며 strategy_type은 보조 수단일 뿐이다. 새 소비자를 추가할 때 strategy_type != 슬롯키 로만 거르면 management가 EXTERNAL인데 슬롯 키를 가진 행에서 뚫린다. process_exit_signals는 배제가 아니라 분기다 - 관측(last_price·highest_price)은 EXTERNAL도 함께 돌고 전략 판정 직전에 빠지며, 이 구조가 3단계 수확 모드의 고점 추적 전제다. 루프 상단으로 continue를 옮기면 3단계가 눈이 먼다. 다음 시작 지점은 2단계 개별 종목 수동 매도이며 지켜야 할 조건 3가지가 app/mcp/router.py docstring에 적혀 있다. 로드맵 정정 한 건 - 3~4단계 알림을 모바일 푸시로 낸다고 적었으나 백엔드에 푸시 인프라가 전무하다(FCM·디바이스 토큰·devices/register 모두 저장소에 0건, main.py 라우터 등록 15개에 해당 없음). 현재 유일한 알림 채널은 텔레그램이므로 3~4단계 알림은 텔레그램으로 내고 FCM은 별도 트랙으로 분리했다. 이 정정은 stock-auto-mobile 세션의 사전점검 회신에서 드러났고 docs/plans/holding_management_modes.md 9절에 반영했다. 모바일에는 1단계 확정 계약과 함께 통지했으며, 모바일 측 선행 과제는 Holdings 화면 신설(현재 종목별 보유 화면 자체가 없어 getHoldings·Holding 타입도 부재)이다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 2단계 개별 종목 수동 매도

### 변경 영향 기록
- 변경 분류: 기능 추가 (신규 엔드포인트) + API 계약 추가 + 매도 실행부 공통화 리팩터링
- 생산자: backend/app/trades/router_account.py(_sell_holding_slice·_resolve_market_price·_resolve_target_holding·SellHoldingRequest 신설, sell_holding 엔드포인트 신설, force_liquidate가 공통 헬퍼를 쓰도록 전환), frontend/lib/api.ts(previewSellHolding·sellHolding·forceLiquidate include_external 인자), frontend/components/PortfolioView.tsx(매도 버튼·확인 모달·결과 배너), locales/ko.json·locales/en.json(portfolio.sell_* 12키), backend/tests/test_manual_holding_sell.py(신규 9건)
- 소비자: 잔고 화면 PortfolioView, 모바일 앱(stock-auto-mobile, 계약 확정 후 통지 예정), 전량 청산 경로(force_liquidate가 같은 헬퍼를 공유)
- 데이터 경계: 스키마 변경 없음. 매도 체결 시 holdings.quantity 차감과 trade_logs 1행 추가는 기존 전량 청산과 동일한 경로를 쓴다. 전량 매도된 슬라이스는 행을 삭제한다. TradeLog.regime_mode는 전량 청산의 LIQUIDATE와 구분되도록 MANUAL_SELL로 남기고 BrokerOrder.source도 MANUAL_SELL이다 - 사후에 봇 청산·전량 청산·개별 매도를 원장에서 갈라 볼 수 있어야 하기 때문이다
- API 계약: POST /api/v1/account/holdings/{ticker}/sell 신설. 요청은 quantity(생략 시 전량)·strategy_type(생략 가능)·confirm(기본 false). confirm=false는 주문을 내지 않고 preview:true와 예상 체결가·매도금액·실현손익을 반환한다. confirm=true만 실제 주문을 낸다. 같은 티커를 여러 슬라이스로 보유하면 400과 후보 목록을 반환하고 strategy_type 지정을 요구한다. 미해결 주문·락 경합은 409, 락 서비스 장애는 503, 미보유는 404, 초과 수량과 장 마감은 400이다
- 문서 반영: docs/API_STANDARD.md 6.1절 신설, docs/plans/holding_management_modes.md 8.1절 구현 결과 추가 및 상태 갱신
- 회귀 테스트: backend/tests/test_manual_holding_sell.py 9건 신규 통과. 네거티브 컨트롤로 confirm 게이트의 실효성을 실증했다 - 프리뷰 분기를 무력화하자 test_preview_does_not_place_any_order와 test_pending_broker_order_blocks_execution_but_not_preview 2건이 실패하며 confirm 없이 주문이 나갔고, 되돌리니 통과했다. force_liquidate를 공통 헬퍼로 전환한 리팩터링은 기존 test_force_liquidation_safety의 ACK_UNKNOWN 시나리오가 그대로 통과함을 먼저 확인한 뒤 진행했다. 프론트는 npx tsc --noEmit과 npm run lint 무오류. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, force_liquidate의 회귀 방어가 여전히 얇다 - 이 경로를 덮는 테스트가 ACK_UNKNOWN 1건뿐이라 공통 헬퍼 전환의 동작 보존을 그 한 건에 의존했다. SIMULATED 다종목 부분 실패 시나리오는 여전히 미검증이다. 둘째, 프리뷰의 예상 체결가는 1분봉 종가이고 실제 시장가 체결은 슬리피지를 먹으므로 사용자가 본 금액과 실체결이 다를 수 있다 - 특히 유동성 낮은 종목에서 벌어진다. 셋째, 프리뷰와 확인 사이에 봇이 같은 종목을 매도하면 확인 시점에 수량이 모자라 400이나 동시성 오류가 난다. 락은 실행 구간만 덮으므로 이 창은 남아 있다. 넷째, 같은 티커의 여러 슬라이스를 한 번에 파는 방법은 없다 - 슬라이스별로 두 번 호출해야 한다
- 인수인계: 매도 실행부의 단일 출처는 _sell_holding_slice다. 새 매도 경로(예: 4단계 방어 자동 청산)를 붙일 때 주문 인텐트나 실현손익 계산을 다시 쓰지 말고 이 헬퍼를 경유해야 한다. 심볼 락은 헬퍼가 아니라 호출자가 잡는 규약이며, 락 실패의 응답 코드가 맥락마다 달라야 하기 때문이다. confirm 게이트는 서버가 소유한다 - 프론트가 프리뷰를 건너뛰고 confirm:true를 바로 보낼 수 있으므로 UI 흐름을 신뢰하지 말고 서버 기본값을 유지해야 한다. 다음 시작 지점은 3단계 수확 모드이며 관측(last_price·highest_price)은 1단계에서 이미 EXTERNAL에도 돌고 있으므로 observed_base_price 앵커와 무장 판정부터 붙이면 된다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 3단계 수확 모드

### 변경 영향 기록
- 변경 분류: 기능 추가 (EXTERNAL 보유분의 opt-in 자동 매도) + API 계약 추가 + 스키마 변경
- 생산자: backend/app/core/models.py(observed_base_price·harvest_enabled·harvest_armed 컬럼 추가), backend/alembic/versions/b8d2fa46c931_add_harvest_mode_columns.py(신규 마이그레이션), backend/app/bot/trade_calculations.py(get_harvest_arm_pct·get_harvest_trailing_pct·check_harvest_arm·check_harvest_breach와 상수 5종 신설), backend/app/bot/scheduler.py(_evaluate_harvest·_HarvestPseudoStrategy 신설, 관측 구간에서 observed_base 기록, EXTERNAL 분기에서 수확 판정 호출), backend/app/trades/router_account.py(HoldingManagementRequest·update_holding_management 신설, get_holdings에 수확 상태 노출), frontend/lib/api.ts(updateHoldingManagement), frontend/components/PortfolioView.tsx(수확 토글 버튼), locales/ko.json·locales/en.json(telegram.harvest_armed·sell_reason.harvest·portfolio.harvest_* 신설), backend/tests/test_harvest_mode.py(신규 13건)
- 소비자: 스케줄러 매도 판정 루프의 EXTERNAL 분기, 텔레그램 알림, GET /api/v1/account/holdings, PATCH /api/v1/account/holdings/{ticker}/management, 프론트 PortfolioView, 모바일 앱(계약 확정 후 통지 예정)
- 데이터 경계: holdings 테이블에 3개 컬럼 추가 - observed_base_price NUMERIC(20,4) NULL, harvest_enabled BOOLEAN NOT NULL DEFAULT 0, harvest_armed BOOLEAN NOT NULL DEFAULT 0. 기존 행은 전부 수확 꺼짐이며 동작이 바뀌지 않는다. observed_base_price는 봇이 그 종목을 처음 관측한 시점의 시세로 한 번만 기록하고 이후 갱신하지 않는다 - 등록 시점의 브로커 평단(사용자 매수가)이 아니라 실제 관측가여야 반토막 종목의 무장이 가능하다. 이 값은 무장 판정과 매도 하한의 앵커를 겸한다. 매도 체결 경로와 실현손익 계산은 기존 Part B를 그대로 쓰므로 원장 형식 변경은 없고 sell_reason만 harvest가 추가된다
- API 계약: PATCH /api/v1/account/holdings/{ticker}/management 신설. 본문은 strategy_type(생략 가능)과 harvest_enabled(생략 가능)이며 주문을 내지 않으므로 confirm 게이트가 없다. BOT_OWNED 보유분에 호출하면 400이다. harvest_enabled=false는 harvest_armed도 함께 해제한다. GET /holdings 응답에서 EXTERNAL 행이 있는 티커에 harvest_enabled·harvest_armed·observed_base_price가 추가된다
- 문서 반영: docs/API_STANDARD.md 6.2절 신설, docs/plans/holding_management_modes.md 5.2절에 확정 파라미터 표 기입 및 상태 갱신
- 회귀 테스트: backend/tests/test_harvest_mode.py 13건 신규 통과(순수 판정 함수 6, 스케줄러 통합 5, 토글 API 2). 네거티브 컨트롤로 하한 가드의 실효성을 실증했다 - check_harvest_breach의 관측 시작가 하한 조건을 무력화하자 test_breach_never_fires_below_observed_base와 test_crash_below_observed_base_is_never_sold 2건이 실패하며 고점 90에서 40으로 폭락한 종목이 실제로 매도됐고(로그에 EXIT SIGNAL과 SUCCESS sold 기록), 되돌리니 통과했다. 반토막 시나리오의 앵커 선택도 테스트로 고정했다 - 본전(100)을 앵커로 쓰면 무장도 매도도 불가능하고 관측가(50)를 쓰면 둘 다 가능함을 같은 입력으로 대조한다. 프론트는 npx tsc --noEmit과 npm run lint 무오류. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 수확 판정은 1분 사이클 관측에 의존하므로 사이클 사이의 급등락은 놓친다. 갭 상승 후 갭 하락으로 한 번에 지나가면 고점 기록 자체가 없다. 둘째, 무장이 래치라서 한 번 켜지면 급등이 되돌아와도 해제되지 않는다. 하한 가드가 손실 매도를 막지만, 관측 시작가 바로 위에서 등락하는 종목은 무장 상태로 오래 머문다. 셋째, 급등 종목의 시장가 청산은 슬리피지가 크다 - 백테스트 수치와 실체결이 가장 크게 벌어지는 구간이며 지정가·분할 청산은 별도 과제다. 넷째, 파라미터는 코드 상수라 조정하려면 재배포가 필요하고 실측 데이터가 아직 0건이다. 무장 임계 ATR 4배와 트레일링 ATR 2배는 근거 있는 추정이지 검증된 값이 아니다. 다섯째, 노이즈 버퍼 2사이클(약 2분)이 급락 국면에서는 반납분이 된다
- 인수인계: 수확 상태(observed_base_price·harvest_armed)는 절대 detached holding의 매핑 컬럼으로 다루면 안 된다. 매핑 컬럼을 세팅하면 h가 dirty가 되고 micro_session의 auto-merge가 version_id를 올려 Part B의 db.merge(h)가 StaleDataError로 사이클을 통째 중단시킨다(last_price·highest_price와 동일 사유). observed_base_snapshot·harvest_armed_snapshot 비영속 속성과 version 비간섭 Core UPDATE 조합을 유지해야 한다. 스냅샷은 DB에서 되읽어 채운다 - 로컬 값으로 덮어쓰면 다음 사이클에 관측 시작가가 현재가로 갱신되어 무장이 영원히 안 된다(구현 중 실제로 겪은 버그다). 테스트는 사이클마다 보유분을 DB에서 다시 읽어야 한다. 운영 경로도 사이클마다 ctx.holdings를 새로 조회하므로 같은 detached 객체를 재사용하는 테스트는 운영과 다른 상태를 만든다. 다음 시작 지점은 4단계 방어 경보이며 guard_baseline_low 래칫은 compute_rolling_box_stop의 단조 증가 불변식을 방향만 뒤집어 재사용한다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 보유 종목 응답의 매도 대상 키 안정화

### 변경 영향 기록
- 변경 분류: 결함 수정 (API 계약의 식별자 불안정) + 계약 명문화
- 생산자: backend/app/trades/router_account.py(get_holdings에 slices 배열 추가), docs/API_STANDARD.md(6절에 매도 대상 키와 id 사용 금지 명시), frontend/components/PortfolioView.tsx(React key를 h.id에서 ticker:strategy_type으로 교체), backend/tests/test_manual_holding_sell.py(slices 노출 검증 1건 추가)
- 소비자: 모바일 앱 보유 종목 화면과 개별 매도 UI(stock-auto-mobile), 웹 PortfolioView, 개별 매도 엔드포인트를 호출하는 모든 클라이언트
- 데이터 경계: 스키마 변경 없음. slices는 기존 holdings 행에서 파생한 읽기 전용 배열이며 각 원소는 strategy_type·management·quantity다. 브로커 응답은 티커 하나로 합쳐 오지만 DB는 슬라이스별로 나뉘므로 이 배열이 그 간극을 메운다
- API 계약: GET /api/v1/account/holdings 각 항목에 slices 배열이 추가된다. 아울러 응답의 id를 매도·설정 요청의 키로 쓰면 안 된다는 것을 계약으로 명시했다 - 시뮬레이터는 DB Holding.id를 주지만 KIS 경로는 목록 순번(idx+1000)을 발급해 한 종목이 청산되면 나머지 행의 id가 전부 밀리고 Toss 경로는 id 필드 자체가 없다. 매도 대상의 안정적인 키는 (ticker, strategy_type)이며 이는 DB 유니크 제약과 동일하다
- 문서 반영: docs/API_STANDARD.md 6절에 매도 대상 키 항목과 slices 항목 2건 추가
- 회귀 테스트: backend/tests/test_manual_holding_sell.py에 KIS 경로의 불안정한 id를 모사하는 브로커로 slices가 두 슬라이스를 모두 노출하는지 검증하는 1건 추가. python scripts/verify_harness.py 전체 통과(pytest 487건)
- 미해결 위험: kis_broker.get_holdings의 id 발급 로직 자체는 그대로 두었다. 표시용으로만 쓰이도록 계약에 못박았을 뿐이므로, 앞으로 누군가 이 필드를 키로 삼으면 같은 사고가 재발한다. 근본 해결은 브로커 어댑터가 순번 id를 아예 발급하지 않는 것인데 웹·모바일 양쪽의 기존 사용처를 정리해야 해서 이번 범위에서 제외했다. 또한 slices는 DB 기준이라 브로커 동기화 전에는 실계좌와 어긋날 수 있다
- 인수인계: 이 결함은 stock-auto-mobile 세션이 1단계 계약을 소비하다 발견해 보고했다. 클라이언트가 매도 대상을 지정할 때는 반드시 (ticker, strategy_type)을 쓰고 id는 표시용으로만 다뤄야 한다. 새 브로커 어댑터를 추가할 때 id를 목록 순번으로 발급하지 말 것 - 안정적인 식별자를 줄 수 없으면 필드를 생략하는 편이 낫다. 모바일에는 2·3단계 계약 전문과 함께 통지했다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 4단계 방어 경보

### 변경 영향 기록
- 변경 분류: 기능 추가 (EXTERNAL 보유분의 opt-in 경보) + API 계약 추가 + 스키마 변경
- 생산자: backend/app/core/models.py(guard_enabled·guard_baseline_score·guard_baseline_low·guard_enabled_at·guard_last_alert_at 컬럼 추가), backend/alembic/versions/c9e3ab571d48_add_defense_guard_columns.py(신규 마이그레이션), backend/app/bot/trade_calculations.py(compute_guard_low·check_guard_breach와 상수 3종 신설), backend/app/bot/scheduler.py(_evaluate_guard·_get_guard_evaluator 신설, EXTERNAL 분기에서 호출), backend/app/trades/router_account.py(HoldingManagementRequest에 guard_enabled 추가, 토글 시 기준선 재설정, get_holdings에 방어 상태 노출), frontend/components/PortfolioView.tsx(toggleHarvest를 toggleSwitch로 일반화, 방어 토글 버튼 추가, Holding 타입에 guard_enabled·slices 추가), locales/ko.json·locales/en.json(telegram.guard_alert·portfolio.guard_* 신설), backend/tests/test_defense_guard.py(신규 11건)
- 소비자: 스케줄러 매도 판정 루프의 EXTERNAL 분기, 텔레그램 알림, GET /api/v1/account/holdings, PATCH /api/v1/account/holdings/{ticker}/management, 프론트 PortfolioView, 모바일 앱(계약 통지 예정)
- 데이터 경계: holdings 테이블에 5개 컬럼 추가 - guard_enabled BOOLEAN NOT NULL DEFAULT 0, guard_baseline_score FLOAT NULL, guard_baseline_low NUMERIC(20,4) NULL, guard_enabled_at DATETIME NULL, guard_last_alert_at DATETIME NULL. 기존 행은 전부 방어 꺼짐이며 동작이 바뀌지 않는다. 기준선은 API가 아니라 스케줄러가 다음 사이클에 채운다 - 켤 때는 시세를 모르기 때문이다. 스위치를 켤 때마다 기준선 두 컬럼과 쿨다운을 비운다. guard_baseline_low는 단조 감소 래칫이라 반등해도 올라가지 않는다. 이 단계는 주문을 내지 않으므로 holdings.quantity·trade_logs·broker_orders에 아무 영향이 없다
- API 계약: PATCH /api/v1/account/holdings/{ticker}/management 본문에 guard_enabled(boolean, 생략 가능)가 추가되고 응답에 guard_enabled·guard_baseline_score·guard_baseline_low가 추가된다. GET /holdings 응답에서 EXTERNAL 행이 있는 티커에 같은 3필드가 추가된다. 켠 직후에는 기준선 두 필드가 null이다
- 문서 반영: docs/API_STANDARD.md 6.3절 신설, docs/plans/holding_management_modes.md 5.3절에 확정 파라미터 표와 고정 평가자 근거 기입 및 상태 갱신
- 회귀 테스트: backend/tests/test_defense_guard.py 11건 신규 통과(순수 판정 함수 3, 스케줄러 통합 5, 토글 API 3). 네거티브 컨트롤로 두 조건 동시 요구의 실효성을 실증했다 - check_guard_breach의 신저가 조건을 무력화하자 test_breach_requires_both_conditions와 test_guard_alerts_only_on_transition_and_never_sells 2건이 실패하며 신저가가 아닌데도 경보가 발송됐고, 되돌리니 통과했다. 이 설계의 가장 큰 함정도 테스트로 고정했다 - 절대 점수선(예: 40점 미만)을 쓰면 이미 12점으로 붕괴된 종목이 켜는 순간 참이 되지만, 전이 기반은 그 자리에 머물러 있으면 울리지 않고 점수가 추가로 무너져야 비로소 울린다. 주문이 나가지 않는다는 것도 단언한다(sell_calls 비어 있음, TradeLog 0건, quantity 불변). 프론트는 npx tsc --noEmit과 npm run lint 무오류. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째이자 가장 중요한 것 - 이 경보가 실제 붕괴를 예측한다는 근거가 아직 없다. strategy_alpha_verdict.md의 결론대로 이 프로젝트는 능동 타이밍 초과 알파를 확인하지 못했고 하방 예측이라고 다를 이유가 없다. -90%로 가는 진짜 원인(상장폐지, 감사의견 거절, 임상 실패, 유상증자)은 기술적 지표가 늦게 반영하거나 갭 하락 한 방으로 반영한다. 5단계 자동 청산은 이 경보를 수 주간 운영해 신호 품질을 실측한 뒤에 결정해야 하며, 그 전에 착수하면 판단 근거 없이 손실을 확정하는 기능을 넣는 것이 된다. 둘째, 파라미터 3종(점수 델타 15점, 유예 30분, 쿨다운 6시간)은 근거 있는 추정이지 검증값이 아니고 코드 상수라 조정에 재배포가 필요하다. 셋째, 고정 평가자를 regime_switching으로 잡았으므로 그 전략의 채점 로직이 바뀌면 기존 보유분의 기준선과 현재 점수가 다른 잣대로 비교된다 - 채점 변경 시 기준선 재설정이 필요하다. 넷째, 쿨다운 6시간 동안은 추가 악화가 있어도 침묵하므로 급격한 붕괴에서 두 번째 경보가 늦다
- 인수인계: 이 단계는 경보만 내고 주문을 내지 않는다는 것이 계약의 전부다. _evaluate_guard는 어떤 경로로도 sell_tasks_args에 값을 넣지 않으며, 5단계를 붙일 때도 이 함수가 직접 주문을 내게 만들지 말고 판정 결과만 반환하도록 분리해야 한다. 판정에는 반드시 갱신 전 기준선을 써야 한다 - 방금 내린 저가로 자기 자신을 비교하면 신저가 조건이 영원히 거짓이 된다(구현 중 실제로 주의한 지점이다). 기준선 재설정 규칙도 지켜야 한다 - 껐다 켤 때 예전 기준선을 물려받으면 꺼져 있던 동안의 하락이 전부 추가 악화로 잡혀 켜자마자 경보가 나간다. 다음 시작 지점은 6단계 위임이며, 5단계는 실측 데이터가 쌓이기 전에는 착수하지 않는다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 개별 매도 중복 요청 가드 수정과 계약 명문화 2건

### 변경 영향 기록
- 변경 분류: 결함 수정 (프론트 중복 요청 가드) + 계약 명문화
- 생산자: frontend/components/PortfolioView.tsx(useRef 동기 래치 도입, requestSellPreview·confirmSell·toggleSwitch 3곳의 state 가드를 래치로 교체), docs/API_STANDARD.md(경로 티커 표기 규약과 수확·방어 상태 부착 기준 2항 추가)
- 소비자: 웹 잔고 화면의 매도·스위치 조작, 모바일 앱(같은 규약을 이미 적용해 회신), 향후 추가될 브로커 어댑터
- 데이터 경계: 스키마·응답 형식 변경 없음. 명문화한 두 규약은 기존 동작을 서술한 것이고 코드 동작을 바꾸지 않는다. 경로 티커는 Holding.ticker와 문자 그대로 일치해야 하며 그 값은 접두사 없는 심볼이다. 현재 저장소에서 슬롯 prefix는 메타데이터로만 쓰이고 Holding.ticker에 적용되지 않음을 확인했다
- API 계약: 신규 엔드포인트나 필드는 없다. 6절에 2항을 추가해 (1) sell·management 경로 티커는 접두사 없는 심볼이고 클라이언트가 임의로 접두사를 붙이거나 떼면 404가 된다는 것, (2) harvest_*·guard_* 필드는 EXTERNAL 행이 하나라도 있으면 붙으므로 혼합 티커에서 합쳐진 management가 BOT_OWNED여도 붙는 것이 의도된 동작이며 클라이언트는 slices로 판정해야 한다는 것을 명시했다
- 문서 반영: docs/API_STANDARD.md 6절에 2항 추가
- 회귀 테스트: 프론트 변경이라 npx tsc --noEmit과 npm run lint로 검증했고 무오류다. python scripts/verify_harness.py 전체 통과. 다만 이 결함 자체를 잡는 자동 테스트는 없다 - 웹에 컴포넌트 테스트 하네스가 없어 연타 시나리오를 재현할 수단이 없다. 모바일 세션이 자기 구현에서 실측으로 재현했고(연타 3번에 confirm=true POST 3번 발송, useRef 래치 적용 후 1번), 웹의 동일 패턴을 지적해 확인한 건이다
- 미해결 위험: 첫째, 웹 컴포넌트 테스트가 없어 이 가드가 앞으로 회귀해도 하네스가 잡지 못한다. 같은 패턴이 다른 화면(관심종목 추가·전략 변경 등)에도 있는지 전수 확인하지 않았다. 둘째, 서버 측 멱등성은 여전히 없다 - 락은 두 번째 요청에 409를 돌려줄 뿐이고, 수량을 지정한 매도라면 첫 요청이 끝난 뒤 도착한 두 번째가 그대로 또 체결된다. 클라이언트 요청 ID 기반 멱등 처리는 별도 과제다. 셋째, 티커 접두사 규약은 문서로만 못박았고 코드가 강제하지 않는다 - 새 브로커 어댑터가 접두사를 실어 보내면 조용히 404가 난다
- 인수인계: 되돌릴 수 없는 액션의 중복 방지는 반드시 useRef 동기 래치를 써야 한다. setState는 리렌더 뒤에야 disabled를 걸기 때문에 같은 틱의 연타를 막지 못하고, 모바일 더블탭·고스트 클릭에서 실제로 재현된다. 서버 락은 이 문제의 해답이 아니다 - 두 번째 요청에 409를 주는 것이지 요청이 안 나가게 하는 것이 아니며, 순차 도착하면 둘 다 성공한다. 이 결함은 stock-auto-mobile 세션이 자기 구현에서 실측으로 발견하고 웹의 동일 패턴을 지적해 잡혔다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 5단계 방어 조치 (섀도 · 부분 청산)

### 변경 영향 기록
- 변경 분류: 기능 추가 (방어 조건 충족 시 동작 선택) + API 계약 추가 + 스키마 변경 + Part B 부분 수량 지원
- 생산자: backend/app/core/models.py(GUARD_ACTION_* 상수와 guard_action·guard_sell_ratio·guard_streak·guard_last_action_at 컬럼 추가), backend/alembic/versions/d1f4c82be607_add_guard_action_columns.py(신규 마이그레이션), backend/app/bot/trade_calculations.py(resolve_guard_sell_qty와 상수 3종 신설), backend/app/bot/scheduler.py(_evaluate_guard를 async 판정 함수로 전환해 조치 인자를 반환하도록 변경, _GuardPseudoStrategy 신설, _execute_single_sell에 sell_qty 파라미터 추가), backend/app/trades/router_account.py(guard_action·guard_sell_ratio 수용과 검증, 응답 노출), frontend/lib/api.ts·frontend/components/PortfolioView.tsx(3모드 선택 UI), locales/ko.json·locales/en.json(sell_reason.guard_liquidate·portfolio.guard_action_* 신설), backend/tests/test_guard_liquidation.py(신규 10건)
- 소비자: 스케줄러 매도 판정 루프의 EXTERNAL 분기와 Part B 매도 실행부, PATCH /api/v1/account/holdings/{ticker}/management, GET /holdings, 프론트 PortfolioView, 모바일 앱(계약 통지 예정)
- 데이터 경계: holdings 테이블에 4개 컬럼 추가 - guard_action VARCHAR NOT NULL DEFAULT 'ALERT_ONLY', guard_sell_ratio FLOAT NOT NULL DEFAULT 0.5, guard_streak INTEGER NOT NULL DEFAULT 0, guard_last_action_at DATETIME NULL. 기존 행은 전부 ALERT_ONLY라 동작이 바뀌지 않는다. LIQUIDATE는 보유 수량의 guard_sell_ratio 비율만 매도하므로 holdings.quantity가 부분 차감되고 TradeLog에 부분 수량으로 기록된다. Part B의 sell_qty 파라미터는 None이 기본이며 기존 모든 매도 경로(손절·트레일링·시그널붕괴·수확·레짐이탈)는 그대로 전량 매도다
- API 계약: PATCH 본문에 guard_action(ALERT_ONLY/SHADOW/LIQUIDATE)과 guard_sell_ratio(0 초과 1 이하)가 추가되고 응답과 GET /holdings에 같은 2필드가 추가된다. guard_enabled가 꺼진 상태에서 ALERT_ONLY 외의 모드로 바꾸면 400이고, 허용되지 않은 값도 400이다. 모드 변경 시 guard_streak을 리셋한다
- 문서 반영: docs/API_STANDARD.md 6.4절 신설, docs/plans/holding_management_modes.md 5.4절 전면 재작성(당초 보류 판단과 그 정정 사유, 3모드, 가드 5겹, SHADOW 로그 형식, 판정과 집행의 분리)
- 회귀 테스트: backend/tests/test_guard_liquidation.py 10건 신규 통과(부분 수량 1, 스케줄러 통합 6, 토글 API 3). 네거티브 컨트롤로 SHADOW의 무주문 보장을 실증했다 - SHADOW 분기를 무력화하자 test_shadow_records_counterfactual_without_ordering이 실패하며 실제 부분 청산이 나갔고(로그에 LIQUIDATE와 partially sold 기록), 되돌리니 통과했다. 기본값 ALERT_ONLY에서는 조건을 10사이클 넘게 충족해도 주문이 나가지 않음을 단언한다. 구현 중 기존 4단계 테스트 3건이 깨졌는데 원인은 로직이 아니라 로그 문자열 충돌이었다 - 새로 넣은 mode 표기가 'Action mode: ALERT_ONLY'였고 테스트가 부분 문자열 'ALERT'로 경보 발생을 판정하고 있어 오탐이 났다. 로그 토큰을 소문자 mode=alert_only로 바꾸고 테스트 프로브를 '[Guard] ALERT'로 좁혀 해소했다. python scripts/verify_harness.py 전체 통과. 다만 첫 실행에서 Playwright가 8건 중 1건(auth-smoke의 localhost 호스트 유지 검사)에 실패했는데 원인은 코드가 아니라 환경이었다 - :3100에 이전 하네스 실행이 남긴 node 프로세스가 살아 있었고 Playwright가 그 서버를 재사용해 옛 빌드를 검사했다. 실측으로 확인했다 - 그 서버는 /login에 308로 /login/ 리다이렉트를 내는데 저장소의 next.config.ts에는 trailingSlash 설정이 없어 동작이 모순이었고, 당일 앞선 하네스 7회는 같은 설정으로 8/8 통과했다. 묵은 프로세스를 정리하고 재실행하니 8/8 통과했다
- 미해결 위험: 첫째, 이 조치가 손실을 줄인다는 근거는 여전히 없다. SHADOW 모드가 그 근거를 모으기 위해 있는 것이며, LIQUIDATE를 권장 설정으로 제시해서는 안 된다. 둘째, 파라미터 3종(연속 10사이클, 일일 1회, 비율 0.5)은 근거 있는 추정이지 검증값이 아니다. 셋째, SHADOW 로그를 ActionLog에 텍스트로 남기므로 분석하려면 파싱이 필요하고 이후 가격과의 대조는 수동이다 - 분석 수요가 확인되면 전용 테이블로 승격해야 한다. 넷째, 부분 청산 후 남은 수량에 대해 24시간 뒤 다시 조치가 나갈 수 있어 반복되면 결국 전량에 수렴한다. 다섯째, Part B에 sell_qty를 뚫으면서 기존 전량 매도 경로 전체가 같은 함수를 공유하게 됐다 - sell_qty를 넘기지 않으면 전량이라는 계약이 깨지면 모든 매도가 영향받는다
- 인수인계: 판정과 집행은 분리되어 있다. _evaluate_guard는 청산이 필요해도 직접 주문을 내지 않고 Part B 인자 튜플을 반환하며, 이 규약을 깨고 함수 안에서 주문을 내면 되돌릴 수 없는 주문이 판정 로직에 숨는다. Part B의 sell_qty는 None이 전량이라는 계약이며 새 매도 경로를 추가할 때 이 기본값에 의존해도 된다. guard_streak은 조건이 끊기면 0으로 리셋되고 모드 변경 시에도 리셋된다 - 이 두 리셋이 없으면 사용자가 청산 모드를 켜는 순간 즉시 팔린다. 로그 문자열을 상태 판정에 쓰는 테스트는 부분 문자열이 아니라 대괄호 접두사를 포함한 정확한 토큰으로 매칭해야 한다(이번에 실제로 3건이 오탐났다). 다음 시작 지점은 6단계 위임이다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 위임 스위치 UI를 카드 푸터에서 전용 모달로 분리

### 변경 영향 기록
- 변경 분류: UI 개선 (레이아웃 결함 수정) + 배지 중복 제거
- 생산자: frontend/components/PortfolioView.tsx(카드 푸터의 위임 스위치 5개를 봇 위임 버튼 1개로 축약, 위임 설정 모달 신설, 상태 배지를 헤더로 이동, EXTERNAL일 때 전략 배지 숨김), locales/ko.json·locales/en.json(portfolio.delegation_*·harvest_title·guard_title 신설)
- 소비자: 웹 잔고 화면 사용자. API·스키마 변경이 없으므로 모바일과 백엔드에는 영향이 없다
- 데이터 경계: 변경 없음. 화면 구성만 바뀌었고 호출하는 엔드포인트와 페이로드는 동일하다
- API 계약: 변경 없음
- 문서 반영: 해당 없음 - 화면 구성은 계약이 아니며 API_STANDARD와 설계 문서가 규정하는 동작은 그대로다
- 회귀 테스트: 실제 화면으로 검증했다. admin 계정의 EXTERNAL 보유분(AAPL 12주)에서 푸터 버튼이 2개(봇 위임·매도)로 줄고 높이가 26/28px로 균일해진 것을 DOM 측정으로 확인했다. 모달을 열어 방어를 켜자 조치 모드 3개가 97px 균등 그리드로 렌더되는 것과, 끄면 기준선·유예시각·streak이 전부 정리되는 것을 DB로 확인했다. npx tsc --noEmit과 npm run lint 무오류. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 웹 컴포넌트 테스트 하네스가 없어 이 레이아웃 회귀를 자동으로 잡을 수단이 없다 - 다음에 버튼이 늘어나면 같은 문제가 재발해도 하네스는 통과한다. 둘째, 배지가 최대 5개(관할권·수확·방어·SIMULATED·전략)까지 늘 수 있어 좁은 화면에서 헤더가 여러 줄이 된다. flex-wrap이라 깨지지는 않지만 카드 높이가 들쭉날쭉해진다. 셋째, 모달 안의 스위치는 여전히 래치 하나로 직렬화되므로 수확과 방어를 빠르게 연속으로 조작하면 두 번째 클릭이 무시된다 - 의도한 동작이지만 사용자에게는 반응이 없는 것처럼 보인다
- 인수인계: 카드 푸터에 새 버튼을 추가하지 말 것. 415px 카드에 버튼 6개를 넣었더니 글자가 줄바꿈되어 읽을 수 없었고, 그것이 이 작업의 계기다. 조작은 위임 설정 모달에, 상태 표시는 헤더 배지에 두는 분리를 유지해야 한다. 모달은 스냅샷이 아니라 폴링 최신값을 따라간다 - delegationTarget으로 티커와 strategy_type만 기억하고 매 렌더에서 holdings 목록에서 다시 찾으며, 종목이 사라지면 조용히 닫힌다. EXTERNAL의 strategy_name은 서버가 '봇 관리 안 함'으로 치환해 내려주므로 전용 배지와 겹친다 - 전략 배지를 EXTERNAL에서 숨기는 조건을 지워서는 안 된다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 다중 슬라이스 티커의 수량 유실 수정

### 변경 영향 기록
- 변경 분류: 결함 수정 (동기화 계약 불일치로 인한 보유 수량 유실)
- 생산자: backend/app/bot/scheduler.py(sync_broker_holdings가 브로커 응답을 티커 단위로 합산한 뒤 비교하도록 정규화 단계 추가), backend/tests/test_holding_management_isolation.py(재현 회귀 1건 추가)
- 소비자: 스케줄러 동기화 경로 전체, 같은 티커를 여러 슬라이스로 보유한 모든 사용자
- 데이터 경계: 스키마 변경 없음. 브로커가 슬라이스·로트별로 여러 행을 주더라도 티커 단위 합계로 정규화한 뒤 db_total_qty와 비교한다. 평단은 수량 가중평균으로 되돌리며 유령 보유 복원 시 avg_price로 쓰인다. 수정 전에는 같은 티커의 슬라이스 합계와 브로커의 개별 행 수량을 대조해 존재하지 않는 차분을 만들어냈다
- API 계약: 변경 없음
- 문서 반영: 해당 없음 - 동기화 내부 결함이며 공개 계약은 불변이다
- 회귀 테스트: 재현 테스트를 먼저 작성해 결함을 실증했다. MSFT를 봇 슬롯 25주와 EXTERNAL 15주로 나눠 든 상태에서 시뮬레이터처럼 행마다 1개씩 내보내는 브로커를 주자 총량이 40주에서 15주로 줄고 EXTERNAL 행이 삭제됐다(assert 15 == 40 실패). 정규화 적용 후 총량 40주와 두 슬라이스가 모두 보존된다. 관련 6개 스위트 66건 전부 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 이 결함으로 유실된 수량은 trade_logs에 남지 않으므로 이미 발생한 유실은 사후 추적이 불가능하다. 운영 DB에서 같은 티커를 여러 슬라이스로 보유한 이력이 있었다면 수량이 조용히 줄었을 수 있다. 둘째, Holding 삭제 경로마다 로깅이 제각각이라 이런 유실이 또 생겨도 원장만으로는 추적이 안 된다 - 삭제 시 무조건 ActionLog를 남기는 가드가 필요하다. 셋째, 실증권 브로커(KIS/Toss)는 티커당 1행을 주므로 이 경로에서는 결함이 드러나지 않았고, 시뮬레이터에서만 재현된다 - 다른 어댑터가 로트별 응답을 주기 시작하면 같은 문제가 재발할 수 있어 소비 측 정규화를 유지해야 한다
- 인수인계: 이 결함은 stock-auto-mobile 세션이 admin 계정에 혼합 슬라이스를 직접 시드하고 관찰하다 발견해 보고했다. 결정적 단서는 매도 로그의 realized_pnl 역산이었다 - 살아남은 행이 봇 슬라이스의 평단(410)을 유지하면서 수량만 EXTERNAL 쪽 값(15)이 된 것을 그것으로 특정했다. sync_broker_holdings의 비교는 항상 티커 단위 합계 대 합계여야 한다. 브로커 어댑터가 무엇을 주든 소비 측에서 정규화하는 규약을 깨지 말 것. 아울러 1단계에서 도입한 EXTERNAL 우선 차감 순서가 이 결함과 만나 보호 대상을 먼저 파괴했다 - 차감 순서를 바꿀 때는 상위 비교 로직이 올바른 단위로 이뤄지는지 함께 확인해야 한다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 방어 알림 문구 평이화와 SHADOW 모드 재명명

### 변경 영향 기록
- 변경 분류: UX 문구 개선 (사용자 대면 알림·라벨). 판정 로직과 API 계약은 불변
- 생산자: locales/ko.json·locales/en.json(telegram.guard_alert·telegram.guard_shadow 전면 재작성, portfolio.guard_action_shadow·guard_action_tip 재작성), backend/app/bot/scheduler.py(SHADOW 발동 시 텔레그램 발송 추가, guard_shadow 인자를 minutes·proceeds로 교체), backend/tests/test_guard_liquidation.py(섀도 알림 발송 검증 1건 추가 및 프로브 갱신)
- 소비자: 텔레그램 알림 수신자, 웹 잔고 화면 위임 모달, 모바일 앱(라벨 동기화 통지 필요)
- 데이터 경계: 변경 없음. guard_action의 저장 값은 여전히 SHADOW이며 표시 이름만 바뀐다
- API 계약: 변경 없음. guard_action은 ALERT_ONLY/SHADOW/LIQUIDATE 그대로다
- 문서 반영: 해당 없음 - 표시 문구는 계약이 아니며 API_STANDARD와 설계 문서가 규정하는 동작은 그대로다
- 회귀 테스트: 두 알림을 실제 렌더해 육안 확인했고, 그 과정에서 조사 오류(트라이앵글가)와 천단위 구분 누락(4762주), 종목명 중복 3건을 잡아 수정했다. SHADOW 발동 시 알림이 실제로 발송되면서도 주문은 나가지 않음을 단언하는 테스트를 추가했다(sell_calls 비어 있음, TradeLog 0건). 방어 관련 23건 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 섀도 기록을 사용자가 볼 수 있는 경로가 텔레그램뿐이다. ActionLog는 관리자 패널에만 노출되고 accountAPI.getActions는 정의만 되어 있을 뿐 쓰는 화면이 없다. 텔레그램 연동을 하지 않은 사용자는 이 모드를 켜도 아무것도 볼 수 없다. 둘째, 알림이 두 갈래로 나뉜다 - 첫 이탈에 경보(쿨다운 6시간), 10사이클 지속에 매도 시점 알림(일일 1회). 사용자가 두 알림을 같은 사건으로 오인할 수 있다. 셋째, 종목 상태 점수를 100점 만점으로 설명했으나 이는 고정 평가자(regime_switching)의 채점이며 그 산식이 바뀌면 같은 종목의 점수 의미가 달라진다
- 인수인계: 알림 문구를 상태 판정에 쓰는 테스트는 반드시 핵심 주장 문장으로 매칭할 것. 이번에도 라벨을 기록만에서 매도 시점 알림으로 바꾸자 기록만을 찾던 프로브가 깨졌다(로그 토큰 충돌에 이어 두 번째다). SHADOW 알림은 조치 발동 시점에만 나가므로 10사이클 지속과 일일 1회 캡이 곧 발송 빈도 상한이다 - 이 가드를 풀면 알림이 분당 1회로 폭주한다. 사용자 대면 문구에서 사이클은 분으로 환산해 말해야 한다(주기가 1분이므로 사이클 수 = 분)

## 보유 종목 삭제 감사 로깅 가드

### 변경 영향 기록
- 변경 분류: 관측성 강화 (삭제 경로 단일화 및 감사 로그 의무화)
- 생산자: backend/app/core/holding_audit.py(delete_holding 신설), backend/app/bot/scheduler.py(동기화 차감·sweep·레짐 이탈·Part B 매도 4곳 전환), backend/app/bot/order_reconciler.py(매도 정산 1곳), backend/app/brokers/simulated_broker.py(미체결 체결 1곳), backend/app/trades/router_account.py(수동 매도·전량 청산 2곳 전환, reset_balance 예외 사유 주석), backend/tests/test_holding_management_isolation.py(감사 로그 검증 1건 추가), backend/tests/test_trading_flow_scenarios.py(sweep 프로브를 감사 형식으로 갱신)
- 소비자: 장애 조사 시 ActionLog를 읽는 개발자·운영자, 관리자 패널 SystemHealth
- 데이터 경계: 스키마 변경 없음. 삭제 1건마다 ActionLog 1행이 WARNING 등급으로 추가된다. 로그와 삭제가 같은 트랜잭션에 묶이므로 지워졌는데 로그가 없거나 그 반대인 상태가 생기지 않는다. 로그 본문에 티커·수량·전략·관할권·평단·행위 경로·사유가 담긴다
- API 계약: 변경 없음
- 문서 반영: 해당 없음 - 내부 관측성 개선이며 공개 계약은 불변이다. 한계와 사용법은 holding_audit.py 모듈 docstring이 소유한다
- 회귀 테스트: 동기화 sweep으로 행이 삭제될 때 감사 로그 1건이 남고 그 안에 티커·수량·관할권·actor·reason이 모두 담기는지 단언하는 테스트를 추가했다. 삭제 관련 7개 스위트 69건 통과. 기존 test_trading_flow_scenarios의 sweep 프로브가 옛 문구를 찾다 깨져 감사 형식으로 옮겼다(로그 문구 프로브가 깨진 세 번째 사례다). python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째이자 가장 중요한 것 - 이 가드는 DB 직접 조작을 막지도 기록하지도 못한다. SQL로 지우면 애플리케이션을 거치지 않으므로 어떤 앱 레벨 필드도 채워지지 않는다. 여기서 얻는 것은 앱 경로가 전부 기록된다는 불변식 하나이며, 그 덕에 로그 없이 사라진 행은 앱이 지운 것이 아니라는 소거법이 선다. DB 조작 자체를 기록하려면 DB 트리거와 append-only 감사 테이블이 필요하고 그것은 별도 과제다. 둘째, 감사 로그가 ActionLog에 쌓이는데 이 테이블은 관리자 패널에만 노출되고 사용자 대면 조회 경로가 없다. 셋째, reset_balance는 의도적으로 제외했다 - ActionLog를 함께 지우므로 남겨도 사라진다. 넷째, 수량이 0이 아닌 채로 줄어드는 경우(동기화 차감 중간 단계)는 여전히 별도 Sync Guard 로그에만 의존한다 - 삭제만 의무화했고 감량은 아니다
- 인수인계: 앞으로 Holding을 지우는 코드를 추가할 때 db.delete를 직접 호출하지 말고 반드시 app/core/holding_audit.delete_holding을 경유할 것. actor는 어느 앱 경로인지, reason은 왜 지우는지를 담아야 하며 둘 다 나중에 원인을 좁히는 데 쓰인다. 커밋은 호출자가 한다 - 로그와 삭제가 같은 트랜잭션에 있어야 불변식이 유지된다. 이 가드가 필요했던 이유는 2026-09-06에 admin의 보유 행이 사라졌을 때 삭제 경로 8곳의 로깅이 제각각이라 앱인지 DB 조작인지조차 가릴 수 없었기 때문이다. 그때 원인을 좁힌 유일한 실마리는 매도 로그의 실현손익 역산이었고 그것은 매도가 한 번이라도 일어났기에 가능했다 - 동기화 차감처럼 매도 없는 경로였다면 흔적이 0이었다

## 방어 조치 게이트를 벽시계 기준으로 교정

### 변경 영향 기록
- 변경 분류: 결함 수정 (조치 게이트의 시간 불안정성) + 사용자 문구 정정
- 생산자: backend/app/core/models.py(guard_streak_started_at 컬럼 추가), backend/alembic/versions/e2a7d5c31f84_add_guard_streak_started_at.py(신규 마이그레이션), backend/app/bot/trade_calculations.py(GUARD_SUSTAIN_MINUTES 신설), backend/app/bot/scheduler.py(_evaluate_guard가 사이클 수와 경과 시간을 모두 요구하도록 게이트 교정, 알림에 실제 경과 분 사용), locales/ko.json·locales/en.json(절반 매도를 부분 매도로 정정, 게이트 안내를 분 기준으로 재작성), backend/tests/test_guard_liquidation.py(벽시계 게이트 불변식 1건 추가 및 기존 테스트에 시간 경과 헬퍼 도입)
- 소비자: 방어 조치 판정 경로, 텔레그램 알림 수신자, 웹·모바일 위임 설정 UI
- 데이터 경계: holdings에 guard_streak_started_at DATETIME NULL 1개 추가. 연속 충족이 처음 시작된 시각을 담으며 조건이 끊기면 NULL로 되돌아간다. 기존 행은 NULL이라 다음 연속 시작 때 채워지고 그 전에는 조치가 나가지 않는다
- API 계약: 변경 없음. 응답 필드도 그대로다
- 문서 반영: 해당 없음 - 판정 규격은 docs/plans/holding_management_modes.md 5.4절이 소유하며 후속 갱신 대상이다
- 회귀 테스트: 실측이 먼저였다. admin action_logs의 Scan Cycle Started 40표본에서 사이클 간격 중앙값 124초, 평균 163초, 최소 104초, 최대 846초를 확인했다. 스케줄러는 interval minutes=1로 등록돼 있으나 사이클이 1분을 넘기면 겹치는 실행이 건너뛰어진다. 그래서 GUARD_SUSTAIN_CYCLES=10이 중앙값 기준 약 20분, 최악 두 시간 이상으로 흔들린다. 게이트에 GUARD_SUSTAIN_MINUTES=20을 추가한 뒤 사이클 15회를 채워도 시간이 흐르지 않으면 매도하지 않고 시간까지 흐르면 매도하는 것을 단언하는 테스트를 넣었다. 기존 조치 테스트 4건이 즉시 사이클을 돌려 전부 실패했는데 그것이 게이트가 작동한다는 증거였고, 시간 경과 헬퍼를 도입해 통과시켰다. 방어 관련 24건 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 사이클 주기가 왜 2분인지는 규명하지 않았다. 사이클 소요 시간이 1분을 넘겨 APScheduler가 실행을 건너뛰는 것으로 보이나 실측하지 않았고, 주기 자체를 줄이거나 사이클을 빠르게 만드는 것은 별도 과제다. 둘째, 최대 간격 846초는 정상 편차로 보기 어렵다 - 그 시점에 무슨 일이 있었는지 확인하지 않았다. 셋째, 수확 모드의 노이즈 버퍼 2사이클도 같은 문제를 갖는다. 2사이클이 2분이 아니라 4분 이상이며 최악의 경우 훨씬 길다. 수확은 조치가 매도이므로 같은 교정이 필요하지만 이번 범위에서 제외했다. 넷째, GUARD_SUSTAIN_MINUTES=20은 중앙값 기준으로 기존 동작과 맞춘 값이지 검증된 값이 아니다
- 인수인계: 사이클 수를 시간으로 환산하지 말 것. 스케줄러 주기는 등록값과 다르고 편차가 크다(실측 104~846초). 지속 조건이 필요하면 반드시 벽시계 시각을 기록해 비교해야 하고, 사용자 알림에 적는 시간도 그 값이어야 한다. 이 결함은 stock-auto-mobile 세션이 action_logs의 Scan Cycle Started 간격이 2분인 것을 보고 "주기가 정말 1분이냐"고 물어 드러났다. 수확 모드의 노이즈 버퍼도 같은 결함을 갖고 있으므로 다음에 손볼 때 함께 교정할 것

## 벽시계 게이트의 연속 시각 리셋 누락 수정

### 변경 영향 기록
- 변경 분류: 결함 수정 (직전 커밋에서 도입한 벽시계 가드의 우회 경로)
- 생산자: backend/app/trades/router_account.py(update_holding_management의 스위치 분기 4곳을 _restart_streak 단일 헬퍼로 통합), backend/tests/test_guard_liquidation.py(분기 4개를 모두 도는 테이블 회귀 1건 추가)
- 소비자: 방어 조치 판정 경로, 웹·모바일 위임 설정 UI
- 데이터 경계: 스키마 변경 없음. 스위치를 하나라도 건드리면 guard_streak과 guard_streak_started_at을 함께 비운다. 비율 변경도 리셋 대상에 포함했다 - 25퍼센트와 100퍼센트는 조치의 결과가 다른 별개의 결정이고, 사용자 문구가 이미 그렇게 말하고 있었다
- API 계약: 변경 없음
- 문서 반영: 해당 없음 - 내부 판정 경로 결함이며 공개 계약은 불변이다
- 회귀 테스트: 재현 테스트를 먼저 작성해 결함을 실증했다. 모드를 LIQUIDATE로 바꾸면 guard_streak은 0이 되지만 guard_streak_started_at이 남아, 스케줄러의 (streak_started_at or now)가 옛 시각을 물려받고 벽시계 20분 가드가 처음부터 통과 상태가 됐다. 분기 4개를 모두 도는 테이블 테스트로 두 축이 함께 0으로 돌아가는지 단언한다. 방어 관련 25건 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 이 결함은 벽시계 가드를 넣은 직후에 이미 존재했다 - 새 컬럼을 추가할 때 그 컬럼을 리셋해야 하는 지점을 전수 확인하지 않았다. 같은 유형이 재발할 수 있으므로 상태 컬럼을 추가할 때는 초기화 지점을 함께 열거해야 한다. 둘째, 사이클 게이트가 남아 있어 즉시 매도로는 이어지지 않았다 - 두 겹 게이트가 서로를 가려 결함이 눈에 띄지 않았고, 다중 방어선이 오히려 탐지를 늦추는 사례다. 셋째, 수확 모드의 노이즈 버퍼는 여전히 사이클 기준이며 같은 교정이 필요하다
- 인수인계: 상태를 담는 컬럼을 새로 추가할 때는 그 값을 0 또는 NULL로 되돌려야 하는 지점을 전부 찾아 함께 처리할 것. 이번에는 분기 4개 중 3개에서 누락이 났고, 그래서 분기마다 쓰지 않고 _restart_streak 헬퍼 하나로 모았다. 새 스위치를 추가하면 그 분기에서도 이 헬퍼를 부르면 된다. 이 결함은 stock-auto-mobile 세션이 문구를 반영하려고 소스를 읽다 발견했다 - 계약을 소비하는 쪽이 구현을 읽는 것이 실제로 결함을 잡는다

## 수확 노이즈 버퍼를 벽시계 기준으로 교정

### 변경 영향 기록
- 변경 분류: 결함 수정 (방어 게이트와 동일 유형의 시간 불안정성 + 인메모리 상태의 재기동 유실)
- 생산자: backend/app/core/models.py(harvest_breach_started_at 컬럼 추가), backend/alembic/versions/f3b8e6a294c1_add_harvest_breach_started_at.py(신규 마이그레이션), backend/app/bot/trade_calculations.py(HARVEST_SUSTAIN_CYCLES·HARVEST_SUSTAIN_MINUTES 신설), backend/app/bot/scheduler.py(_evaluate_harvest의 노이즈 버퍼가 횟수와 경과 시간을 모두 요구하도록 교정, 회복·매도 시 두 축 함께 리셋), backend/tests/test_harvest_mode.py(회복 리셋·재기동 보존 회귀 2건 추가, 수명주기 테스트에 시간 경과 단계 추가), docs/plans/holding_management_modes.md(5.2절 노이즈 버퍼 규격 갱신)
- 소비자: 수확 매도 판정 경로. API 계약과 UI는 무관하다
- 데이터 경계: holdings에 harvest_breach_started_at DATETIME NULL 1개 추가. 트레일링 이탈이 시작된 시각을 담고 회복하거나 매도가 확정되면 NULL로 되돌린다. 관측 횟수는 기존 인메모리 BREACH_COUNT_CACHE를 그대로 쓴다 - 재기동하면 두 번을 다시 관측해야 하지만 그것은 무해한 반면, 시각이 초기화되면 급등이 꺾인 뒤에도 매도가 계속 미뤄진다
- API 계약: 변경 없음
- 문서 반영: docs/plans/holding_management_modes.md 5.2절의 노이즈 버퍼 서술을 사이클 기준에서 횟수+시간 기준으로 갱신
- 회귀 테스트: 기존 수명주기 테스트가 즉시 실패했고 그것이 새 시간 하한이 작동한다는 증거였다. 관측 횟수를 채워도 시간이 안 지나면 팔지 않고 시간까지 흐르면 파는 단계를 테스트에 추가했다. 아울러 방어에서 겪은 결함이 여기서 재발하지 않도록 두 축 동시 리셋을 단언하는 회귀와, 인메모리 카운터가 비워져도 이탈 시각이 보존되는 회귀를 추가했다. 수확·방어 관련 40건 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 첫째, 봇 소유 포지션의 노이즈 버퍼(손절·트레일링·롤링박스)는 여전히 사이클 기준이며 인메모리다. 같은 결함을 갖지만 라이브 A/B 계좌들이 그 경로로 매매 중이라 동작 변경의 영향 범위가 커서 이번에 건드리지 않았다 - 별도 과제로 다뤄야 한다. 둘째, HARVEST_SUSTAIN_MINUTES=2는 기존 동작(2사이클 약 4분)보다 짧은 하한이라 현재 조건에서는 대기를 늘리지 않지만, 주기가 빨라지면 이 값이 실제 하한이 된다. 검증된 값이 아니다. 셋째, 왜 사이클 주기가 등록값 1분이 아니라 약 2분인지는 여전히 규명하지 않았다
- 인수인계: 지속 조건을 다룰 때는 횟수와 시간을 모두 요구하고, 회복·확정 시 두 축을 반드시 함께 되돌릴 것. 방어 게이트에서 한 축만 리셋해 가드가 우회된 사고가 이미 있었고, 여기서는 그 재발을 막는 회귀를 처음부터 넣었다. 상태를 인메모리에 둘지 컬럼에 둘지는 재기동 시 그 값이 0이 되는 것이 안전한 방향인지로 판단한다 - 관측 횟수는 다시 세면 그만이라 인메모리로 두었고, 대기 시각은 0이 되면 매도가 미뤄지므로 컬럼에 두었다
