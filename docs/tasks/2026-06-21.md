# 2026-06-21 일자별 현황판

## [CoreFix] 회원가입 이후 스캐너·자동매매 핵심 경로 회귀 수정

### 목표
- 복수 KIS 주문이 모두 종결된 뒤 자동매매가 안전하게 재개되도록 한다.
- 로그인 사용자가 본인 관심종목의 최신 분석 신호를 조회하도록 스캐너 응답 계약을 정합화한다.
- 수동·예약 스캔의 중복 실행과 전역 캐시 경쟁을 차단한다.
- 스캐너 최신 신호 API에 사용자 인증을 적용한다.
- 회원가입부터 사용자별 스캔과 복수 주문 종결까지 연결된 회귀 테스트를 보강한다.

### 작업 목록
- `[/]` 복수 주문 종결 후 사용자 봇 재개 상태 수정
- `[/]` 사용자별 관심종목 신호를 `/scanner/latest` 응답에 안전하게 결합
- `[/]` 수동·예약 해외시장 스캔 단일 실행 가드 적용
- `[/]` `/scanner/latest` 인증 의존성 적용
- `[/]` 백엔드 회귀 테스트 및 프론트 정적 검증

### 검증 기록
- `backend/venv/Scripts/python.exe -m py_compile` 수정 Python 파일 전체 통과
- 핵심 주문·스캐너 회귀 테스트 17건 통과
- 신규 사용자 통합 흐름 테스트 1건 통과
- 백엔드 전체 테스트 통과: 148 passed
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- 수정 Markdown/Python 파일 UTF-8 NFC 검증 통과
