# 2026-06-14 Task Board (전략 다국어 번역 격리)

## 진행 작업 목록

- `[x]` 의존성 패키지 등록 (`PyYAML` 추가)
- `[x]` `backend/app/translations/strategies.yml` 생성 (70여개 전략의 ko/en 명칭 정의)
- `[x]` `backend/app/translations/translator.py` 구현
  - 앱 시작 시 YAML 로드 및 캐시
  - `translate_strategy(strategy_type, locale)` 구현
  - 기존 `stock_translations` 내 `STRATEGY:%` 찌꺼기 데이터 정리(Clean-up) 로직 구현
- `[x]` `backend/app/bot/multi_strategy_manager.py` 리팩토링
  - `_get_name_for_strategy`가 `Translator.translate_strategy`를 사용하도록 정돈
  - 하드코딩된 대리 맵 제거 및 DB 임시조회 제거
- `[x]` `backend/app/translations/router.py` 내 `GET /api/translations/strategies` API 엔드포인트 구현
- `[x]` 검증 단계
  - 문법 및 컴파일 체크 (`python -m py_compile`)
  - 단위 테스트 작성 및 통과 검증 (`test_strategy_translations.py`)

- `[/]` 로그인 기능 및 2026년 6월 커밋 회귀·결함 점검
  - 로그인/세션/권한 경로의 백엔드·프론트엔드 구현과 테스트 재검토
  - 6월 변경분의 보안, 데이터 격리, 마이그레이션, 런타임 회귀 위험 점검
  - 확인된 결함 리팩터링 및 회귀 테스트 보강
  - 백엔드 컴파일·테스트와 프론트엔드 ESLint·TypeScript 검증
  - 수정: 로그인·회원가입 실패 응답이 silent refresh를 실행하던 Axios 401 인터셉터 범위 오류
  - 수정: refresh 요청에 타임아웃이 없어 초기 인증 화면이 무기한 대기할 수 있던 문제
  - 수정: bcrypt 72바이트 초과 비밀번호가 회원가입·비밀번호 변경에서 500 오류를 만들던 문제
  - 수정: 비-Alembic 기존 DB를 `head`로 stamp하여 인증·주문 마이그레이션을 건너뛰던 문제
  - 수정: 실행 디렉터리에 따라 Alembic `script_location`을 찾지 못하던 문제
  - 검증: 백엔드 `py_compile` 통과, pytest 93건 통과
  - 검증: 프론트엔드 ESLint·TypeScript·프로덕션 빌드 통과, Playwright E2E 7건 통과
  - 검증: 통합 하네스 6단계 및 NFC 검사 통과
  - 후속 검토: 전체 사용자 관심종목을 글로벌 스캐너 시드에 합치는 멀티테넌시 경계 재설계
  - 수정: Playwright 로그인 실패 테스트가 초기 refresh 완료 전 라우트를 교체해 인증 상태가 경합하던 테스트 결함

- `[/]` 배틀 아레나 (종합 백테스트 대항전) 캐시 경로 및 최초 로드 날짜 전송 개선
  - `[x]` 백엔드 라우터([router.py](file:///d:/dev/workspace/stockAuto/backend/app/admin/router.py)) 하드코딩 경로 제거 및 날짜 검증 구현
  - `[x]` 백엔드 시뮬레이터([backtest_runner.py](file:///d:/dev/workspace/stockAuto/backend/app/admin/backtest_runner.py)) 캐시 경로를 프로젝트 상대 경로로 다이내믹 변경
  - `[x]` 프론트엔드([BacktestTournament.tsx](file:///d:/dev/workspace/stockAuto/frontend/components/admin/BacktestTournament.tsx)) 마운트 시 기본 날짜(2025년) 쿼리 전송 및 Tooltip 타입 수정
  - `[x]` 백엔드 컴파일 검증 및 프론트엔드 Lint & TypeScript 무결성 테스트 완료
  - `[/]` 더블체크 보완 완료: 날짜 형식·순서 검증, 안전한 해시 캐시 키, 최초 요청 취소 및 경합 방지
  - `[/]` 라우터·캐시 경로 회귀 테스트와 프론트엔드 재검증 통과, 사용자 승인 대기

- `[/]` 글로벌 스캐너 멀티테넌시 경계 재설계 (`STO-8`)
  - `[/]` 시장 공용 시드에서 사용자별 관심종목 제거
  - `[/]` 사용자 자동매매 흐름에서 본인 관심종목만 명시적으로 결합
  - `[/]` 관리자·백테스트 등 공용 실행 경로의 데이터 범위 명확화
  - `[/]` 사용자 간 관심종목 혼합 방지 회귀 테스트 통과
  - `[/]` 구현 및 검증 완료, 사용자 승인 대기

- `[/]` 해외시장 거래 시간 확장 (프리마켓·정규장·에프터장, `STO-9`)
  - `[/]` 미국 거래일·조기 종료일을 반영한 시장 세션 판정 재검증
  - `[/]` 비정규장 신규 매수 비중 50% 축소
  - `[/]` KIS 공식 규격 재검증: `32`/`34`는 세션 코드가 아닌 LOO/LOC이므로 활성 세션은 현재가 지정가 `00` 사용
  - `[/]` 폐장 세션 자동 매수·매도 차단
  - `[/]` 수동 청산·자동매매·주문 재조정 계약 회귀 테스트 통과
  - `[/]` MOCK/SIMULATED 동작 및 백엔드 전체 검증 통과
  - `[/]` 구현 및 검증 완료, 사용자 승인 대기
  - `[/]` 시장 세션 문자열을 `MarketSession` Enum과 이름 있는 시간 상수로 단일화

## 최종 검증

- 백엔드 수정 파일 `py_compile` 통과
- 백엔드 전체 `pytest` 통과
- 프론트엔드 ESLint 및 TypeScript 오류 0건
- Next.js 프로덕션 빌드 통과
- Playwright E2E 7건 통과
- 수정 파일 19개 NFC 검사 통과
