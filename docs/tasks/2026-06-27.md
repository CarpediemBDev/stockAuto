# 2026-06-27 작업 현황판

## 1. 관리자 설정 API 리다이렉트 회귀 GitHub Issue 등록 및 종료
- **상태:** `[x]`
- **작업 설명:** `/admin/settings`의 거래 모드 카드 미표시 원인을 GitHub Issue로 등록하고, 원인과 해결 방법을 공유한 뒤 이미 수정 완료된 이슈로 close한다.

### 변경 영향 기록
- 변경 분류: [Process] 해결 완료 버그 이슈 원장화
- 생산자: GitHub Issue
- 소비자: 다음 작업자, 버그 원인 추적자
- 데이터 경계: 코드 변경 없음. GitHub 이슈 메타데이터만 생성/종료
- API 계약: 변경 없음
- 문서 반영: GitHub Issue #10 생성 및 `completed` 종료 상태 기록
- 회귀 테스트: 코드 변경 없음. 전일 검증 결과를 이슈 본문에 기록
- 미해결 위험: 없음
- 인수인계: GitHub Issue [#10](https://github.com/CarpediemBDev/stockAuto/issues/10)에 증상, 원인, 해결 방법, 검증 결과를 공유했고 `completed` 사유로 close 완료.

## 2. 리뷰 지적 사항 후속 보정
- **상태:** `[x]`
- **작업 설명:** 전일 현황판 번호 중복과 관리자 설정 API OpenAPI 스키마 노출 경로 불일치를 리뷰 지적대로 수정한다.

### 변경 영향 기록
- 변경 분류: [Bugfix] 문서 번호 및 API 명세 정합성 보정
- 생산자: `docs/tasks/2026-06-26.md`, `backend/app/admin/router.py`
- 소비자: 다음 작업자, Swagger/OpenAPI 명세 사용자, 개인 투자 설정 화면
- 데이터 경계: 사용자 데이터 변경 없음. 인증 토큰을 포함한 `/api/v1/admin` 요청 경계는 유지
- API 계약: `/api/v1/admin`을 주력 명세 경로로 노출하고 `/api/v1/admin/`는 호환 경로로 유지하되 스키마에서 숨긴다.
- 문서 반영: 당일 현황판에 작업 및 검증 결과 기록
- 회귀 테스트: 백엔드 admin 라우터 py_compile, 프론트 lint/typecheck 통과
- 미해결 위험: 없음
- 인수인계: `docs/tasks/2026-06-26.md`의 중복 번호를 `## 5.`로 보정했고, `/api/v1/admin`을 OpenAPI 기본 노출 경로로 유지하도록 admin 라우터 데코레이터를 정리했다.

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\admin\router.py` 통과
- `npm run lint` 통과
- `npx tsc --noEmit` 통과

## 3. 토스증권 거래 모드 지원 범위 차단
- **상태:** `[R]`
- **작업 설명:** 개인 투자 설정 화면에서 토스증권 선택 시 StockAuto가 검증하지 않은 MOCK/REAL 거래 모드를 선택·저장·실행하지 못하도록 브로커별 지원 모드 계약을 정리한다.

### 변경 영향 기록
- 변경 분류: [Safety] 브로커별 거래 모드 capability 계약 및 UI/API 검증 보강
- 생산자: `backend/app/bot/broker_factory.py`의 브로커 카탈로그와 `get_broker_client`, `backend/app/admin/router.py`의 설정 저장·인증 검증 API
- 소비자: `frontend/app/admin/settings/page.tsx` 개인 투자 설정 화면, 자동매매 브로커 팩토리, 관리자 설정 API 사용자
- 데이터 경계: 사용자별 `UserSettings.trade_mode`, `UserSettings.broker_provider`, `BrokerCredential.verified_trade_mode` 조합 검증. 기존 저장값은 자동 변경하지 않고 저장·실행 진입에서 차단한다.
- API 계약: `/api/v1/admin/settings` 응답의 `available_brokers.supported_modes`가 실제 지원 가능 모드만 노출되며, 미지원 조합 저장·인증 검증은 400 오류로 차단한다.
- 문서 반영: 당일 현황판 및 장기 시스템 문서의 브로커 확장 설명을 실제 지원 범위와 맞춘다.
- 회귀 테스트: 브로커 카탈로그 단위 테스트, admin 라우터 py_compile, 프론트 lint/typecheck, 전체 verify_harness 통과
- 미해결 위험: 토스증권의 공식 실전/모의 지원 정책이 바뀌면 capability 값을 재검토해야 한다.
- 인수인계: 토스증권은 현재 StockAuto 내에서 `SIMULATED`만 허용하고, KIS는 기존 3모드 지원을 유지한다. 사용자 최종 승인 전까지 `[R]` 상태를 유지한다.

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\bot\broker_factory.py backend\app\admin\router.py backend\tests\test_trading_catalog.py` 통과
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_trading_catalog.py -q` 통과: `4 passed`
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `backend\venv\Scripts\python.exe scripts\verify_harness.py` 통과: 백엔드 pytest `171 passed`, Playwright E2E `8 passed`

## 4. 해외 정규장 기반 에프터장 상승 후보 스캐너 MVP
- **상태:** `[R]`
- **작업 설명:** 해외 정규장 흐름을 점수화하고 에프터장 확인 신호를 결합해 상승 가능 후보를 랭킹으로 제공하는 스캐너 MVP를 구현한다. 초기 버전은 자동매수 없이 후보, 점수, 근거, 위험 플래그를 API와 프론트 화면에 표시한다.

### 변경 영향 기록
- 변경 분류: [Feature] 해외 스캐너 API 계약 및 프론트 표시 경로 확장
- 생산자: `backend/app/scanner/after_hours_scanner.py`, `backend/app/scanner/data_provider.py`, `backend/app/scanner/router.py`
- 소비자: `frontend/app/scanner/page.tsx`, `frontend/components/AfterHoursScanner.tsx`, `frontend/components/ScannerTabs.tsx`, 사용자 스캔 화면
- 데이터 경계: 공용 해외 시장 데이터 기반 후보 랭킹이며 사용자별 관심종목 데이터와 결합하지 않는다.
- API 계약: `GET /api/v1/scanner/after-hours-candidates`, `POST /api/v1/scanner/after-hours-candidates/refresh`를 추가하고 응답에 `scope`, `sync_status`, `updated_at`, `universe_size`, 후보별 `score`, `signal_type`, `reasons`, `risk_flags`, `catalyst_keywords`, `details`를 포함한다.
- 문서 반영: `docs/API_STANDARD.md`, `docs/SCANNER_DATA_FLOW.md`, 당일 현황판에 공용 후보 랭킹 경계를 기록했다.
- 회귀 테스트: 수정 Python 파일 py_compile, 스캐너 라우터 테스트, 프론트 lint/typecheck, 전체 verify_harness 통과
- 미해결 위험: 실제 에프터장 분봉/호가 제공 범위가 브로커·데이터 소스별로 다를 수 있어 MVP는 가용 데이터 기반 추정 신호로 동작할 수 있다.
- 인수인계: 에프터장 후보는 `/scanner/latest` 자동매매 후보와 섞지 않는 공용 관찰용 캐시다. UI는 마켓 스캐너의 세 번째 탭 `에프터장 후보`에서 수동 갱신하며, 사용자 최종 승인 전까지 `[R]` 상태를 유지한다.

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\scanner\data_provider.py backend\app\scanner\after_hours_scanner.py backend\app\scanner\router.py backend\tests\test_scanner_router.py` 통과
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_scanner_router.py -q` 통과: `13 passed`
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `npx playwright test scanner-complex.spec.ts` 통과: `1 passed`
- `backend\venv\Scripts\python.exe scripts\verify_harness.py` 통과: 백엔드 pytest `174 passed`, Playwright E2E `8 passed`

## 5. 운영 오픈 전 전체 품질 풀스캔
- **상태:** `[R]`
- **작업 설명:** 운영 오픈 전 치명 오류, 프로세스 누락, 계산 오류, API/프론트 계약 불일치, 테스트 하네스 실패 여부를 백엔드·프론트엔드·문서 계약·배포 절차 관점에서 전체 감사한다.

### 변경 영향 기록
- 변경 분류: [Audit] 배포 전 품질 검증 및 리스크 식별
- 생산자: 현재 코드베이스 전체, 최근 변경된 스캐너·관리자 설정·거래 모드 관련 파일
- 소비자: 운영 사용자 화면, API 라우터, 자동매매 스케줄러, 테스트 하네스, 배포 전 검증 절차
- 데이터 경계: 코드 변경 없음. 사용자별 데이터, 공용 스캐너 캐시, 거래 모드 경계가 실제 테스트와 정적 검사로 유지되는지 확인
- API 계약: 코드 변경 전 감사 단계. 라우터 응답 포장, 프론트 API 타입, 신규 스캐너 엔드포인트 일치 여부 확인
- 문서 반영: 감사 결과와 검증 명령 결과를 당일 현황판에 기록. 실제 코드 수정이 필요한 결함 발견 시 별도 항목으로 분리
- 회귀 테스트: 백엔드 py_compile/pytest/verify_harness, 프론트 npm lint/typecheck/build, 핵심 E2E 및 정적 검색
- 미해결 위험: Redis 실연동 검증은 로컬/CI Redis 서비스가 없으면 skip될 수 있음. 프론트 스캐너 API 실패 상태와 에프터장 탭 E2E 상세 검증은 후속 품질 개선 후보로 남김
- 인수인계: 풀스캔 결과 운영 차단급으로 분류한 Docker prod 프로필, 스윙 레거시 캐시, 에프터장 갱신 부하/쓰기 경계는 항목 6에서 보수 완료. 다음 AI는 항목 6 수정 파일과 검증 결과를 먼저 확인

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\scanner\swing_prediction_cache.py backend\app\scanner\after_hours_scanner.py backend\app\scanner\router.py backend\app\translations\translator.py backend\tests\test_scanner_router.py backend\tests\test_release_artifacts.py` 통과
- `backend\venv\Scripts\python.exe -m pytest backend\tests -q` 통과: `178 passed`
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `npm run build` 통과
- `backend\venv\Scripts\python.exe scripts\verify_harness.py` 통과: 백엔드 pytest `178 passed`, Playwright E2E `8 passed`

## 6. 운영 오픈 전 차단급 감사 결함 보수
- **상태:** `[R]`
- **작업 설명:** 풀스캔에서 발견된 운영 차단급 리스크를 우선 보수한다. 대상은 Docker 운영 프로필 기본값, 스윙 예측 레거시 캐시 응답 호환성, 에프터장 후보 갱신의 외부 호출/DB 쓰기 경계 및 반복 갱신 부하 제어다.

### 변경 영향 기록
- 변경 분류: [Safety] 운영 배포 프로필·API 응답 호환성·공용 스캐너 부하 경계 보강
- 생산자: `backend/Dockerfile`, `backend/.env.prod.example`, `backend/app/scanner/swing_prediction_cache.py`, `backend/app/scanner/after_hours_scanner.py`, `backend/app/scanner/router.py`
- 소비자: Docker 기반 백엔드 운영 배포, `/api/v1/scanner/swing-predict`, `/api/v1/scanner/after-hours-candidates`, `/scanner` 프론트 화면, 공용 yfinance/translation/DB 리소스
- 데이터 경계: 사용자별 데이터 변경 없음. 공용 스윙 스냅샷 후보는 레거시 필드 누락 시 기본값으로 정규화하고, 에프터장 후보 이름은 메모리/DB 캐시 조회까지만 허용해 자동 번역 DB 쓰기를 차단
- API 계약: 기존 응답 필드를 유지하되 레거시 스윙 후보의 필수 숫자·불리언 필드를 기본값으로 보강하고, 에프터장 refresh는 서버 TTL 안에서는 새 외부 스캔 대신 캐시를 반환
- 문서 반영: 당일 현황판에 감사 결과와 검증 결과 기록. 장기 문서는 운영 프로필/스캐너 계약 변경 범위 확인 후 필요 시 갱신
- 회귀 테스트: 관련 py_compile, 스캐너 pytest, 프론트 lint/typecheck/build, 전체 verify_harness
- 미해결 위험: Docker 이미지 실제 빌드/기동 smoke와 Redis 실서비스 연결 검증은 배포 환경에서 별도 확인 필요
- 인수인계: `backend/Dockerfile`은 `APP_ENV=prod`/기본 `PORT=8000`을 명시한다. 스윙 후보 캐시는 `normalize_swing_candidate(s)`로 레거시 최소 스냅샷을 UI 계약 필드로 보강한다. 에프터장 후보는 `Translator.translate_cached`만 사용하고 최신 캐시 TTL 안에서는 외부 스캔을 생략한다.

### 검증 결과
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_release_artifacts.py backend\tests\test_scanner_router.py -q` 통과: `17 passed`
- `backend\venv\Scripts\python.exe -m pytest backend\tests -q` 통과: `178 passed`
- `npm run build` 통과
- `backend\venv\Scripts\python.exe scripts\verify_harness.py` 통과: 백엔드 pytest `178 passed`, Playwright E2E `8 passed`
