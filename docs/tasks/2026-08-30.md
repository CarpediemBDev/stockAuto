# 2026-08-30 Task Board

## 신규 작업
- [R] 승인대기 항목 일괄 승격 (사용자 지시 "순서대로 처리해" → 질의 응답 "12건 전부 승인"): 2026-08-02·08-16·08-17·08-23 보드의 `[R]` 17건과 08-17의 중복 `[/]` 1건(F1~F4, 같은 파일에 [R]로도 적혀 있어 상태가 어긋나 있었음)을 `[x]`로 승격하고 승인 주석을 부착한다. 7월 보드의 미승인 `[R]` 83건은 사용자에게 보고한 범위가 아니므로 손대지 않는다.
- [R] 4세대 AI Graph Engineering 도입 정본 신설 및 선결 과제 7건 반영 (GitHub Issue #97, 08-17 보드의 미착수 `[ ]` 1건 + 미완 `[/]` 1건 이행): docs/AI_GRAPH_ENGINEERING.md가 어느 커밋에도 존재한 적이 없어(git log --all 무결과) 08-17에 기록된 "정본에 반영" 작업이 대상 파일 없이 방치돼 있었다. Issue #97 원안에 설계 검토 코멘트의 선결 과제 7건을 반영한 정본을 신설한다 — Phase 1을 관측·설명 레이어로 재정의, Aggregator 고정 가중치 제거 및 전략 calculate_score 위임(SSOT 복원), max_iterations 순환 가드, "결정론적" 검증 기준 폐기 후 V1~V5 대체, AI 쿼터·타임아웃 재설계 규약, Phase 2 진입 게이트(IC 비교)와 fail-close 정책, 런타임 비용·손익분기 산정.

### 변경 영향 기록
- 변경 분류: 문서 계층 전용 (현황판 상태 승격 + 아키텍처 정본 신설 + 교차 참조 정정). 제품 코드 변경 없음
- 생산자: docs/AI_GRAPH_ENGINEERING.md(신설), docs/README.md(상시 유지 문서 표 1행 등록), docs/strategy_alpha_verdict.md(§10 참조 항목을 미병합 우회 문구에서 실제 링크로 교체), docs/tasks/2026-08-02.md, docs/tasks/2026-08-16.md, docs/tasks/2026-08-17.md, docs/tasks/2026-08-23.md, docs/tasks/2026-08-30.md
- 소비자: AI 그래프 Phase 1 착수 판단, docs/README.md 색인을 통한 참조 경로, GitHub Issue #97 후속 논의, 다음 세션의 잔여 부채 판독
- 데이터 경계: 문서 계층 한정. 백엔드·프론트엔드·DB 스키마·마이그레이션·API 전부 무변경. 개발 DB 접근 없음
- API 계약: API 스키마 변경 없음
- 문서 반영: docs/AI_GRAPH_ENGINEERING.md, docs/README.md, docs/strategy_alpha_verdict.md, docs/tasks/2026-08-02.md, docs/tasks/2026-08-16.md, docs/tasks/2026-08-17.md, docs/tasks/2026-08-23.md, docs/tasks/2026-08-30.md
- 회귀 테스트: python scripts/verify_harness.py 전체 통과. 문서에 인용한 코드 사실은 전부 실측 확인함 — backend/app/ai/gemini_client.py:10 DEFAULT_GEMINI_TIMEOUT_SECONDS = 8.0, backend/app/scanner/scanner.py:85-86 AI_NEWS_GUARANTEED_SLOTS=3 / AI_NEWS_MAX_SLOTS=10, backend/app/core/models.py:384 class SwingScoreOutcome, backend/app/scanner/score_calibration.py 모듈 주석의 미검증 진술, backend/app/bot/order_reconciler.py 존재. 이슈 코멘트가 market_session.py 경로를 상위로 적었으나 실제 경로는 backend/app/bot/market_session.py임을 확인해 문서에 정정 반영함. 승격 대상 18건은 승격 후 4개 보드에 잔여 [R]·[/]이 0건임을 재검색으로 확인함
- 미해결 위험: 본 작업은 설계 문서까지이며 StateGraph 코드는 한 줄도 작성하지 않았다. 문서 §12의 선결 과제 4·5·6(AI 쿼터 정책 재설계, 심층감사 노드 타임아웃 30초 이상 재산정, 스윙 예측·캘리브레이션 잡 Batch API 전환)은 코드 변경이 필요한 미해결 상태이며 그중 4·5는 Phase 1 착수의 선행 조건이다. 비용 수치는 공개 단가와 코드상 스케줄 주기에 기반한 추정치라 실제 호출량에 따라 변동한다. 7월 보드의 미승인 [R] 83건은 이번 승격 범위 밖이라 그대로 남아 있어, 현황판 전체를 훑는 다음 세션이 이를 진행 중 작업으로 오독할 수 있다
- 인수인계: docs/AI_GRAPH_ENGINEERING.md는 명세가 아니라 범위 제한 문서다. §0이 Phase 2 착수 금지를, §8이 해제 조건(SwingScoreOutcome 기반 IC·히트레이트 비교 + strategy_alpha_verdict.md §6 반증 조건 R1~R6)을 규정하며, 이 두 절은 strategy_alpha_verdict.md §7 금지 항목에 종속된다. Phase 1을 실제로 착수하려면 문서 §12의 4·5를 먼저 처리해야 한다. 다음 시작 지점은 사용자에게 보고한 잔여 부채 3번 묶음 — 폴백 치환 검출 가드, 전략별 기대상태 스냅샷 회귀 가드, 청산 경로 필드 누락 11종, volume_ma20 기본값 퇴화 잠복(gex_pinning.py:27, chaikin_atr.py:31), 챔피언십 34매치 미실행, Memurai 중지로 미검증된 pytest 6건

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 신규 작업 3
- [R] 청산 결손 5종 신규 진입 차단 (사용자 지시 "새로 사는 것만 막는 쪽으로 진행해"): asqs·macro_momentum·strategy_c·strategy_b·exploded_c는 진입은 되는데 시그널 청산이 죽어 손절·트레일링으로만 정리된다. 기존 보유분(각 3·3·5·3·2건)은 건드리지 않고 신규 진입만 막는다. is_selectable=0은 카탈로그 조회와 전략 변경 검증에서만 쓰이고 스케줄러는 보지 않으므로, 이미 그 전략으로 설정된 계정 5개(각 1개씩 존재)의 매수를 멈추려면 진입 채점 경로 차단이 함께 필요하다. 착수 시점에는 3종으로 파악했으나, 청산 결손 관측 범위를 선택가능 전략에서 카탈로그 전체로 넓히자 이미 선택 불가 상태이던 strategy_b와 exploded_c도 같은 결함으로 매수 중임이 드러나 범위를 5종으로 확장했다.

### 변경 영향 기록
- 변경 분류: 라이브 매매 동작 변경 (청산 결손 전략의 신규 진입 차단). 청산 경로 무변경
- 생산자: backend/app/scanner/signal_contract.py(ENTRY_BLOCKED_STRATEGIES 신설), backend/app/bot/scheduler.py(process_entry_signals 진입 채점 전 차단), backend/alembic/versions/c8d1e5f20b74_block_exit_degraded_strategies.py(신설), backend/tests/test_live_entry_signal_contract.py(불변식 테스트 3건 교체·추가), backend/tests/strategy_entry_states.json(상태 5종 갱신)
- 소비자: backend/app/bot/scheduler.py 매수 경로, backend/app/strategy_catalog/router.py 카탈로그 조회, backend/app/admin/router.py 전략 변경 검증
- 데이터 경계: strategies 테이블의 is_selectable 컬럼 3행(asqs, macro_momentum, strategy_c). 마이그레이션 c8d1e5f20b74(head, down_revision b7c3d9e14a20). holdings·trade_logs·user_settings 무변경. strategy_b와 exploded_c는 이미 is_selectable=0이라 DB 변경 대상이 아니고 스케줄러 차단만 적용했다
- API 계약: API 스키마 변경 없음. 카탈로그 응답에서 3종이 빠지는 데이터 변화만 있다
- 문서 반영: docs/tasks/2026-08-30.md
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과. test_live_entry_signal_contract.py 21건 통과. 차단 배선을 AST로 검증하는 테스트를 추가하고 네거티브 컨트롤로 확인했다 - 스케줄러의 차단 조건을 무력화하자 "매수 경로가 ENTRY_BLOCKED_STRATEGY_SET을 보지 않는다"로 반려됐다. 차단 위치도 실측 확인했다 - 차단은 process_entry_signals의 1605행이고 진입 채점은 같은 함수 1641행이라 채점보다 앞서며, 청산 채점은 process_exit_signals의 1016행이라 무손상이다. 기대 상태 스냅샷은 진입가능 73에서 68로 줄고 차단-청산결손 5종이 신설됐으며 변경된 5종이 정확히 대상과 일치했다
- 미해결 위험: 차단 5종은 기존 보유분(asqs 3, macro_momentum 3, strategy_c 5, strategy_b 3, exploded_c 2)을 그대로 들고 있고, 이 포지션들은 여전히 시그널로 청산되지 않아 손절·트레일링에만 의존한다. 사용자 지시가 "새로 사는 것만 막는" 것이므로 의도된 상태지만, 청산 경로 배선이나 수동 정리 전까지 이 보유분은 자동 청산 수단이 제한된다. 또한 차단된 5종에 계정이 붙어 있어 그 계정들은 앞으로 거래 0건이 되며, 관리자 화면에서는 "봇은 도는데 거래가 없는" 상태로 보인다
- 인수인계: is_selectable=0은 카탈로그 조회(strategy_catalog/router.py)와 전략 변경 검증(admin/router.py)에서만 쓰이고 스케줄러는 보지 않는다. 그래서 카탈로그 차단만으로는 이미 그 전략으로 설정된 계정의 매수가 멈추지 않으며, 실제 차단은 signal_contract.py의 ENTRY_BLOCKED_STRATEGY_SET을 스케줄러가 보고 수행한다. 이 사실을 모르고 마이그레이션만 추가하면 아무것도 막히지 않는다. 차단 해제는 청산 경로에 필요한 필드를 배선한 뒤 ENTRY_BLOCKED_STRATEGIES에서 빼고 스냅샷을 갱신하는 순서다. strategy_b와 exploded_c는 이번 실측에서 새로 드러났다 - 선택 불가 상태라 선택가능 전략만 훑던 기존 측정에서 빠져 있었고, 청산 결손 관측 범위를 카탈로그 전체로 넓히자 나타났다

## 신규 작업 2 (잔여 부채)
- [R] 폴백 치환 검출 가드 배선 — 판정 문서 §8이 "자동 검출 수단이 없다"고 남긴 마지막 구멍을 막는다. 필드 존재 검사는 폴백 경로를 통과한다(값이 실제로 채워지므로). 전략은 정상 채점되지만 측정 대상이 바뀌며, opening_range_breakout이 orb_high_30m 대신 BB_upper를 읽던 시절 이름은 ORB인데 실제로는 볼린저 돌파를 재고 있었다. check_signal_field_contract.py에 AST 검출 2종(기본값 자리 대체, `if X == 0.0` 후 재대입)을 추가하고 signal_contract.py의 FALLBACK_SUBSTITUTIONS 선언과 목록 차분으로 대조한다.
- [R] 전략별 기대 상태 스냅샷 회귀 가드 배선 — 판정 문서 §8이 "§4 결함을 구조적으로 재발 방지하는 유일한 장치"로 지목한 가드. 종수 임계값이 아니라 목록 차분으로 설계했다(퇴화 3종 차단이라는 정당한 수정이 미진입 종수를 73→76으로 늘렸던 전례 때문). backend/tests/strategy_entry_states.json 스냅샷과 scripts/update_strategy_entry_states.py 갱신기를 신설한다.
- [R] 청산 경로 결손 실측 및 회귀 가드 — 08-23 인수인계의 "누락 11종"을 재측정하니 지표 SSOT 통합 효과로 4종으로 줄어 있었다. 그중 3종(asqs, macro_momentum, strategy_c)은 진입은 되는데 시그널 청산이 죽은 상태이며, 이 조합은 기존 카탈로그 차단(is_selectable=0) 그물에 걸리지 않는다. 목록을 가드로 고정한다.
- [R] volume_ma20 기본값 퇴화 잠복 종결 확인 — 08-23이 미해결 위험으로 남긴 chaikin_atr·gex_pinning의 기본값 1.0 패턴은 실측 결과 두 파일 모두 이미 volume_baseline 존재 확인 가드가 들어가 있어 종결 상태다(opening_range_breakout 포함 3종 전부). 위험 노트만 낡았다.
- [R] verify_harness 타임아웃 예산 2건 상향 (pytest 240초→600초, Playwright E2E 180초→420초) — 전체 pytest 스위트 실측 소요가 약 250초로 종전 예산을 넘겨 TimeoutExpired로 죽고 있었고, E2E는 8건 통과에 2.7분(약 162초)이 걸려 180초 예산에 아슬아슬하게 걸쳐 같은 커밋에서 통과와 실패가 번갈아 나왔다. 타임아웃으로 죽으면 실패 원인이 로그에 남지 않아 진단 자체가 불가능해지고, 테스트 실패와 예산 초과가 구분되지 않는다.

### 변경 영향 기록
- 변경 분류: 계약 회귀 가드 신설 (폴백 치환 검출, 전략별 기대 상태 차분, 청산 경로 결손 고정) 및 검증 하네스 예산 조정. 전략 채점 로직 변경 없음
- 생산자: scripts/check_signal_field_contract.py(폴백 치환 AST 검출 2종 및 선언 대조 추가), backend/app/scanner/signal_contract.py(FALLBACK_SUBSTITUTIONS 신설), scripts/update_strategy_entry_states.py(신설), backend/tests/strategy_entry_states.json(신설), backend/tests/test_live_entry_signal_contract.py(회귀 테스트 6건 추가), scripts/verify_harness.py(pytest 및 Playwright E2E 타임아웃 상향), docs/strategy_alpha_verdict.md(§8 후속 과제 완료 반영)
- 소비자: scripts/verify_harness.py Step 2 및 Step 6, 전략 신규 추가·수정 시의 계약 검사 경로, 다음 세션의 퇴화 판독
- 데이터 경계: 정적 계약 검사 계층과 테스트 계층. 전략 채점식·DB 스키마·API 스키마·마이그레이션 전부 무변경. 스냅샷 생성기는 strategies 테이블을 읽기만 한다
- API 계약: API 스키마 변경 없음
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과(pytest 전건 통과, Playwright E2E 통과). 신규 테스트 6건 포함 test_live_entry_signal_contract.py 19건 통과. 네거티브 컨트롤 3종을 실제로 실행해 반려를 확인했다 — (가) FALLBACK_SUBSTITUTIONS에서 선언 1건 제거 시 "선언되지 않은 폴백 치환"으로 반려, (나) 코드에 없는 유령 선언 추가 시 "제거 필요"로 반려, (다) opening_range_breakout에 orb_high_30m->BB_upper 폴백을 되살리자 정확히 그 치환을 지목해 반려. 기대 상태 스냅샷도 유령 전략 주입으로 반려를 확인했다. 폴백 치환 실측 9건이며 원본이 전부 라이브 가용이라 대체 측정 중인 전략은 0건. 기대 상태 실측은 진입가능 73 / 미진입-지표누락 20 / 미진입-의도차단 4 / 자율 3. 청산 경로 결손 실측은 4종
- 문서 반영: docs/strategy_alpha_verdict.md(§8 가드 2번 항목 및 후속 과제 절 갱신), docs/tasks/2026-08-30.md
- 미해결 위험: 진입 가능 + 시그널 청산 결손 3종(asqs, macro_momentum, strategy_c)은 가드로 고정만 했고 고치지 않았다. 이 전략들은 라이브에서 포지션을 잡은 뒤 시그널로 빠져나오지 못하며 손절·트레일링으로만 정리된다. 해소책은 두 갈래이고 성격이 다르다 — (가) 카탈로그 차단(is_selectable=0, 기존 마이그레이션 b7c3d9e14a20 선례)은 신규 진입만 막고 기존 보유분은 그대로 두며, (나) 청산 경로 fail-safe 전환은 결손 시 홀딩이 아니라 청산 쪽으로 판정을 뒤집어 기존 보유분을 즉시 정리한다. 어느 쪽이든 실거래 동작이 바뀌므로 사용자 결정이 필요해 이번 범위에서 제외했다. 또한 Redis(Memurai) 윈도우 서비스는 여전히 중지 상태이고 권한 부족으로 기동할 수 없어, 본 검증은 memurai.exe를 사용자 프로세스로 직접 띄운 상태에서 수행했다. 재부팅하면 사라지므로 관리자 권한으로 서비스를 기동해야 영구 해소된다
- 인수인계: 폴백 치환은 signal_contract.py의 FALLBACK_SUBSTITUTIONS가 단일 원장이며 "전략파일:원본필드->대체필드" 형식이 AST 검출 결과와 정확히 일치해야 한다. 치환을 추가·제거하면 가드가 반려하므로 무엇을 실제로 재고 있는지가 항상 diff에 남는다. 원본이 UNSUPPORTED/PENDING인 치환은 가드가 "대체 측정 중"으로 경고하되 반려하지는 않는다(선언돼 있으면 의도된 상태이므로). 기대 상태 스냅샷은 손으로 고치지 말고 python scripts/update_strategy_entry_states.py로 갱신하며, 갱신 시 사유를 당일 현황판에 반드시 남긴다 — 사유 없는 갱신은 이 가드를 무의미하게 만든다. 상태 판정 로직은 테스트 파일이 단독 소유하고 갱신기는 그것을 호출만 하므로, 판정 기준이 두 벌로 갈라지지 않는다. 다음 시작 지점은 위 미해결 위험의 (가)/(나) 결정과, 사용자에게 보고한 챔피언십 34매치 미실행 건이다
