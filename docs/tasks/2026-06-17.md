# 2026-06-17 작업 현황판

## 작업 목록

- [x] 스캐너 화면 500 오류 로그 분석 및 조치
  - 실제 원인 정정: 신규 상장/거래정지 주식의 지표(VWAP, EMA120)가 `float('nan')`으로 할당되어 FastAPI `JSONResponse`에서 직렬화 실패(`ValueError`)로 인한 500 에러 발생
  - 기존 원인 정정: 스윙 예측 Playwright/greenlet 에러는 Fallback으로 정상 방어되어 500 에러와 무관함
  - 조치: `scanner.py` 내의 `float('nan')` 및 Pandas `NaN` 결측치를 `None`으로 안전하게 치환
  - 깃허브 이슈: #7
  - 검증: 백엔드 직접 `/api/v1/scanner/latest` 200 확인
  - 검증: 프론트 프록시 `/api/v1/scanner/latest` 200 확인
  - 검증: `/scanner` 페이지 200 확인
  - 추가 조치: 토스 Node/Puppeteer 실패, malformed JSON, Node 미설치 상황의 fallback 테스트 보강
  - 추가 검증: `pytest backend/tests/test_toss_scraper_runner.py backend/tests/test_scanner_router.py` 17건 통과
  - 추가 조치: Toss, Yahoo Finance, Naver 종목 수집을 `get_seed_tickers()` 단일 병합 경로로 통합
  - 정책: 세 소스 중 하나라도 성공하면 해당 종목으로 분석 진행, 전체 실패 시 안전 기본 리스트 사용
  - 추가 검증: `pytest backend/tests/test_toss_scraper_runner.py backend/tests/test_scanner_router.py backend/tests/test_scanner_scoring.py backend/tests/test_scanner_tenant_isolation.py` 22건 통과
- [x] 네이버 크롤러를 토스와 동일한 Node/Puppeteer subprocess 방식으로 통일
  - 조치: Python Playwright 기반 `playwright_scraper.py` 제거
  - 조치: `naver_scraper.js`와 `naver_scraper_runner.py` 추가로 토스와 동일한 Node/Puppeteer subprocess 구조 적용
  - 검증: Python Playwright/greenlet/async_playwright 앱 코드 참조 제거 확인
  - 검증: `py_compile`, `node --check backend/app/scanner/naver_scraper.js` 통과
  - 검증: `pytest backend/tests/test_naver_scraper_runner.py backend/tests/test_toss_scraper_runner.py backend/tests/test_scanner_router.py backend/tests/test_scanner_scoring.py backend/tests/test_scanner_tenant_isolation.py` 25건 통과
  - 검증: `npm run lint`, `npx tsc --noEmit` 통과
  - 추가 조치: 종목 발굴 로그를 `print`에서 `stockauto` logger로 통일하고 source별 count를 병합 단계에서 출력
  - 추가 조치: Python 래퍼 파일명을 `toss_scraper_runner.py`, `naver_scraper_runner.py`로 정리해 JS 스크래퍼와 실행 래퍼 역할을 구분
  - 추가 검증: `pytest backend/tests/test_toss_scraper_runner.py backend/tests/test_naver_scraper_runner.py backend/tests/test_scanner_tenant_isolation.py` 15건 통과
  - 추가 검증: `pytest backend/tests/test_toss_scraper_runner.py backend/tests/test_naver_scraper_runner.py backend/tests/test_scanner_router.py backend/tests/test_scanner_scoring.py backend/tests/test_scanner_tenant_isolation.py` 25건 통과
- [x] 85개 전략에 대한 유저 1:1 매핑 생성 및 가짜 매핑 방지 (총 85명 구성)
- [ ] 프론트엔드 전역 SWR 라이브러리 도입 및 백엔드 즉시 응답 캐싱 리팩토링 ([Linear STO-15](https://linear.app/stockauto/issue/STO-15/프론트-데이터-패칭백엔드-캐시-계약-표준화))
