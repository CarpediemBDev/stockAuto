# 2026-06-28 작업 현황판

## 6. 공통 기능 분산 구현 일괄 SSOT 정리
- **상태:** `[x]`
- **작업 설명:** 관심종목 등록, 프론트 API/액션 호출, 실현손익 계산처럼 동일 책임이 여러 파일에 흩어진 구현을 공통 서비스·훅·계산 함수로 모아 변경 지점을 축소한다.

### 변경 영향 기록
- 변경 분류: [Refactor] 기능 책임 SSOT화 및 API 계약 드리프트 제거
- 생산자: `backend/app/watchlist/services.py`, `backend/app/bot/trade_calculations.py`, `frontend/lib/api.ts`, `frontend/hooks/useWatchlistActions.ts`
- 소비자: 관심종목 라우터, 스캐너/관심종목 프론트 컴포넌트, 자동매매 매도 처리, 강제청산, 백테스트 계산, 대시보드 거래 로그 조회
- 데이터 경계: 사용자별 관심종목과 거래 로그 계산값의 기존 DB 스키마는 변경하지 않음
- API 계약: `/watchlist`, `/trades`, `/account/*`, `/admin/*`, `/report/*` 기존 계약을 유지하고 잘못된 `/trades/logs` 소비 경로를 제거
- 문서 반영: 영향 없음 - API/DB 계약 자체는 변경하지 않고 기존 엔드포인트 소비 경로와 내부 책임 분산만 정리했으므로 장기 계약 문서 변경은 생략
- 회귀 테스트: py_compile, 관심종목/주문/강제청산/거래흐름 pytest, 백테스트 pytest, 전체 backend pytest, frontend lint/typecheck/build, Playwright E2E 실행
- 미해결 위험: `scripts/verify_harness.py`의 계약 단계는 기존 스테이징된 스캐너 변경 파일이 우선 검사되어 실패. 나머지 compile/NFC/Alembic/pytest/frontend/E2E 단계는 통과
- 인수인계: 관심종목 등록은 `backend/app/watchlist/services.py`와 `frontend/hooks/useWatchlistActions.ts`, 실현손익 계산은 `backend/app/bot/trade_calculations.py`, 프론트 API mutation은 `frontend/lib/api.ts`를 기준으로 후속 변경

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\watchlist\router.py backend\app\watchlist\services.py backend\app\bot\trade_calculations.py backend\app\bot\order_reconciler.py backend\app\bot\scheduler.py backend\app\bot\backtest_engine.py backend\app\admin\backtest_runner.py backend\app\trades\router_account.py` 통과
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_api_integration.py::test_auth_and_watchlist_routes_share_isolated_test_database backend\tests\test_new_user_core_flow.py::test_new_user_watchlist_scan_and_multiple_order_resume_flow backend\tests\test_order_reconciler.py backend\tests\test_force_liquidation_safety.py backend\tests\test_trading_flow_scenarios.py -q` 통과
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_backtest_metrics.py backend\tests\test_backtest_tournament_safety.py backend\tests\test_backtest_data_range.py -q` 통과
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `npm run build` 통과
- `backend\venv\Scripts\python.exe scripts\verify_harness.py`는 계약 영향 기록 단계만 기존 스테이징된 스캐너 파일로 실패, 이후 backend compile/NFC/Alembic/179개 pytest/frontend static/8개 Playwright E2E 통과

## 5. 스윙 예측 볼린저 밴드폭 필드명 정정
- **상태:** `[R]`
- **작업 설명:** 스윙 예측 API의 `squeeze_pct` 필드명을 계산 의미가 명확한 `bollinger_band_width_percentile`로 변경하고, 생산자·캐시 정규화·프론트 소비자·E2E mock·테스트를 모두 동기화한다.

### 변경 영향 기록
- 변경 분류: [API 계약] 스윙 예측 후보 지표 필드명 정정
- 생산자: `backend/app/scanner/swing_predictor.py`, `backend/app/scanner/swing_prediction_cache.py`
- 소비자: `frontend/components/SwingPredictorCard.tsx`, `frontend/e2e/scanner-complex.spec.ts`, 스캐너 API 응답 테스트
- 데이터 경계: 기존 persisted swing snapshot의 `squeeze_pct`는 정규화 단계에서 신규 `bollinger_band_width_percentile`로 호환 변환
- API 계약: `squeeze_pct`를 `bollinger_band_width_percentile`로 대체. 의미는 현재 볼린저 밴드폭의 과거 대비 백분위이며 낮을수록 수축 강함
- 문서 반영: 당일 현황판 기록. 필요 시 API/데이터 흐름 문서 추가 반영
- 회귀 테스트: py_compile, 스캐너 pytest, 멀티테넌시 pytest, 프론트 lint/typecheck, Playwright scanner E2E, 전체 검색 통과
- 미해결 위험: `squeeze_pct`는 레거시 persisted snapshot 호환 변환과 본 작업 기록에만 남김
- 인수인계: API 신규 필드는 `bollinger_band_width_percentile`이다. 다음 AI는 `rg "squeeze_pct"` 결과가 레거시 호환 처리/테스트/현황판 설명 외에 남지 않는지 확인

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\scanner\swing_predictor.py backend\app\scanner\swing_prediction_cache.py backend\tests\test_scanner_router.py` 통과
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_scanner_router.py -q` 통과: `16 passed`
- `backend\venv\Scripts\python.exe -m pytest backend\tests\test_scanner_multitenancy.py -q` 통과: `2 passed`
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `npx playwright test scanner-complex.spec.ts` 통과: `1 passed`
- `rg "squeeze_pct|bollinger_band_width_percentile"` 확인 완료: 옛 필드명은 레거시 변환 및 현황판 설명에만 잔존

## 1. StockAuto 운영 리스크 검증 스킬 추가
- **상태:** `[R]`
- **작업 설명:** 단순 문법 오류가 아니라 운영 프로세스 오류, 거래 계산 실수, API/프론트 계약 불일치, 배포 런타임 리스크를 점검하는 Codex 개인 스킬 `stockauto-release-risk-auditor`를 추가한다.

### 변경 영향 기록
- 변경 분류: [Process] AI 검증 워크플로우 보강
- 생산자: Codex 개인 스킬 `stockauto-release-risk-auditor`
- 소비자: StockAuto 운영 오픈 전 감사, 계산 검증, 프로세스 검토를 수행하는 AI 에이전트
- 데이터 경계: 애플리케이션 런타임 데이터 변경 없음. 개인 Codex 스킬 디렉터리와 당일 작업 현황판만 변경
- API 계약: 변경 없음
- 문서 반영: 본 현황판에 스킬 추가 목적과 검증 결과 기록
- 회귀 테스트: 스킬 `quick_validate.py` 검증, 생성 파일 내용 확인
- 미해결 위험: 스킬은 개인 Codex 스킬 경로에 생성됨. 다른 장비/계정에서 사용하려면 동일 경로로 복사 또는 별도 설치 필요
- 인수인계: 다음 AI는 `$CODEX_HOME/skills/stockauto-release-risk-auditor/SKILL.md`를 읽고 운영 리스크 감사 시 해당 스킬을 사용. 반복 점검은 스킬의 `scripts/check_release_artifacts.py`, `scripts/check_numeric_invariants.py`, `scripts/check_contract_coverage.py`를 프로젝트 루트 인자로 실행

### 검증 결과
- `python C:\Users\Im\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\Im\.codex\skills\stockauto-release-risk-auditor` 통과
- `python C:\Users\Im\.codex\skills\stockauto-release-risk-auditor\scripts\check_release_artifacts.py D:\dev\workspace\stockAuto` 통과
- `python C:\Users\Im\.codex\skills\stockauto-release-risk-auditor\scripts\check_numeric_invariants.py D:\dev\workspace\stockAuto` 통과
- `python C:\Users\Im\.codex\skills\stockauto-release-risk-auditor\scripts\check_contract_coverage.py D:\dev\workspace\stockAuto` 통과

## 2. 에프터장 상승 후보 관심종목 및 매매전략 즉시 연동
- **상태:** `[R]`
- **작업 설명:** 마켓 스캐너 에프터장 후보 탭에서 포착된 종목을 관심종목(Watchlist) 등록 및 자동매수 감시 전략으로 즉시 전환할 수 있는 UX 및 연동 기능을 개발한다.

### 변경 영향 기록
- 변경 분류: [Feature] 스캐너 후보 관심종목 연동 UX 보강
- 생산자: `frontend/components/AfterHoursScanner.tsx`
- 소비자: 마켓 스캐너 에프터장 탭 사용자, 관심종목 API (`/watchlist`)
- 데이터 경계: 사용자별 관심종목(`Watchlist`) DB 등록 연동
- API 계약: `POST /watchlist` 기존 계약 재사용 (`{ ticker, ticker_name }`)
- 문서 반영: 본 현황판에 연동 개발 및 검증 결과 기록
- 회귀 테스트: 컴포넌트 JSX / TSX 정적 타입 검사 및 린트 검증
- 미해결 위험: 없음
- 인수인계: 에프터장 후보 테이블 각 행 우측에 [관심등록] 버튼이 배치되어 클릭 시 관심종목으로 즉시 추가됨.

## 3. 매매보고서 업데이트 계획서 상세 작성
- **상태:** `[R]`
- **작업 설명:** 기존 `/report` 매매보고서를 실전 분석용 대시보드(누적 손익 차트, 전략별 승률/PF 통계, 매매일지 복기)로 강화하기 위한 기능 설계 계획서를 작성한다.

### 변경 영향 기록
- 변경 분류: [Documentation] 트레이딩 보고서 고도화 설계 및 로드맵 수립
- 생산자: `docs/REPORT_UPDATE_PLAN.md`
- 소비자: StockAuto 개발자 및 백엔드/프론트엔드 분석 모듈 설계자
- 데이터 경계: 문서 작성으로 시스템 런타임 데이터 영향 없음
- API 계약: 신규 API 명세 시안 포함 (`GET /api/v1/report/analytics`, `POST /api/v1/report/trades/{id}/note`)
- 문서 반영: `docs/REPORT_UPDATE_PLAN.md` 신규 생성
- 회귀 테스트: N/A (문서 작성)
- 미해결 위험: 없음
- 인수인계: 상세 업데이트 로드맵 문서를 참조하여 후속 개발 시 대시보드 차트 및 승률 통계 구현 진행.

## 4. 백테스트 아레나(토너먼트) 효율성 점검
- **상태:** `[R]`
- **작업 설명:** 백테스트 토너먼트 시스템의 연산 속도, 데이터 처리 부하, 과적합 방지 구조 및 슬리피지/환율 반영 정합성을 종합 점검한다.

### 변경 영향 기록
- 변경 분류: [Audit] 백테스트 토너먼트 배틀 아레나 효율성 및 정합성 감정
- 생산자: `docs/BACKTEST_ARENA_AUDIT.md`, `backend/run_tournament.py` 분석
- 소비자: 자동매매 전략 검증 및 토너먼트 아레나 운영자
- 데이터 경계: 72개 전략 비교 연산 구조 및 데이터 로딩 분석
- API 계약: 변경 없음
- 문서 반영: `docs/BACKTEST_ARENA_AUDIT.md` 신규 생성
- 회귀 테스트: 백엔드 `py_compile` 검증 통과
- 미해결 위험: 전략별 데이터 재로딩 오버헤드가 존재하므로 전역 메모리 캐싱 및 병렬화 구조 적용 권고
- 인수인계: 백테스트 아레나 점검 보고서를 참조하여 토너먼트 속도 최적화(Multiprocessing/Memory Preload) 적용 제안.

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend\app\scanner\router.py` 통과
- `docs/REPORT_UPDATE_PLAN.md` 및 `docs/BACKTEST_ARENA_AUDIT.md` 작성 완료

## 7. 시뮬레이션 수익성 검증 중심 SaaS & 0원 인프라 실행 전략 수립 (개정 3판)
- **상태:** `[x]`
- **작업 설명:** 유저 지침에 따라 시뮬레이션 수익성 판결 전까지 토스증권 REAL/MOCK 연동을 보류(Hold) 처리하고, 장외 시간 백테스트 토너먼트 자동 구동 및 전략별 수익성 판결 엔진 구축을 최우선(Phase 1)으로 재조정함. (유저 최종 승인 완료)

### 변경 영향 기록
- 변경 분류: [Documentation] 시뮬레이션 수익성 검증 및 장외 백테스팅 자동화 중심 전략 수립
- 생산자: `docs/PRODUCTION_DEPLOYMENT_STRATEGY.md`, `docs/OFF_MARKET_AUTOMATION_PLAN.md`
- 소비자: StockAuto 개발진 및 백테스팅/스케줄러 구현 담당자
- 데이터 경계: 문서 정정으로 시스템 런타임 데이터 영향 없음
- API 계약: N/A
- 문서 반영: `docs/PRODUCTION_DEPLOYMENT_STRATEGY.md`, `docs/OFF_MARKET_AUTOMATION_PLAN.md` 및 본 현황판 업데이트
- 회귀 테스트: N/A (문서 작성)
- 미해결 위험: 없음
- 인수인계: 작성된 무결성 자동화 계획서(`docs/OFF_MARKET_AUTOMATION_PLAN.md`)에 따라 8번 작업(독립 서브프로세스 스케줄러 개발) 착수.

## 8. 메모리 완전 격리형 장외시간 자동화 스케줄러 구현
- **상태:** `[/]`
- **작업 설명:** 메인 웹 서버 다운 방지를 위한 독립 서브프로세스(`subprocess`) 기반 백테스트 및 리포팅 자동화 스케줄러 모듈(`app/scheduler/off_market_task.py`)을 구현한다.

### 변경 영향 기록
- 변경 분류: [Feature] 런타임 무결성 보장 장외 백그라운드 스케줄러 개발
- 생산자: `backend/app/scheduler/off_market_task.py`
- 소비자: StockAuto 백엔드 스케줄러 런타임, 대시보드 백테스트 캐시 매니저
- 데이터 경계: 백테스트 캐시 데이터 및 일일 매매 결산 리포트 데이터 적재
- API 계약: N/A (백그라운드 스케줄러)
- 문서 반영: 본 현황판 작업 적재 및 기록
- 회귀 테스트: py_compile 및 백그라운드 태스크 모의 실행 테스트
- 미해결 위험: 외부 주가 API Rate Limit 대비 백오프 설정 필요
- 인수인계: 서브프로세스 락 및 RAM 센서 가드가 정상 작동하는지 확인.


