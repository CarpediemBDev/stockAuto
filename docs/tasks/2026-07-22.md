# 2026-07-22 Task Board

## 작업 브랜치
- `fix/telegram-bot-linking-order`

## 신규 작업
- [R] 스캐너 릴레이·에쿼티 게이트·롤링 박스 스탑 3종을 작업 브랜치로 통합 정착 (Claude 세션): 다중 세션 공유 체크아웃에서 브랜치 이동·stash·reset이 반복되며 3회 소실됐던 구현분을 스크래치패드 백업 기준으로 재적용하고, i18n을 포함한 현행 브랜치 위에서 마이그레이션 체인·import 정합성을 회복해 하네스 통과 상태로 확정
- [R] 스캐너 점수 캘리브레이션 기록 잡 (Claude 세션): 스윙 예측 점수와 익일 실제 등락을 짝지어 누적하는 관측 레이어 신설. 점수의 절반(예측)은 기존 swing_prediction_snapshots에 이미 매일 쌓이고 있었고 빠진 결과(익일 종가) 쪽을 채우는 구조. 표본이 쌓이면 점수→실현 수익 매핑 보정(등화회귀)의 입력이 된다

### 변경 영향 기록 (스윙 점수 캘리브레이션 기록 잡)
- 변경 분류: 백엔드 기능 추가 (사후 검증용 관측 레이어, 매매 판정 무관여)
- 생산자: backend/app/scanner/score_calibration.py 신규 — record_pending_outcomes가 swing_prediction_snapshots에서 미관측 예측일을 최대 5일치 골라 종목별 일봉을 조회하고 예측일 다음 거래일 종가를 붙여 적재. 순수 함수 resolve_next_close(주말·휴장일 건너뛰고 예측일보다 뒤인 첫 봉만 채택)와 compute_return_pct(Decimal 연산)로 판정 로직 분리. summarize_calibration은 점수 구간별 승률·평균 수익률 집계
- 소비자: backend/app/bot/scheduler.py가 swing_score_calibration_wrapper를 매일 08:30 cron(max_instances=1, coalesce=True)으로 등록 — 스윙 예측 갱신(08:00) 직후 실행되며 별도 이벤트 루프로 격리하고 예외를 전부 흡수해 트레이딩 사이클에 전파되지 않는다
- 데이터 경계: swing_score_outcomes 테이블 신설 (predicted_date·ticker·score·baseline_close·observed_close·observed_date·return_pct·created_at). 복합 유니크 제약(predicted_date, ticker)으로 잡 멱등성 보장. swing_prediction_snapshots는 읽기 전용. 마이그레이션 89c6a78e09ec(down_revision 78f9aaecb6fc, 테이블 존재 가드 내장) 적용 완료
- API 계약: 변경 없음 (외부 노출 엔드포인트 없음. 축적만 수행하며 조회 API는 표본 확보 후 별도 과제)
- 문서 반영: docs/SCHEMA.md에 swing_score_outcomes 테이블 명세(컬럼·제약·적재 주체) 추가
- 회귀 테스트: backend/tests/test_score_calibration.py 신규 19건(주말 갭 건너뛰기·예측일 당일/이전 봉 배제·익일 종가 미도래 시 재시도·멱등 재실행·종목 단위 실패 격리·백필 윈도우 초과 무시·기준가 0 방어·손상 JSON 방어·Decimal 정밀도·구간 집계) 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 익일 종가만 관측하므로 며칠에 걸쳐 상승하는 스윙 특성을 과소평가할 수 있음 — 다기간(3일·5일) 관측 추가는 후속 과제. 종가 기준이라 갭 상승 후 장중 하락 같은 경로 의존적 결과는 구분하지 못함. 보정 알고리즘(등화회귀) 자체는 미구현이며 최소 수 주간 표본 축적이 선행되어야 의미가 생김. 예측일당 상위 30종목으로 제한해 하위 후보의 예측력은 관측되지 않음
- 인수인계: 축적 현황은 swing_score_outcomes 테이블 행 수로 확인하고, 점수 구간별 성적은 summarize_calibration(db)로 조회한다. 표본이 충분해지면 다음 단계는 (1) 점수 구간별 승률 단조성 검증, (2) 예측력 없는 지표 식별 후 swing_predictor.py 가중치 조정, (3) 등화회귀 보정 도입 순서. 미커밋 상태(Git 동결) — 사용자 커밋 지시 대기

### 변경 영향 기록 (3종 기능 브랜치 통합 정착)
- 변경 분류: 백엔드 기능 통합 (기존 구현분의 브랜치 이관·정합성 복구, 신규 설계 없음)
- 생산자: backend/app/scanner/watchlist_relay.py(신규 복원), backend/app/bot/equity_gate.py(신규 복원), backend/app/bot/trade_calculations.py의 compute_rolling_box_stop·check_rolling_box_breach, backend/app/strategies/base_strategy.py의 use_rolling_box_stop·rolling_box_window 기본 속성
- 소비자: backend/app/scanner/scanner.py(릴레이 유니버스 편입·Stage 1 면제·Stage 2 예약 슬롯·rolling_box_low 공급), backend/app/bot/scheduler.py(에쿼티 게이트 배분 계수·롤링 박스 래칫 영속화·이탈 판정), backend/app/backtests/backtest_engine.py 및 backend/app/admin/backtest_runner.py(백테스트 2경로 동일 SSOT 함수 호출)
- 데이터 경계: holdings.rolling_stop_price 컬럼과 docs/SCHEMA.md 명세는 선행 커밋 1c1de9c·97a5ac9에 이미 반영됨. 마이그레이션 78f9aaecb6fc(down_revision 57fac21b1d0f, 컬럼 존재 가드 내장)로 체인 정상화 — alembic heads 단일 head 확인. TradeLog·SwingPredictionSnapshot은 읽기 전용
- API 계약: 변경 없음 (외부 노출 없음. calculate_entry_quantity의 equity_gate_factor는 기본값 1.0으로 기존 호출부 호환, 스캐너 응답에 RELAY_* source 태그와 details.rolling_box_low가 추가되나 기존 소비자는 미참조)
- 문서 반영: 영향 없음 - docs/SCHEMA.md의 holdings.rolling_stop_price 명세가 선행 커밋에 이미 포함되어 이번 통합분에서 추가 갱신할 장기 문서가 없음
- 회귀 테스트: backend/tests/test_watchlist_relay.py 17건 + test_equity_gate.py 14건 + test_rolling_box_stop.py 15건 = 신규 46건 전체 통과. 기존 test_scanner_scoring.py::test_scan_market_expert_maps_realtime_strategy_fields가 릴레이 도입으로 로컬 DB의 스윙 스냅샷 종목(CPRX)을 함께 스캔해 결과 2건이 되어 반려됐고, 해당 테스트에 get_relay_priority_map 몽키패치를 추가해 hermetic하게 복구(테스트 의도는 시드 종목 필드 매핑 검증이라 릴레이는 격리 대상). python scripts/verify_harness.py 전체 통과
- 미해결 위험: 스캐너 릴레이에 런타임 킬 스위치가 없음 — scan_market_expert가 매 스캔마다 무조건 로컬 DB(스윙 스냅샷)와 애프터장 캐시를 읽어 유니버스에 종목을 추가하므로, 오작동 시 코드 수정 없이는 끌 수 없고 테스트도 명시적 몽키패치 없이는 DB 상태에 오염된다(본 세션에서 기존 스캐너 테스트 1건이 실제로 이 경로로 깨졌음). system_settings 기반 토글(SETTING_ENABLE_GEMINI_NEWS_ANALYSIS와 동일 패턴) 추가를 후속 과제로 권고. 롤링 박스 스탑은 백테스트 A/B에서 ON/OFF 성과 차이가 없어(전략 6종×2기간, 박스 청산 0~2건) 전 전략 기본 OFF 휴면 상태로 탑재됨 — 활성화는 별도 A/B 근거 확보 후에만 할 것. 에쿼티 게이트 임계값(PF 1.2/1.0, 계수 0.6/0.3)은 경험적 초기값으로 실운영 데이터 축적 후 재조정 필요. 애프터장 캐시는 인메모리 전용이라 저녁~아침 사이 서버 재시작 시 릴레이 해당 몫 소실(스윙 스냅샷만으로 동작, 오동작 아님)
- 인수인계: 상세 설계·A/B 수치는 docs/tasks/2026-07-19.md(릴레이·에쿼티 게이트)와 docs/tasks/2026-07-20.md(롤링 박스)의 변경 영향 기록 참조. 공유 체크아웃 다중 세션 경합으로 본 작업물이 3회 소실된 이력이 있으므로, 브랜치 전환·stash·reset 전 반드시 git status로 타 세션 미커밋 작업 확인 필요. 미커밋 상태(Git 동결) — 사용자 커밋 지시 대기. 다음 작업은 스캐너 점수 캘리브레이션 기록 잡
