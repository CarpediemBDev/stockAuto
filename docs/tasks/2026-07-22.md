# 2026-07-22 Task Board

## 작업 브랜치
- `feat/rolling-box-trailing-stop` (PR #60, 병합 완료)
- `feat/scanner-relay-equity-gate` (PR #61, 병합 완료)
- `fix/telegram-bot-linking-order` (스윙 점수 캘리브레이션 관측 잡)

## 신규 작업
- [R] 스캐너 릴레이·에쿼티 커브 게이트 복원 및 07-20 현황판 손상 복구 — 07-19 등록분이었으나 07-20 세션 충돌로 소실되어 stash에만 잔존하던 휴면 모듈 2종을 복원하고, 파일이 손상된 07-20 현황판을 stash 원본으로 복구
- [R] 롤링 박스 트레일링 스탑 본체 복원 — 07-20 세션 충돌로 저장소에서 소실되고 stash에만 잔존하던 매도 엔진 보조 청산 조건을 복원. 진입 후 절대 최고가 기준 ATR 트레일링이 횡보 구간에서 옛 고점에 앵커링되어 느슨해지는 문제를, 최근 N봉 저점 박스 하단 이탈 청산으로 보완. 제미나이 원안의 치명 결함(스탑이 주가를 따라 내려가는 역주행)을 래칫(단조 증가) 조건으로 보정. 전 전략 기본 OFF 휴면 탑재

- [R] 07-19 현황판 stash 잔존 기록 복원 및 stash@{0} 정리 — 07-20 세션 충돌로 두 갈래로 갈라진 07-19 현황판 중 main에 누락된 갈래를 stash에서 옮기고, 코드 실물과 대조해 사실이 다른 항목(텔레그램 한글화 허위 완료 주장)을 정정. 이로써 stash 고유 내용이 0이 되어 drop 가능

- [R] 스윙 예측 점수 캘리브레이션 관측 잡 신설 (Claude 세션): 스윙 예측 점수와 익일 실제 등락을 짝지어 누적하는 사후 검증 원장 추가. 예측 점수 쪽은 이미 swing_prediction_snapshots에 매일 08:00 쌓이고 있어, 빠져 있던 결과(익일 종가) 쪽만 채우는 구조로 구현. 매매 판정에는 일절 관여하지 않는 순수 관측 레이어

- [R] 롤링 박스 박스 길이 불일치·rolling_box_window 라이브 무시 해소 (Claude 세션): 박스를 봉 개수가 아니라 시간 길이(분)로 재정의해 라이브(15분봉)와 백테스트(전략 인터벌)가 같은 실시간 길이를 쓰도록 통일하고, 전략이 선언한 박스 길이가 라이브에서도 존중되도록 스캐너→스케줄러 경로를 수정. PR #60에 기록된 미해결 위험 2건 해소
- [R] 텔레그램 알림 다국어화 미착수분 완료 및 i18n 결함 3건 수정 — 07-19에 완료로 기록됐으나 코드가 없던 텔레그램 영문 알림 한글화를 실제로 수행하고, 그 과정에서 발견한 실사용 결함(사용자에게 키 문자열 노출, 언어 설정 무시, 딕셔너리 누락으로 관리자 화면 키 노출)을 함께 고침

- [R] 프론트엔드 i18n 잔여 치환 1단계: NavBar (Claude 세션) - 07-20/07-21 현황판이 완료로 기록했으나 저장소에 없던 상단바 치환을 실제로 수행. 노출 빈도가 가장 높은 화면부터 단계적으로 진행하는 첫 조각

### 변경 영향 기록 (롤링 박스 트레일링 스탑 본체 복원)
- 변경 분류: 백엔드 기능 추가 (매도 엔진 보조 청산 조건, 전 전략 기본 비활성 opt-in 휴면 탑재)
- 생산자: backend/app/bot/trade_calculations.py에 SSOT 순수 함수 2종 추가 — compute_rolling_box_stop(래칫: 새 스탑 = max(직전 스탑, 최근 N봉 저점), 단조 증가 불변식)과 check_rolling_box_breach(수익권 진입 후에만 활성, Decimal 연산). backend/app/strategies/base_strategy.py에 use_rolling_box_stop=False·rolling_box_window=10 기본 속성. backend/app/scanner/scanner.py의 calculate_rolling_box_low가 15분봉에서 형성 중인 마지막 봉을 제외한 완성 봉 N개 저점을 details.rolling_box_low로 공급
- 소비자: 매도 판정 3경로가 동일 SSOT 함수를 호출 — backend/app/bot/scheduler.py(라이브: 래칫을 holdings.rolling_stop_price에 version 비간섭 Core UPDATE로 영속화, 기존 highest_price 영속화와 완전히 동일한 패턴을 따라 StaleData 회귀를 차단), backend/app/backtests/backtest_engine.py(홀딩 dict의 rolling_stop 필드, 불타기 시 기존 래칫 유지), backend/app/admin/backtest_runner.py(MockHolding.rolling_stop)
- 데이터 경계: holdings.rolling_stop_price 컬럼을 읽고 쓴다. 컬럼과 마이그레이션(78f9aaecb6fc)은 PR #59에서 이미 main에 반영되어 이번 변경에 스키마 변경분은 없음. NULL = 미시드 상태로 발동하지 않으며 기존 행은 무영향
- API 계약: 변경 없음 - 외부 노출 없음. rolling_box_low는 스캐너 details 내부 필드이며 응답 스키마에 포함되지 않음
- 문서 반영: 영향 없음 - docs/SCHEMA.md의 holdings.rolling_stop_price ERD·컬럼 명세가 선행 커밋에 이미 포함되어 있고, 이번 변경은 그 컬럼을 소비하는 로직만 추가하므로 갱신할 장기 문서가 없음
- 회귀 테스트: backend/tests/test_rolling_box_stop.py 신규 15건(래칫 단조 증가·하락 시퀀스 역주행 차단·수익권 활성 가드·미시드 스탑 미발동·Decimal 입력 보존·형성 봉 제외·윈도우 미달 None·빈 DataFrame 방어·전략 기본 OFF·모델 컬럼 존재) 통과. python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 세 가지. ①박스 길이가 라이브와 백테스트에서 다르다 — 스캐너는 15분봉 10개(2.5시간) 고정, 백테스트는 전략 인터벌 10개(1h 백테스트면 10시간). 현황판에 기록된 A/B 검증(전략 6종×2기간, 1h, BASE)은 1h 축에서 돌린 결과이므로 "성과 차이 없음" 결론이 라이브 2.5시간 박스로 그대로 전이된다는 보장이 없다. ②BaseStrategy.rolling_box_window가 라이브에서는 무시된다 — 스캐너는 종목당 하나의 값만 계산하므로 전역 DEFAULT_ROLLING_BOX_WINDOW(10) 고정이며, 전략이 20으로 선언해도 라이브는 10으로 동작한다(백테스트만 전략별 값을 존중). ③A/B에서 ON/OFF 성과 차이가 없었다(수익률·MDD 동일, 박스 청산 0~2건) — 기존 ATR 트레일링과 2회 연속 확정 가드가 거의 항상 먼저 발동. 켤 실증 근거가 아직 없어 전 전략 기본 OFF로 둔다. ①②가 해소되기 전에는 어떤 전략도 활성화하지 말 것
- 인수인계: 활성화 방법은 전략 클래스에 use_rolling_box_stop=True 선언 하나뿐이며, 위 미해결 위험 ①②를 먼저 정리한 뒤 별도 A/B 근거를 확보하고 켤 것. 본 작업은 격리 클론에서 수행했다 — 07-18·07-20에 이어 07-21에도 다중 세션이 같은 체크아웃을 공유하다 브랜치 전환 경합으로 작업물이 덮어써지는 사고가 재발했기 때문(reflog HEAD@{0} checkout: moving from feat/rolling-box-trailing-stop). 남은 후속 과제는 07-19 등록분인 스캐너 릴레이·에쿼티 커브 게이트 2종 복원과 docs/tasks/2026-07-20.md 파일 손상 복구(5행 문장 중도 절단, 40~53행이 15~28행 중복)

### 변경 영향 기록 (스캐너 릴레이·에쿼티 커브 게이트 복원)
- 변경 분류: 백엔드 기능 추가 2종 (스캔 유니버스 확장 레이어, 매수 배분 메타 리스크 레이어). 둘 다 자체 저장소·스키마 없이 기존 캐시와 기록만 읽는 순수 추가형
- 생산자: backend/app/scanner/watchlist_relay.py 신규 — 전일 애프터장 스캐너 캐시(read_after_hours_candidate_cache)와 밤 스윙 예측 DB 스냅샷(read_swing_prediction_cache)을 읽어 우선 감시 티커 맵을 만든다. 소스별 점수 하한(65.0/60.0)·소스당 상한 15개·신선도 상한 72시간(주말 갭 고려) 적용. backend/app/bot/equity_gate.py 신규 — (전략 슬롯 × 레짐)별 최근 청산 트레이드의 롤링 윈도우 프로핏 팩터로 매수 배분 계수를 산출한다(PF 1.2 이상 1.0, 1.0 이상 0.6, 미만 0.3)
- 소비자: backend/app/scanner/scanner.py의 scan_market_expert가 릴레이 맵을 스캔 유니버스에 병합하고(동기 DB 접근이라 asyncio.to_thread로 격리), 릴레이 종목에 Stage 1 필터 면제를 부여하며, merge_reserved_candidates로 상위 25컷에서 밀린 릴레이 후보를 최대 5개까지 예약 슬롯으로 추가 편입한다. backend/app/bot/scheduler.py의 process_entry_signals가 슬롯별 게이트 계수를 조회해 calculate_entry_quantity의 배분 곱셈 항으로 전달한다
- 데이터 경계: 스키마 변경 없음. 읽기 전용 — trade_logs(user_id·strategy_type·trade_type·regime_mode·realized_pnl·executed_at 조회)와 애프터장 인메모리 캐시, 스윙 예측 DB 스냅샷만 읽는다. 신규 테이블·컬럼·마이그레이션 없음
- API 계약: 변경 없음 - 외부 노출 없음. 릴레이 출처 태그(RELAY_AFTER_HOURS·RELAY_SWING)는 스캐너 내부 source_map에만 병기된다
- 문서 반영: 영향 없음 - 스키마·API 변경이 없고 두 모듈 모두 기존 계약을 읽기만 하므로 docs/SCHEMA.md·docs/API_STANDARD.md에 갱신할 내용이 없음
- 회귀 테스트: backend/tests/test_watchlist_relay.py 17건(신선도 경계·점수 하한·소스당 상한·중복 제거·소스 실패 격리·예약 슬롯 순수 추가성·태그 판별)과 backend/tests/test_equity_gate.py 14건(콜드 스타트 무개입·무손실 윈도우 분모 0 특이점·PF 구간별 계수·Decimal 합산·조회 실패 fail-open) 통과. backend pytest 전체 314건 통과. python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 세 가지. ①에쿼티 게이트의 모든 실패 경로가 계수 1.0으로 fail-open이라, 조회가 깨져도 로그 경고만 남고 게이트가 조용히 무력화된다 — 이번 복원에서 TradeLog 컬럼 6종이 실재함을 스키마와 직접 대조해 확인했으나, 향후 컬럼명이 바뀌면 테스트가 아니라 운영에서만 드러난다. ②스로틀 로그의 f"{gate_meta.get('profit_factor'):.2f}"는 profit_factor가 None이면 TypeError다. 현재는 계수가 1.0 미만인 경로(marginal·unhealthy)가 항상 float를 세팅하므로 도달 불가능하지만, PF→계수 매핑을 손대면 라이브 매수 루프에서 터지는 잠복 지뢰다. 복원 충실성을 위해 원본 그대로 두었으니 매핑 수정 시 함께 방어할 것. ③릴레이 예약 슬롯이 Stage 2 정밀 분석 대상을 최대 25→30개로 늘려 스캔 1회당 데이터 조회가 최대 20% 증가한다(Gemini AI 호출은 _AiQuotaPolicy의 10회 상한이 별도로 방어하므로 쿼터 초과는 없음)
- 인수인계: 두 모듈 모두 기존 동작을 바꾸지 않는 순수 추가형이며, 릴레이는 소스 실패 시 기존 스캔과 완전히 동일하게 동작하고 게이트는 표본 8건 미만이면 무개입이다. 본 작업도 격리 클론에서 수행했다 — 07-21 세션에서 다중 세션이 같은 체크아웃을 공유하다 브랜치 전환 경합으로 작업물이 덮어써지는 사고가 재발했기 때문. 남은 후속 과제는 07-19 등록분 ④스캐너 점수 캘리브레이션 기록 잡(스윙 예측 점수와 익일 실제 등락 축적)

### 변경 영향 기록 (스윙 점수 캘리브레이션 관측 잡)
- 변경 분류: 백엔드 기능 추가 (사후 검증용 관측 레이어, 매매 판정 무관여)
- 생산자: backend/app/scanner/score_calibration.py 신규 — record_pending_outcomes가 swing_prediction_snapshots에서 미관측 예측일을 최대 5일치 골라 종목별 일봉을 조회하고 예측일 다음 거래일 종가를 붙여 적재. 순수 함수 resolve_next_close(주말·휴장일을 건너뛰고 예측일보다 뒤인 첫 봉만 채택)와 compute_return_pct(Decimal 연산)로 판정 로직을 분리했고, summarize_calibration이 점수 구간별 승률·평균 수익률을 집계
- 소비자: backend/app/bot/scheduler.py가 swing_score_calibration_wrapper를 매일 08:30 cron(max_instances=1, coalesce=True)으로 등록 — 스윙 예측 갱신(08:00) 직후 실행되며 별도 이벤트 루프로 격리하고 예외를 전부 흡수해 관측 실패가 트레이딩 사이클에 전파되지 않는다
- 데이터 경계: swing_score_outcomes 테이블 신설 (predicted_date·ticker·score·baseline_close·observed_close·observed_date·return_pct·created_at). 복합 유니크 제약(predicted_date, ticker)으로 잡 재실행 멱등성 보장. swing_prediction_snapshots는 읽기 전용. 마이그레이션 89c6a78e09ec(down_revision 78f9aaecb6fc, 테이블 존재 가드 내장)
- API 계약: 변경 없음 (외부 노출 엔드포인트 없음. 축적만 수행하며 조회 API는 표본 확보 후 별도 과제)
- 문서 반영: docs/SCHEMA.md에 swing_score_outcomes 테이블 명세(컬럼·제약·적재 주체) 추가
- 회귀 테스트: backend/tests/test_score_calibration.py 신규 19건(주말 갭 건너뛰기·예측일 당일/이전 봉 배제·익일 종가 미도래 시 재시도·멱등 재실행·종목 단위 실패 격리·백필 윈도우 초과 무시·기준가 0 방어·손상 JSON 방어·Decimal 정밀도·구간 집계) 통과. 기존 test_scanner_scoring.py가 릴레이의 로컬 DB 조회로 실제 종목(CPRX)을 함께 스캔해 반려됐던 건도 get_relay_priority_map 몽키패치로 hermetic하게 복구. python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 익일 종가 한 시점만 관측하므로 며칠에 걸쳐 오르는 스윙 특성을 과소평가한다 — 다기간(3일·5일) 관측 추가는 후속 과제. 종가 기준이라 갭 상승 후 장중 하락 같은 경로 의존적 결과는 구분하지 못한다. 보정 알고리즘(등화회귀)은 미구현이며 최소 수 주간 표본 축적이 선행되어야 의미가 생긴다. 예측일당 상위 30종목으로 제한해 하위 후보의 예측력은 관측되지 않는다
- 인수인계: 축적 현황은 swing_score_outcomes 행 수로 확인하고 점수 구간별 성적은 summarize_calibration(db)로 조회한다. 표본이 충분해지면 (1) 점수 구간별 승률 단조성 검증, (2) 예측력 없는 지표 식별 후 swing_predictor.py 가중치 조정, (3) 등화회귀 보정 도입 순으로 진행한다. 본 세션에서 확인한 롤링 박스 미해결 위험 2건(라이브 15분봉 박스와 백테스트 인터벌 박스의 길이 불일치, BaseStrategy.rolling_box_window가 라이브에서 무시됨)은 위 롤링 박스 기록에 정리되어 있으며 활성화 전 반드시 해소할 것

### 변경 영향 기록 (롤링 박스 박스 길이 정의 교정)
- 변경 분류: 백엔드 버그 수정 (라이브·백테스트 간 박스 정의 불일치 해소, 전 전략 기본 OFF 유지)
- 생산자: backend/app/bot/trade_calculations.py에 박스 길이를 봉 개수가 아닌 시간 길이(분)로 정의하는 SSOT 추가 — DEFAULT_ROLLING_BOX_MINUTES(150분=2.5시간, 기존 라이브 동작과 동일)·MAX_ROLLING_BOX_MINUTES(480분, 라이브 공급 상한)·MIN_ROLLING_BOX_BARS(2)·LIVE_BOX_BAR_MINUTES(15)와 환산 함수 resolve_rolling_box_bars(분→봉 수, 반올림 후 최소 봉 수 보정)·bar_minutes_for_interval(인터벌→봉 길이, 1d는 정규장 390분)·순수 함수 compute_box_low(저점 목록에서 최근 N봉 최저값). backend/app/strategies/base_strategy.py의 rolling_box_window(봉 개수)를 rolling_box_minutes(분)로 교체
- 소비자: backend/app/scanner/scanner.py는 박스 하단을 미리 확정하지 않고 collect_recent_lows_15m으로 완성 15분봉 저점 목록(details.recent_lows_15m, 라이브 상한 32봉)만 공급하도록 변경 — 스캐너는 어느 전략이 시그널을 쓸지 모르기 때문. backend/app/bot/scheduler.py가 전략별 rolling_box_minutes를 resolve_rolling_box_bars로 환산해 compute_box_low로 박스 하단을 산출. backend/app/backtests/backtest_engine.py도 동일 환산 함수로 인터벌별 봉 수를 계산해 rolling_box_low 시리즈를 생성
- 데이터 경계: 변경 없음 (holdings.rolling_stop_price 컬럼·마이그레이션 무변경, 신규 테이블 없음). 스캐너 details의 rolling_box_low 필드가 recent_lows_15m으로 대체됐으나 내부 필드로 API 응답 스키마에는 포함되지 않음
- API 계약: 변경 없음 (외부 노출 없음. BaseStrategy.rolling_box_window는 rolling_box_minutes로 대체됐고 저장소 전수 검색 결과 외부 참조 0건)
- 문서 반영: 영향 없음 - 스키마·API 계약 변경이 없는 내부 계산 정의 교정으로, 불변식은 코드 주석과 회귀 테스트로 집행하며 활성화 조건 변화는 본 현황판에 기록
- 회귀 테스트: backend/tests/test_rolling_box_stop.py 15건에서 26건으로 확장(라이브 기본값이 기존 10봉과 동일 유지·타임프레임별 동일 시간 길이 환산·일봉 최소 봉 수 보정·라이브 공급 상한 클램프·0/음수 방어·bar_minutes 0 예외·저점 목록 상한과 순서 보존·NaN/0 제거·윈도우 미달 None). python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 교정 후 A/B를 다시 돌린 결과, 라이브와 같은 15분봉 축(26-05-01~05-30, 박스 150분)에서는 3개 전략 모두 박스 청산이 0건으로 ON/OFF 성과가 완전히 동일했다 — 다만 15분봉은 yfinance 보관 한도로 기간이 1개월이라 매매 표본이 7~13건에 불과해 "효과 없음"이 아니라 "발동 사례 미관측"으로 읽어야 한다. 1h 축(26-04-01~05-30)에서는 교정으로 박스가 10봉(10시간)에서 3봉(3시간)으로 짧아지며 청산이 0~2건에서 7~8건으로 늘었고 regime_switching 승률 43.10→43.97, senior_simple 수익률 -0.94→-0.90·MDD -3.11→-3.07로 미미하게 개선됐으나 유의미한 수준은 아니다. 따라서 전 전략 기본 OFF를 유지하며, 켜려면 15분봉 축에서 충분한 표본을 확보한 별도 A/B가 선행되어야 한다
- 인수인계: PR #60에 기록된 미해결 위험 2건(라이브 15분봉 박스와 백테스트 인터벌 박스의 길이 불일치, BaseStrategy.rolling_box_window가 라이브에서 무시됨)이 본 변경으로 모두 해소됐다. 이제 전략이 rolling_box_minutes로 선언한 길이가 라이브·백테스트 양쪽에서 동일한 실시간 길이로 해석되며, 480분을 넘는 선언은 라이브 공급 한도에 맞춰 환산 단계에서 잘려 조용한 미발동이 발생하지 않는다. 활성화 방법은 전략 클래스에 use_rolling_box_stop=True 선언 하나이며 위 미해결 위험의 A/B 조건을 먼저 충족할 것
### 변경 영향 기록 (텔레그램 알림 다국어화 및 i18n 결함 수정)
- 변경 분류: 버그 수정 3건 + 미착수 작업 완료 (사용자 노출 문구 다국어화, 스키마·API 무변경)
- 생산자: backend/app/core/i18n.py — get_msg에 이름 있는 매개변수 default를 정식 추가하고(기존에는 **kwargs로 새어 포맷 인자로 오해됐다), 요청 언어에 키가 없으면 ko로 강등한 뒤 default를 쓰고 그래도 없을 때만 키 문자열로 내려가며 WARNING을 남기도록 개선. 알림 언어 결정의 SSOT로 resolve_user_language(db, user_id) 신설 — UserSettings.language를 조회하고 실패 시 항상 ko로 폴백한다. locales/ko.json·en.json에 telegram.order_recovery_ambiguous와 ui.order_resolved, admin.users 4키 추가하고 참조가 하나도 없던 빈 exception 네임스페이스 제거
- 소비자: backend/app/bot/order_discovery.py — 유일한 영문 텔레그램 알림(*Order Recovery Ambiguous*)을 i18n 키로 이관하고 하드코딩 user_lang="ko" TODO를 resolve_user_language로 교체. backend/app/bot/order_reconciler.py — 동일하게 하드코딩 언어 제거. frontend/components/admin/UserManagement.tsx — 반쪽 치환이던 제목({t('title')} 뒤에 "& 실시간 아레나"가 하드코딩)을 단일 키로 합치고, 의미가 맞지 않던 admin.translation 네임스페이스(번역 관리자용) 참조를 admin.users로 교정하며 미사용 tTrans 훅 제거
- 데이터 경계: 스키마 변경 없음. UserSettings.language를 읽기 전용으로 조회한다(기존 컬럼, 마이그레이션 불필요). ActionLog 메시지는 손대지 않았다 — 코드베이스 전체가 영문 운영 로그 규약이라 이것만 한글화하면 관례가 깨진다
- API 계약: 변경 없음 - 엔드포인트·스키마 무변경. I18n.get_msg 시그니처에 default가 추가됐으나 기존 호출부는 전부 호환된다
- 문서 반영: 영향 없음 - 스키마·API 변경이 없고 사용자 노출 문구와 그 조회 경로만 고쳤으므로 docs/SCHEMA.md·docs/API_STANDARD.md에 갱신할 내용이 없음
- 회귀 테스트: backend/tests/test_i18n_messages.py 신규 16건(키 부재 시 날 키 미노출·default가 포맷 인자로 오염되지 않음·미지원 언어 ko 강등·언어 조회 실패 폴백·ko/en 키 집합 일치·플레이스홀더 일치·빈 네임스페이스 금지·프론트 소비 키 실재 검증) 통과. backend pytest 전체 349건 통과. python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 프론트엔드 하드코딩 텍스트 치환은 여전히 대부분 미완이다. locales/korean_strings_report.json(500KB)이 추출해 둔 문자열 중 실제로 딕셔너리에 옮겨진 것은 admin.users 4키뿐이며, useTranslations를 쓰는 컴포넌트는 layout.tsx와 UserManagement.tsx 둘뿐이다. 07-20·07-21 현황판이 완료로 기록한 nav·settings·dashboard·scanner·report 네임스페이스 치환은 저장소에 존재하지 않는다(07-19 텔레그램 한글화와 같은 유형의 미달성 기록). 신규 test_i18n_messages.py의 프론트 소비 키 검증이 앞으로 같은 사고(소비자 코드만 병합되고 딕셔너리 누락)를 CI에서 차단한다
- 인수인계: 잔여 화면 치환은 화면 단위로 나눠 진행할 것 — 한 PR에 몰면 500KB 추출분 전체를 한 번에 검토해야 해서 실효 검증이 불가능하다. 착수 순서는 사용자 노출 빈도 기준으로 NavBar → 대시보드 → 스캐너 → 리포트 → 설정 권장. 각 화면을 옮길 때마다 ko/en 양쪽을 동시에 채워야 test_i18n_messages.py의 키 집합 일치 검사를 통과한다

### 변경 영향 기록 (프론트엔드 i18n 1단계 - NavBar)
- 변경 분류: 프론트엔드 다국어 치환 (UI 문구만 이동, 로직·스키마·API 무변경)
- 생산자: locales/ko.json과 locales/en.json에 nav 네임스페이스 신설 - 메뉴 라벨 3종, 드롭다운 라벨·상태 문구 13종, 토스트 5종을 양쪽 언어에 동시 등록. 사용자 인사말은 한국어 존칭 어미를 영어로 옮길 수 없어 ko는 "{username}님", en은 "{username}"으로 플레이스홀더만 공유하도록 분리
- 소비자: frontend/components/NavBar.tsx - useTranslations("nav")로 하드코딩 문구 19곳을 치환. 모듈 최상단 navItems 배열은 모듈 로드 시점에 로케일이 정해지지 않아 라벨을 확정할 수 없으므로 text 대신 labelKey를 들고 있다가 렌더 시 t()로 푼다
- 데이터 경계: 변경 없음 - 번역 딕셔너리 JSON 키 추가뿐이며 DB·테이블·컬럼 무관
- API 계약: 변경 없음 - 엔드포인트·스키마 무변경. 언어 전환 시 호출하는 PUT /auth/me/language는 기존 계약 그대로 사용
- 문서 반영: 영향 없음 - UI 문구를 딕셔너리로 옮긴 치환 작업이라 docs/SCHEMA.md·docs/API_STANDARD.md에 갱신할 계약이 없음
- 회귀 테스트: backend/tests/test_i18n_messages.py에 NavBar 동적 라벨 키 검증 1건 추가(총 17건). t(item.labelKey)는 동적 호출이라 기존 소비 키 정규식이 잡지 못하므로 navItems 배열에서 labelKey를 직접 읽어 ko/en 양쪽 딕셔너리와 대조한다. npx tsc --noEmit 0건, npm run lint 0건, python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 언어 변경 토스트 한 곳은 의도적으로 리터럴을 남겼다 - useTranslations는 변경 전 로케일에 묶여 있어 t()로 뽑으면 새 언어가 아니라 옛 언어로 안내되기 때문이며, 코드에 사유를 주석으로 남겼다. 또한 백엔드가 실행되지 않는 격리 클론에서 작업해 로그인 후 상단바를 브라우저로 직접 확인하지는 못했다. tsc·lint·키 실재 검증·E2E 스모크로 대체 검증했으며, 실제 화면 확인은 로컬 기동 시 함께 볼 것
- 인수인계: 다음 조각은 노출 빈도 순서로 대시보드(frontend/app/page.tsx) -> 마켓 스캐너(app/scanner) -> 매매 보고서(app/report) -> 개인/런타임 설정(app/admin/settings)이다. 화면마다 ko/en을 동시에 채워야 키 집합 일치 검사를 통과한다. 원문은 locales/korean_strings_report.json의 frontend 항목에서 파일 경로로 찾을 수 있다
