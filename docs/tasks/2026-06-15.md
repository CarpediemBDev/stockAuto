# 2026-06-15 작업 현황판

## 작업 목록

- [x] 최근 커밋 리뷰 후속: 자산 스냅샷·시뮬레이션 계산·아레나 차트 정합성 수정
  - [x] 증권사 잔고 조회 실패값이 0원 스냅샷으로 저장되지 않도록 방어
  - [x] 스냅샷 최소 저장 간격과 거래 모드별 이력 분리 적용
  - [x] 시뮬레이션 원금의 환율 기준 고정 및 회귀 테스트 추가
  - [x] 아레나 차트의 가상 초기값 제거와 UTC 타임스탬프 명시
  - [x] Alembic 리비전의 외부 YAML 의존 제거 및 후속 카탈로그 시드 검증

- [x] 데이터베이스 기반 전략 관리 테이블(Strategy Table) 도입 및 외래 키 매핑
  - [x] `backend/app/translations/strategies.yml` 내 명칭 고유화 완료
  - [x] `backend/app/core/models.py` 내 `Strategy` 테이블 정의 및 각 모델(UserSettings, Holding, TradeLog, BrokerOrder) 외래 키 연결
  - [x] Alembic 마이그레이션 적용 및 기존 데이터 외래 키 바인딩 완료
  - [x] `backend/app/core/migrator.py` 내 전략 Upsert 동기화 및 `admin11` 시딩 추가
  - [x] `backend/app/translations/translator.py`가 DB `strategies` 테이블을 조회하도록 개편
  - [x] 백엔드 컴파일 검증 및 하드 하네스 검증 실행
  - [x] SQLite 물리 외래 키(FK) 활성화 및 회귀 테스트 추가 (`PRAGMA foreign_keys=ON`)
  - [x] 경쟁 계정 시더가 기존 사용자 설정을 덮어쓰지 않도록 멱등성 보강
  - [x] 관리자 자산 곡선을 실제 계좌 평가 스냅샷 기반으로 전환
  - [x] 관리자 차트 Recharts `any`/ESLint 우회 제거
  - [x] 사용자 소유권 FK는 유지하고 `strategy_type` 문자열 FK만 제거
  - [x] `strategies.yml` 제거 및 `strategies` 테이블 완전 SSOT 전환

- [x] 프로젝트 전역 SSOT 잔여 중복 통합
  - [x] 전략 문자열 FK를 모델·마이그레이션·실제 DB 기준으로 제거 통일
  - [x] 전략 표시명을 `strategies` 테이블 기반으로 통일
  - [x] 증권사 제공자 목록과 인증 클라이언트 생성을 단일 레지스트리로 통합
  - [x] 거래 모드 정의와 프론트 표시 메타데이터를 백엔드 계약으로 통합
  - [x] 가상 시작자금 기본값을 백엔드 상수 및 API 응답으로 통합
  - [x] SSOT 회귀 테스트와 전체 하드 하네스 검증
  - 검증: 백엔드 전체 테스트 117건 통과
  - 검증: `npm run lint`, `npx tsc --noEmit`, `npm run build` 통과
  - 검증: Alembic head/check 및 NFC 검사 통과
