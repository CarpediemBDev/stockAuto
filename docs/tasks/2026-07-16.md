# 2026-07-16 Task Board

## 신규 작업
- [x] SSE 스트림 정리 경로 안정화 (Claude 세션): pubsub `unsubscribe` 실패 시에도 `aclose` 보장(커넥션 누수 방지) + `get_message` 루프의 Redis `ConnectionError`/`TimeoutError` 우아한 종료 처리 (`backend/app/sse/router.py`). 당일 verify_harness 8/8 통과 후 사용자 승인 — 다만 커밋 전 워킹트리가 stash@{0}(index 235fa34)로 정리되며 이 현황판 원본 항목과 함께 유실됨. 07-18 stash에서 복원·재커밋(a9b3839)해 PR #51(머지 커밋 1ada06c)로 main 병합 완료. 본 항목은 유실됐던 [x] 마감의 사후 복원 기록 (경위 상세: docs/tasks/2026-07-18.md)

### 변경 영향 기록
- 변경 분류: 백엔드 안정성 리팩터 (SSE 스트림 정리 경로·Redis 순단 예외 처리, 계약 무변경)
- 생산자: backend/app/sse/router.py `stream_events.event_generator` (subscribe를 try 내부로 이동, get_message에 Redis ConnectionError/TimeoutError 캐치 후 break, finally를 unsubscribe/aclose 이중 try-finally로 분리)
- 소비자: GET /api/v1/events 스트림 구독 클라이언트(프론트 fetch 기반 SSE) — 프레임 형식·이벤트 이름·인증 방식 무변경, Redis 순단 시 스트림이 예외 대신 정상 종료되어 기존 백오프 재연결 로직이 그대로 동작
- 데이터 경계: 변경 없음 (채널 구성 public/user/admin 및 유저별 라우팅 로직 미접촉)
- API 계약: 변경 없음 (응답 형식·상태 코드·헤더 동일, docs/API_STANDARD.md 영향 없음)
- 문서 반영: 영향 없음 - 외부 API 계약·스키마·운영 절차 무변경인 내부 정리 경로 리팩터로, 코드 내 주석으로 불변식을 기록함
- 회귀 테스트: 07-16 로컬 verify_harness 8/8 통과(pytest 255건 시점) + 07-18 재커밋 시 pre-commit 훅 8/8 재통과(pytest 268건) + PR #51 CI harness pass
- 미해결 위험: 클라이언트 강제 취소(CancelledError) 중 finally의 await가 재취소되는 극단 케이스에서는 aclose가 생략될 수 있음(기존과 동일한 asyncio 구조적 한계)
- 인수인계: main 병합 완료(1ada06c)로 작업 종결. stash@{0}에는 이 작업 외 타 세션 잔재(당시 QA 세션 중간 발견 메모 포함)가 남아 있으므로 pop/drop 전 내용 확인 권장 — scheduler.py 매수(≈1636행)·매도(≈1072행) safe_broker_call에서 strategy_type/buy_stage/regime_mode/signal_score 누락으로 미체결 주문이 simulated_broker 기본값 "regime_switching"으로 오태깅됨. 재발 방지로 simulated_broker.buy_order/sell_order의 strategy_type 기본값 제거(필수 인자화) 검토 포함.

### 변경 영향 기록
- 변경 분류: 버그 수정 (SIMULATED 주문 생성 시 전략 태깅 오류)
- 생산자: backend/app/bot/scheduler.py (매수/매도 주문 경로), backend/app/services/simulated_broker.py (buy_order/sell_order)
- 소비자: simulated_broker.process_unfilled_orders (미체결 체결 시 Holding/TradeLog 생성), unfilled_orders 테이블, Holding.(ticker, strategy_type) 조회 경로
- 데이터 경계: unfilled_orders.strategy_type / holdings.strategy_type / trade_logs.strategy_type — DB 스키마 변경 없음, 값 정확성만 교정
- API 계약: 외부 API 계약 변경 없음 (내부 broker 호출 시그니처만 영향)
- 문서 반영: 영향 없음 - DB 스키마·외부 API 계약 변경이 없는 내부 브로커 호출 인자 누락 수정이므로 SCHEMA.md/API_STANDARD.md 등 장기 문서 갱신 대상 아님
- 회귀 테스트: python scripts/verify_harness.py (pytest 포함) 통과 필수
- 미해결 위험: 메인 체크아웃에서 구버전(버그 포함) 백엔드가 계속 가동 중 — 수정이 main에 머지·배포(재기동)되기 전까지 SIMULATED 미체결 오태깅이 재발함(정합화 도중에도 user 80 KVUE 신규 발생 확인). 머지 후 백엔드 재기동 및 unfilled_orders 재점검 필요
- 인수인계: 코드 수정 완료·verify_harness 전 단계(8/8) SUCCESS. 오염 데이터 정합화는 사용자 승인 후 2026-07-16 실행 완료 — 백업 backend/stockauto.backup-remediation-20260716.db 생성 후 단일 트랜잭션으로 ① user 1 잔재 BUY 미체결 18건 삭제, ② 오태깅 SELL 미체결 중복 9건 삭제·잔여 13건을 홀딩 실제 전략으로 재태깅(user 80 KVUE→vwap_only 신규 발생분 포함, 검증 쿼리 불일치 0건), ③ user 79 BNS holdings.id=194·trade_logs.id=1070을 exploded_c로 재태깅. 롤백 필요 시 위 백업 파일로 복원

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
