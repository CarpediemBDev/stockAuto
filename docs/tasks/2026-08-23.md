# 2026-08-23 Task Board

## 신규 작업
- [x] 관리자 사용자 목록의 1:1 전략 매핑 전제 복구 — 테스트 잔여 계정 15개 삭제 (개발 DB 데이터 정리) <!-- APPROVED: 2026-08-23/사용자 승인 -->
- [x] pytest의 개발 DB 오염 차단 — tests/conftest.py에 DB 격리 픽스처 추가 후 재생성된 target_signup_user 재삭제 <!-- APPROVED: 2026-08-23/사용자 승인 -->

### 변경 영향 기록
- 변경 분류: 테스트 인프라 및 DB 격리
- 생산자: backend/tests/conftest.py
- 소비자: backend/tests/*, backend/stockauto.db
- 데이터 경계: pytest 테스트 런타임 및 개발 SQLite DB
- API 계약: API 스키마 변경 없음
- 문서 반영: docs/tasks/2026-08-23.md
- 회귀 테스트: python scripts/verify_harness.py 8/8 통과, pytest backend/tests 423/423 PASS, guard_dev_database_untouched 픽스처로 테스트 실행 전후 개발 DB 테이블(users, stock_translations, strategies, trade_logs) 행 수 불변 입증
- 미해결 위험: sqlite3 직접 연결 등으로 app.core.database의 SessionLocal을 우회해 개발 DB에 직접 쓰는 새 경로가 발생하면 guard_dev_database_untouched 가드가 테스트 세션 자체를 즉시 실패시킴.
- 인수인계: isolate_default_database 픽스처가 sys.modules에 로드된 모든 SessionLocal 참조를 인메모리 SQLite 세션 팩토리로 교체하여 get_db() 의존성 및 백그라운드 호출까지 완벽 격리함. strategies/stock_translations 참조 테이블은 읽기 전용으로 인메모리 복제함.

## 분석·문서 (코드 변경 없음)
- [R] 능동 타이밍 전략의 QQQ 초과 알파 부재 최종 판정 문서 작성 — docs/strategy_alpha_verdict.md 신설

### 변경 영향 기록
- 변경 분류: 문서 신설 (판정 원장). 코드 변경 없음
- 생산자: docs/strategy_alpha_verdict.md 신규 작성, docs/README.md 전략 문서 표에 1행 등록
- 소비자: 신규 전략 기획 착수 판단, 성과 리포트 작성 코드(scripts/run_institutional_backtest.py의 alpha_vs_qqq 배선), docs/strategy_specification.md 및 docs/strategy_map.md 참조 경로
- 데이터 경계: 문서 계층 한정. 백엔드·프론트엔드·DB 스키마·API 전부 무변경. 개발 DB는 mode=ro 읽기 전용 조회만 수행했고 쓰기 없음
- API 계약: API 스키마 변경 없음
- 문서 반영: docs/strategy_alpha_verdict.md(신설), docs/README.md(전략 문서 표 1행 추가), docs/tasks/2026-08-23.md
- 회귀 테스트: 인용 수치를 개발 DB와 코드에서 직접 산출. 매도 9,368건 중 퇴화 3종 제외 정상 표본 6,608건 거래당 -0.2242%(수수료 복원 gross -0.0242%, t -0.89), 전략 94종 calculate_score 대 get_cutoff_score 대조로 진입가능 21 미진입 73, 정상 18종의 청산 경로 필드 대조로 완전 7종 누락 11종, obs 3계정 peak-to-trough MDD 재계산(QQQ -5.43% TQQQ3x -17.45% core_satellite -8.54%). NFC 정규화 3파일 확인. python scripts/verify_harness.py 8/8 전체 통과(exit 0). 백테스트 엔진 이름 드리프트 추가 실측으로 전략이 읽는 142종 중 26종이 엔진에 없음을 확인하고 2장 전제를 하향 보정
- 미해결 위험: 문서 §4.2 수치는 main @982bb49 클린 트리 기준이고 §4.5(c)는 3개 슬라이스 병합 후 워킹트리 기준이라 두 기준이 공존한다. 병합 커밋 후 §4.2 기준 커밋 해시를 갱신해야 한다. AI_GRAPH_ENGINEERING.md가 main 미병합이라 §10에서 링크 대신 미병합 사실 명시로 우회했으므로 병합 시 정상 링크로 교체 필요. 판정의 1차 근거인 2,590종 챔피언십은 본 세션이 재실행하지 않고 기존 기록을 인용했다. 청산 경로 필드 누락 11종은 미수정 상태라 §3.1.2 표는 청산 경로 보강 후 재측정이 필요하다
- 인수인계: 이 문서는 명세가 아니라 판정이며 §6 반증 조건 R1~R6을 모두 만족하는 측정이 나올 때만 재개정한다. §7에 금지 항목 5건(신규 능동 타이밍 전략 추가, AI 그래프 Phase 2 상시 루프, 알파 근거 구독 과금, QQQ 벤치마크 없는 성과 리포트, 현 배선 상태 라이브 장기 가동)을 명시했다. §8 회귀 가드는 반드시 전략별 기대 상태 목록의 차분 검사여야 한다. 단순 미진입 종수 임계값은 오판한다 — 병합 전후 모두 미진입 73으로 종수가 같지만 구성은 퇴화 3종이 부활 3종으로 교체된 질적 변화였다

## 신규 작업 2
- [R] 라이브 신호 필드 계약 SSOT 및 스캐너 배선 복구 — signal_contract.py 신설, 스캐너 details 8개 표준 필드 병합 및 일봉 시간축 정규화, 상시 가드 배선
- [R] 조건 퇴화 3종 무조건 매수 차단 — 누락 지표를 0.0으로 비교해 항상 참이 되는 진입 조건 봉쇄 (darvas_box, donchian_breakout, opening_range_breakout)
- [R] 지수 레버리지 레짐(Leveraged Regime) 20년 다차원 파라미터 그리드 서치 — 45개 파라미터 조합 x 4개 자산 x 4개 헤지 모드 20년 전수 시뮬레이션 및 강건성 리포트 작성

### 변경 영향 기록
- 변경 분류: 라이브 신호 계약 복구 및 지수 레버리지 레짐 파라미터 그리드 최적화
- 생산자: backend/app/scanner/signal_contract.py, backend/app/scanner/scanner.py, backend/app/scanner/data_provider.py, backend/app/strategies/base_strategy.py, backend/app/strategies/darvas_box.py, backend/app/strategies/donchian_breakout.py, backend/app/strategies/opening_range_breakout.py, scripts/check_signal_field_contract.py, scripts/run_leveraged_regime_grid.py, docs/LEVERAGED_REGIME_GRID_REPORT.md, docs/SIGNAL.md
- 소비자: backend/app/bot/scheduler.py, backend/app/scanner/scanner.py, scripts/verify_harness.py, backend/tests/*
- 데이터 경계: 라이브 스캐너 시그널 계층, 전략 채점 계층, 백테스트 그리드 러너. DB 스키마·API 스키마 변경 없음
- API 계약: API 스키마 변경 없음
- 문서 반영: docs/SIGNAL.md(6장 신설), docs/LEVERAGED_REGIME_GRID_REPORT.md(신설), docs/strategy_alpha_verdict.md, docs/tasks/2026-08-23.md
- 회귀 테스트: python scripts/check_signal_field_contract.py 통과, pytest backend/tests/test_live_entry_signal_contract.py 8/8 통과, pytest backend/tests/test_change_contract_harness.py 9/9 통과, pytest backend/tests/test_leveraged_regime.py 15/15 통과, pytest backend/tests 전체 통과, scripts/run_leveraged_regime_grid.py 20년 전수 그리드 시뮬레이션 완결
- 미해결 위험: 라이브 미지원 외부 데이터 필요 22종(옵션체인, 호가, 수급 등)은 signal_contract.py에 UNSUPPORTED로 명시되었으며 카탈로그 노출 시 선택 불가 처리 예정. 관측 구간 불일치(relative_strength, dist_to_high)는 일봉 기준으로 정규화 완료됨.
- 인수인계: CANONICAL_FIELDS 8종이 scanner.py의 build_canonical_metrics를 통해 details에 자동 병합된다. 신규 지표 추가 시 반드시 signal_contract.py를 단일 원장으로 수정해야 하며 check_signal_field_contract.py 상시 가드가 verify_harness Step 2에서 검사함.

## 분석 (코드 변경 없음)
- [R] 라이브 진입 경로 전략 미발화 결함 실측 분석 — 카탈로그 100종 중 72종 영구 미진입(계정 72개 수익률 정확히 0.0), 3종은 조건 퇴화로 무조건 매수
  - 원인: 라이브 채점 입력(app/scanner/scanner.py:563 details 30키)과 전략이 요구하는 필드 불일치. BaseStrategy._safe_get 기본값 0.0이 누락을 조용히 삼킨다.
  - 퇴화 3종(darvas_box -43.25%, opening_range_breakout -24.31%, donchian_breakout -20.93%): close > 누락필드(0.0) 비교가 항상 참 → 무조건 진입, 청산 조건도 동시에 무력화.
  - 복구 난이도: 가격데이터로 계산 가능 49종(is_penny 4종 포함), 외부 데이터 필요 22종, 설계상 복합/자율 4종은 정상.
  - 파급: 백테스트는 121개 지표 컬럼으로 채점하고 라이브는 30키로 채점하므로 백테스트·챔피언십 성과가 라이브로 이전되지 않는다. 기본 2슬롯 모드(multi_slot)의 episodic_pivot 슬롯(50%)도 미진입 상태.

### 변경 영향 기록
- 변경 분류: 조건 퇴화 3종 진입 봉쇄 (라이브·백테스트 공통 채점 결함 수정)
- 생산자: backend/app/strategies/base_strategy.py, backend/app/strategies/darvas_box.py, backend/app/strategies/donchian_breakout.py, backend/app/strategies/opening_range_breakout.py, backend/tests/test_live_entry_signal_contract.py
- 소비자: backend/app/bot/scheduler.py 진입 채점 경로, backend/app/backtests/backtest_engine.py
- 데이터 경계: 전략 스코어링 계층. DB 스키마·API 스키마 변경 없음
- API 계약: API 스키마 변경 없음
- 문서 반영: docs/tasks/2026-08-23.md
- 회귀 테스트: python scripts/verify_harness.py 8/8 통과(pytest 431 passed, Playwright E2E 8 passed). 신규 test_live_entry_signal_contract.py 8건은 진입 키 목록을 app/scanner/signal_contract.py의 LIVE_SIGNAL_KEYS에서 직접 읽어 스캐너 변경 시 자동으로 어긋남을 검출한다. 수정 전 코드로 되돌려 네거티브 컨트롤 실패도 확인했다
- 미해결 위험: 청산 경로는 이번 수정 대상이 아니라 기존 보유분은 시그널 청산이 아니라 손절·트레일링으로만 정리된다. chaikin_atr과 gex_pinning에 volume_ma20 기본값 1.0 패턴이 남아 있어 해당 지표가 PENDING에서 풀리는 순간 같은 퇴화가 재현된다
- 인수인계: donchian_high_20 / orb_high_30m / volume_ma20 / BB_upper는 라이브뿐 아니라 백테스트 엔진에도 생산자가 없어 donchian_breakout과 opening_range_breakout은 백테스트에서도 무조건 매수였다. 이번 수정으로 두 경로 모두 거래 0건이 되며 기존 토너먼트 성적은 무효다. darvas_high/darvas_low는 backtest_engine.py:512에서 생산되므로 백테스트는 정상 동작한다. 필드 읽기는 _safe_get(row, '리터럴') 호출 형태를 유지했으므로 check_signal_field_contract.py 정적 가드와 호환된다
