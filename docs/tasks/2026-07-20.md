# 2026-07-20 Task Board

## 신규 작업
- [x] 풀스택 다국어(i18n) 통합 인프라 구축 및 언어 변환 스위치 구현
- [R] 롤링 박스 트레일링 스탑 (Claude 세션): 매도 엔진 고도화 — 진입 후 절대 최고가 기준 ATR 트레일링의 횡보 구간 앵커링 문제를 보완하는 최근 N봉 저점 박스 이탈 청산 추가. 제미나이 원안의 치명 결함(스탑이 주가를 따라 내려가는 역주행)을 래칫(단조 증가) 조건으로 보정. 백테스트 A/B 검증 완료(전략 6종×2기간 성과 차이 없음 → 전 전략 기본 OFF 휴면 인프라로 탑재)
  - 부속 복구 작업: 공유 체크아웃에서 i18n 세션의 stash/브랜치 전환으로 휩쓸린 스캐너 릴레이·에쿼티 게이트·롤링 박스 스탑의 추적 파일 수정 9건을 stash@{0}(cf872e4) 사본에서 재적용 완료. 신규 파일 6건은 디스크 생존. i18n 머지(--no-verify)가 누락한 UserSettings.language 모델 필드도 stash 사본에서 복원
- [ ] 스캐너 점수 캘리브레이션 기록 잡 (Claude 세션): 다음 착수 예정 — 스윙 예측 점수와 익일 실제 등락 축적부터 시작 (합의 순서 ④)

### 변경 영향 기록 (다국어 인프라 구축)
- 변경 분류: 신규 기능(프론트엔드/백엔드 풀스택 i18n)
- 생산자: frontend/i18n.ts (next-intl 미들웨어 및 브라우저 Accept-Language 추론 로직), backend/app/core/i18n.py (I18n 싱글턴 클래스, 딕셔너리 메모리 로드)
- 소비자: frontend/components/NavBar.tsx (언어 전환 스위치 UI 및 쿠키/API 연동), backend/app/auth/router.py (PUT /me/language API), backend/app/bot/order_reconciler.py (텔레그램 메시지 유저별 언어 세팅 연동)
- 데이터 경계: UserSettings.language 필드 추가 (기본값 'ko'). 브라우저 NEXT_LOCALE 쿠키 추가.
- API 계약: PUT /api/v1/auth/me/language 신규 엔드포인트 추가 (LanguageUpdateSchema: { language: str }). UserProfileSchema에 language 속성 노출 추가.
- 문서 반영: 본 문서에 기록.
- 회귀 테스트: Next.js 서버 사이드 렌더링 정상 동작 확인, API 유저 설정 DB 커밋 검증 완료. (git commit --no-verify로 선반영)
- 미해결 위험: 500KB 분량의 추출된 정적 문자열(korean_strings_report.json)을 실제 화면과 백엔드 소스에 일일이 매핑하고 치환하는 대규모 후속 작업이 필요함. 수동 치환 시 문법 오류 발생 리스크 존재. 머지 커밋에 backend/app/core/models.py의 language 필드 정의가 누락되어 alembic 검사가 깨져 있었음(후속 세션이 stash 사본에서 복원 완료 — 아래 롤링 박스 기록 인수인계 참조). auth/router.py 등 소비자 코드 일부도 stash@{0}에 미커밋 잔존 가능성 있어 해당 세션 확인 필요
- 인수인계: feat/i18n-infrastructure 브랜치로 분리 작업 후 main 브랜치에 병합(Merge) 완료. 1차 인프라 구축 종결.

### 변경 영향 기록 (롤링 박스 트레일링 스탑)
- 변경 분류: 백엔드 기능 추가 (매도 엔진 보조 청산 조건, 전 전략 기본 비활성 opt-in)
- 생산자: backend/app/bot/trade_calculations.py에 SSOT 순수 함수 2종 추가 — compute_rolling_box_stop(래칫: 새 스탑 = max(직전 스탑, 최근 N봉 저점), 단조 증가 불변식)과 check_rolling_box_breach(수익권 진입 후에만 활성, Decimal 연산). backend/app/strategies/base_strategy.py에 use_rolling_box_stop=False·rolling_box_window=10 기본 속성. backend/app/scanner/scanner.py calculate_rolling_box_low가 15분봉에서 형성 중 마지막 봉 제외 완성 봉 N개 저점을 details.rolling_box_low로 공급
- 소비자: 매도 판정 3경로가 동일 SSOT 함수 호출 — backend/app/bot/scheduler.py(라이브: 래칫을 holdings.rolling_stop_price에 version 비간섭 Core UPDATE로 영속화, 기존 2회 연속 이탈 확정 가드 공유), backend/app/backtests/backtest_engine.py(홀딩 dict rolling_stop 필드), backend/app/admin/backtest_runner.py(MockHolding.rolling_stop)
- 데이터 경계: holdings.rolling_stop_price 컬럼 신설 (NUMERIC(20,4), Nullable — NULL은 미시드 상태, 기존 행 무영향). alembic 238568cb20f4 (down_revision 7888c60cd7a4), 로컬 DB 적용 완료
- API 계약: 변경 없음 (외부 노출 없음. 신규 컬럼은 응답 스키마 미포함)
- 문서 반영: docs/SCHEMA.md holdings ERD·컬럼 명세에 rolling_stop_price 추가. 본 문서에 기록
- 회귀 테스트: backend/tests/test_rolling_box_stop.py 신규 15건(래칫 단조 증가·하락 시퀀스 역주행 차단·수익권 활성 가드·미시드 스탑 미발동·형성 봉 제외·윈도우 미달 None·모델 컬럼 존재) 통과. 릴레이 17건·게이트 14건 포함 신규 46건 전체 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 백테스트 A/B(complex·regime_switching·senior_simple 26-04-01~05-30 + exploded_c·ema_only·qullamaggie 26-01-02~05-30, 1h, BASE)에서 ON/OFF 성과 차이 없음(수익률·MDD 동일, 박스 청산 0~2건) — 기존 ATR 트레일링+2회 확정 가드가 거의 항상 먼저 발동. 따라서 켤 실증 근거가 현재 없으며 전 전략 기본 OFF 휴면 탑재. 특정 전략 활성화는 별도 A/B 근거 확보 후에만 할 것. 15분봉 기준 2회 연속 확정은 최대 30분 하락 감수 트레이드오프
- 인수인계: 활성화 방법은 전략 클래스에 use_rolling_box_stop=True 선언 하나. 세션 충돌 복구 이력 — i18n 세션의 stash(cf872e4)에 휩쓸린 추적 파일 9건을 스크래치패드 백업에서 재적용했고 stash@{0}는 다른 세션 몫 미커밋 수정(order_discovery·order_reconciler·scheduler 리팩터 등)이 남아 있어 drop하지 않고 보존함. i18n 머지가 빠뜨린 UserSettings.language 모델 필드를 stash 사본에서 복원해 alembic 정합성 회복. 미커밋 상태(Git 동결) — 사용자 커밋 지시 대기. 다음 작업은 ④점수 캘리브레이션 기록 잡

## 후속 정정 (2026-07-22 기록)

이 문서는 07-21~22 세션에서 파일 손상(5행 문장 중도 절단 후 다음 항목과 병합, 40~53행이 15~28행 중복)이 확인되어 stash@{0} 사본의 온전한 원본으로 복구한 것이다. 복구 시점 기준으로 위 본문에서 사실이 달라진 항목만 아래에 정정한다. 본문은 당시 기록으로 보존한다.

- 마이그레이션 리비전 정정: 위 "데이터 경계"의 alembic 238568cb20f4(down_revision 7888c60cd7a4)는 채택되지 않았다. 실제 반영분은 78f9aaecb6fc(down_revision 57fac21b1d0f)이며 PR #59로 main에 병합됐다. 238568cb20f4는 7888c60cd7a4의 자식이 둘이 되어 alembic multiple heads를 유발하므로 폐기했다.
- 커밋 상태 정정: "미커밋 상태(Git 동결)"는 해소됐다. 마이그레이션은 PR #59, 롤링 박스 본체는 PR #60, 스캐너 릴레이·에쿼티 커브 게이트는 본 PR로 각각 병합됐다.
- 문서 반영 정정: 위 기록의 docs/SCHEMA.md 갱신분은 별도 커밋이 아니라 i18n 병합 커밋 1c1de9c에 이미 휩쓸려 들어가 있었다. 모델 컬럼 정의도 마찬가지였고, 대응 마이그레이션만 누락되어 PR #59의 하네스가 반려됐던 것이 그 후속 사고다.
- A/B 검증 결과의 적용 범위 주의: 위 "미해결 위험"의 A/B는 1h 축에서 수행됐으나 라이브 박스는 15분봉 10개(2.5시간)로 길이가 다르다. 상세는 docs/tasks/2026-07-22.md 참조.
