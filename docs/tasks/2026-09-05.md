# 2026-09-05 Task Board

## 신규 작업
- [x] admin 계정에 HCTI(헬스케어 트라이앵글) 실계좌 매수 이력 11건 DB 수동 이관 — 손절 로직 실측 테스트용. 3회 시행으로 관할권별 청산 여부를 확정하고 원복까지 완료 <!-- APPROVED: 2026-09-11 사용자 승인 (PR #121 머지 후) -->

### 변경 영향 기록
- 변경 분류: 데이터 이관 (코드 변경 없음)
- 생산자: 일회성 스크립트로 trade_logs 11행(user_id=1, ticker=HCTI, BUY) 및 holdings 1행 직접 INSERT. 애플리케이션 코드는 수정하지 않았다
- 소비자: app/brokers/simulated_broker.py get_account_balance·get_holdings, app/bot/scheduler.py 매도 판정 루프, 대시보드 보유종목·거래이력 API
- 데이터 경계: 스키마 변경 없음. 가격 단위는 기존 규약대로 USD. 토스 표기 원화를 체결일(전 거래일) USD/KRW 종가로 환산했고, 환산가가 HCTI 실제 일중 고저 범위에 11건 중 10건 적중하는 것으로 티커·연도·날짜 매핑을 교차검증했다. executed_at은 체결일 16:00 UTC(=KST 익일 01:00)로 토스 KST 표기일과 일치한다
- API 계약: 변경 없음
- 문서 반영: 해당 없음
- 회귀 테스트: 코드 변경이 없어 verify_harness 미실행. 대신 삽입 전 DB 백업(스크래치패드 stockauto.db.bak-20260905), 중복 방지 선검사, 삽입 후 대조 조회로 검증했다. 수량 4762주·매입원가 $7,072.75·평단 $1.4852가 스크린샷 11건 합계와 일치함을 확인
- 미해결 위험: 이관분은 전량 원복됐고 admin 계좌에 잔재가 없다(trade_logs 0건, holdings 0건, 삭제 후 사이클을 한 번 지난 스냅샷이 9,659,069원/-3.41%로 원복 계산값과 일치). 다만 account_equity_snapshots 500건은 남겨 두었다 - 보존 정책이 이관 이전 기록을 이미 정리해 되돌릴 기준점이 없고, 지우면 admin 자산곡선이 통째로 사라지기 때문이다. 자산곡선을 판독할 때 2026-09-04부터 2026-09-10 구간에 실재하지 않았던 -48% 포지션이 반영되어 있음을 감안해야 한다. 실행 스크립트 3종은 스크래치패드에 있어 세션 종료 시 사라진다 - 재현이 필요하면 이 인수인계의 절차를 보고 다시 만들어야 한다
- 인수인계: 테스트 종결. 세 번의 시행에서 변수는 관할권 하나였고 결과가 갈렸다. (1차 2026-09-05, 격리 기능 이전) 이관 3분 만에 매도, (2차 2026-09-08, management=BOT_OWNED) 매도되어 실현손익 -3,283.63달러, (3차 2026-09-09~10, management=EXTERNAL) 10사이클 이상·백엔드 재기동 2회를 거치고도 매도 0건. 두 번의 매도는 모두 손절이 아니라 시그널 붕괴로 나갔다 - 손절은 노이즈 버퍼에 막혀 1/2에서 지연됐고 같은 사이클의 signal_collapse가 버퍼 없이 즉시 발동했다. 버퍼 2회 규칙은 이탈(breach) 계열에만 적용되며 smart_exit·signal_collapse는 단발로 나간다는 것이 실측으로 확인된 셈이다. 3차에서는 last_price가 매 사이클 실시세로 갱신됐으므로 미관측이 아니라 관측 후 판정 직전 분기임이 증명됐고, observed_base_price 자동 확정·guard_baseline_low 단조 감소 래칫(반등해도 따라 올라가지 않음)·수확 하한 가드도 함께 실측됐다. 관할권을 EXTERNAL에서 BOT_OWNED로 되돌릴 때는 management와 strategy_type을 반드시 함께 바꿔야 한다 - management만 바꾸면 EXTERNAL 분기에서는 빠지는데 strategy_type='external'이 슬롯 조회에 걸려 전략 판정에도 진입하지 못하는 사각지대가 생긴다(scheduler.py의 is_external 분기). 이 작업에서 파생된 후속 두 건은 별도 현황판이 소유한다 - 수확 임계값 화면 표시는 2026-09-09, ATR 시간 프레임 불일치 실증은 2026-09-10이다. 후자는 이 테스트 중 임계값이 15%/5% 하한에 눌려 있는 것을 보고 발견했다. 로그 판독 시 backend/logs/stockauto.log에 pytest 흔적이 섞여 실거래로 오인하기 쉽다([TestStrategy], peak 90.00, avg=100.0000, Order: SIM-H1 등) - 판단 근거는 로그가 아니라 DB다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
