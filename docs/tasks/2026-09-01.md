# 2026-09-01 Task Board

## 신규 작업

- [/] 토스 크롤러 무징후 전면 실패 복구 (사용자 지시 "토스 크롤러부터 고쳐줘"): 종목 발굴 유니버스의 3개 소스 중 토스가 6개 탭 전부 0건을 반환하는데도 경고 로그 한 줄만 남기고 조용히 넘어가, 유니버스가 네이버+야후 2소스로만 돌고 있었다. 실측 결과 (1) 랭킹 응답 핸들러가 `json.result`를 배열로 가정하나 실제 스키마는 `result.products`, (2) DOM 대체 경로가 읽는 `a[href$="/order"]` 앵커의 innerText가 전부 빈 문자열로 바뀌어 두 경로 모두 무력화됐다. 티커 문자열 추측을 걷어내고 productCode → 심볼 공식 조회로 전환한다.

### 변경 영향 기록
- 변경 분류: 스캐너 외부 수집 경로 버그 수정 (Node 크롤러 1개 재작성). DB 스키마·API 스키마·프론트엔드 무변경
- 생산자: backend/app/scanner/toss_scraper.js(재작성), docs/tasks/2026-09-01.md
- 소비자: backend/app/scanner/toss_scraper_runner.py(fetch_toss_market_scanners), backend/app/scanner/discovery.py(get_seed_tickers), 그 하위의 scanner.py·swing_predictor.py 유니버스 전체
- 데이터 경계: 크롤러 프로세스의 stdout JSON 계약(`{탭키: [티커...]}`) 그대로 유지. 러너·discovery는 무변경이므로 파이썬 계층 계약 경계는 건드리지 않는다. 외부 조회는 토스 공개 조회 API 읽기 전용이며 인증·주문 경로에 접근하지 않는다
- API 계약: 자체 API 스키마 변경 없음. 외부 의존 계약이 바뀐다 - 랭킹 데이터를 DOM 텍스트 정규식이 아니라 종목 링크 href의 productCode로 뽑고, `GET https://wts-info-api.tossinvest.com/api/v2/stock-infos?codes=` 배치 조회로 공식 symbol을 받는다
- 문서 반영: 영향 없음 - 외부 사이트 수집 경로의 내부 구현만 교체했고 크롤러 stdout JSON 계약(`{탭키: [티커...]}`)과 discovery 인터페이스가 그대로라 docs/ 장기 문서에 반영할 계약 변경이 없다. 수집 방식 전환과 검증 절차는 이 현황판에 기록했다
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과(pytest 444건, Playwright E2E 8건, exit 0). 메인 체크아웃에 다른 세션의 매크로 지표 슬라이스가 진행 중이라 혼합 트리에서는 test_live_entry_signal_contract.py 2건이 그쪽 미완성 계약(inflation_expectation, yield_curve_spread) 때문에 실패했다. 그래서 HEAD에 이 슬라이스 파일만 올린 격리 클론을 만들어 하네스를 다시 돌려 순수 통과를 받았다. 크롤러 본체는 실측으로 확인했다 - 토스 6개 탭 524건(고유 361종목) 20.7초, 네이버 5개 탭 249건(고유 188종목) 1.6초이며 양쪽 각각 무작위 40개 표본을 야후 심볼 조회에 대조해 전부 실존 종목임을 확인했다
- 미해결 위험: 이 크롤러는 토스 화면 구조에 계속 의존한다. 탭 버튼 value(biggest_total_amount 등)와 종목 링크 href 형식(/stocks/{productCode}/order)이 바뀌면 다시 0건이 되며, 이번에는 그 사실이 stderr에 탭별로 찍히지만 여전히 사람이 로그를 봐야 안다 - discovery의 LATEST_DISCOVERY_STATS에 소스별 실패를 드러내는 일은 다음 슬라이스로 남겼다. 둘째, 탭 클릭 후 2초 고정 대기 뒤 화면을 읽으므로 렌더가 늦으면 직전 탭 목록을 현재 탭으로 오귀속할 수 있다 - 다만 discovery가 모든 태그를 하나의 유니버스로 합치고 TOSS_* 태그는 점수에 쓰이지 않아(source_map은 WATCHLIST·릴레이 판정과 표시에만 사용) 영향은 표시상에 그친다. 셋째, 이미지·폰트·미디어 차단으로 20초까지 줄였으나 러너 타임아웃 45초와의 여유는 약 24초뿐이라 네트워크가 크게 느려지면 다시 타임아웃될 수 있다. 넷째, 이 Node 스크립트는 자동 테스트 사각지대다 - 파이썬 러너(test_toss_scraper_runner.py)는 stdout 계약만 검증하므로 크롤러 본체의 회귀는 실행해 봐야만 드러난다. 다섯째, 종류주 점→하이픈 변환은 미국 표기 기준이며 국내 종목을 시장 코드로 걸러내는 전제 위에서만 안전하다.
- 인수인계: 검증 절차는 `cd backend && venv/Scripts/python.exe -c "import asyncio; from app.scanner.toss_scraper_runner import fetch_toss_market_scanners; print(asyncio.run(fetch_toss_market_scanners()))"` 한 줄이면 된다 - 6개 탭이 각각 100건 근처(급상승·급하락은 국내 종목을 걸러내 40~80건)로 나오면 정상이고, 0건이면 stderr 메시지가 원인을 지목한다. 다음 슬라이스 후보는 두 가지다. (1) 네이버 크롤러도 같은 병을 앓고 있다 - naver_scraper.js의 extractVisibleTickers가 페이지의 모든 a/span/strong 텍스트에 정규식을 돌려 S&P의 'S'·'P', 'ETF', 'ADR', 로마숫자 'II'·'III', 회사명 조각 'YSX'(실제 종목은 YSXT) 같은 가짜 티커를 유니버스에 넣고 있다. 이번 토스와 같은 방식(종목 링크 기반 추출)으로 바꾸는 것이 사용자가 처음 보고한 'possibly delisted' 로그의 남은 원인을 없애는 길이다. (2) 소스별 실패의 가시화 - 토스가 6개 탭 전부 0건이었는데도 아무도 몰랐던 이유는 fetch_toss_market_scanners가 빈 결과를 경고 로그 한 줄로 끝내기 때문이다.

## 신규 작업 3

- [/] 네이버 크롤러 가짜 티커 혼입 제거 (사용자 지시 "네이버 크롤러도 같은 방식으로 고쳐줘"): naver_scraper.js의 extractVisibleTickers가 페이지의 모든 a/span/strong 텍스트에 정규식을 돌려 대문자 덩어리를 전부 티커로 승격시키고 있었다. 실측 결과 고유 223개 중 'S'·'P'(S&P 분해), 'ETF', 'ADR', 'VIX', 'I'·'II'·'III'(회사명 로마숫자), 'YSX'(실제 종목은 YSXT), 'UY', 'TSMC' 같은 존재하지 않는 심볼이 대량 포함됐다. 사용자가 처음 보고한 "possibly delisted" 로그의 실제 발생원이다. 토스와 동일하게 화면 텍스트 추측을 걷어내고 공식 API의 symbolCode로 전환한다.

### 변경 영향 기록
- 변경 분류: 스캐너 외부 수집 경로 버그 수정 (Node 크롤러 1개 재작성). DB 스키마·API 스키마·프론트엔드 무변경
- 생산자: backend/app/scanner/naver_scraper.js(재작성), docs/tasks/2026-09-01.md
- 소비자: backend/app/scanner/naver_scraper_runner.py(fetch_naver_us_rankings), backend/app/scanner/discovery.py(get_seed_tickers), 그 하위의 scanner.py·swing_predictor.py 유니버스 전체
- 데이터 경계: 크롤러 프로세스의 stdout JSON 계약(`{탭키: [티커...]}`) 그대로 유지. 러너·discovery는 무변경. 외부 조회는 네이버 공개 조회 API 읽기 전용이며 로그인·개인화 경로에 접근하지 않는다
- API 계약: 자체 API 스키마 변경 없음. 외부 의존 계약이 바뀐다 - 화면 텍스트 정규식 대신 탭별 공식 엔드포인트를 직접 호출한다. 거래대금=priceTop, 거래량=top, 상승=up, 하락=down은 `GET /front-api/worldstock/nation/stock/list`의 result.stocks[].symbolCode를, 인기토론은 `GET /front-api/discussion/ranking/list/price`의 itemCodes를 `POST /front-api/realTime/unified`로 심볼 해석해 얻는다. 이 과정에서 puppeteer 의존이 제거된다
- 문서 반영: 영향 없음 - 외부 사이트 수집 경로의 내부 구현만 교체했고 크롤러 stdout JSON 계약(`{탭키: [티커...]}`)과 discovery 인터페이스가 그대로라 docs/ 장기 문서에 반영할 계약 변경이 없다. 수집 방식 전환과 검증 절차는 이 현황판에 기록했다
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과(pytest 444건, Playwright E2E 8건, exit 0). 메인 체크아웃에 다른 세션의 매크로 지표 슬라이스가 진행 중이라 혼합 트리에서는 test_live_entry_signal_contract.py 2건이 그쪽 미완성 계약(inflation_expectation, yield_curve_spread) 때문에 실패했다. 그래서 HEAD에 이 슬라이스 파일만 올린 격리 클론을 만들어 하네스를 다시 돌려 순수 통과를 받았다. 크롤러 본체는 실측으로 확인했다 - 토스 6개 탭 524건(고유 361종목) 20.7초, 네이버 5개 탭 249건(고유 188종목) 1.6초이며 양쪽 각각 무작위 40개 표본을 야후 심볼 조회에 대조해 전부 실존 종목임을 확인했다
- 미해결 위험: 브라우저를 걷어내고 공개 API를 직접 호출하므로 네이버가 UA·Referer 외의 토큰이나 쿠키를 요구하도록 바꾸면 즉시 실패한다 - 다만 이번에는 탭별 실패 사유가 stderr에 남고(HTTP 상태·API 거부 메시지), 러너가 빈 결과를 실패로 판정한다. 둘째, stockPriceSortType 열거값(marketValue|up|down|top|priceTop|dividend)과 인기토론의 rankingType=foreignStock은 네이버 내부 계약이라 예고 없이 바뀔 수 있다. 셋째, 랭킹 4개 탭은 각 50건 1페이지만 받는다 - 화면의 탭이 실제로 보내는 요청과 동일하게 맞춘 값이며, 늘리려면 page를 증분해야 한다. 넷째, 결과에 워런트·권리주(PSNYW, GIWWR, KLXER 등)와 유닛(VACI-U)이 섞여 들어온다. 네이버가 실제로 거래 가능 종목으로 노출하는 항목이라 걸러내지 않았으나, 야후 표기가 다른 종류가 있으면 하위 조회에서 개별 실패할 수 있다. 다섯째, 이 Node 스크립트도 토스와 마찬가지로 자동 테스트 사각지대다.
- 인수인계: 검증은 `cd backend && venv/Scripts/python.exe -c "import asyncio; from app.scanner.naver_scraper_runner import fetch_naver_us_rankings; print(asyncio.run(fetch_naver_us_rankings()))"` 한 줄이면 되고 1~2초에 끝난다(이전 puppeteer 방식은 20초 이상). 5개 탭이 각 50건 근처로 나오면 정상이다. 실측 249건/고유 188종목이었고 무작위 40개 표본을 야후에 대조해 전부 실존 종목임을 확인했다. puppeteer 의존이 사라졌지만 backend/app/scanner/package.json의 puppeteer는 toss_scraper.js가 계속 쓰므로 제거하면 안 된다.

## 신규 작업 4
