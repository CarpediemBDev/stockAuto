# 2026-06-25 작업 현황판

## 1. 모의투자 수익률 환율 왜곡 보정
- **상태:** `[x]`
- **작업 설명:** `LocalSimulatedBroker.get_account_balance()`에서 미투자 현금의 환율 변동분이 수익률로 반영되는 문제를 제거하고, 수익률을 원화 기준 거래 손익률로 계산하도록 테스트를 보강했다.

### 변경 영향 기록
- **변경 분류:** 데이터 흐름, 백그라운드 스냅샷 계산
- **생산자:** `backend/app/bot/simulated_broker.py`의 `LocalSimulatedBroker.get_account_balance`
- **소비자:** `backend/app/bot/scheduler.py`의 `admin_balance_cache_sync`, `backend/app/admin/router.py`의 사용자 목록 수익률 표시
- **데이터 경계:** 사용자별 `user_id` 기준 `Holding`, `TradeLog`, `AccountEquitySnapshot` 계산 경계 유지
- **API 계약:** 응답 필드 변경 없음. `profit_rate`, `profit_loss`, `total_asset` 계산에서 미투자 현금 환율 변동 효과를 수익률에서 제외
- **문서 반영:** 영향 없음 - API/DB 스키마 변경 없이 계산 보정과 테스트 보강만 수행
- **회귀 테스트:** 미거래 사용자 환율 변동 수익률 0%, 동일 USD 손익에서 환율 변경 시 수익률 고정 테스트 추가
- **미해결 위험:** 기존 DB에 저장된 과거 왜곡 스냅샷은 별도 데이터 보정 작업을 수행하지 않음
- **인수인계:** 과거 `AccountEquitySnapshot` 보정이 필요하면 별도 마이그레이션 또는 정리 스크립트 범위로 시작

### 검증 결과
- `backend\venv\Scripts\python.exe -m py_compile backend/app/bot/simulated_broker.py backend/tests/test_simulated_broker_balance.py` 통과
- `backend\venv\Scripts\python.exe -m pytest backend/tests/test_simulated_broker_balance.py -q` 통과: `2 passed`
- `backend\venv\Scripts\python.exe -m pytest backend/tests/test_admin_equity_snapshots.py -q` 통과: `3 passed`
- `backend\venv\Scripts\python.exe scripts/verify_harness.py` 실패: 기존 `test_rate_limiter.py`, `test_scheduler_runtime_safety.py`, `test_toss_scraper_runner.py`, `test_trading_flow_scenarios.py` 실패가 남아 있었음

## 2. Redis 무한 대기 수정
- **상태:** `[R]`
- **작업 설명:** `scheduler.py`의 `run_user_trading_flow`에서 DB 세션 연결 실패 시 Redis Lock 반환이 누락되어 무한 대기처럼 보일 수 있는 흐름을 `try/finally` 구조로 보정했다.

## 3. TradeLog 매수 기록 영구 누락 복구
- **상태:** `[R]`
- **작업 설명:** `scheduler.py`와 `order_reconciler.py`에서 매수 체결 성공 후 `Holding`만 생성/갱신되고 `TradeLog` 기록 생성이 누락되던 버그를 수정했다.

## 4. 음수 보유 수량 방어
- **상태:** `[R]`
- **작업 설명:** `sync_broker_holdings`에서 실제 계좌와 DB 간 주식 수량 불일치가 발생했을 때 차액을 하나의 전략 보유분에 몰아 넣어 음수 수량이 기록될 수 있던 흐름을 보정했다.

## 5. Reconciler 레이스 컨디션 차단
- **상태:** `[R]`
- **작업 설명:** `reconcile_open_orders_once`에 비동기 Redis Lock(`acquire_symbol_order_lock`)을 도입해 동일 주문 또는 동일 종목에 대한 중복 reconciliation을 차단했다.

### 2~5 공통 변경 영향 기록
- **변경 분류:** [Bugfix] 치명 오류 수정
- **생산자:** `backend/app/bot/scheduler.py`, `backend/app/bot/order_reconciler.py`
- **소비자:** 주문 상태 관리 데몬, 프론트엔드 거래 기록 표시 컴포넌트
- **데이터 경계:** `Holding`, `TradeLog`, `BrokerOrder` 데이터 정합성 보장 강화
- **API 계약:** 변경 없음 - 백그라운드 트랜잭션 무결성 강화
- **문서 반영:** `docs/tasks/2026-06-25.md` 반영
- **회귀 테스트:** `verify_harness.py` 실행 및 관련 테스트 보강
- **미해결 위험:** SQLite 환경에서는 행 잠금 보장이 제한되므로 Redis Lock 기반 간접 동시성 제어에 의존
- **인수인계:** 장기적으로 DB 행 잠금/트랜잭션 격리를 강화하는 마이그레이션 검토 필요

## 6. 품질 검사 및 테스트 하네스 보강
- **상태:** `[x]`
- **작업 설명:** `verify_harness.py` 실행 과정에서 드러난 `test_trading_flow_scenarios.py`, `scheduler.py`, `test_api_integration.py`, `test_scheduler_runtime_safety.py` 관련 실패를 정리하고 테스트 타임아웃을 보정했다.

### 변경 영향 기록
- **변경 분류:** [Bugfix] 테스트 하네스 안정화
- **생산자:** `backend/app/bot/scheduler.py`, `scripts/verify_harness.py`
- **소비자:** 백그라운드 매매 흐름 및 전체 검증 하네스
- **데이터 경계:** 테스트 DB 세션과 운영 DB 세션 경계 유지
- **API 계약:** 변경 없음
- **문서 반영:** 영향 없음 - 테스트 안정화 작업
- **회귀 테스트:** 전체 pytest 및 `verify_harness.py` 타임아웃 확장 후 검증
- **미해결 위험:** 없음
- **인수인계:** 테스트 안정화 내용 확인 후 추가 작업 없음

## 7. DB 마이그레이션 롤백 및 스키마 원상 복구
- **상태:** `[x]`
- **작업 설명:** `trade_mode` 및 `broker_provider` 컬럼 추가를 위해 시도되었던 최근 Alembic 마이그레이션을 롤백하고 해당 컬럼을 삭제해 DB 스키마를 원래 상태로 복구했다.

### 변경 영향 기록
- **변경 분류:** [Refactor] DB 스키마 롤백 및 정리
- **생산자:** `backend/alembic/versions/37240920731f`, `4a5b6c7d8e9f`, `5b6c7d8e9f0a`, `6c7d8e9f0a1b`, `d5e98c93f4d5`
- **소비자:** DB 스키마 의존 컴포넌트 전체
- **데이터 경계:** `holdings`, `broker_orders`, `trade_logs` 테이블의 `trade_mode`, `broker_provider` 컬럼 제거
- **API 계약:** 변경 없음
- **문서 반영:** `docs/tasks/2026-06-25.md` 반영
- **회귀 테스트:** 로컬 검증
- **미해결 위험:** 없음
- **인수인계:** 해당 마이그레이션 파일들의 문법 오류 수정 후 커밋 완료

## 8. 품질 검사 기반 종합 리팩터링
- **상태:** `[/]`
- **작업 설명:** 여러 에이전트가 지적한 보안 취약점, 아키텍처 위반, 성능/동시성 병목, 테스트 커버리지 누락 및 코드 스타일 문제를 해결하기 위한 종합 리팩터링 수행 중.

### 변경 영향 기록
- **변경 분류:** [Refactor] Security, Architecture, Performance, Testing
- **생산자:** `auth/router.py`, `core/rate_limiter.py`, `core/locks.py`, `bot/scheduler.py`, `bot/order_reconciler.py`
- **소비자:** 전체 시스템
- **데이터 경계:** 인증 쿠키, Redis Lock, 주문 원장, 자동매매 세션 경계
- **API 계약:** Origin 헤더 검증 추가, Rate Limit 예외 응답 변경 가능성 검토 필요
- **문서 반영:** `docs/tasks/2026-06-25.md`
- **회귀 테스트:** `pytest` 전체 수행 예정
- **미해결 위험:** 대규모 리팩터링으로 인한 회귀 버그 가능성
- **인수인계:** 2026-06-26 작업에서 미커밋 회귀를 이어서 검토
