# 2026-07-12 Task Board

## core_satellite 전략 정식화 — 슬롯모드 문자열을 독립 전략 클래스(SSOT)로 승격
- [/] 사용자 지시: obs_core가 쓰는 `core_satellite`가 지금은 MultiStrategyManager 하드코딩 슬롯모드라 독립 전략 스크립트가 아님. 이를 `CoreSatellite`(BaseStrategy 서브클래스) 정식 클래스로 만들어 구성(leveraged_regime 70% + strategy_c 30%)의 단일 출처로 삼고, strategy_factory에 등록. MultiStrategyManager는 하드코딩 대신 클래스의 선언된 구성을 읽어 슬롯을 생성. 기존 obs_core 슬롯·가중치·프리픽스·매매 동작 100% 보존(행동 무변경 리팩터링).

### 변경 영향 기록
- 변경 분류: 리팩터링 — 복합 전략 core_satellite의 구성을 매니저 하드코딩에서 독립 전략 클래스(SSOT)로 이관, 팩토리 등록
- 생산자: backend/app/strategies/core_satellite.py(신규 CoreSatellite 클래스·SATELLITE_SLOTS 구성), backend/app/strategies/strategy_factory.py(core_satellite 분기 등록), backend/app/bot/multi_strategy_manager.py(core_satellite 슬롯을 클래스 구성에서 파생), backend/tests/test_leveraged_regime.py(core_satellite 클래스·슬롯 파생 케이스 추가)
- 소비자: obs_core 계정 자동매매 루프(MultiStrategyManager), get_strategy 소비 경로(translator·admin), 전략 카탈로그
- 데이터 경계: 없음 — 코드 구조 리팩터링. strategies 테이블 core_satellite 행은 기존 시드 유지, 신규 데이터 없음
- API 계약: 변경 없음 — 전략 식별자 core_satellite 기존 유지, 슬롯 구성값(70/30) 불변
- 문서 반영: 영향 없음 - 내부 전략 구조 리팩터링으로 구성값·식별자·API 무변경이라 docs/ 장기 계약 문서 영향 없음. 본 현황판에 기록
- 회귀 테스트: test_leveraged_regime.py에 CoreSatellite 팩토리·is_composite·SATELLITE_SLOTS·매니저 슬롯 파생 동일성 케이스 추가. python scripts/verify_harness.py 전체 통과 필요
- 미해결 위험: 리팩터링 후 MultiStrategyManager('core_satellite') 슬롯이 기존과 완전 동일해야 obs_core 라이브 동작 무변경 — 테스트로 슬롯 키·가중치 동일성 강제. 실행 중 서버는 모듈 캐시라 재기동 시 반영
- 인수인계: 구현→verify_harness→[R] 보고. Git 동결 규칙상 커밋은 사용자 지시 시. 커밋 시 별도 브랜치 분리 필요

## 신규 작업
- [R] 안티그래비티 신규 및 휩소/TTL 결함 핫픽스 통합 적용

### 변경 영향 기록
- 변경 분류: 백엔드 아키텍처 및 알고리즘 핫픽스
- 생산자: data_provider.py, backtest_engine.py, run_tournament.py, turn_of_month.py, antigravity_surge.py, hma_swing.py
- 소비자: run_tournament.py, backtest_engine.py, 실시간 매매 봇
- 데이터 경계: 공용 Parquet 디스크 캐시 및 개별 전략 내부 메모리 연산
- API 계약: yfinance 다운로드 캐싱 및 subprocess IPC 데이터 형식 변경
- 문서 반영: docs/tasks/2026-07-12.md
- 회귀 테스트: pytest scenario harness 및 Playwright E2E smoke
- 미해결 위험: 없음
- 인수인계: 신규 전략 17종이 정상 탑재되고 실거래 stale 캐시 버그와 백테스트 다바스 박스 오류가 수정됨
