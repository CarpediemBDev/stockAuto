# 2026-07-19 Task Board

## 신규 작업
- [x] StrategyCatalog 전략 선택 저장 계약 수정 — 전략만 바꿔도 trade_mode 미전송으로 422/설정 유실되던 버그 해결 (PR #46 대체분)
- [R] 스캐너 릴레이 파이프라인 (Claude 세션): 애프터장 스캐너 캐시 + 스윙 예측 DB 스냅샷을 읽어 다음날 아침 15분 스캐너의 우선 감시 티커 리스트를 만드는 신규 모듈 backend/app/scanner/watchlist_relay.py 추가, scanner.py 유니버스 정렬에 연결. DB 스키마 무변경, 소스 소실 시 정상 스캔으로 자연 강등
- [R] 에쿼티 커브 게이트 (Claude 세션): (전략 슬롯×레짐)별 최근 청산 완료 트레이드 롤링 윈도우의 프로핏 팩터로 신규 매수 배분 계수(1.0/0.6/0.3)를 산출하는 신규 모듈 backend/app/bot/equity_gate.py 추가, 스케줄러 수량 산출 직전에 곱셈 적용. DB 읽기 전용, 콜드 스타트(표본 부족) 시 계수 1.0 고정

### 변경 영향 기록
- 변경 분류: 버그수정(프론트엔드)
- 생산자: frontend/components/StrategyCatalog.tsx (handleSelectStrategy → POST /admin)
- 소비자: backend/app/admin/router.py update_user_settings (POST /admin, 전체 덮어쓰기)
- 데이터 경계: UserSettings(trade_mode·broker_provider·telegram_chat_id·telegram_enabled·strategy_type) — 클라이언트가 기존 설정을 함께 실어 보내 유실 방지
- API 계약: POST /admin SettingsUpdateSchema(trade_mode 필수) 불변 — 프론트가 필수·보존 필드를 채워 계약 준수. 스키마/백엔드 무변경
- 문서 반영: 영향 없음 - E2E 테스트 Mock 수정 건이므로 별도 기술 문서 갱신 불필요
- 회귀 테스트: npx tsc --noEmit(0), npm run lint(0 error/0 warning), python scripts/verify_harness.py 전체 통과(Playwright E2E 포함, exit 0)
- 미해결 위험: MOCK/REAL 모드에서 전략 변경 시 브로커 인증(verified) 재검증 통과 필요 — 기존 설정 페이지 저장과 동일 동작이라 신규 위험 아님
- 인수인계: PR #46(fix/strategy-catalog-data-loss)과 동일 목적, 본 브랜치가 대체. 병합 후 PR #46 close. auth-smoke E2E는 목 추가 없이 통과 확인되어 PR #46의 e2e 변경분은 불필요로 제외함. 미커밋 상태(Git 동결) — 사용자 커밋/푸시/머지 대기

### 변경 영향 기록 (스캐너 릴레이 파이프라인)
- 변경 분류: 백엔드 기능 추가 (스캐너 파이프라인 연결, DB 스키마 무변경)
- 생산자: backend/app/scanner/watchlist_relay.py 신규 — 애프터장 캐시(read_after_hours_candidate_cache)와 스윙 예측 캐시(read_swing_prediction_cache, 인메모리→DB 스냅샷 폴백)를 읽어 우선 감시 티커 맵을 생성. 소스별 채택 기준: 애프터장 score 65 이상·스윙 score 60 이상, 소스당 상위 15개, 신선도 72시간 이내
- 소비자: backend/app/scanner/scanner.py scan_market_expert — (1) 시드 유니버스에 릴레이 티커 합류 및 source_map에 RELAY_* 태그 병기, (2) Stage 1 점수 면제(WATCHLIST와 동급), (3) Stage 2 상위 25 컷에서 밀린 릴레이 후보를 예약 슬롯 최대 5개로 추가 편입(순수 추가형 — 기존 컷 구성·순서 불변, AI 슬롯 배정 순서상 릴레이 추가분은 후순위)
- 데이터 경계: DB 읽기 전용(SwingPredictionSnapshot 조회), 신규 테이블·컬럼 없음. 릴레이 자체 저장소 없음 — 두 소스의 기존 캐시를 읽기만 함
- API 계약: 변경 없음 (스캐너 응답 형식 동일, source 필드에 RELAY_AFTER_HOURS/RELAY_SWING 태그가 추가될 수 있으나 기존 소비자는 목록 포함 여부만 검사)
- 문서 반영: docs/tasks/2026-07-19.md 본 항목. 전략 카탈로그 무관(전략 아닌 스캐너 인프라)
- 회귀 테스트: backend/tests/test_watchlist_relay.py 신규 17건(신선도 경계·점수 하한·소스당 상한·중복 제거·소스 개별/전체 실패 강등·예약 슬롯 순수 추가성·중복 편입 방지) 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 릴레이 편입으로 Stage 2 후보가 최대 30개(25+5)로 늘어 정밀 분석 비용이 소폭 증가. 애프터장 캐시는 인메모리 전용이라 저녁~아침 사이 서버 재시작 시 해당 몫 소실(스윙 스냅샷만으로 동작, 오동작 아님). get_relay_priority_map은 동기 DB 접근이 있어 asyncio.to_thread로 격리했으나 콜드 스타트 시 DB 왕복 1회 발생
- 인수인계: 효과 관측은 다음 개장일 스캔 로그의 "[Relay] N priority tickers merged" 라인으로 확인. 채택 기준 상수(AFTER_HOURS_MIN_SCORE·SWING_MIN_SCORE·MAX_PER_SOURCE·RELAY_RESERVED_SLOTS)는 watchlist_relay.py 상단에 모여 있음. 미커밋 상태(Git 동결) — 사용자 커밋 지시 대기

### 변경 영향 기록 (에쿼티 커브 게이트)
- 변경 분류: 백엔드 기능 추가 (자금관리 메타 레이어, DB 읽기 전용·매매 판정 로직 무변경)
- 생산자: backend/app/bot/equity_gate.py 신규 — TradeLog에서 (user×전략 슬롯×레짐)의 최근 30일 내 청산(SELL, realized_pnl 존재) 최신 20건을 읽어 프로핏 팩터를 Decimal로 산출, PF 1.2 이상=1.0 / 1.0~1.2=0.6 / 1.0 미만=0.3 배분 계수 반환. 표본 8건 미만 콜드 스타트·무손실 윈도우·내부 오류는 전부 1.0(무개입) 폴백
- 소비자: backend/app/bot/scheduler.py process_entry_signals — 슬롯별 1회 게이트 계수를 조회해 calculate_entry_quantity의 신규 파라미터 equity_gate_factor(기본값 1.0)로 전달, 제안 금액에 곱셈 적용. 스로틀 발동 시 WARNING 로그 기록. 자율 슬롯·레짐 진입 경로(process_autonomous_slots)는 게이트 비대상(스캐너 시그널 기반 신규 진입만 게이트)
- 데이터 경계: TradeLog 읽기 전용, 신규 테이블·컬럼 없음. 2슬롯 자본 격리 장벽 불변(슬롯 내 배분 금액만 축소)
- API 계약: 변경 없음 (외부 노출 없음. calculate_entry_quantity 신규 파라미터는 기본값 1.0으로 기존 호출부 호환)
- 문서 반영: docs/tasks/2026-07-19.md 본 항목
- 회귀 테스트: backend/tests/test_equity_gate.py 신규 14건(콜드 스타트·PF 경계값 1.2/1.0 정밀 판정·무손실 분모 0 특이점·전손실·Decimal 정밀도·DB 오류 fail-open·수량 산출 곱셈 반영·기본값 하위 호환) 통과. python scripts/verify_harness.py 전체 통과
- 미해결 위험: 게이트는 배분 축소만 하고 진입 자체는 차단하지 않음(계수 0.3에서도 최소 1주 매수 가능 경로 존재 — calculate_entry_quantity의 final_qty=1 보정 유지). 스로틀 상태에서 거래 빈도 감소로 윈도우 갱신이 느려지는 자기 고착은 30일 시간 감쇠로 완화했으나 완전 제거는 아님. 임계값(PF 1.2/1.0, 계수 0.6/0.3)은 경험적 초기값으로 실운영 데이터 축적 후 재조정 필요
- 인수인계: 스로틀 발동 여부는 액션 로그의 "[Equity Gate]" WARNING으로 확인. 다음 단계는 ②롤링 박스 트레일링 스탑(래칫 보정판, trade_calculations.py SSOT 함수 + 소비처 3곳 교체 + holdings 컬럼 추가 alembic — 백테스트 A/B 검증 선행)과 ④점수 캘리브레이션(기록 잡 먼저). 미커밋 상태(Git 동결) — 사용자 커밋 지시 대기

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

### 변경 영향 기록 (다국어 지원 인프라 및 소비자 코드 병합)
- 변경 분류: 프론트엔드/백엔드 공통 (기능 개선 및 버그 수정)
- 생산자: frontend/package.json, frontend/i18n.ts, backend/app/core/i18n.py
- 소비자: frontend/components/NavBar.tsx, frontend/lib/api.ts, backend/app/auth/router.py, backend/app/bot/order_reconciler.py, backend/app/core/models.py
- 데이터 경계: UserSettings 테이블에 language 필드 추가, 쿠키 NEXT_LOCALE 사용.
- API 계약: PUT /auth/me/language 엔드포인트 신설. GET /auth/me 응답에 language 포함.
- 문서 반영: SCHEMA.md 언어 필드 스키마 반영 완료.
- 회귀 테스트: npm run lint(0), npx tsc --noEmit(0), python scripts/verify_harness.py (0)
- 미해결 위험: 없음.
- 인수인계: 머지 완료 시 다음 단계로 한국어 하드코딩 텍스트 실전 치환 작업(Dashboard 등) 진행.

## stash 복원분 (2026-07-22 기록)

07-20 세션 충돌 당시 이 현황판은 두 세션이 각자 편집한 두 갈래로 갈라졌고, main에는 한쪽만 남았다. `stash@{0}`에만 있던 나머지 갈래를 stash drop 전에 여기로 옮긴다. 옮기면서 **코드 실물과 대조해 사실이 다른 항목은 아래에 정정을 병기했다.** 원래 문구는 보존한다.

### 당일 등록됐으나 main 현황판에서 누락됐던 작업 항목

- [x] 롤링 박스 트레일링 스탑 (Claude 세션): 매도 엔진 고도화 — 진입 후 절대 최고가 기준 ATR 트레일링의 횡보 구간 앵커링 문제를 보완하는 최근 N봉 저점 박스 이탈 청산을 추가. 제미나이 원안의 치명 결함(스탑이 주가를 따라 내려가는 역주행)을 래칫(단조 증가) 조건으로 보정. trade_calculations.py SSOT 순수 함수 + 백테스트 엔진 A/B 검증 선행 + 소비처 3곳(scheduler·backtest_engine·backtest_runner) 공유 + holdings.rolling_stop_price 컬럼(alembic)
  - 정정: 원문은 `[/]`(진행 중)이었으나 07-22 기준 완료됐다. 컬럼·마이그레이션(78f9aaecb6fc)은 PR #59, 본체는 PR #60으로 병합. 상세와 활성화 전 해소 조건은 docs/tasks/2026-07-22.md 참조
- [ ] 텔레그램 영문 메시지 전면 한글화: order_discovery.py, order_reconciler.py 내의 영문 알림을 한글로 변경.
  - 정정: 원문은 `[R]`(구현·검증 완료, 승인 대기)이었으나 **해당 코드 변경은 어디에도 존재하지 않는다.** 07-22에 확인한 결과 order_discovery.py와 order_reconciler.py는 stash@{0} 버전과 main 버전이 완전히 동일하며, order_discovery.py의 텔레그램 문구는 여전히 영문(`[ORDER INTENT ABORTED]`, `[ORDER DISCOVERY AMBIGUOUS]`, `Order Recovery Ambiguous` 등)이다. order_reconciler.py에 있는 한글은 docstring·주석이지 전송 문구가 아니다. 따라서 미착수로 되돌려 재등록한다
- [ ] 풀스택 공통 JSON 다국어(i18n) 시스템 도입: 프론트엔드 및 백엔드 전역에서 공통 JSON 딕셔너리를 사용하여 텍스트(UI, 알림, 예외 메시지 등)를 동적 렌더링/발송하도록 아키텍처 개편
  - 정정: 인프라와 1차 소비자 코드는 PR #58~#59 계열로 병합 완료. 잔여 화면 하드코딩 텍스트 치환은 07-20·07-21 현황판에서 이어짐

### 변경 영향 기록 (텔레그램 한글화) — 미달성 기록으로 보존

아래는 stash에 남아 있던 원문 그대로다. 위 정정대로 **대응 코드가 실재하지 않으므로 완료 기록으로 인정하지 않는다.** 실제 착수 시 이 기록을 채워 다시 쓸 것.

- 변경 분류: 리팩터링(한글 통일)
- 생산자: backend/app/bot/order_discovery.py (미아 주문 알림), backend/app/bot/order_reconciler.py (체결 상태 알림)
- 소비자: 사용자 텔레그램 수신처
- 데이터 경계: 변경 없음
- API 계약: 변경 없음
- 문서 반영: docs/tasks/2026-07-19.md 본 항목
- 회귀 테스트: 파이썬 구문 검사(py_compile) 완료
- 미해결 위험: 없음
- 인수인계: 기존 신규 작업과 충돌 없는 텍스트 변경. 사용자 승인/커밋 대기
