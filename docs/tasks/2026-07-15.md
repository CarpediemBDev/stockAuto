# 2026-07-15 Task Board

## 신규 작업
- [x] 알렘빅 마이그레이션 후 stockauto 파일 로거 사망 버그 수정 — env.py fileConfig에 disable_existing_loggers=False 적용 (verify_harness 통과, 사용자 승인·PR 머지, 이슈 #30)

### 변경 영향 기록
- 변경 분류: 버그 수정 (로깅 인프라)
- 생산자: backend/alembic/env.py (fileConfig 호출부 — disable_existing_loggers=False 적용, config_file_name None 가드 추가)
- 소비자: backend/app/core/logging.py의 stockauto 로거를 쓰는 전체 백엔드 (스케줄러·SSE·거래 로그 등 마이그레이션 이후 파일 로그 전부)
- 데이터 경계: 없음 (DB 스키마·데이터 변경 없음, 로깅 설정만 변경)
- API 계약: 없음 (HTTP API 무변경)
- 문서 반영: 없음 (코드 주석으로 원인 기록)
- 회귀 테스트: backend/tests/test_migrator_safety.py::test_migration_keeps_stockauto_logger_alive 신규 추가, 파일 내 3건 통과. 실측: run_migrations_programmatically() 실행 후 logger.disabled=False 및 마커 라인 stockauto.log 기록 확인
- 미해결 위험: alembic.ini의 root/alembic 콘솔 핸들러 재설정으로 마이그레이션 중 콘솔 라인 중복 출력은 기존과 동일하게 잔존 (파일 로그와 무관)
- 인수인계: 다음 서버 기동 시 stockauto.log가 "[Migration] ... 완료" 이후에도 계속 쌓이는지 육안 확인 권장

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
