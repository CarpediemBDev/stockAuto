# 2026-07-15 Task Board

## 신규 작업
- [x] 알렘빅 마이그레이션 후 stockauto 파일 로거 사망 버그 수정 — env.py fileConfig에 disable_existing_loggers=False 적용 (verify_harness 통과, 사용자 승인, 이슈 #30 close, PR #31 머지 `7900eca`, 실서버 기동 검증 완료)

### 변경 영향 기록
- 변경 분류: 버그 수정 (로깅 인프라)
- 생산자: backend/alembic/env.py (fileConfig 호출부 — disable_existing_loggers=False 적용, config_file_name None 가드 추가)
- 소비자: backend/app/core/logging.py의 stockauto 로거를 쓰는 전체 백엔드 (스케줄러·SSE·거래 로그 등 마이그레이션 이후 파일 로그 전부)
- 데이터 경계: 없음 (DB 스키마·데이터 변경 없음, 로깅 설정만 변경)
- API 계약: 없음 (HTTP API 무변경)
- 문서 반영: 없음 (코드 주석으로 원인 기록)
- 회귀 테스트: backend/tests/test_migrator_safety.py::test_migration_keeps_stockauto_logger_alive 신규 추가, 파일 내 3건 통과. 실측: run_migrations_programmatically() 실행 후 logger.disabled=False 및 마커 라인 stockauto.log 기록 확인. verify_harness 전체 통과(로컬 2회 + PR CI harness 통과)
- 미해결 위험: alembic.ini의 root/alembic 콘솔 핸들러 재설정으로 마이그레이션 중 콘솔 라인 중복 출력은 기존과 동일하게 잔존 (파일 로그와 무관, 필요 시 후속 과제). 로컬 기동 로그에 Redis 미기동 시 RedisLockUnavailable 트레이스백 관찰됨 — 본 건과 무관한 기존 동작
- 인수인계: 전 파이프라인 종결 — 이슈 #30 생성·close, PR #31 머지(머지 커밋 7900eca, main 반영·로컬 main fast-forward 완료), fix/alembic-env-logger-disable 브랜치는 원격·로컬 모두 삭제. 최종 실서버 검증: python run.py local 기동 후 "[Migration] ... 완료" 라인 이후에도 stockauto.log가 892→1,274줄로 지속 증가(스케줄러·스캐너 로그 정상 타임스탬프 기록) 확인, 검증용 서버는 종료(포트 8000 해제 확인). 잔여 작업 없음. 참고: 작업 트리에 본 건과 무관한 타 세션 변경(docs/README.md, docs/tasks/2026-07-13.md, docs/learn/, docs/plans/cross_platform_synergy_blueprint.md)이 미커밋 상태로 남아 있음 — 본 건에서 건드리지 않았음

## SIMULATED 잔고 누적 실현손익 계산을 SQL SUM으로 이관 (전량 스캔 제거)
- [x] 사용자 지시: LocalSimulatedBroker.get_account_balance가 누적 실현손익을 구할 때 유저의 모든 SELL TradeLog를 .all()로 전량 로드해 파이썬 sum으로 합산(simulated_broker.py:29-51). 거래 이력이 쌓일수록 매 호출마다 O(행수) 로드·객체화로 느려짐. 이를 DB측 func.sum(realized_pnl) 스칼라 집계로 교체해 행 로드 없이 숫자 하나만 받도록 한다. realized_pnl은 Numeric(20,4) Decimal이라 SQL SUM도 정밀 연산이라 기존 결과와 동일. 행동 보존 리팩터(성능·정확도 무변경).
- 실측 검증: python scripts/verify_harness.py 전체 통과(EXIT=0). 계약·수치/프로세스/카오스/롤백·컴파일278·NFC443·alembic·pytest 255 passed·프론트 tsc/lint·Playwright E2E 8/8. 신규 test_simulated_realized_pnl_sql_sum_matches_python_sum로 소수점 scale4·음수·유저격리·BUY/NULL 제외를 파이썬 Decimal 합산 기준값과 동일함을 강제. 프로덕션(비-SQLite decimal)·로컬(SQLite float 경유) 양쪽 안전 위해 scale 4 양자화.

### 변경 영향 기록
- 변경 분류: 성능 리팩터 — 누적 실현손익 집계를 파이썬 전량 스캔에서 DB SQL SUM으로 이관(행동 보존)
- 생산자: backend/app/bot/simulated_broker.py(get_account_balance의 trade_logs 전량 로드+파이썬 sum → func.sum 스칼라 집계로 교체), backend/tests/test_simulated_broker_balance.py(집계 동일성 회귀 케이스 추가)
- 소비자: SIMULATED 모드 잔고를 쓰는 전 경로 — /account/balance, 1분 스케줄러 스냅샷, 백그라운드 갱신, reset/force-liquidate 후 재계산
- 데이터 경계: TradeLog 읽기 전용 집계(쓰기 없음). 스키마·데이터 변경 없음
- API 계약: 변경 없음 — /account/balance 응답값 불변(누적손익 계산 결과 동일, 계산 방식만 내부 변경)
- 문서 반영: 영향 없음 - 내부 계산 방식 리팩터로 응답값·API·계약 무변경이라 docs/ 장기 계약 문서 갱신 불필요. 본 현황판에만 기록
- 회귀 테스트: test_simulated_broker_balance.py에 SQL SUM 결과가 기존 파이썬 sum과 동일함을 강제하는 케이스 추가(0건·다건·음수 손익 포함). python scripts/verify_harness.py 전체 통과 필요
- 미해결 위험: SQL SUM은 매도 0건 시 NULL 반환 → to_decimal(None)=0으로 방어. realized_pnl NULL 행은 기존 필터(realized_pnl != None) 유지로 제외. trade_logs가 합산 외 용도 없음을 확인하고 쿼리 자체를 스칼라로 대체
- 인수인계: 구현·검증 완료, 사용자 승인으로 [x] 마감. perf/simulated-realized-pnl-sql-sum 브랜치로 PR 진행

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
