# 2026-07-24 Task Board

## 신규 작업
- [x] SSE 감사 리포트 잔여 결함 4건 수정 (Claude 세션, 전용 worktree D:\dev\workspace\stockAuto-sse / 브랜치 fix/sse-audit-followups): 07-16 감사 교차검증에서 타당 판정 후 보류했던 저위험 다듬기 4건. ①결함3 publish_sync의 이벤트 루프 블로킹 — 전수 조사 결과 async 핸들러에서 발행하는 유일한 지점은 watchlist/router.py:49(add_to_watchlist)이므로 그 호출만 asyncio.to_thread로 격리(publish_sync 자체의 의도된 동기 설계는 보존). ②결함5 SSE 클라이언트의 리프레시 만료 터미널 처리 누락 — lib/sse.ts가 refreshAuthSession 실패 시 백오프 재연결만 반복(30초 캡)하고 로그아웃하지 않아, axios 인터셉터에 있는 clearAuth+로그인 리다이렉트 규약과 불일치. ③결함6 전역 에러 바운더리 부재 — app/error.tsx 신설(Next 16.2 규약 확인: reset이 아닌 unstable_retry 프롭 사용, v16.2.0 도입). ④결함7 AccountBalance 파생 연산 매 렌더 재계산 — walletAllocations/totalCalculated를 useMemo로 격리(조기 반환 위로 이동해 hooks 규칙 준수). 원 리포트의 결함8(BotSignals 스켈레톤 플래핑)은 SWR v2 isLoading이 백그라운드 재검증에서 true가 되지 않아 불성립으로 판정, 수정 대상에서 제외 <!-- APPROVED: 2026-07-25 사용자 "승인" (PR #81 병합 후) -->

### 변경 영향 기록
- 변경 분류: 안정성·UX 다듬기 4건 (백엔드 발행 경로 스레드 격리 1건 + 프론트 세션 종료 처리·에러 바운더리·메모이제이션 3건, 계약 무변경)
- 생산자: backend/app/watchlist/router.py(async add_to_watchlist의 sse.notify_watchlist를 asyncio.to_thread로 격리, asyncio import 추가), frontend/lib/sse.ts(EventStreamClient.endSession 신설 — stop+clearAuth+로그인 리다이렉트, 401 리프레시 실패 시 백오프 재연결 대신 호출), frontend/app/error.tsx(신규 App Router 에러 바운더리, Next 16.2 unstable_retry 프롭 사용), frontend/components/AccountBalance.tsx(walletAllocations·totalCalculated를 useMemo로 격리하고 조기 반환 위로 이동)
- 소비자: POST /watchlist 호출 클라이언트(응답·상태 코드 동일, 발행이 스레드로 빠져 루프 블로킹만 제거), SSE 구독 전 화면(리프레시 토큰 만료 시 30초 주기 영구 재연결 대신 즉시 로그아웃 — axios 인터셉터와 동일 규약), 모든 페이지·중첩 레이아웃(런타임 오류 시 백지 대신 폴백 UI), 대시보드 잔고 카드(격리형 지갑 목록 렌더 결과 불변)
- 데이터 경계: 변경 없음 (DB·Redis 채널·이벤트 이름·페이로드 형식 미접촉, 발행 실행 위치만 이동)
- API 계약: 변경 없음 (엔드포인트 시그니처·응답 스키마·SSE 프레임 형식 동일, docs/API_STANDARD.md 영향 없음)
- 문서 반영: 영향 없음 - 외부 계약·스키마 무변경인 내부 실행 경로·클라이언트 UX 보강으로, 불변식은 코드 주석(발행 스레드 격리 이유, hooks 위치 고정, error.tsx가 루트 레이아웃을 못 감싸는 한계)으로 집행
- 회귀 테스트: 전용 worktree(stockAuto-sse)에서 py_compile 통과, backend pytest 전체 통과(exit 0, 366건), npx tsc --noEmit 통과, npm run lint 통과(react-hooks/preserve-manual-memoization 위반 1건은 지역 상수 추출로 해소). 전체 python scripts/verify_harness.py 결과는 인수인계 참조
- 미해결 위험: app/error.tsx는 Next 규약상 루트 layout.tsx 자체의 오류를 잡지 못함 — 완전 커버에는 app/global-error.tsx가 추가로 필요(별도 과제). SSE 세션 종료가 window.location.href 전체 리다이렉트라 SPA 라우팅보다 무겁지만 axios 인터셉터 기존 동작과 일치시켜 규약 단일화를 택함. publish_sync 자체의 동기 설계는 의도적으로 보존(async 발행 지점이 늘면 동일 격리를 반복 적용해야 함)
- 인수인계: 07-16 SSE 감사 리포트의 잔여 4건 종결. 원 리포트 결함8(BotSignals 스켈레톤 플래핑)은 SWR v2 isLoading 특성상 불성립으로 판정해 미수정. 작업은 docs/MULTI_SESSION_WORKTREE.md 규칙에 따라 전용 worktree D:\dev\workspace\stockAuto-sse(브랜치 fix/sse-audit-followups)에서만 수행했고 메인 체크아웃·타 세션 파일은 일절 건드리지 않음. worktree에는 검증용으로 .env 3종·DB 복사 + frontend/node_modules 정션을 두었으며 병합 후 git worktree remove로 정리 가능

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
