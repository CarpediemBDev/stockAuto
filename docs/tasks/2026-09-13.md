# 2026-09-13 Task Board

## 신규 작업
- [x] CLAUDE.md → AGENTS.md import 전환 (링크 참조를 `@AGENTS.md` 자동 주입으로 교체) <!-- APPROVED: 2026-09-13 사용자 승인 -->
- [x] 전역 PreToolUse 훅 `block_dangerous.py` 의 `dd` 패턴 오탐 수정 + 미등록 고아 사본 정리 <!-- APPROVED: 2026-09-13 사용자 승인 -->
- [x] AGENTS.md 레거시 규칙 정리 (Antigravity/Codex 잔재 삭제, Git 조항 중복 통합, 롤플레이 주입문 압축, Linear 섹션 폐지) <!-- APPROVED: 2026-09-13 사용자 승인 -->
- [x] AGENTS.md가 단일 기준으로 지목하는 하위 문서 정합화 (`docs/AI_WORKFLOW.md` Linear·서브에이전트 참조 제거, `docs/MULTI_SESSION_WORKTREE.md` 실재하지 않는 `stockAuto-ag` 배치 정정) <!-- APPROVED: 2026-09-13 사용자 승인 -->

### 변경 영향 기록
- 변경 분류: 문서/운영 규칙 정리 및 개발 도구(훅) 버그 수정. 런타임 코드·API·DB 스키마 변경 없음.
- 생산자: AGENTS.md(규칙 SSOT), CLAUDE.md(세션 부트스트랩), ~/.claude/hooks/block_dangerous.py(전역 PreToolUse 가드)
- 소비자: 본 저장소에서 기동하는 모든 AI 세션(Claude Code 등) 및 인간 개발자. 훅은 전역 등록이므로 NexHubStudio·stock-auto-mobile 세션도 소비자에 포함된다.
- 데이터 경계: 없음. 사용자 데이터·DB·캐시를 읽거나 쓰지 않는다.
- API 계약: 변경 없음. FastAPI 라우터·응답 래퍼·프론트 계약 모두 무관.
- 문서 반영: 본 작업 자체가 문서 반영이다. AGENTS.md 본문 정리, CLAUDE.md 도구 번역표 폐지, 8절의 워크트리 훅 재연결 안내 문장을 실제 전역 등록 상태에 맞게 현행화. AGENTS.md가 단일 기준으로 지목하는 docs/AI_WORKFLOW.md 와 docs/MULTI_SESSION_WORKTREE.md 도 같은 범위에서 정합화했다.
- 회귀 테스트: python scripts/verify_harness.py 전체 통과 (SUCCESS). 계약 검사 2건, 백엔드 컴파일 342파일, NFC 576파일, Alembic 최신, pytest, 프론트 lint/tsc, Playwright E2E 모두 OK. 최초 통과 이후 AGENTS.md 9-1 락 규칙과 본 인수인계 문구를 추가했으므로, 타 세션과 직렬화를 합의하고 락이 빈 상태에서 한 번 더 실행해 최종 문서 상태로 재확인했다(재실행도 SUCCESS). 훅 패턴은 하네스 범위 밖이라 별도 회귀표 12케이스(현황판 경로·날짜 양식·실제 블록 쓰기 대소문자·of= 유무)로 검증해 전 케이스 통과했고, 훅에 직접 payload를 먹여 deny/ask/통과 판정도 대조했다.
- 미해결 위험: 훅의 dd 패턴을 of= 인자 동반 조건으로 좁혔다. of= 없이 리다이렉션으로 디스크를 덮어쓰는 형태는 기존 패턴 `>\s*/dev/sd` 가 계속 잡으므로 방어는 이중으로 유지된다. 다만 of= 를 쓰는 비파괴적 이미지 백업(dd if=/dev/sda of=backup.img)도 DENY 로 걸리는 과차단이 남는다. 빈도가 극히 낮고 DENY 쪽으로 안전하게 기운 오차라 수용한다.
- 인수인계: 하네스 1차 실행은 BLOCKED, 2차 실행은 SUCCESS 였다. 원인은 이 체크아웃에서 하네스를 동시에 두 개 돌릴 수 없다는 점이다. frontend/.next-e2e 의 next build 락 때문에 나중에 진입한 실행이 Another next build process is already running 으로 웹서버 기동에 실패한다. 동시에 작업하던 다른 세션이 자신의 실행도 본 세션의 01:33(PID 3140), 01:39(PID 16492) 실행과 연달아 충돌해 두 번 깨졌다고 알려와 교차 확인했다. 편집 중간 상태를 쓸어담은 탓이라는 최초 추정은 철회한다. 커밋·푸시 훅도 전체 하네스를 돌리므로 커밋만 해도 같은 충돌이 나며, 하네스 실행과 커밋은 착수 전에 다른 세션에 알려 직렬화해야 한다. 본 작업의 변경분은 문서 5개(AGENTS.md, CLAUDE.md, docs/AI_WORKFLOW.md, docs/MULTI_SESSION_WORKTREE.md, 본 현황판)와 전역 훅 1개뿐이다. backend/app/bot/scheduler.py 와 backend/tests/test_harvest_mode.py 는 동시 작업 중인 다른 세션의 슬라이스다. 미추적 파일 backend/scripts/evaluate_atr_timeframe.py 는 2026-09-10 현황판의 진행 중 항목 "ATR 시간 프레임 불일치 실증"의 산출물이며, yfinance 쿼터를 라이브 스캐너와 공유해 장 세션이 CLOSED 일 때만 실행 가능해 아직 한 번도 실행되지 않았다. 출처 불명 파일이 아니므로 삭제하지 말 것. 백테스트 결과가 나온 뒤 해당 작업 세션이 처리할 몫이다. 커밋 트리거를 받으면 main 직접 커밋 금지 원칙에 따라 docs/ 타입 단기 브랜치를 만들고 위 문서 5개만 명시적으로 스테이징해야 한다. git add -A 금지.

## 수확 임계값 기록의 공유 객체 오염 제거

- [x] `_persist_harvest_thresholds`가 Core UPDATE 뒤 공유 detached holding의 매핑 컬럼(`harvest_arm_pct`·`harvest_trailing_pct`)을 직접 세팅하는 두 줄을 제거한다. 같은 함수의 독스트링이 금지한 행위이며, 중복 쓰기 방지 비교는 사이클마다 DB에서 새로 읽는 값으로 이미 성립한다 <!-- APPROVED: 2026-09-13 사용자 승인 -->

### 변경 영향 기록
- 변경 분류: 내부 정리 (런타임 동작·API·스키마 변경 없음). PR #121 감사에서 비차단 지적으로 남긴 항목의 후속.
- 생산자: backend/app/bot/scheduler.py(_persist_harvest_thresholds 말미 두 줄 제거), backend/tests/test_harvest_mode.py(회귀 1건 추가, _run_cycle이 추적 객체를 반환하도록 확장)
- 소비자: 없음. 함수는 표시용 컬럼을 DB에 남기는 역할만 하고 외부 호출자는 스케줄러 자신뿐이다. API 응답·프론트·모바일은 DB 값을 읽으므로 영향이 없다.
- 데이터 경계: DB 쓰기 경로는 그대로다(version 비간섭 Core UPDATE 1건). 제거 대상은 커밋 이후 메모리 객체에 같은 값을 덧쓰던 두 줄뿐이라 저장되는 값과 시점이 달라지지 않는다.
- API 계약: 변경 없음.
- 문서 반영: 영향 없음 - 계약·스키마 문서가 규정하는 것은 컬럼의 의미와 표시 전용 원칙이고, 이번 변경은 그 규정을 지키는 방식만 바로잡는다. 컬럼·응답·판정 규칙이 그대로이므로 고칠 문장이 없다.
- 회귀 테스트: backend/tests/test_harvest_mode.py에 test_threshold_record_leaves_the_shared_holding_clean 1건 추가. 사이클이 끝난 뒤 공유 holding 인스턴스가 dirty가 아님(sqlalchemy inspect(obj).modified is False)을 단언하고, 동시에 DB에는 값이 정상 기록됐는지도 확인한다. 기존 version_id 단언만으로는 이것이 고정되지 않는다 - micro_session이 빠져나가며 expunge_all로 객체를 떼어내 더럽혀진 객체가 flush되지 않고 다음 사이클에 새 객체로 교체돼 증상이 가려지기 때문이다. 네거티브 컨트롤로 제거한 두 줄을 되살리자 이 1건만 정확히 실패했고(modified가 True) 되돌리니 통과했다. python scripts/verify_harness.py 8/8 통과 - pytest 584건, tsc, eslint, Playwright 9건. 앞선 2회는 타 세션(workspace-07) 하네스와의 실행 경합으로 8단계가 BLOCKED이었다. 두 번 다 테스트 단언 실패는 0건이고 webServer 기동 단계에서 next build 락 충돌(Another next build process is already running)로 깨졌다 - 코드가 아니라 경합이 원인이라는 판별 근거다. 해당 세션과 직렬화를 합의한 뒤 락이 빈 상태에서 재실행하자 동일 트리에서 전부 통과했다.
- 미해결 위험: 기능적 위험은 낮다고 판단한다 - 제거한 두 줄은 DB 쓰기가 끝난 뒤 메모리 객체에 같은 값을 덧쓰던 것뿐이라 저장 값과 시점이 달라지지 않고, 중복 쓰기 방지 비교는 사이클마다 DB에서 새로 읽은 값으로 성립한다(운영 경로가 ctx.holdings를 매 사이클 재조회한다). 다만 이 전제가 깨지면(한 사이클에서 같은 보유분을 두 번 처리하거나 ctx.holdings를 재사용하도록 바꾸면) 매 사이클 같은 값을 다시 쓰는 낭비가 생길 수 있다 - 기능 오류는 아니고 UPDATE 한 건이 늘어나는 수준이다.
- 인수인계: 이 두 줄을 다시 넣지 말 것. 매핑 컬럼을 공유 detached holding에 세팅하면 auto-merge가 version_id를 올려 Part B 매도의 db.merge(h)가 StaleDataError로 사이클을 통째 중단시키며, 이는 last_price·highest_price·observed_base_price가 모두 비영속 스냅샷으로 우회하는 이유다. 새로 추가한 회귀 테스트가 dirty 여부로 이것을 고정한다. 하네스 8/8 통과로 검증은 끝났고 상태는 [R](승인대기)다. 다음 시작 지점은 사용자 승인 또는 커밋 트리거다. 커밋 트리거는 아직 받지 않았다 - 커밋 시 AGENTS.md·CLAUDE.md·docs/AI_WORKFLOW.md(타 세션 소유)와 backend/scripts/evaluate_atr_timeframe.py(미완 작업)는 스테이징에서 제외해야 한다.

## ATR 시간 프레임 불일치 실증 종결

- [x] 2026-09-10 현황판의 진행 중 항목 "ATR 시간 프레임 불일치 실증"을 이어받아 전체 표본 백테스트를 실행하고 결론을 기록한 뒤, 산출 스크립트 `backend/scripts/evaluate_atr_timeframe.py`를 커밋한다. 브랜치: `chore/atr-timeframe-verdict` <!-- APPROVED: 2026-09-13 사용자 승인 (PR #125 머지 후) -->

### 변경 영향 기록
- 변경 분류: 분석 도구 커밋과 실증 결론 기록. 런타임 코드·API·DB 스키마·매매 로직 변경 없음.
- 생산자: backend/scripts/evaluate_atr_timeframe.py(신규, 수동 실행 전용), docs/tasks/2026-09-10.md(실행 로그·결론·9필드), 본 현황판
- 소비자: 없음. 어떤 런타임 경로도 스크립트를 import 하지 않는다.
- 데이터 경계: stockauto.db trade_logs SELECT 전용, yfinance 일봉 다운로드. 쓰기 없음. 장 세션 CLOSED에서만 실행.
- API 계약: 변경 없음.
- 문서 반영: 영향 없음 - 코드 동작이 바뀌지 않아 장기 문서에 고칠 문장이 없다. 결과·결론·위험의 상세 원장은 docs/tasks/2026-09-10.md의 해당 항목이다.
- 회귀 테스트: 스크립트 전체 표본 실행 exit 0(2,559건). 커밋은 pre-commit 훅의 verify_harness 전체 통과를 거쳐 생성됐다.
- 미해결 위험: 결론(교체 안 함)을 흔들 수 있는 판독 기준의 구조적 편향, A 근사의 전제, simulate()의 봉 내 청산 순서 비대칭, 코드 약속과 실제 상수 동작의 불일치가 남는다. 상세는 2026-09-10 현황판 미해결 위험 다섯 항목.
- 인수인계: 결론은 일봉 ATR 교체 안 함(개선분 중앙값 -0.98%). 다음 시작 지점은 사용자 승인([R]→[x])과, 상수로 굳은 동적 임계를 코드·문서에 맞춰 정리할지에 대한 사용자 결정이다. 오늘 이 체크아웃은 세 세션이 공유했으며 PR #123·#124 머지 후 쓰기 세션이 이 세션 하나로 줄어 전용 worktree 없이 진행했다.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 매매 사이클 성능 회귀 대응 (계측 · 방어 조치 게이트)

브랜치: `fix/2026-09-13-cycle-profile-guard-gate`

모바일 세션 보고로 확인된 회귀다. NY 정규장 기준 사이클 간격 중앙값이 8월 129~155초에서 W36 346초, W37 426초로 늘었고 발생 창은 8/25~8/31이다. 이전 기준선(124초·125초)은 휴장 표본이 섞여 회귀를 가렸다. Redis 장애·가동 유저 증가·지표 CPU 비용 가설은 측정으로 기각했으며 원인은 미확정이다.

- [x] 매매 사이클 구간별 소요 시간 계측 — 현재 `[Cycle] Duration`은 총합만 남겨 어느 구간에서 시간이 새는지 알 수 없다. 사이클 전역 단계와 사용자 흐름 내부 단계를 구간별로 누적해 사이클 종료 시 한 줄로 남긴다. 장중 측정은 백엔드 재기동 후 사용자 판단 <!-- APPROVED: 2026-09-13 사용자 승인 (PR #126 병합) -->
- [x] 방어 조치 게이트를 시간 주도로 교정 — `GUARD_SUSTAIN_CYCLES=10`과 20분을 모두 요구해 정규장 주기(약 426초)에서는 실제 약 71분이 걸린다. 화면 문구 "20분"과 괴리가 크다. 횟수를 작은 하한으로 낮춰 20분이 게이트를 주도하게 한다 <!-- APPROVED: 2026-09-13 사용자 승인 (PR #126 병합) -->

### 변경 영향 기록
- 변경 분류: 관측성 추가(사이클 구간 계측) + 매도 게이트 파라미터 변경(방어 조치 연속 확인 하한 10 → 3). 스키마·API 계약 변경 없음
- 생산자: backend/app/bot/cycle_profiler.py(신설 - CycleProfile, phase, 루프 막힘 감시), backend/app/bot/scheduler.py(async_trading_loop 사이클 구간·감시 태스크·Profile 요약 줄, run_user_trading_flow 사용자 구간, micro_session DB 구간, process_exit_signals의 analyze_single_ticker 구간, 방어 게이트 주석), backend/app/bot/trade_calculations.py(GUARD_SUSTAIN_CYCLES 10 → 3과 근거 주석)
- 소비자: 운영자(stockauto.log의 [Cycle] Profile 줄), _evaluate_guard의 조치 게이트(SHADOW 알림·LIQUIDATE 부분 청산 시점), 모바일·웹의 방어 조치 안내 문구("20분 넘게 이어지고 여러 차례 확인"), backend/tests/test_cycle_profiler.py, backend/tests/test_guard_liquidation.py
- 데이터 경계: DB 스키마·쓰기 경로 변경 없음. 계측은 프로세스 메모리의 ContextVar와 로그에만 남으며 사이클 밖(수동 매도·테스트·백필)에서는 기록하지 않는다. 방어 조치는 guard_streak·guard_streak_started_at 컬럼을 그대로 쓰고 임계만 바뀐다
- API 계약: HTTP 계약 변경 없음. 방어 조치의 시간 계약이 문서와 실제가 일치하도록 바뀐다 - 20분 지속과 그 사이 최소 3회 연속 확인. docs/API_STANDARD.md 6.4절 갱신
- 문서 반영: docs/API_STANDARD.md 6.4절, docs/plans/holding_management_modes.md 5.4절 가드 3항(하한 변경 경위 포함), docs/SYSTEM_MANUAL.md에 "매매 사이클 주기와 계측 로그 읽는 법" 절 신설(틱이 버려지는 원리, Profile 줄 해석, 휴장 표본 배제 원칙)
- 회귀 테스트: backend/tests/test_cycle_profiler.py 신규 11건 - 사이클 밖 무기록, 누적·예외 시 기록, gather·to_thread 전파, 루프 막힘 감지와 정상 루프 무기록, 매매 루프 통합(요약 줄·감시 태스크 회수·조기 반환 시 요약 생략), 실제 run_user_trading_flow 배선. backend/tests/test_guard_liquidation.py에 계약 테스트 1건 추가(20분 경과 후 2회는 보류, 3회에서 조치) - 숫자를 리터럴로 고정했다. 네거티브 컨트롤: 하한을 10으로 되돌리면 계약 테스트가, 2로 낮추면 계약 테스트와 연속 리셋 테스트가 깨지는 것을 확인했다. 계측 통합 테스트는 작성 직후 실패했는데 원인이 계측 결함이었다 - 사이클 마지막 막힘 구간에서 감시 태스크가 늦게 깨어난 것을 확인하기 전에 취소되어 기록이 사라졌다. 취소 시에도 해당 구간의 지연을 계산해 남기도록 고친 뒤 통과했다
- 미해결 위험: 첫째, 회귀의 원인은 여전히 미확정이다. 계측은 원인을 찾는 도구이며 백엔드가 현재 정지 상태라 장중 측정은 재기동 후에만 가능하다. 둘째, 하한 3은 정규장 주기(약 7분)에서 20분 동안 관측되는 횟수에 맞춘 값이라 주기가 10분 이상으로 더 느려지면 여전히 횟수가 시간보다 늦게 충족된다(3회 = 20분 초과). 셋째, 수확·손절의 지연은 이번 변경으로 줄지 않는다 - 사이클 사이에는 관측 자체가 없으므로 사이클 주기를 줄여야만 해소된다. 넷째, 루프 막힘 감시는 0.1초마다 깨어나는 태스크를 하나 추가하므로 극히 작지만 사이클에 부하를 더한다. 다섯째, 모바일 폴링에 추가된 GET /account/holdings가 실계좌에서 증권사 API를 캐시 없이 호출하는 문제는 이번 범위가 아니다(현재 가동 97명 전원 SIMULATED라 잠재 위험)
- 인수인계: 다음 시작 지점은 백엔드 재기동 후 NY 정규장 중 [Cycle] Profile 줄 수집이다. 먼저 loop.blocked가 cycle.user_flows_wall의 대부분인지 본다 - 그렇다면 원인은 네트워크 대기가 아니라 루프를 붙잡는 동기 작업이고 해법은 해당 구간을 to_thread로 빼는 것이다. 아니라면 누적이 큰 user.* 구간과 exit.analyze_single_ticker 호출 횟수(사용자별 중복 조회)를 본다. 회귀 발생 창은 NY 8/25~8/31이며 그 사이 백엔드 커밋은 e2942ff·92c25ed·9d753d3·bbcec0a·481e21f다. 이 중 e2942ff의 지표 계산 비용은 1회 0.17초로 측정되어 기각했다. 기준선은 반드시 휴장 표본을 빼고 정규장끼리 비교한다. 방어 조치 문구는 모바일 세션이 "20분"으로 확정하기로 했다. 커밋은 하지 않았다 - 사용자 트리거 대기. 워킹트리의 docs/tasks/2026-09-09.md 한 줄 변경은 stockauto-92 세션 소유라 스테이징에서 제외해야 한다
