# 2026-07-03 (금) - 코드 품질 개선 및 결함 수정 일지

## 백엔드 거래 안전성 및 스케줄러 안정화 패치 (Phase 1)

- [x] 백엔드 거래 안전성 및 스케줄러 안정화 패치 (Phase 1)
  - [x] `scheduler.py` 내 `execute_and_poll_order` 호출 시 `symbol_lease` 분산 락 주입
  - [x] `process_exit_signals` 내 미정의 `db` 변수 사용 NameError 제거 및 `with micro_session` 가드로 통일
  - [x] `async_trading_loop` 예외 분기 시 `finally` 블록에서 `db.close()` 세션 클린업 보장
  - [x] APScheduler 스레드 내 비동기 호출 시 `asyncio.run_coroutine_threadsafe`를 사용하도록 보정
  - [x] 스케줄러 루프 기동 전 `LocalSimulatedBroker.process_unfilled_orders(db)`를 정기적으로 수행하도록 복원
  - [x] `database.py` 내의 미사용 `micro_session()` 중복 정의 삭제
  - [x] SQLite 환경의 커넥션 풀을 과도한 50개 설정 대신 StaticPool 또는 소량으로 최적화

### 변경 영향 기록
- 변경 분류: `데이터 흐름`, `API 계약`, `동시성 제어`, `리소스 관리`
- 생산자: `backend/app/bot/scheduler.py`, `backend/app/bot/simulated_broker.py`, `backend/app/core/database.py`
- 소비자: 자동 매매 스케줄러 루프, SQLite 커넥션 관리자, 백엔드 메모리 DB 세션 관리
- 데이터 경계: user_id 기준 분산 락 소유 및 개별 사용자 DB 세션 격리
- API 계약: 변경 없음 (내부 트랜잭션 및 스레딩 모델 개선)
- 문서 반영: `docs/tasks/2026-07-03.md`
- 회귀 테스트: py_compile, pytest, chaos/rollback 테스트
- 미해결 위험: 없음
- 인수인계: 수정 후 backend py_compile 및 pytest 통과 검증 후 Phase 2로 이관

---

## 프론트엔드 Hydration 에러 방어 및 데이터 왜곡 수정 (Phase 2)

- [x] 프론트엔드 Hydration 에러 방어 및 데이터 왜곡 수정 (Phase 2)
  - [x] `timezoneStore.ts` 내 `useTimezone` 수화(Hydration) 타이밍 불일치 방어벽 주입
  - [x] `BacktestTournament.tsx` 내 다전략 평가 곡선 시계열 축의 왜곡 및 데이터 평탄화 결함 수정
  - [x] `BacktestTournament.tsx` 등 다수 UI 내 Null/Undefined 수치 포맷팅 시 `toLocaleString` Crash 방어
  - [x] SWR `202 Accepted` 응답 처리 시 에러 발생 대신 UI 레벨의 동적 폴링 방식으로 개편
  - [x] `AssetTrendChart.tsx` 내 하드코딩된 가중치 역산 공식 제거 및 실제 자산 스냅샷 API(`/account/history`) 바인딩으로 전환
  - [x] `PortfolioView.tsx` 내 최고가 대비 하락 게이지 Division by Zero 결함 방어
  - [x] `OverseasScanner.tsx`, `PortfolioView.tsx` 등의 인라인 `<style>` 태그 제거 후 Tailwind/globals.css로 이관
  - [x] `confirm()` 기본 브라우저 창 제거 및 `sonner` 토스트 및 프리미엄 다이얼로그 모달로 개편

### 변경 영향 기록
- 변경 분류: `프론트엔드 아키텍처`, `API 계약`, `UI 시각화`
- 생산자: `frontend/store/timezoneStore.ts`, `frontend/components/admin/BacktestTournament.tsx`, `frontend/components/AssetTrendChart.tsx`, `frontend/components/PortfolioView.tsx`
- 소비자: Next.js 대시보드 화면, 백테스트 분석기, 포트폴리오 뷰, 다국어 시간대 표시 컴포넌트
- 데이터 경계: 사용자별 타임존 설정(Zustand), 개별 백테스트 시계열 데이터
- API 계약: `202 Accepted` 응답이 에러로 취급되던 방식에서 정상 응답 형태로 수용되도록 fetcher 수정
- 문서 반영: `docs/tasks/2026-07-03.md`
- 회귀 테스트: npm run lint, npx tsc --noEmit, Playwright E2E 테스트
- 미해결 위험: 없음
- 인수인계: 전체 빌드 통과 및 E2E 테스트 통과 최종 검증
