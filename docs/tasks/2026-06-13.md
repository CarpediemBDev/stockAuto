- [x] Edge에서 HttpOnly 로그인 후 새로고침 시 세션이 해제되는 원인 분석 및 수정 (#6)
  - 원인: 과거 `Path=/` refresh 쿠키와 현재 `Path=/api/v1/auth` 쿠키가 Edge 프로필에 함께 남아 백엔드가 만료된 값을 선택
  - 조치: 동명 쿠키 후보 중 JWT와 DB가 모두 유효한 토큰을 선택하고 로그인·갱신 시 레거시 루트 쿠키 삭제
  - 검증: Edge 엔진 중복 쿠키 재현, 새로고침 후 `/` 유지 및 refresh `200` 확인
  - GitHub Issue: https://github.com/CarpediemBDev/stockAuto/issues/6 (`completed`)

- [/] 전체 사용자 관리 > 사용자 상세 프로필 내 구동 알고리즘 전략 한국어 번역 추가
  - 조치: 번역 정보를 소스에 하드코딩하지 않고, 백엔드 list_users API에서 StockTranslation DB/캐시 연동 및 폴백이 처리된 strategy_name을 반환하여 프론트엔드가 이를 동적으로 렌더링하도록 조치 (번역 사전 페이지에서 수정 가능)
  - 검증: 수정 후 백엔드 컴파일(py_compile) 및 프론트엔드 빌드/TypeScript 무결성 검증 (npm run lint & npx tsc --noEmit)


