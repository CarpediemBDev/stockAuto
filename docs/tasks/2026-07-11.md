# 2026-07-11 Task Board

## 미커밋 작업 트리아지 및 검증 커밋 — 스캐너 AI 뉴스 분석 호출 예산 정책화

- [R] 세션 시작 시 미커밋 33건을 유형별로 전수 확인하고, 커밋 가능한 항목만 선별해 브랜치(feat/leveraged-regime-connors-2026-07-10)로 분리·검증 커밋·머지한다.
- 트리아지 결과: ①승인된([R]) 코어-새틀라이트 레버리지 레짐 기능(전략 2종·마이그레이션 2종·팩토리·스케줄러 자율 슬롯·시드 유저·회귀 테스트) → 커밋 1로 확정. ②스캐너 AI 뉴스 분석 호출 예산 정책화 + 감성 소스 태깅 → 본 커밋(커밋 2)로 확정. ③제외: 스크래치/디버그 일회성 8종(scratch_*.py, test_gemini.py), P3 토너먼트 진행중(미승인) 7종(run_tournament.py, run_all_tournaments.py, backtest_engine.py, data_provider.py, V5 리포트, mass-backtest-tournament 스킬, README), 청산 스코어 로직을 반전시키는 의미불명 변경 2종(darvas_box.py, hma_swing.py).
- 스캐너 변경 요지: Gemini 무료 티어 RPM 한도(15) 방어를 위한 AI 호출 관리를 인라인 카운터에서 _AiQuotaPolicy 객체로 캡슐화(보장 슬롯 상위 3·엘리트 임계 s1_score>=90·상한 10). AI 호출이 실제 gemini 응답을 못 받으면 release_slot으로 예산을 복원해 로컬 폴백이 무료 예산을 잠식하지 않도록 예외 안전 보장. news_analyzer 결과에 source 필드(gemini/local/none)를 추가해 스캐너가 실제 AI 적중 여부를 판별.

### 변경 영향 기록
- 변경 분류: 리팩토링 및 안정화 — 스캐너의 Gemini AI 뉴스 분석 호출 예산을 정책 객체로 분리하고 감성 결과에 소스 출처를 태깅
- 생산자: backend/app/scanner/scanner.py(_AiQuotaPolicy 신설·예외 안전 예산 복원), backend/app/scanner/news_analyzer.py(source 필드 gemini/local/none 추가·폴백 로그 사유 구분), backend/tests/test_news_analyzer.py(source=gemini 검증)
- 소비자: 스캐너 Stage 2 정밀 분석 루프(scan_market_expert), 뉴스 감성 점수 소비 경로(신호 스코어링), Gemini 무료 티어 쿼터 관리
- 데이터 경계: 외부 데이터·스키마 변경 없음. Gemini API 호출 빈도만 정책적으로 제어(스캔 1회당 최대 10회, 보장 3회). news_analysis 반환 dict에 source 키 가산
- API 계약: 공개 REST 계약 무변경. analyze_news_sentiment 반환 dict에 source 필드 가산(내부 계약, 하위호환 — 기존 키 유지)
- 문서 반영: 본 현황판 기록. 공개 API/스키마 계약 문서 변경 없음
- 회귀 테스트: verify_harness 전체 통과(계약·카오스/롤백·컴파일257·NFC410·alembic 단일헤드·pytest 230 passed·프론트 tsc/lint·Playwright E2E 8/8). test_news_analyzer가 gemini 경로 source=gemini 검증
- 미해결 위험: (1)_AiQuotaPolicy는 스캔 회차 단위 인메모리 예산 — 다중 워커/프로세스 병렬 스캔 시 프로세스별 독립 카운트라 전역 RPM 상한을 초과할 수 있음(현행 단일 스캔 루프에서는 안전). (2)source 판별은 문자열 상수 매칭이라 news_analyzer 반환 규약과 강결합 — 반환 키 변경 시 예산 복원 로직 동기 수정 필요
- 인수인계: 커밋 2로 확정(브랜치 feat/leveraged-regime-connors-2026-07-10). 검증용 Redis는 로컬 Memurai(6379)로 기동해 order_reconciler/lockout 테스트 통과. 제외 항목(P3 토너먼트·스크래치·darvas/hma)은 워킹트리에 미커밋 상태로 잔존 — 별도 승인 후 처리.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
