# 2026-07-11 Task Board

## 미커밋 작업 트리아지 및 검증 커밋 — 스캐너 AI 뉴스 분석 호출 예산 정책화

- [R] 세션 시작 시 미커밋 33건을 유형별로 전수 확인하고, 커밋 가능한 항목만 선별해 브랜치(feat/leveraged-regime-connors-2026-07-10)로 분리·검증 커밋·머지한다.
- 트리아지 결과: ①승인된([R]) 코어-새틀라이트 레버리지 레짐 기능(전략 2종·마이그레이션 2종·팩토리·스케줄러 자율 슬롯·시드 유저·회귀 테스트) → 커밋 1로 확정. ②스캐너 AI 뉴스 분석 호출 예산 정책화 + 감성 소스 태깅 → 본 커밋(커밋 2)로 확정. ③제외: 스크래치/디버그 일회성 8종(scratch_*.py, test_gemini.py), P3 토너먼트 진행중(미승인) 7종(run_tournament.py, run_all_tournaments.py, backtest_engine.py, data_provider.py, V5 리포트, mass-backtest-tournament 스킬, README), 청산 스코어 로직을 반전시키는 의미불명 변경 2종(darvas_box.py, hma_swing.py).
- 스캐너 변경 요지: Gemini 무료 티어 RPM 한도(15) 방어를 위한 AI 호출 관리를 인라인 카운터에서 _AiQuotaPolicy 객체로 캡슐화(보장 슬롯 상위 3·엘리트 임계 s1_score>=90·상한 10). AI 호출이 실제 gemini 응답을 못 받으면 release_slot으로 예산을 복원해 로컬 폴백이 무료 예산을 잠식하지 않도록 예외 안전 보장. news_analyzer 결과에 source 필드(gemini/local/none)를 추가해 스캐너가 실제 AI 적중 여부를 판별.

### 변경 영향 기록
- 변경 분류: 리팩토링 및 안정화 — 스캐너의 Gemini AI 뉴스 분석 호출 예산을 정책 객체로 분리하고 감성 결과에 소스 출처를 태깅
- 생산자: backend/app/scanner/scanner.py(_AiQuotaPolicy 신설·예외 안전 예산 복원), backend/app/scanner/news_analyzer.py(source 필드 gemini/local/none 추가·폴백 로그 사유 구분), backend/tests/test_news_analyzer.py(source=gemini 검증)
- 소비자: 스캐너 Stage 2 정밀 분석 루프(scan_market_expert), 뉴스 감성 점수 소비 경로(신호 스코어링), Gemini 무료 티어 쿼터 관리
- 데이터 경계: 외부 데이터·스키마 변경 없음. Gemini API 호출 빈도만 정책적으로 제어(스캔 1회당 최대 10회, 보장 3회). news_analysis 반환 dict에 source 키 가산
- API 계약: 공개 REST 계약 무변경. analyze_news_sentiment 반환 dict에 source 필드 가산(내부 계약, 하위호환 — 기존 키 유지)
- 문서 반영: 영향 없음 - 스캐너 AI 뉴스 예산 정책·감성 소스 태깅은 런타임 정책 객체와 내부 dict 키 가산만으로 공개 API/DB 스키마 계약 문서 변경 없음
- 회귀 테스트: verify_harness 전체 통과(계약·카오스/롤백·컴파일257·NFC410·alembic 단일헤드·pytest 230 passed·프론트 tsc/lint·Playwright E2E 8/8). test_news_analyzer가 gemini 경로 source=gemini 검증
- 미해결 위험: (1)_AiQuotaPolicy는 스캔 회차 단위 인메모리 예산 — 다중 워커/프로세스 병렬 스캔 시 프로세스별 독립 카운트라 전역 RPM 상한을 초과할 수 있음(현행 단일 스캔 루프에서는 안전). (2)source 판별은 문자열 상수 매칭이라 news_analyzer 반환 규약과 강결합 — 반환 키 변경 시 예산 복원 로직 동기 수정 필요
- 인수인계: 커밋 2로 확정(브랜치 feat/leveraged-regime-connors-2026-07-10). 검증용 Redis는 로컬 Memurai(6379)로 기동해 order_reconciler/lockout 테스트 통과. 제외 항목(P3 토너먼트·스크래치·darvas/hma)은 워킹트리에 미커밋 상태로 잔존 — 별도 승인 후 처리.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## [핫픽스+재통합] 매매 사이클 DetachedInstanceError 제거(trade_mode 스냅샷) + SIMULATED 매도 StaleDataError 근본 수정(holdings) 결합

- [R] 세션 밖 ORM 접근으로 인한 두 결함을 한 번에 정리한다: (1) trade_mode를 값 스냅샷으로 떠 db_settings DetachedInstanceError 제거, (2) holdings auto-merge가 version_id를 올려 SIMULATED 매도 db.merge(h)를 죽이던 StaleDataError를 load=False 병합·관측값 로컬화로 근본 제거.
- 경위: trade_mode 스냅샷(이 세션)과 holdings 수정(백그라운드 세션, task_58247869)이 서로 다른 베이스에서 진행됨을 발견. 백그 holdings 수정은 expire_on_commit=False 전제 위에서 설계·검증됐는데, trade_mode 핫픽스가 그 옵션을 제거해 결합 시 Holding DetachedInstanceError로 충돌. 사용자 승인 하에 이 워킹트리로 holdings 3변경을 이식하고, expire_on_commit=False를 복원(step 0 되돌림)해 결합 검증 완료.
- 판단 근거: step 0(expire_on_commit=False 제거)의 목적이던 StaleData 위험은 holdings 수정이 version 상승을 원천 제거해 해소됨. 따라서 expire_on_commit=False를 복원해도 안전하며, trade_mode 스냅샷은 추가 방어로 유지.

### 변경 영향 기록
- 변경 분류: 버그 수정 및 안정화 — 매매 사이클의 세션 밖 ORM 접근 결함 2종(db_settings DetachedInstanceError·holdings StaleDataError)을 값 스냅샷과 load=False 병합/관측값 로컬화로 제거
- 생산자: backend/app/bot/scheduler.py(TradingFlowContext.trade_mode 신설·prepare_trading_flow_context 스냅샷 복사·세션 밖 trade_mode 읽기 5곳 교체·micro_session holdings auto-merge load=False 전환·process_exit_signals 관측값 로컬화 및 highest_price Core UPDATE·전량매도 ctx.holdings 프루닝·expire_on_commit=False 복원 2곳), backend/tests/test_scheduler_detached_snapshot.py(회귀 3종 신설)
- 소비자: process_exit_signals 매도 판단/실행 경로, process_entry_signals 매수 경로의 trade_mode 분기(is_kis_order), sync_broker_holdings·슬롯 배분 등 holdings 스냅샷을 세션 밖에서 읽는 micro_session 소비자, 유저 대면 잔고 API(last_price 스냅샷)
- 데이터 경계: 외부 데이터·스키마 무변경. holdings.last_price/highest_price는 version 비간섭 Core UPDATE로만 반영, version_id는 실제 수량·삭제 변경 시에만 상승. broker sell_order 호출과 DB 반영의 원자성 개선(매도 후 merge 실패로 인한 미반영 위험 제거)
- API 계약: 공개 REST 계약 무변경. 내부 TradingFlowContext에 trade_mode 필드 가산(하위호환), micro_session 계약(holdings 재부착은 load=False 읽기 전용, 실제 영속은 명시적 merge) 명확화
- 문서 반영: docs/mistake_note.md에 [Bug-003] 신설(근본 원인·해결·재발 방지 교훈). 본 현황판 기록. 공개 API/스키마 계약 문서 변경 없음
- 회귀 테스트: verify_harness 통과(계약 제외 전 단계 — 컴파일275·NFC435·카오스/롤백·pytest 234 passed·프론트 tsc/lint·Playwright E2E 8/8). test_scheduler_detached_snapshot 3종: (1)트레이드모드 스냅샷이 커밋하는 micro_session 후에도 안정 (2)detach된 db_settings 재-merge 시 StaleData/Detached 없음 (3)두 종목 SIMULATED 전량 매도가 StaleData 없이 성사(sell_calls==2·잔여 holdings==0). 기존 스케줄러 계열 25종 통과
- 미해결 위험: (1)ctx.holdings ORM 객체를 세션 경계 밖에 보관하는 패턴 자체는 잔존 — 근본 해소는 HoldingView DTO 전환(후속 과제). (2)load=False는 세션 내 in-memory version을 사용하므로 sync→exit→entry 호출 순서에 의존 — 순서 변경 시 stale version 재검토 필요. (3)부분 체결 후 Part B merge가 loop 원본(last_price 미설정)을 병합하면 last_price가 잠시 되돌아갈 수 있으나 다음 사이클 Core UPDATE로 자가 치유. (4)trade_mode 외 다른 db_settings 속성을 세션 밖에서 새로 읽으면 동일 detach 버그 재발 가능
- 인수인계: trade_mode 스냅샷(이 세션)+holdings 수정(task_58247869)을 이 워킹트리로 결합·검증 완료. 백그 워크트리(claude/sad-khayyam-5a6ccd)는 낡은 베이스라 미사용. 커밋은 사용자 명시 지시 시에만. 후속(holdings→HoldingView DTO)은 별도 과제로 분리.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)


