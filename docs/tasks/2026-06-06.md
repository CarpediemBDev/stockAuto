# 2026-06-06 작업 현황판

## 작업 목록

- [ ] 보안/인증/운영 리스크 후속 정비 백로그
  - [ ] 기본 `admin/admin123` 시딩 및 `is_running=True` 기본값 제거/안전화 검토: `migrator.py` line 75
  - [ ] KIS secret 평문 응답 차단 재검증: `admin/router.py` line 102
  - [ ] `username == "admin"` 기반 권한 판정 제거 및 역할/권한 모델 전환 검토: `admin/router.py` line 168
  - [ ] 번역 CRUD 무인증 접근 차단: `translations/router.py` line 25
  - [ ] 전체 사용자 대상 텔레그램 리포트 발송 범위 격리: `report/router.py` line 76
  - [ ] 500 응답의 traceback 노출 차단 및 운영용 에러 응답 표준화: `main.py` line 60
  - [ ] `import` 시점 마이그레이션/시딩 실행 제거 및 앱 수명주기 기반 실행으로 이동: `main.py` line 27
  - [ ] 전체 사용자 관심종목 스캔 혼합 방지 및 사용자별 데이터 격리: `discovery.py` line 10
  - [ ] async 라우트 내 동기 브로커 호출 분리/비동기화: `router_account.py` line 168
  - [ ] polling 중첩 방지 적용 및 프론트 폴링 재진입 보호: `usePolling.ts` line 14
  - [ ] 구현 시 백엔드 변경 파일 `python -m py_compile` 검증, 프론트 변경 시 `npm run lint` 및 `npx tsc --noEmit` 검증

- [x] 커밋 전 미커밋 변경 최종 QA 검토
  - [x] 변경 파일 범위와 임시 파일 포함 여부 확인
  - [x] 백엔드 KIS 인증정보/브로커/스케줄러 변경 리뷰
  - [x] 프론트 관리자 설정 화면 변경 리뷰
  - [x] 테스트 및 필수 검증 명령 실행
  - [x] 커밋 준비 전 보고 및 사용자 승인 대기
  - 검토 중 KIS 브로커가 `session` 인자를 넘기지만 `KISClient` 주문 메서드가 받지 않아 MOCK/REAL 주문이 실패하는 결함 확인
  - `KISClient` 매수/매도 주문에 시장 세션별 주문 구분 코드(`00`, `32`, `34`) 적용 및 회귀 테스트 추가
  - `git diff --check` 공백 검증 통과
  - `.\venv\Scripts\python.exe -m py_compile ...` 통과
  - `.\venv\Scripts\python.exe -m pytest` 37개 테스트 통과
  - `npm run lint` 및 `npx tsc --noEmit` 통과
  - 루트 `replace_*.py` 임시 치환 스크립트는 커밋 대상 제외 필요

- [x] KIS 인증정보 write-only 분리 및 응답 노출 차단
  - [x] `GET /api/v1/admin/`에서 KIS secret 원문 제거
  - [x] KIS 인증정보 검증/저장/삭제 API 분리
  - [x] KIS 인증정보 암호화 저장 및 기존 평문 호환 처리
  - [x] 프론트 설정 화면에서 secret write-only 입력으로 전환
  - [x] 백엔드/프론트 검증 및 회귀 테스트 추가
  - `.\venv\Scripts\python.exe -m py_compile app\core\credentials.py app\core\models.py app\bot\kis_api.py app\bot\scheduler.py app\admin\router.py tests\test_admin_settings_safety.py tests\test_api_integration.py alembic\versions\c4f5a6b7c8d9_add_kis_credential_verification_status.py` 통과
  - `.\venv\Scripts\python.exe -m pytest` 31개 테스트 통과
  - `npm run lint` 및 `npx tsc --noEmit` 통과
  - 실제 `backend/.env.local`, `.env.dev`, `.env.prod`에도 `KIS_CREDENTIAL_MASTER_KEY` Fernet 키 추가
  - 실제 env 3종 Fernet 키 형식 검증 및 `.\venv\Scripts\python.exe -m pytest tests\test_admin_settings_safety.py` 6개 테스트 통과
  - 실제 env 3종과 example env 3종의 섹션 순서를 `Header -> JWT -> KIS -> Telegram -> Gemini` 기준으로 정렬
  - `.env.local` 선두 BOM 제거, Fernet 키 형식 재검증 및 `.\venv\Scripts\python.exe -m pytest tests\test_admin_settings_safety.py` 6개 테스트 재통과
  - 사용자 최종 승인에 따라 완료 처리
  - `.\venv\Scripts\python.exe -m py_compile ...` 통과
  - `.\venv\Scripts\python.exe -m pytest` 37개 테스트 통과
  - `npm run lint` 및 `npx tsc --noEmit` 통과
  - 실제 env 3종 Fernet 키 형식, 수정 대상 NFC, Alembic 단일 head(`c4f5a6b7c8d9`) 확인

- [/] 스캐너 수동 스캔 API 수정 재검증
  - 백엔드 라우터, 프론트 API, 라우터 테스트 최신 상태 확인
  - 인증, HTTP 메서드, 백그라운드 응답 처리, 중복 실행 보호 검토
  - 백엔드/프론트 검증 명령 실행
  - `/scanner/overseas`, `/scanner/swing-predict/refresh` 라우터/프론트/테스트 모두 `POST` 기준 확인
  - 수동 스캔 버튼은 백그라운드 시작 안내 후 폴링 갱신에 맡기는 구조 확인
  - 스윙 예측 refresh는 예약/해제 락 기반 중복 실행 방지 확인
  - 수동 해외 스캔 `/scanner/overseas`는 별도 중복 실행 락 미적용 확인
  - `.\venv\Scripts\python.exe -m py_compile app\scanner\router.py app\scanner\swing_prediction_cache.py app\bot\scheduler.py tests\test_scanner_router.py` 통과
  - `.\venv\Scripts\python.exe -m pytest tests\test_scanner_router.py` 8개 테스트 통과
  - `.\venv\Scripts\python.exe -m pytest` 27개 테스트 통과
  - `npm run lint` 및 `npx tsc --noEmit` 통과

- [ ] 해외시장 거래 시간 확장 (프리/정규/에프터장)
  - [ ] `app.bot.scheduler`: 시장 세션 감지 로직 개편 (`get_market_session`)
  - [ ] `app.bot.multi_strategy_manager`: 비정규장 비중 축소(50%) 방어 로직 적용
  - [ ] `app.bot.kis_api` & `app.bot.kis_broker`: 동적 주문 코드(`00`, `32`, `34`) 할당
  - [ ] `test_scheduler_cycle_context`: 테스트 코드 호환성 수정
  - [ ] MOCK 모드 시뮬레이션 및 `py_compile` 사전 검증

- [/] 실전 자동매매 주문 무결성 및 운영 안전성 보강
  - [/] KIS 체결 미확인 주문을 가상 체결로 기록하지 않고 사용자 봇 자동 정지
  - [/] 부분 매도 체결 시 실제 체결 수량만 반영하고 잔여 보유 수량 유지
  - [/] NYSE 공식 휴장일 및 조기 종료일 기준 시장 세션 판정
  - [/] 기존 사용자 DB에서도 `role` 마이그레이션이 안전하게 적용되도록 보정
  - [/] 경쟁용 사용자 시딩을 명시적 opt-in으로 제한하고 자동매매 기본 정지
  - [/] 운영 500 응답에서 내부 예외 및 traceback 노출 차단
  - [/] 백엔드 `py_compile`/pytest 및 프론트엔드 lint/typecheck/E2E 검증
  - `.\venv\Scripts\python.exe -m pytest` 49개 테스트 통과
  - 수정 백엔드 파일 `python -m py_compile` 통과
  - 기존 10명 데이터 DB 복사본 Alembic 최신 head(`bd07f17fb172`) 업그레이드 통과
  - `ADMIN` 1명, `USER` 9명, 로그인 실패 횟수 기본값 `0`, refresh token 테이블 확인
  - `python scripts\verify_harness.py` 전체 통과
  - 백엔드 119개 파일 compile, pytest 49개, 프론트 lint/typecheck, Playwright 4개 통과
  - 수정 대상 11개 텍스트 파일 NFC 검증 통과

- [/] 완전 무인 주문 원장 및 자동 재조정
  - [/] `broker_orders` 영구 주문 원장 및 Alembic 마이그레이션
  - [/] 주문 제출 결과 원장 기록 및 멱등 체결 반영
  - [/] 부분/완전/미체결 주문 자동 재조회
  - [/] 장기 미확인/오류 주문 안전 정지 및 자동 재개 정책
  - [/] 스케줄러 재조정 작업 및 회귀 테스트
  - [/] 전체 하네스 검증
  - KIS MOCK/REAL 주문을 `broker_orders` 원장에 기록하고 누적 체결수량과 DB 반영수량을 분리
  - 부분 체결 후 동일 응답 재수신 시 추가 반영 0건, 최종 체결 시 차이 수량만 반영
  - 미해결 주문 발생 시 자동 정지, 30초 주기 재조회, 모든 주문 종결 후 조건부 자동 재개
  - 사용자가 직접 정지하면 자동 재개 예약 해제, 미해결 중 웹/관리자/텔레그램 재시작 차단
  - 미해결 중 거래 모드 및 KIS 인증정보 변경/삭제 차단
  - 기존 10명 DB 복사본 Alembic 최신 head(`e1f2a3b4c5d6`) 업그레이드 통과
  - `broker_orders` 테이블 26개 컬럼 생성 및 기존 사용자 10명 보존 확인
  - 백엔드 120개 파일 compile, pytest 52개 통과
  - 프론트 lint/typecheck 및 Playwright 4개 통과
  - `python scripts\verify_harness.py` 전체 통과
  - 수정 대상 16개 텍스트 파일 NFC 검증 통과

- [/] 주문 사전 원장 및 고아 주문 자동 복구
  - [/] KIS 공식 주문/체결내역 API 필드와 연속조회 계약 확인
  - [/] 주문 의도 상태 및 복구 매칭 메타데이터 Alembic 추가
  - [/] KIS 주문 전 사전 원장 커밋과 단일 전송 보장
  - [/] 응답 미수신 주문 `ACK_UNKNOWN` 전환
  - [/] KIS 주문내역 기반 고아 주문 자동 연결
  - [/] 모호한 후보 및 외부 주문 안전 정지 정책
  - [/] 서버 시작/주기 복구 배치 및 장애 주입 테스트
  - [/] 전체 하네스 및 기존 DB 마이그레이션 검증
  - 주문 상태를 `INTENT_CREATED -> SUBMITTING -> SUBMITTED/PARTIAL/FILLED`로 분리
  - 네트워크 응답이 불명확하면 재전송 없이 `ACK_UNKNOWN`으로 전환
  - KIS 주문 전 내부 UUID 원장 커밋 및 운용사지정주문번호 전달
  - 공식 주문체결내역 TR ID `TTTS3035R/VTTS3035R`와 연속조회 키 적용
  - KIS 주문번호 검색 대신 전체 주문내역 조회 후 애플리케이션 필터링
  - 매수/매도, 티커, 수량, 가격, 거래소가 단일 일치할 때만 주문번호 자동 연결
  - 복수 후보는 `AMBIGUOUS` 처리 후 자동 연결 거부 및 봇 정지 유지
  - 서버 시작 시 복구 후 스케줄러 기동, 이후 1분 주기 고아 주문 탐색
  - 장애 주입 테스트에서 주문 재전송 0회, 주문내역 조회만으로 원장/보유수량 복구 확인
  - 기존 10명 DB 복사본 최신 head(`f2a3b4c5d6e7`) 업그레이드 통과
  - 기존 사용자 10명 보존, `broker_order_no` nullable 및 사전 원장 35개 컬럼 확인
  - 백엔드 121개 파일 compile, pytest 56개 통과
  - 프론트 lint/typecheck 및 Playwright 4개 통과
  - `python scripts\verify_harness.py` 전체 통과
  - 수정 대상 21개 텍스트 파일 NFC 검증 통과
