# 2026-06-19 일자별 현황판

## [AuditFix] 전체 소스 스캔 후 1차 권장 처리 적용

### 목표
- 스캐너 반환 계약과 테스트 기대값을 정렬해 백엔드 테스트 실패를 해소한다.
- 현재 프론트 런타임 버전에 맞춰 Docker Node 버전을 정리한다.
- 백엔드/프론트 런타임 버전 및 의존성 고정 기준을 보강한다.
- 프론트 lint 경고와 불필요한 `setTimeout`/lint 우회 코드를 정리한다.

### 작업 목록
- `[/]` 스캐너 API 계약 및 관련 테스트 정합성 수정
- `[/]` 프론트 Docker Node 런타임 버전 정리
- `[/]` Python/Node 의존성 버전 고정 기준 보강
- `[/]` 프론트 lint 경고 및 수동 스캔 상태 처리 개선
- `[/]` 백엔드 py_compile, pytest, 프론트 lint, tsc, build 검증

### 검증 기록
- `python -m py_compile app/scanner/naver_scraper_runner.py tests/test_naver_scraper_runner.py tests/test_scanner_router.py` 통과
- `python -m pytest tests/test_naver_scraper_runner.py tests/test_scanner_router.py` 통과: 12 passed
- `python -m pytest` 통과: 136 passed
- `python -m compileall -q app tests` 통과
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `npm run build` 통과

## [Backend] 전역 응답 래퍼 모듈 결함 수정

### 목표
- 전역 응답 래퍼가 `content-length` 불일치 없이 JSON 응답을 안정적으로 포장하도록 수정한다.
- 라우터의 수동 `success_response()` 반환을 줄여 자동 포장 규칙과 맞춘다.

### 작업 목록
- `[/]` `app/core/response.py` 헤더 처리 및 JSON 응답 판별 보강
- `[/]` 수동 `success_response()` 호출 라우터 정리
- `[/]` py_compile 및 관련 pytest 검증

### 검증 기록
- `python -m py_compile app\core\response.py app\admin\router.py app\scanner\router.py app\report\router.py app\trades\router_account.py app\trades\router_market.py app\translations\router.py app\watchlist\router.py tests\test_global_response_wrapper.py tests\test_api_integration.py` 통과
- `python -m py_compile tests\test_force_liquidation_safety.py` 통과
- `python -m pytest tests\test_global_response_wrapper.py -q` 통과
- `python -m pytest tests\test_api_integration.py -q` 통과
- `python -m pytest tests\test_scanner_router.py tests\test_market_overview.py -q` 통과
- `python -m pytest tests\test_force_liquidation_safety.py -q` 통과
- `python -m pytest -q` 통과

## [AuditFix] 추가 문제 후보 2차 정리

### 목표
- Docker 빌드 컨텍스트에서 `.env`, 로컬 DB, venv, node_modules 등 민감/대용량 파일 유입을 차단한다.
- 실제 프론트 런타임(Next.js 16, React 19, Node 20+)과 프로젝트 지침의 불일치를 정리한다.
- 프론트 패키지 엔진 기준을 명시해 Docker/로컬 런타임 차이를 줄인다.
- 낮은 위험 범위에서 `print()` 기반 운영 로그를 표준 logger로 정리한다.

### 작업 목록
- `[/]` 백엔드/프론트 Docker ignore 정책 추가
- `[/]` AGENTS 프론트 기술 스택 기준 갱신
- `[/]` 프론트 package engines 기준 추가
- `[/]` 스케줄러/라이프스팬 일부 print 로그 logger 전환
- `[/]` 백엔드 py_compile, pytest, 프론트 lint, tsc, build 검증

### 검증 기록
- `python -m py_compile app/main.py app/bot/scheduler.py` 통과
- `python -m pytest` 통과: 136 passed
- `npm run lint` 통과
- `npx tsc --noEmit` 통과
- `npm run build` 통과

## [Frontend] 매매보고서 화면 UI/UX 프리미엄 디자인 전면 개편

### 목표
- 매매보고서 페이지(`frontend/app/report/page.tsx`)의 전체적인 레이아웃 및 글래스모피즘 테마를 프리미엄 수준으로 개선한다.

### 작업 목록
- `[x]` `page.tsx` 프리미엄 글래스모피즘 및 애니메이션 효과 적용
- `[x]` ESLint 및 TypeScript 컴파일 검증

## [Frontend] 전역 폰트 및 기본 사이즈 표준화

### 작업 목록
- `[x]` Next.js `next/font/google` 최적화를 통한 `Noto Sans KR` 전역 도입
- `[x]` `layout.tsx` 내 Body 기본 폰트 크기 `14px` 하향 조정
- `[x]` `globals.css` 및 Tailwind 변수를 통한 시스템 폰트 강제 매핑
- `[x]` 관련 미사용 폰트 변수(Geist) 정리 및 린트 검증 통과
