# 2026-06-04 업무 일지 (StockAuto)

## 📌 주요 작업 목표
- 스캐너 API 보안 및 아키텍처 결함 수정

## 📋 세부 작업 목록
- [/] 15분 수동 스캔 API 보안 수정 검증
    - [/] 백엔드 라우터와 프론트 API 호출 방식 확인
    - [ ] 인증, HTTP 메서드, 백그라운드 처리, 캐시 갱신 동작 검토
    - [ ] 관련 테스트 및 정적 검증 실행
    - 확인 결과: 백엔드 `/scanner/overseas`와 `/scanner/swing-predict/refresh`는 `POST`로 변경됨
    - 확인 결과: 프론트 `scannerAPI.runOverseasScan`, `refreshSwingPredict`는 아직 `GET` 호출 상태
    - 확인 결과: 스캐너 라우터 테스트는 아직 기존 `GET` 호출 기대값을 사용
    - 검증: `app\scanner\router.py`, `tests\test_scanner_router.py` py_compile 통과
    - 검증 차단: `app\bot\scheduler.py` line 307 문법 오류로 `pytest tests\test_scanner_router.py` 수집 실패
    - 검증: 프론트 `npm run lint`, `npx tsc --noEmit` 통과
    - 재검증 결과: 프론트 `scannerAPI.runOverseasScan`, `refreshSwingPredict`는 `POST` 호출로 변경됨
    - 재검증 결과: 수동 스캔 버튼은 백그라운드 시작 토스트만 표시하고 즉시 결과 배열을 대입하지 않도록 변경됨
    - 재검증 결과: 스캐너 라우터 테스트는 여전히 기존 `GET` 호출 기대값을 사용
    - 재검증 결과: 수동 해외 스캔 백그라운드 작업에는 중복 실행 방지 락이 아직 없음
    - 재검증: `app\scanner\router.py`, `tests\test_scanner_router.py` py_compile 통과
    - 재검증 차단: `app\bot\scheduler.py` line 307 문법 오류로 `pytest tests\test_scanner_router.py` 수집 실패
    - 재검증: 프론트 `npm run lint`, `npx tsc --noEmit` 통과

- [x] `/api/v1/scanner/overseas` 엔드포인트 수정
    - [x] `GET` 메서드를 `POST`로 변경
    - [x] 인증 추가 (`Depends(get_current_user)`)
    - [x] 응답 대기 지연을 막기 위해 FastAPI `BackgroundTasks`를 통한 비동기 처리 적용
- [x] `/api/v1/scanner/swing-predict/refresh` 엔드포인트 수정
    - [x] `GET` 메서드를 `POST`로 변경
- [x] 프론트엔드 API 연동 수정 (`api.ts`)
    - [x] `runOverseasScan` 및 `refreshSwingPredict` 메서드를 `POST`로 변경
    - [x] `OverseasScanner.tsx` 등 컴포넌트에서 비동기 응답 처리 오류(Crash) 수정
- [x] 백엔드 치명적 서버 다운(Network Error) 원인 해결
    - [x] 이전 세션에서 `app/bot/scheduler.py`에 중복 복사되어 발생한 `SyntaxError` 제거 및 서버 정상 구동 확인
- [/] 전체 프로젝트 스캔 기반 리팩토링 버그 리포트 작성
    - [/] 백엔드/프론트엔드 소스, 테스트, 설정 파일 점검
    - [/] 정적 검사 및 실행 가능한 검증 명령으로 오류 수집
    - [/] 리팩토링 우선순위별 버그 리포트 문서화
    - 작성 문서: `docs/BUG_REPORT_2026-06-04.md`
    - 검증 결과: 백엔드 `app\bot\scheduler.py` line 307 문법 오류로 `compileall`, `pytest`, `verify_harness` 실패
    - 검증 결과: 프론트 `npm run lint`, `npx tsc --noEmit`, `npm run build` 통과

- [/] Next.js 네트워크 오류 원인 진단
    - [/] 실행 중인 프론트엔드/백엔드 포트와 API 호출 경로 확인
    - [/] 브라우저 콘솔/네트워크 오류 및 백엔드 응답 상태 대조
    - [/] 수정 필요 범위 정리 및 검증 명령 확인
    - 확인 결과: Next.js는 `http://localhost:8000/api/v1`을 호출하지만 8000 포트가 리스닝 상태가 아님
    - 확인 결과: `backend\app\bot\scheduler.py` 307라인 `SyntaxError: expected 'except' or 'finally' block`로 `import app.main` 실패
    - 확인 결과: `TradingFlowContext` 필드와 `prepare_trading_flow_context()` 일부가 이미 108라인대에 존재하는데 307라인대에 중복 삽입되어 `process_exit_signals()`의 `try:` 블록을 깨뜨림
    - 추가 확인: 서버 재기동 후 브라우저 직접 확인 결과 `/`, `/scanner`, `/report` 단순 진입에는 Next.js 런타임 오버레이 없음
    - 추가 확인: `/scanner` 스윙 탭 `수동 갱신` 클릭 후 120초 경과 시 Next 개발 팝업 `Console Error` 발생
    - 팝업 내용: `components/SwingPredictorCard.tsx (55:15)` `Failed to refresh swing predictions: "timeout of 120000ms exceeded"`
    - 원인 후보: 백엔드 `/scanner/swing-predict/refresh`가 `await refresh_swing_prediction_cache(...)`로 장시간 스캔 완료를 기다려 프론트 Axios 120초 타임아웃에 걸림

- [/] Next.js 개발 오버레이 오류 일괄 점검 및 수정
    - [/] 메인 화면과 주요 라우트의 브라우저 오버레이/콘솔 오류 수집
    - [/] 스윙 갱신 타임아웃 및 처리 흐름 수정
    - [/] 메인/리포트 화면 차트 렌더링 경고 수정
    - [/] 프론트/백엔드 정적 검증 실행
    - 수정 결과: `/scanner/swing-predict/refresh`는 즉시 `refreshing` 응답을 반환하고 실제 분석은 `BackgroundTasks`에서 실행
    - 수정 결과: 처리된 프론트 오류는 `reportHandledError`로 기록해 Next 개발 오버레이 `Console Error` 유발을 차단
    - 수정 결과: 리포트/관리자 백테스트 Recharts `ResponsiveContainer`에 고정 높이 및 최소 크기 가드 추가
    - 검증: `python -m py_compile backend\app\scanner\router.py backend\app\scanner\swing_prediction_cache.py backend\tests\test_scanner_router.py` 통과
    - 검증: `backend\venv\Scripts\python.exe -m pytest backend\tests\test_scanner_router.py` 5 passed
    - 검증: 프론트 `npm run lint`, `npx tsc --noEmit` 통과
    - 브라우저 재검증: `/scanner` 스윙 수동 갱신, `/report`, `/` 기준 시각 이후 error/warn 로그 0건 및 Next 오버레이 없음

- [x] 세력돌파 예측(스윙) 결과 미출력(빈 목록) 버그 수정
    - [x] `app/scanner/swing_predictor.py`의 `is_bullish_trend` 변수 산출 시 NumPy boolean 타입 반환 문제 파악
    - [x] NumPy boolean 타입으로 인한 `swing_prediction_cache.py`에서의 `json.dumps()` 직렬화(Serialization) 실패 예외 처리 및 타입 캐스팅 적용
    - [x] 백엔드 `py_compile` 및 백그라운드 스캐너 작동 재검증

- [/] 스윙 예측 백그라운드 갱신 상태 캐시 유지 버그 수정
    - [/] `GET /swing-predict` 폴링 시 `refreshing` 상태가 조기 해제되는 원인 확인
    - [/] 백그라운드 갱신 실패 시 `failed`/`stale` 상태가 전역 캐시에 기록되지 않는 원인 확인
    - [/] 전역 캐시 상태 전이 수정 및 테스트 보강
    - 수정 결과: `refreshing_swing_response()`가 전역 캐시에 `refreshing` 상태를 직접 기록하도록 변경
    - 수정 결과: 백그라운드 갱신 실패 시 기존 후보가 있으면 `stale`, 후보가 없으면 `failed`를 전역 캐시에 기록하도록 변경
    - 수정 결과: 스윙 갱신은 FastAPI `BackgroundTasks`/이벤트 루프 태스크 대신 별도 daemon thread에서 실행해 HTTP 응답 경로와 분리
    - 테스트 보강: POST 직후 다음 GET이 `refreshing`을 유지하는 케이스, 실패 후 `failed`/`stale` 상태가 유지되는 케이스 추가
    - 검증: `python -m py_compile backend\app\scanner\swing_prediction_cache.py backend\app\scanner\router.py backend\tests\test_scanner_router.py` 통과
    - 검증: `backend\venv\Scripts\python.exe -m pytest backend\tests\test_scanner_router.py` 8 passed
    - 실서버 확인 참고: 현재 8000번 개발 서버는 이전 장기 요청으로 `CloseWait`/`Established` 연결이 다수 누적되어 단일 `/auth/login`도 타임아웃. 코드 반영 검증에는 서버 재시작 필요
