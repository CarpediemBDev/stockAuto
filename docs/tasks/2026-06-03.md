# 2026-06-03 작업 현황판

## 작업 목록

- [x] Git 업로드 직전 변경 파일 검토 제한사항 확인 및 실전 안전 가드 최우선 수정
  - Git 명령은 프로젝트 규칙상 명시 신호 전까지 사용하지 않음
  - KIS 검증 실패 시 REAL/MOCK 저장 차단 또는 SIMULATED 후퇴 로직 정합성 점검
  - REAL 주문 경로에서 `is_real_enabled` 안전 스위치 강제 적용
  - 강제청산 API의 내부 접두사 티커 처리 점검
  - 수정 파일 검증 수행

- [x] `AGENTS.md` 단일 지침 파일 유지 및 `CLAUDE.md` 참조 정리
  - `CLAUDE.md` 삭제
  - 문서 내 `CLAUDE.md` 안내를 `AGENTS.md` 기준으로 수정
  - 삭제 후 참조 검색 및 검증 수행

- [x] yfinance 401 및 스케줄러 로그 폭증 원인 기반 수정
  - yfinance singleton crumb/cookie 충돌을 막기 위한 provider 직렬화 및 캐시 보강
  - FX 환율 캐시 동시 첫 진입 중복 다운로드 차단
  - 장외 및 스캐너 캐시 미스 상황에서 사용자별 중복 티커 분석 차단
  - 백엔드 컴파일 및 하드 하네스 검증 수행

- [x] 시장 상태 캐시 및 `run_user_trading_flow` 구조 리팩토링
  - QQQ 시장 상태 캐싱/동시성 로직 검토 및 더 나은 공통 컨텍스트 구조 설계
  - 시장 상태 조회를 사용자별 호출에서 자동매매 사이클 1회 조회 후 주입 구조로 변경
  - 장 개장 여부도 사용자별 재계산 대신 자동매매 사이클 공통 값으로 주입
  - `run_user_trading_flow` 대형 함수의 책임 분리
  - 보유 종목 매도 감시와 신규 매수 진입 로직을 별도 함수로 분리
  - 신규 매수 진입 내부의 피라미딩 단계 판단, 재진입 쿨다운, 주문 수량 계산, 예수금 부족 알림, 체결 반영, 체결 메시지 발송을 헬퍼 함수로 분리
  - 변경 후 백엔드 컴파일 및 하드 하네스 검증 수행

- [x] pytest 기반 백엔드 테스트 하네스 1차 구축
  - 테스트 의존성 및 실행 구조 추가
  - FX 환율 캐시 동시성/TTL 동작 테스트 추가
  - 시장 상태 캐시 async lock/TTL 동작 테스트 추가
  - 스케줄러 사이클 공통 context 주입 동작 테스트 추가
  - `verify_harness.py`에 백엔드 테스트 실행 단계 연결
  - pytest 단독 실행 5개 테스트 통과
  - 확장된 `verify_harness.py` 전체 통과

- [/] 백엔드 안전 동작 테스트 하네스 2차 구축
  - REAL 안전락 상태에서 매수/매도 주문 호출이 차단되는지 검증
  - fake broker 기반 `run_user_trading_flow` 핵심 매수/매도 시나리오 검증
  - KIS 설정 검증 실패 후 저장 차단 경로 테스트 가능성 검토
  - 외부 Yahoo/KIS 호출 없이 mocking 기반으로 실행
  - 확장된 `verify_harness.py` 전체 통과 검증
