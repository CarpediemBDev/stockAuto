# 2026-08-22 Task Board

## 신규 작업
- [x] UI/UX CSS 공통화 및 기초 디자인 시스템(Primitive UI) 구축, 도메인 색상 SSOT 통합 <!-- APPROVED: 2026-08-23/사용자 승인 -->
- [x] 디자인 시스템 1차 이관 코드 리뷰에서 발견된 실버그 3건 수정 (번역 키 누락, Input 패딩 소실, 클라이언트 지시어 삭제) <!-- APPROVED: 2026-08-23/사용자 승인 -->
- [x] 표면색 하드코딩 55곳을 globals.css 토큰 유틸리티(bg-surface-card, bg-surface-card-subtle)로 치환해 빌드 시점 SSOT 확보 <!-- APPROVED: 2026-08-23/사용자 승인 -->
- [x] 손익 부호 기반 색상 하드코딩 16곳을 getProfitColor/getRegimeTheme로 흡수하고 surfaceStyles와 components/ui 프리미티브의 스타일 문자열 중복 제거 <!-- APPROVED: 2026-08-23/사용자 승인 -->

### 변경 영향 기록
- 변경 분류: UI/UX 아키텍처 및 디자인 시스템 리팩토링
- 생산자: frontend/app/globals.css, frontend/components/ui/*, frontend/lib/theme.ts
- 소비자: frontend/components/*, frontend/app/*
- 데이터 경계: 프론트엔드 UI 프레젠테이션 계층 (백엔드 API 변경 없음)
- API 계약: API 스키마 변경 없음 (기존 API 통신 인터페이스 100% 호환 유지)
- 문서 반영: docs/tasks/2026-08-22.md
- 회귀 테스트: python scripts/verify_harness.py 8/8 통과, npx tsc --noEmit 통과, npm run lint 통과, 로그인 화면 실측으로 치환 전후 픽셀 동일 확인(카드 12,16,29,0.80 / 인풋 8,12,22,1.00 / 패딩 16px), 신규 토큰 surface-card-highlight와 color-mix 기반 surface-glass 브라우저 해석 확인
- 미해결 위험: 뉴스 센티먼트 색상(POSITIVE/NEGATIVE/중립)이 PortfolioView와 OverseasScanner에 동일 로직으로 중복돼 있고 theme.ts에 해당 유틸이 없다. blue 계열 강조색이 여러 화면에 남아 indigo와 혼재한다. 상승=초록 하락=빨강으로 색상 관례가 뒤집혀 기존 사용자에게 의미가 반대로 읽힐 수 있으므로 별도 고지가 필요하다. 런타임 CSS 변수 교체를 통한 라이브 테마 전환은 theme inline 특성상 미검증이다. AssetTrendChart 등락 뱃지와 OverseasScanner 갭 뱃지는 흡수 과정에서 배경 투명도와 테두리가 미세하게 바뀌었다.
- 인수인계: globals.css 토큰이 실제 SSOT로 동작함을 확인했다. 생성 CSS가 var(--surface-card)와 color-mix로 토큰을 참조하며, 토큰 값을 임시 변경해 재빌드하니 번들에 그대로 반영됐다. 손익 부호 기반 색상은 전부 getProfitColor로 흡수했고 남은 emerald/rose 조건부 색상은 봇 가동 상태, 외부 API 헬스, 실거래 모드 경고, 리스크 등급, 뉴스 센티먼트처럼 손익과 무관한 의미라 의도적으로 남겼다. 다음 시작 지점은 센티먼트 색상 유틸을 theme.ts에 추가해 두 화면의 중복을 없애고 blue 계열 강조색을 indigo로 마저 통일하는 작업이다.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
