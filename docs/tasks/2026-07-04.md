# 2026-07-04 Task Board

## 미커밋 소스 리뷰 및 고성능 동시성 아키텍처 개편

- [x] `database.py`: 전역 `expire_on_commit=False` 제거 (Lost Update 방지) 및 `NullPool` 유지 (PgBouncer 호환)
- [x] `scheduler.py`: 내부 `db.merge(load=False)` 꼼수 제거 및 `load=True` 복원으로 Phantom Update 차단
- [x] `models.py`: `UserSettings`, `Holding` 등 다빈도 조회/수정 테이블에 `version_id_col` (낙관적 락) 적용
- [x] `simulated_broker.py`: 잔고 차감 시 파이썬 계산(Read-Modify-Write)을 버리고 DB Atomic In-place Update (`UPDATE ... SET amount = amount - X`)로 교체
- [x] `simulated_broker.py`: 체결 처리 시 `order_id`를 활용한 멱등성 검증 추가 (Double Fill 중복 매수 차단)
- [x] `router_account.py`: 다중 종목 강제 청산 루프의 트랜잭션 원자성(Partial Commit 방지) 보장
- [x] `docs/ARCHITECTURE_V2.md`: PostgreSQL 이관 대비 고성능 동시성(하이브리드 락 및 Atomic 연산) 설계 문서 동기화

## 프론트엔드 연동 및 무결성 검증 (Integration)
- [x] 프론트엔드 정적 분석 (`npm run lint`, `npx tsc --noEmit`) 무결성 입증
- [ ] 관리자 페이지(`admin/settings`) 강제 청산 버튼 동작 및 API 호출 테스트
- [ ] 로컬 환경(Backend + Frontend) 동시 구동을 통한 E2E 시각적 연동 확인

## 변경 영향 계약 (Change Impact)
*   **변경 분류**: 동시성 제어 아키텍처 고도화
*   **생산자**: `database.py`, `models.py`, `scheduler.py`, `simulated_broker.py`
*   **소비자**: 백그라운드 봇 전체, 관리자/사용자 API 전체
*   **데이터 경계**: 사용자 잔고, 설정 상태, 미체결 주문
*   **회귀 테스트**: `pytest backend/` 전수 실행 (100% Pass 목표)
*   **미해결 위험**: `version_id_col` 마이그레이션 적용 시 기존 DB 스키마 갱신(alembic) 필요.
