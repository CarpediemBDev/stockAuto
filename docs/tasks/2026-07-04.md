# 2026-07-04 Task Board

## 미커밋 소스 리뷰 및 고성능 동시성 아키텍처 개편

- [x] `database.py`: 전역 `expire_on_commit=False` 제거 (Lost Update 방지) 및 `NullPool` 유지 (PgBouncer 호환)
- [x] `scheduler.py`: 내부 `db.merge(load=False)` 꼼수 제거 및 `load=True` 복원으로 Phantom Update 차단
- [x] `models.py`: `UserSettings`, `Holding` 등 다빈도 조회/수정 테이블에 `version_id_col` (낙관적 락) 적용
- [x] `simulated_broker.py`: 잔고 차감 시 파이썬 계산(Read-Modify-Write)을 버리고 DB Atomic In-place Update (`UPDATE ... SET amount = amount - X`)로 교체
- [x] `simulated_broker.py`: 체결 처리 시 `order_id`를 활용한 멱등성 검증 추가 (Double Fill 중복 매수 차단)
- [x] `router_account.py`: 다중 종목 강제 청산 루프의 트랜잭션 원자성(Partial Commit 방지) 보장
- [x] `docs/ARCHITECTURE_V2.md`: PostgreSQL 이관 대비 고성능 동시성(하이브리드 락 및 Atomic 연산) 설계 문서 동기화

## 프론트엔드 연동 및 무결성 검증 (Integration)
- [x] 프론트엔드 정적 분석 (`npm run lint`, `npx tsc --noEmit`) 무결성 입증
- [ ] 관리자 페이지(`admin/settings`) 강제 청산 버튼 동작 및 API 호출 테스트
- [ ] 로컬 환경(Backend + Frontend) 동시 구동을 통한 E2E 시각적 연동 확인

## 문서 정합성 및 하네스 강화 (Docs SSOT & Harness)
- [x] `README.md` ↔ `AGENTS.md` 기술 스택 표기를 `package.json` 실제값으로 통일 (Next.js 16, Tailwind CSS v4, React 19)
- [x] `docs/README.md` 인덱스 신설 (문서별 SSOT 주제 매핑)
- [x] 일회성 리포트/플랜 문서 `docs/archive/` 이관 (BUG_REPORT/TYPESCRIPT_ANY/BACKTEST_ARENA/REPORT_UPDATE 4종, git mv 이력 보존)
- [x] `scripts/new_task.py` 변경영향기록 자동 스캐폴딩 하네스 추가 (verify_harness 필드 SSOT import)

## 변경 영향 계약 (Change Impact)
*   **변경 분류**: 동시성 제어 아키텍처 고도화
*   **생산자**: `database.py`, `models.py`, `scheduler.py`, `simulated_broker.py`
*   **소비자**: 백그라운드 봇 전체, 관리자/사용자 API 전체
*   **데이터 경계**: 사용자 잔고, 설정 상태, 미체결 주문
*   **회귀 테스트**: `pytest backend/` 전수 실행 (100% Pass 목표)
*   **미해결 위험**: `version_id_col` 마이그레이션 적용 시 기존 DB 스키마 갱신(alembic) 필요.

### 변경 영향 기록
- 변경 분류: 문서 정합성(SSOT) 보정 — 기술 스택 표기 통일 및 문서 인덱스 신설 (코드 로직 무변경)
- 생산자: README.md, AGENTS.md, docs/README.md(신규)
- 소비자: 프로젝트 문서를 참조하는 모든 AI 에이전트·개발자 (런타임 코드 소비자 없음)
- 데이터 경계: 없음 — 사용자 데이터/DB/캐시 경계에 영향 없음
- API 계약: 변경 없음 — API 엔드포인트·응답 스키마 무변경
- 문서 반영: README.md·AGENTS.md의 Next.js 15→16, Vanilla CSS→Tailwind v4, React 18→19 표기를 frontend/package.json(next 16.2.6) 실제값에 일치. docs/README.md 문서 SSOT 인덱스 신설
- 회귀 테스트: verify_harness.py(계약/NFC/불변식/카오스/롤백 가드) 통과, 스택 표기 모순 grep 0건 확인
- 미해결 위험: 일회성 리포트/플랜 문서(BUG_REPORT_2026-06-04, TYPESCRIPT_ANY_REPORT 등)의 docs/archive/ 이관은 사용자 승인 대기 중(미실행)
- 인수인계: 다음 작업 후보 — (1) 일회성 문서 archive 이관, (2) 전략 문서 4종 SSOT 경계 재점검, (3) 하네스 강화(계약 필드 자동 스캐폴딩)

## 하네스 가드 우회 구멍 차단 (Harness Fail-Closed)
- [x] `verify_harness.py` `get_changed_files`: 일부만 staging 시 unstaged 민감 파일이 검사에서 누락되던 구멍 차단 (staged+unstaged+untracked 합집합, CI 동작 보존)

### 변경 영향 기록
- 변경 분류: 검증 하네스 fail-closed 보강 — 계약 가드 우회 구멍 차단 (검사 대상 산정 로직만 변경, 판정 규칙 무변경)
- 생산자: scripts/verify_harness.py (get_changed_files)
- 소비자: git pre-commit hook, CI, 수동 검증 실행 전체 (verify_harness를 호출하는 모든 경로)
- 데이터 경계: 없음 — 런타임 사용자 데이터/DB/캐시와 무관 (개발 검증 도구)
- API 계약: 변경 없음
- 문서 반영: 본 기록 및 현황판 태스크에 반영. AGENTS.md 규칙 텍스트 변경 없음(로직 강화만)
- 회귀 테스트: 재현 시나리오(무해 파일만 staged + 민감 파일 unstaged)에서 AGENTS.md·verify_harness.py 감지 확인, CI 경로 조기반환 로직 보존 확인
- 미해결 위험: 로컬 검증이 이제 작업트리 전체를 보므로, 커밋 안 할 unstaged 민감 파일이 있으면 계약 기록을 요구함(의도된 fail-closed, 필요 시 stash 권장)
- 인수인계: 다음 작업 후보 — 하네스 실패 메시지에 정확한 `- 필드:` 형식 예시 삽입, AGENTS.md 최상단 '읽기 순서 3줄' 고정
