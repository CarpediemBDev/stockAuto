# 2026-06-24 작업 현황판

## 1. 스케줄러 Micro-Session 아키텍처 리팩터링
- **상태:** `[x]` (사용자 승인 완료)
- **작업 설명:** `app/bot/scheduler.py`의 `run_user_trading_flow`에서 1유저 당 1커넥션을 쥐고 장시간(증권사 API 대기시간 포함) 유지하던 구조를 Fetch -> Network I/O -> Commit 의 Micro-Session 방식으로 쪼개어 DB 커넥션 풀(QueuePool) 고갈 문제를 해결합니다.

### 변경 영향 기록 (Change Contract)
- **변경 분류:** 백엔드 코어 스케줄러 / DB 연결 최적화
- **생산자:** `backend/app/bot/scheduler.py` (`run_user_trading_flow`, `prepare_trading_flow_context`)
- **소비자:** 모든 활성 사용자의 백그라운드 자동매매 루프, 증권사 API 콜백 처리 모듈
- **데이터 경계:** KIS/TOSS API 통신 간 SQLAlchemy Session 독립, DB 객체 Expunge 처리
- **API 계약:** 변경 없음 (단, 백그라운드 로직 성능 대폭 상향)
- **문서 반영:** `docs/ARCHITECTURE_V2.md` 신설
- **회귀 테스트:** 로컬 모의투자(SIMULATED) 환경 가동 시 15명 이상 유저에 대해 QueuePool Timeout 발생 여부 점검 및 `Scan Cycle Started` 로그 확인.
- **미해결 위험:** SQLAlchemy 객체를 Session에서 Expunge한 뒤 외부 함수에서 Lazy-loading 접근 시 DetachedInstanceError 발생 가능성 존재 (전체 Eager-load 적용 필요).
- **인수인계:** -
