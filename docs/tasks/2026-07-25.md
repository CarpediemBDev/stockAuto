# 2026-07-25 Task Board

## 신규 작업
- [x] 전역 최상위 에러 바운더리(global-error.tsx) 추가 (Claude 세션, 사용자 지시 "남은항목 진행", 전용 worktree stockAuto-sse / 브랜치 fix/global-error-boundary): 07-24 SSE 감사 후속 4건(PR #81) 마무리 시 미해결로 남긴 과제. app/error.tsx는 Next 규약상 루트 layout.tsx·template 자체의 오류를 잡지 못하므로, 이를 담당하는 app/global-error.tsx를 신설해 백지 화면(WSOD) 위험을 완전 차단. Next 16.2 규약 확인: 활성 시 루트 레이아웃을 대체하므로 자체 html/body 태그 필수·폰트/globals.css/Provider 부재이라 외부 의존 없는 인라인 스타일로 자립, metadata export 미지원이라 React title 컴포넌트로 대체, unstable_retry 프롭 사용(v16.2.0 도입). 순수 신규 파일 1개 추가로 기존 동작 무변경. <!-- APPROVED: 2026-07-25 사용자 "승인" (PR #83 병합 후) -->

### 변경 영향 기록
- 변경 분류: 프론트 안정성 보강 (최상위 에러 바운더리 신규 파일 1개 추가, 기존 동작 무변경)
- 생산자: frontend/app/global-error.tsx 신규 (루트 레이아웃 오류 시 폴백 UI, 자체 html/body + 인라인 스타일 자립, unstable_retry 재조회 버튼 + 대시보드 링크)
- 소비자: 루트 layout.tsx·template 렌더 중 던져진 런타임 오류(app/error.tsx가 못 잡는 최상위 경계) — 정상 흐름·기존 error.tsx 담당 범위(페이지·중첩 레이아웃)에는 영향 없음
- 데이터 경계: 변경 없음 (DB·API·상태 저장소 미접촉, UI 폴백 전용)
- API 계약: 변경 없음 (엔드포인트·응답 스키마 무변동)
- 문서 반영: 영향 없음 - 프레임워크 규약(Next App Router special file) 준수한 UI 폴백 추가로 외부 계약·스키마 무변경, 규약 근거는 코드 상단 주석으로 집행
- 회귀 테스트: 전용 worktree(stockAuto-sse)에서 npx tsc --noEmit·npm run lint 통과, python scripts/verify_harness.py 전체 실행 결과는 인수인계 참조. 커밋 시 pre-commit 훅으로 동일 하네스 재검증
- 미해결 위험: global-error는 Next 규약상 개발 모드에선 error 오버레이가 먼저 떠 프로덕션 빌드에서만 실제 노출됨(정상 동작). 자체 html/body라 루트 레이아웃의 i18n Provider·폰트가 빠지므로 이 화면 문구는 하드코딩 한국어(전역 오류 특성상 Provider 신뢰 불가하므로 의도된 선택)
- 인수인계: 07-24 SSE 감사 후속(PR #81)의 유일 미해결 과제 종결. error.tsx(세그먼트)+global-error.tsx(루트) 2계층으로 App Router 에러 경계 완비. 다중 세션 격리 규칙에 따라 전용 worktree에서 작업, 메인 체크아웃·타 세션 파일 무접촉

- [R] 다중 세션 작업 격리(worktree) 규칙 SSOT 반영 (Claude 세션, 사용자 지시): 여러 세션이 하나의 워킹트리를 공유해 발생한 커밋 오염·detached HEAD·브랜치 강제 전환을 근본 차단하기 위한 규칙 문서화. 1단계로 docs/MULTI_SESSION_WORKTREE.md 전용 규칙서 신설 + docs/README.md 인덱스 등록해 PR #71 머지 완료. 2단계로 핵심 규칙을 SSOT인 AGENTS.md §9(멀티 에이전트 협업 프로토콜)에 §9-2로 요약 반영하고 상세 문서로 연결(브랜치 docs/multi-session-worktree-rules, PR #79). 병행 조치로 쓰기 세션 2개 기준 worktree를 실제 생성(D:\dev\workspace\stockAuto-ag, 브랜치 ag/base)하고 venv·node_modules·env·DB·launch.json을 각자 설치·복사해 백엔드 import 및 프론트 tsc 통과까지 검증. 동일 파일(AGENTS.md)을 편집 중인 다른 세션의 변경 2줄은 커밋에서 제외하고 워킹트리에 보존함.

### 변경 영향 기록
- 변경 분류: 문서·프로세스 규칙 추가 (코드 변경 없음)
- 생산자: AGENTS.md (§9-2 세션별 worktree 격리 규칙 9줄 추가), docs/MULTI_SESSION_WORKTREE.md (전용 규칙서 신규, PR #71 머지), docs/README.md (Living SSOT 인덱스에 규칙서 등록, PR #71 머지)
- 소비자: 이 저장소에서 파일을 수정하는 모든 주체 — Claude 세션, Antigravity, 기타 AI 에이전트, 사람(IDE/터미널). 콜드 스타트 시 docs/README.md 인덱스와 AGENTS.md §9-2를 통해 규칙에 도달함
- 데이터 경계: 변경 없음 (DB·스키마·캐시 무관, 문서 전용)
- API 계약: 변경 없음 (엔드포인트·응답 스키마 무변동)
- 문서 반영: 영향 없음 - 장기 문서 반영은 선행 PR #71에서 docs/MULTI_SESSION_WORKTREE.md 신설 및 docs/README.md 인덱스 등록으로 이미 완료했고, 본 변경은 그 정본을 AGENTS.md §9-2에서 요약·링크만 하므로 추가 장기 문서 수정 대상이 없음
- 회귀 테스트: PR #71 CI harness 통과(1m21s). AGENTS.md 변경분은 PR #79에서 CI harness 재검증. worktree 실동작은 ag 폴더에서 백엔드 import OK 및 프론트 npx tsc --noEmit 통과로 확인
- 미해결 위험: 백엔드 포트 8000이 backend/run.py에 하드코딩되어 worktree별 분리가 불가하므로 앱 구동과 verify_harness는 한 번에 한 폴더만 실행해야 함(규칙서 6절에 명시). Redis 6379 공유로 봇 동시 구동 시 주문 락 간섭 가능. 향후 run.py 포트를 환경변수화하면 제약 해소 가능(규칙서 8절 후보로 기록)
- 인수인계: 규칙서 본문(PR #71)과 AGENTS.md §9-2(PR #79) 반영 완료. 쓰기 세션용 worktree(stockAuto-ag)는 의존성 설치까지 끝나 즉시 사용 가능. 동일 파일을 동시 편집 중인 다른 세션의 AGENTS.md 변경 2줄(마이그레이션 안전성 검사·현황판 자가 승격 차단)은 그 세션이 직접 커밋하도록 워킹트리에 보존해 둠

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## 신규 작업 (i18n 8단계)
- [R] 프론트엔드 i18n 잔여 치환 8단계: 관리자 화면 6종 (Claude 세션) - admin/page·admin/system/page·SystemSettings·TranslationManager·SystemHealth·BacktestTournament의 하드코딩 문구 110곳을 admin_ui 네임스페이스로 이관

### 변경 영향 기록 (프론트엔드 i18n 8단계 - 관리자 화면 6종)
- 변경 분류: 프론트엔드 다국어 치환 (UI 문구만 이동, 로직·스키마·API 무변경)
- 생산자: locales/ko.json과 locales/en.json에 admin_ui 네임스페이스 신설 - 114키를 양쪽 언어에 동시 등록. 기존 admin.users 네임스페이스와 충돌을 피하려 admin_ui로 분리했다
- 소비자: frontend/app/admin/page.tsx(11곳)·app/admin/system/page.tsx(1곳, 동일 문구 2회)·components/admin/SystemSettings.tsx(10곳)·TranslationManager.tsx(31곳)·SystemHealth.tsx(21곳)·BacktestTournament.tsx(36곳)이 useTranslations("admin_ui")로 총 110곳을 치환. 구조상 손이 더 간 두 곳: BacktestTournament의 종목 map 콜백 인자가 t라 훅 t를 가려 ticker로 개명했고, SystemSettings는 SETTING_TITLES를 렌더하는 하위 컴포넌트 SettingCard가 별도 스코프라 거기에도 훅을 추가하고 상수를 키(label_gemini 등)로 바꿔 t()로 조회하게 했다
- 데이터 경계: 변경 없음 - 번역 딕셔너리 JSON 키 추가뿐이며 DB·테이블·컬럼 무관. TranslationManager의 등록/수정/삭제 실제 동작 로직은 건드리지 않았다
- API 계약: 변경 없음 - 엔드포인트·스키마 무변경
- 문서 반영: 영향 없음 - UI 문구를 딕셔너리로 옮긴 치환 작업이라 docs/SCHEMA.md·docs/API_STANDARD.md에 갱신할 계약이 없음
- 회귀 테스트: 기존 test_i18n_messages.py의 프론트 소비 키 검증이 신규 admin_ui 키 114종을 자동 대조한다(총 18건 통과). npx tsc --noEmit 0건, npm run lint 0건(map 인자 개명·SettingCard 훅·useEffect 의존성 t 추가 포함), python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: frontend/app/global-error.tsx(한글 10줄)는 이번 조각에서 의도적으로 제외했다 - 이 전역 에러 바운더리는 루트 레이아웃을 통째로 대체해 NextIntlClientProvider가 존재하지 않으므로, useTranslations를 넣으면 에러 화면 자체가 런타임 크래시를 낸다(파일 주석도 "Provider가 모두 사라지므로 외부 의존 없이 자립" 명시). 이 파일의 다국어화가 필요하면 NEXT_LOCALE 쿠키를 직접 읽어 인라인 분기하는 별도 방식이어야 한다. 또한 백엔드가 실행되지 않는 격리 클론이라 관리자 6개 화면을 로그인 후 브라우저로 직접 확인하지는 못했다(딕셔너리 로딩은 3단계 standalone 빌드로 실증됨)
- 인수인계: 이로써 관리자 화면을 포함한 주요 UI 치환이 사실상 완료됐다. 명시적으로 남긴 것은 global-error.tsx(Provider 밖, 별도 방식 필요) 하나다. korean_strings_report.json의 frontend 항목으로 잔여 하드코딩을 추가 점검할 수 있다
