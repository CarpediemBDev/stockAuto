# 2026-09-14 Task Board

## ATR 동적 임계의 실제 동작(상수 고정)을 주석·문서에 명시
- [x] 손절·트레일링·수확 임계·매수 수량이 실제 매매에서는 5분봉 ATR 입력 탓에 늘 하한(수량은 상한)값으로 동작한다는 사실을 문서에 명시하고, ATR 산출 지점에 문서를 가리키는 주석 한 줄을 남긴다. 코드 동작은 바꾸지 않는다(사용자 선택 A안). 작업 위치: 전용 worktree `D:\dev\workspace\stockAuto-atr-docs` (origin/main 6cec640, 공유 체크아웃은 stockauto-f3이 scheduler.py 편집 중) <!-- APPROVED: 2026-09-15 사용자 승인 (PR #129 머지 후) -->

### 변경 영향 기록
- 변경 분류: 문서 정정과 주석 1줄. 실행 코드·API·DB 스키마·매매 동작 변경 없음(사용자 선택 A안). 파이썬 변경은 backend/app/scanner/scanner.py의 주석 1줄뿐이다.
- 생산자: docs/strategy_specification.md(3장 E절 신설, B·D절 정정), docs/strategy_architecture.md, docs/plans/holding_management_modes.md, docs/FILTER.md, docs/SCHEMA.md, backend/app/scanner/scanner.py(5분봉 ATR 산출 지점에 E절을 가리키는 주석 1줄)
- 소비자: 코드 소비자 없음(주석 변경). 문서 소비자는 전략을 추가·튜닝하거나 백테스트 결과를 해석하는 사람과 AI 세션, 수확 임계 화면 값을 읽는 사용자다.
- 데이터 경계: 없음. DB·캐시·외부 호출을 읽거나 쓰는 경로가 바뀌지 않는다. holdings.harvest_arm_pct·harvest_trailing_pct에 실측상 대부분 15와 5가 기록된다는 사실만 SCHEMA에 적었다.
- API 계약: 변경 없음.
- 문서 반영: docs/strategy_specification.md 3장에 E절 신설(입력 시간 프레임, 항목별 실제 값 표, 전략별 base 차이, 백테스트와의 차이, 동작 유지 사유)과 B·D절의 존재하지 않는 상한(8.0%·12.0%) 표기 정정. docs/strategy_architecture.md 메서드 설명, docs/plans/holding_management_modes.md 수확 파라미터 표와 2.2절, docs/FILTER.md ATR 항목(없는 ATR 익절선 표기 정정), docs/SCHEMA.md 수확 임계 컬럼 설명에 실제 동작을 명시했다.
- 회귀 테스트: 런타임 변경이 없어 신규 테스트는 없다. 문서에 쓴 주장은 코드로 대조했다 - ATR은 scheduler의 손절·트레일링·수확·수량에서만 소비되고 전략 점수 계산과 익절선에는 쓰이지 않으며(signal_contract는 필드 목록만 보유), 백테스트 ATR은 indicator_metrics가 백테스트 봉 간격 df로 계산하고, base_strategy에는 손절·트레일링 상한이 없다. 최초안(코드 주석 4개 파일)으로 python scripts/verify_harness.py 8/8 통과 후, 사용자 검토로 코드 주석을 scanner.py 1줄로 줄여 재검증했다 - 8/8 통과(계약 검사 민감 파일 1개 기록 확인, 컴파일 337파일, NFC 579파일, Alembic 최신, pytest 596건, tsc·eslint, Playwright 9건). 두 번 모두 stockauto-f3에 착수를 알린 뒤 직렬로 돌렸다.
- 미해결 위험: 첫째, 실제 매매와 일봉 백테스트가 서로 다른 손절·수량 규칙으로 돈다는 사실을 문서화했을 뿐 해소하지 않았다. 과거 전략 토너먼트·백테스트 결과는 실제 매매에 없는 동적 손절과 가변 수량으로 산출된 것이라 실전 성과를 과대 또는 과소 추정했을 수 있다. 둘째, 매수 수량이 거의 항상 기본 할당의 1.5배로 산정된다는 사실은 이번에 처음 명시됐고, ATR 교체 비교(2026-09-10)는 손절·트레일링만 평가해 수량 영향은 검증되지 않았다. 셋째, 문서의 실측 수치(5분봉 ATR% 중앙값 0.19%)는 10종목 5일치 표본이고 시장 변동성이 커지면 하한을 넘는 종목 비율이 늘 수 있다. 넷째, strategy_specification.md B·D절의 2회 연속 이탈 버퍼 설명은 이후 시간 하한이 추가된 현행 규칙과 다를 수 있으나 이번 범위 밖이라 손대지 않았다.
- 인수인계: 코드 동작은 그대로다. 실제 매매 값의 정본 설명은 docs/strategy_specification.md 3장 E절이며 다른 문서와 scanner.py 주석은 그곳을 가리킨다. 최초안은 scheduler.py·trade_calculations.py·base_strategy.py에도 주석을 넣었으나 같은 설명의 반복이고 측정 수치(중앙값 0.19%)를 코드에 박으면 낡은 주석이 되며, scheduler.py는 stockauto-f3이 미커밋 편집 중이라 사용자 검토에서 되돌렸다. 작업 위치는 전용 worktree D:\dev\workspace\stockAuto-atr-docs(origin/main 6cec640, detached HEAD)다. f3과 이 파일(docs/tasks/2026-09-14.md)이 둘 다 신규 추가라 나중에 병합되는 쪽이 add/add 충돌을 해소해야 한다. 검증은 끝났고 상태는 [R]이다. 커밋 트리거는 받지 않았다. 다음 시작 지점은 사용자 승인 또는 커밋 트리거이며, 받으면 worktree에서 docs/ 브랜치를 만들고 이 목록의 파일만 스테이징한다.

## 사이클 계측 요약 줄에 시장 세션 표기

브랜치: `fix/2026-09-14-cycle-session-label`

- [R] 사이클 계측 요약 줄에 시장 세션 표기 추가 및 휴장 관련 부정확한 설명 정정 — 2026-09-13 PR #126의 `[Cycle] Profile` 줄은 "휴장 사이클에는 남지 않는다"고 문서화했으나, 휴장이어도 보유 종목이 있는 유저가 있으면 사용자 흐름이 시작되어 줄이 남는다. 세션 표기가 없어 휴장 표본이 정규장 표본과 섞인다

### 변경 영향 기록
- 변경 분류: 관측성 정정(로그 형식에 세션 표기 추가) + 부정확한 주석·문서 정정. 매매 판정·주문 동작 변경 없음
- 생산자: backend/app/bot/scheduler.py(async_trading_loop의 cycle_session 기록, [Cycle] Duration 줄 끝에 (session=…) 추가, [Cycle] Profile 줄 머리에 session=… 추가, 휴장 오해 주석 정정), backend/app/bot/cycle_profiler.py(has_user_flows 독스트링 정정)
- 소비자: 운영자·분석 세션(stockauto.log의 [Cycle] Duration·Profile 줄을 읽어 기준선을 잡는 쪽), 모바일 세션(재기동 후 측정 계획), backend/tests/test_cycle_profiler.py
- 데이터 경계: DB·API·스키마 무관. 로그 줄 형식만 바뀐다. Duration 줄은 "Duration: 12.3s"까지의 앞부분을 유지하고 뒤에 괄호로 세션을 붙여 기존 정규식 파싱(`Duration: ([\d.]+)s`)이 깨지지 않는다. Profile 줄은 이번 사이클 PR #126에서 막 도입되어 기존 소비자가 없다
- API 계약: 변경 없음
- 문서 반영: docs/SYSTEM_MANUAL.md "매매 사이클 주기와 계측 로그 읽는 법" 절 정정 - 로그 표에 세션 표기 반영, "Profile 줄이 남았다고 정규장 사이클인 것은 아니다" 단락 추가(조기 반환 조건, 세션 값 목록, 시작 시점 기준, 워밍업 사이클 제외), "휴장이면 사용자 흐름을 건너뛴다"를 "매도·매수 판정을 건너뛴다"로 정정
- 회귀 테스트: backend/tests/test_cycle_profiler.py 신규 2건 + 기존 2건 보강. 휴장이어도 보유 유저가 있으면 Profile 줄이 session=CLOSED로 남는 실제 동작을 고정했고, 휴장·보유 유저 없음이면 조기 반환하되 Duration에 CLOSED가 붙는 것, 세션 판정 전에 끝난 사이클에는 세션을 지어내지 않는 것, 정규장 줄의 세션 표기와 Duration 파싱 호환을 확인했다. 네거티브 컨트롤: Profile 줄에서 세션 표기를 빼면 정규장 통합 테스트와 휴장 계약 테스트가 깨진다
- 미해결 위험: 첫째, 세션은 사이클 시작 시점 기준이라 정규장 개장(9:30 ET) 직전에 시작해 개장 후까지 이어진 사이클은 PRE_MARKET으로 표기된다. 사이클이 5~7분 걸리는 현재 주기에서는 개장·폐장 경계마다 한두 줄이 이 영향을 받는다. 둘째, 이 수정은 백엔드 재기동 후에야 로그에 반영된다 - 현재 운영 중인 프로세스(2026-09-13 23:36 KST 기동)는 세션 표기 없는 형식으로 줄을 남긴다. 셋째, PR #126 이전 로그와 이번 재기동 이후 수정 전 로그는 세션 표기가 없으므로 타임스탬프로 NY 세션을 판정해야 한다. 넷째, 2026-09-13 현황판·PR #126 본문의 "휴장 사이클은 요약을 남기지 않는다"는 서술은 승인된 기록이라 고치지 않고 여기에 정정 사실을 남긴다
- 인수인계: 커밋하지 않았다(사용자 커밋 트리거 대기). 반영하려면 병합 후 백엔드 재기동이 필요하며, 첫 유효 표본은 병합·재기동 이후 처음 맞는 NY 정규장(9:30 ET 이후) 사이클이다. 2026-09-14 정규장은 병합 전이라 세션 표기 없는 형식으로 기록됐다. 분석 시 `session=REGULAR_MARKET`인 Profile 줄만 쓰고 재기동 직후 첫 사이클은 제외한다. 워킹트리의 docs/tasks/2026-09-09.md 한 줄은 stockauto-27(=stockauto-92 [b59824]) 소유라 스테이징에서 제외해야 한다

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
