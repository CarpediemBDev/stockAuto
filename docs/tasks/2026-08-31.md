# 2026-08-31 Task Board

## 신규 작업
- [x] 브로커 보유분 동기화의 락 자기경합 수정 인계·마무리 (사용자 지시 "그 슬라이스도 마무리해서 같이 커밋하고 머지해"): 워킹트리에 커밋되지 않은 슬라이스가 남아 있었고 담당 세션이 동작하지 않아 본 세션이 인계받아 마무리한다. run_user_trading_flow가 이미 사용자 락을 잡은 뒤 sync_broker_holdings를 호출하는데 그 안에서 같은 락을 다시 잡으려 해, Redis 락이 비재진입이라 항상 lease가 None이 되고 동기화가 매 실행 조용히 건너뛰어지고 있었다. 안쪽 중복 획득을 제거해 자기경합을 없앤다. 직렬화는 바깥 락이 그대로 수행하므로 동시성 보호는 줄지 않는다. <!-- APPROVED: 2026-08-31/유저승인("승인") -->
- [x] 2026-08-30 현황판 승인 승격 10건 (사용자 지시 "승인") <!-- APPROVED: 2026-08-31/유저승인("승인") -->

### 변경 영향 기록
- 변경 분류: 동시성 결함 수정 (사용자 작업 락의 재진입 시도 제거). 락 획득 지점 자체는 유지
- 생산자: backend/app/bot/scheduler.py(sync_broker_holdings의 중복 락 획득·해제 제거), backend/tests/test_trading_flow_scenarios.py(비재진입 락 환경 자가치유 시나리오 1건 신설), docs/tasks/2026-08-30.md(승인 승격 10건), docs/tasks/2026-08-31.md
- 소비자: backend/app/bot/scheduler.py의 run_user_trading_flow 경로, 증권사 보유분 자가치유(수량 증가 반영, 유령 보유 복원, DB 고스트 레코드 청소)
- 데이터 경계: 스케줄러 런타임과 Redis 락 계층. DB 스키마·마이그레이션·API 스키마 전부 무변경. holdings 테이블은 자가치유가 정상 동작하면서 실제 증권사 잔고와 다시 일치하게 된다
- API 계약: API 스키마 변경 없음
- 문서 반영: 영향 없음 - 락 획득 지점과 직렬화 범위가 그대로이고 외부 계약·운영 절차가 바뀌지 않아 장기 문서에 반영할 규칙 변화가 없다. 결함과 수정 근거는 신설 테스트의 docstring과 본 기록이 소유한다
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과. backend pytest 448건 통과. 인계 전 상태를 그대로 믿지 않고 네거티브 컨트롤로 실증했다 - sync_broker_holdings에 중복 락 획득을 되돌리자 신설 테스트가 "Skipped sync due to lock unavailability" 로그 부재 단언에서 실패했고, 제거 상태로 복원하니 통과했다. 즉 수정 전에는 동기화가 매 실행 건너뛰어지고 있었다. 신설 테스트는 실제 Redis와 같은 비재진입 락 페이크(이미 잡힌 사용자면 None 반환)를 주입해 acquired_count 1과 released_count 1, 잔여 락 0을 함께 단언하므로 중복 획득이 다시 들어오면 반드시 깨진다
- 미해결 위험: 기존 락 페이크가 항상 리스를 내주는 관대한 구현이라 이 결함이 테스트를 통과해 왔다. 같은 계열 결함이 다른 경로에 남아 있을 수 있으며, 사용자 락을 잡는 다른 서브루틴이 상위에서 이미 잡힌 상태로 호출되는지는 이번에 전수 조사하지 않았다. 또한 본 커밋의 코드 슬라이스는 다른 세션이 작성한 것을 본 세션이 검증·인계한 것이라 서명이 실제 작성자와 다르다
- 인수인계: run_user_trading_flow가 scheduler.py 1867행에서 사용자 락을 잡고 1906행에서 sync_broker_holdings를 호출한다. 하위 서브루틴이 같은 락을 다시 잡으면 비재진입이라 즉시 자기경합으로 실패하며, 그 실패가 예외가 아니라 조용한 skip으로 처리돼 로그를 뒤지지 않으면 드러나지 않는다. 앞으로 사용자 락을 쓰는 코드를 추가할 때는 호출 사슬에서 이미 잡혀 있는지 먼저 확인할 것. 락 관련 테스트를 쓸 때 페이크가 항상 성공을 돌려주면 이 계열 결함을 통째로 놓치므로 반드시 비재진입 페이크를 쓴다

## 신규 작업 2
- [x] 로그인 IP 한도와 사용자 한도 분리 인계·마무리 (사용자 지시 "그것도 검증해서 마무리하고 머지까지 해"): 워킹트리에 남아 있던 두 번째 미커밋 슬라이스를 인계받아 검증하고 마무리한다. 로그인 RateLimiter가 peer_max_requests 없이 max_requests=5만 주고 있어 IP 한도가 사용자 한도와 같은 5회로 묶여 있었다. 공용 IP(가정·사무실·NAT) 뒤에서 서로 다른 사용자 5명이 시도하면 그 IP 전체가 1분간 로그인 불가가 된다. 브루트포스 방어는 계정 단위로 성립해야 하므로 IP 상한만 30으로 푼다. 아울러 브루트포스 통합 테스트의 격리를 보강하고 Playwright webServer 타임아웃 여유를 늘린다. <!-- APPROVED: 2026-08-31/유저승인("승인") -->

### 변경 영향 기록
- 변경 분류: 인증 레이트리밋 정책 조정 (IP 단위 상한만 완화, 사용자 단위 상한 불변) 및 테스트 격리·타임아웃 보강
- 생산자: backend/app/auth/router.py(login 엔드포인트에 peer_max_requests=30 배선), backend/tests/test_rate_limiter.py(로그인 배선 고정 테스트 신설), backend/tests/test_api_integration.py(브루트포스 테스트 전 프로세스 전역 폴백 윈도우 초기화 및 flushall 예외 허용), frontend/playwright.config.ts(webServer 타임아웃 180초에서 300초)
- 소비자: POST /api/v1/auth/login 요청 경로, backend/app/core/rate_limiter.py의 이중 한도 적용, Playwright E2E 기동
- 데이터 경계: 인증 요청 계층과 Redis 레이트리밋 키. DB 스키마·마이그레이션·API 스키마 전부 무변경. rate_limiter.py 자체는 이미 peer_max_requests를 지원하고 있어 무변경이며 이번 것은 배선만이다
- API 계약: API 스키마 변경 없음. 429 응답 발생 조건이 IP 단위에서만 완화된다
- 문서 반영: 영향 없음 - 외부 응답 규격과 사용자당 방어 강도가 그대로이고, signup은 이미 같은 배선이라 API_STANDARD의 레이트리밋 조항에 바뀌는 규칙이 없다. 정책 근거는 신설 테스트의 docstring과 본 기록이 소유한다
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과. test_rate_limiter.py 9건 통과. 인계 전 상태를 믿지 않고 네거티브 컨트롤로 실증했다 - login에서 peer_max_requests=30을 빼자 신설 테스트가 같은 IP의 6번째 사용자에서 429로 실패했고(assert 429 != 429), 복원하니 통과했다. 즉 수정 전에는 공용 IP 뒤 6번째 사용자부터 로그인이 막히고 있었다. 사용자당 상한이 느슨해지지 않았는지도 같은 테스트가 단언한다 - 동일 사용자명 6회째는 여전히 429다. 최초 네거티브 컨트롤은 router.py에 peer_max_requests가 signup과 login 두 곳에 있어 첫 번째(signup, 이미 main에 병합됨)만 지우는 바람에 통과해버렸고, login 줄을 정확히 지목해 다시 확인했다
- 미해결 위험: IP당 상한이 5에서 30으로 올라가므로 한 IP에서 여러 계정을 훑는 스프레이 공격의 분당 시도 횟수가 6배가 된다. 단일 계정 브루트포스는 사용자당 5회 그대로라 영향이 없다. 계정 잠금(5회 실패 시)이 여전히 최종 방어선이며, 이 완화는 가용성과 공격 표면의 의도된 교환이다. 또한 test_api_integration.py의 폴백 윈도우 초기화는 Redis 장애 시 프로세스 전역 딕셔너리가 테스트 간 누수되는 것을 막는 위생 조치인데, 원래의 실패를 본 세션이 직접 재현하지는 않았다. Playwright 타임아웃 상향도 콜드 빌드가 180초를 넘길 수 있다는 근거에 따른 여유 확보이며 타임아웃 실패를 직접 재현하지는 않았다
- 인수인계: RateLimiter는 IP 키와 주체 키 두 개를 각각 검사한다. peer_max_requests를 주지 않으면 IP 키 한도가 max_requests로 떨어져 두 한도가 같아지고, 그 순간 공용 IP 전체가 한 계정 몫으로 묶인다. 계정 단위 방어를 조이려고 max_requests를 낮출 때마다 IP 한도가 함께 조여지는 구조이므로, 앞으로 key_field를 쓰는 엔드포인트를 추가하면 peer_max_requests를 반드시 함께 지정한다. 신설 테스트가 login 배선을 고정하므로 값을 빼면 즉시 반려된다. 이 코드 슬라이스도 다른 세션이 워킹트리에 남긴 미커밋 작업을 본 세션이 검증·인계한 것이라 서명이 실제 작성자와 다르다

## 분석 (코드 변경 없음)
- [x] 락 자기경합 전수 조사 및 종목 락 키 설계 결정 문서화 (사용자 지시 "락 자기경합 다른 경로도 전수 조사해줘" → "심볼 락 키 건 결정해서 문서화하고 조사 결과도 현황판에 남겨줘"): 직전에 고친 sync_broker_holdings 재진입과 같은 계열 결함이 다른 경로에 남아 있는지 정적 호출 그래프로 전수 확인하고, 조사 중 드러난 종목 락 키의 슬롯 부재 문제를 결정해 정본에 남긴다. 제품 코드는 변경하지 않는다. <!-- APPROVED: 2026-08-31/유저승인("승인") -->

### 변경 영향 기록
- 변경 분류: 분석 및 설계 결정 문서화. 제품 코드 변경 없음
- 생산자: docs/ARCHITECTURE_V2.md(5장 신설 - 락 원장, 종목 락 키 결정과 근거, 재진입 금지 규약 3가지, 전수 조사 결과와 한계), docs/tasks/2026-08-31.md
- 소비자: 락을 새로 도입하거나 기존 락 키를 바꾸려는 모든 작업, 동시성 관련 코드 리뷰, 락 테스트 작성
- 데이터 경계: 문서 계층 한정. 백엔드·프론트엔드·DB 스키마·마이그레이션·API 전부 무변경. 개발 DB는 holdings 테이블을 mode=ro로 조회만 했다
- API 계약: API 스키마 변경 없음
- 문서 반영: docs/ARCHITECTURE_V2.md(5장 신설), docs/tasks/2026-08-31.md
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과. 조사는 AST 정적 분석으로 수행했다. 락 획득 지점은 acquire_user_operation_lock 2곳, acquire_symbol_order_lock 5곳, acquire_bootstrap_file_lock 1곳으로 전수 열거했고 각 지점의 소속 함수를 정의 범위로 확정했다. 전이 폐포(깊이 6)로 같은 키 중첩을 검사해 2건이 걸렸으나 정의 범위 대조로 오탐임을 확인했다 - _execute_single_sell(1121-1318)과 _execute_single_buy(1720-1856)이 각각 process_exit_signals(937-1322)와 process_entry_signals(1568-1861) 안에 정의된 중첩 함수라 부모 노드 순회에 자식의 락 획득이 딸려 들어온 것이며, scheduler의 종목 락 획득은 827·1134·1734 세 곳뿐이고 전부 중첩 헬퍼 내부다. 해제는 7개 지점 전부 finally 안에서 release 1회씩이고, 획득 실패는 전부 RedisLockUnavailable fail-close와 lease is None 로그 후 skip으로 갈라져 있다. 플레인 threading.Lock 7개도 직접·전이 모두 중첩 0건이다
- 미해결 위험: 조사는 정적 분석이고 함수 이름 기준 매칭이라 모듈이 달라도 이름이 같으면 합쳐지고 동적 디스패치와 간접 호출은 따라가지 못한다. 실제로 중첩 함수 정의 때문에 오탐 2건이 나왔다. 또한 이번 조사는 재진입만 다루었고 서로 다른 잡이 동시에 도는 프로세스 간 경합(예: 리컨실러 잡과 트레이딩 플로우)은 별개 문제군으로 다루지 않았다. 종목 락의 슬롯 부재로 인한 건너뛰기는 개발 DB 기준 현재 사례 0건이라 실측 재현이 아니라 스키마와 코드 경로로 도출한 잠재 조건이다
- 인수인계: 종목 락 키에 strategy_type을 넣지 않는 것은 결정이며 결함이 아니다. 락이 지키는 것은 DB 파티션이 아니라 증권사 계좌이고, 계좌 포지션은 종목당 하나이며 슬롯은 회계 구분일 뿐이다. 슬롯별로 락을 쪼개면 두 슬롯이 동시에 주문을 제출해 계좌 기준 과대 포지션이 생기고, sync_broker_holdings가 유령 보유의 소속 슬롯을 최근 BUY TradeLog로 추정하는 휴리스틱도 모호해진다. 로그에서 Another order is in progress를 보고 슬롯이 막혔다며 키에 슬롯을 더하는 수정을 하지 말 것. 근거 전문은 ARCHITECTURE_V2.md 5.2절에 있다. 락 테스트를 새로 쓸 때 페이크가 항상 리스를 내주면 재진입 결함군을 통째로 놓치므로 반드시 비재진입 페이크를 쓴다

## 신규 작업 3
- [R] 청산 결손 진입 차단 철회 및 잘못된 전제 정정 (사용자 지시 "되돌리기 진행해줘"): 13종 진입 차단은 "청산 경로 결손이면 시그널 청산이 불가능하다"는 검증되지 않은 전제 위에 세워졌고, 로그 실측으로 그 전제가 거짓임이 드러났다. 차단·예외를 철회하고 문서를 정정하며, 같은 오판을 반복하지 않도록 측정 지표를 읽기 기준에서 청산 반응성 기준으로 교체한다.

### 변경 영향 기록
- 변경 분류: 라이브 매매 동작 변경 철회 (진입 차단·예외 제거) 및 측정 지표 교체. 청산 경로 무변경
- 생산자: backend/alembic/versions/d4e2f8a13c65_restore_exit_degraded_strategies.py(신설, is_selectable 복원), backend/app/scanner/signal_contract.py(ENTRY_BLOCKED_STRATEGIES·ENTRY_BLOCK_EXEMPTIONS·is_entry_blocked 제거), backend/app/bot/scheduler.py(진입 차단 분기 및 import 제거), backend/tests/test_live_entry_signal_contract.py(읽기 기준 청산 결손 검사를 청산 반응성 기준으로 교체, 차단 관련 테스트 5건 제거, 불변식 2건 신설), scripts/update_strategy_entry_states.py(스냅샷 필드를 exit_path_gaps에서 exit_unresponsive로 교체), backend/tests/strategy_entry_states.json(재생성), docs/SIGNAL.md(7장을 진입 차단에서 청산 반응성으로 재작성), docs/tasks/2026-08-23.md(원 주장에 정정 노트), docs/tasks/2026-08-30.md(차단 항목에 철회 노트)
- 소비자: backend/app/bot/scheduler.py 매수 경로, backend/app/strategy_catalog/router.py 카탈로그 조회, 향후 청산 관련 판단
- 데이터 경계: strategies 테이블의 is_selectable 3행 복원(asqs, macro_momentum, strategy_c). 마이그레이션 d4e2f8a13c65(head, down_revision c8d1e5f20b74). b7c3d9e14a20이 내린 22종은 근거가 달라 건드리지 않았다. holdings·trade_logs·user_settings 무변경
- API 계약: API 스키마 변경 없음. 카탈로그 응답에 3종이 복귀한다
- 문서 반영: docs/SIGNAL.md(7장 재작성), docs/tasks/2026-08-23.md, docs/tasks/2026-08-30.md, docs/tasks/2026-08-31.md
- 회귀 테스트: python scripts/verify_harness.py 8/8 전체 통과. test_live_entry_signal_contract.py 19건 통과. 철회 근거를 네 갈래로 실측했다 - (1) 청산 로그 20,532건 중 시그널 붕괴 91%, 손절 5.5%, 트레일링 1.3%이고 차단 대상일수록 시그널 비중이 높다(asqs 2,346건 중 2,276건, 손절 0건). (2) 차단 13종의 보유 포지션 나이가 다른 전략과 같은 3.1일이라 묶인 포지션이 없다. (3) 상승·횡보·하락 입력으로 직접 채점하니 13종 전부 cutoff 아래로 붕괴한다. (4) 코드상 결손 필드가 게이트가 아니라 가점·감점이거나 OR 항의 하나다. 새 지표는 읽기가 아니라 채점 결과를 재며, 도입하자마자 기존 지표가 놓쳤던 진짜 청산 불가 5종(cross_asset, obi_ofa, pairs_trading, pca_knn, pdufa_calendar)을 찾아냈다. 그 5종은 전부 진입 자체가 불가라 위험 조합은 0종이다. 스냅샷은 112종 진입가능 84 / 미진입-지표누락 20 / 미진입-의도차단 4 / 자율 4로 재생성했다
- 미해결 위험: macro_momentum의 침체 경보는 여전히 영구 False다(기본값 spread=0.1, bei=2.0). 매크로 청산 사유 하나가 죽은 상태이며 EMA200 청산은 정상이라 이번 철회 범위에 넣지 않았다. 매크로 시계열을 연동하거나 기본값을 재설계해야 해소된다. 또한 차단 코드는 백엔드가 차단 커밋보다 1분 14초 먼저 기동한 덕에 라이브에서 한 번도 실행되지 않았으나, 이는 운이었지 설계가 아니다. 현재 도는 백엔드는 락 자기경합 수정도 실리지 않은 구코드이며 시간당 1,170건의 Sync Guard skip이 계속 발생 중이라 재시작이 필요하다
- 인수인계: 이번 오판의 연쇄를 기록해 둔다. 2026-08-23 보드 인수인계에 "기존 보유분은 시그널 청산이 아니라 손절·트레일링으로만 정리된다"는 검증되지 않은 문장이 있었고, 본 세션이 그것을 사실로 취급한 채 진입 경로의 퇴화 패턴을 청산 경로에 대칭으로 추론해 차단까지 갔다. 네거티브 컨트롤은 여러 번 돌렸지만 전부 "가드가 결함을 잡는가"만 검증했고 "그 결함이 실재하는가"는 확인하지 않았다. action_logs에서 EXIT SIGNAL 사유 분포를 한 번 세는 것으로 30초에 판명될 일이었다. 앞으로 매매 동작을 바꾸는 판단에는 정적 측정만으로 착수하지 말고 반드시 운영 로그나 실제 채점으로 결함의 실재를 먼저 확인한다. 새 지표는 그 교훈을 코드로 고정한 것이며, 같은 오판을 재현했다면 위험 조합 0종으로 즉시 불필요함을 알려줬을 검사다

