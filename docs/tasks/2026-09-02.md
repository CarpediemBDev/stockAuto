# 2026-09-02 Task Board

## 신규 작업
- [x] 텔레그램 매매/매도 알림 전량 유실 복구 (fire-and-forget 발송 태스크가 asyncio.run 종료에 취소되던 문제) <!-- APPROVED: 2026-09-03 사용자 승인 — 근거는 본 현황판 인수인계 기록. 마커는 2026-09-06 세션이 소급 부착 -->
- [x] 체결 후속 태스크(잔고 스냅샷) 취소 방지용 사이클 드레인 도입 및 취소 예외 분류 교정 <!-- APPROVED: 2026-09-03 사용자 승인 — 근거는 본 현황판 인수인계 기록. 마커는 2026-09-06 세션이 소급 부착 -->
- [x] 백엔드·프론트 기동 상태 점검 및 최근 5일 수정분 회귀 확인 <!-- APPROVED: 2026-09-03 사용자 승인 — 근거는 본 현황판 인수인계 기록. 마커는 2026-09-06 세션이 소급 부착 -->
- [ ] 미체결 지정가 주문 만료 정책 부재 (최고 47일 경과분 496건 잔존) — 매매 규약 변경이라 승인 후 착수

### 변경 영향 기록
- 변경 분류: 결함 수정
- 생산자: app/core/telegram.py send_message_async, app/bot/scheduler.py _run_cycle_with_drain·_log_task_result
- 소비자: scheduler 매수·매도 체결 알림, order_reconciler, order_discovery, 대시보드 잔고 스냅샷
- 데이터 경계: DB 스키마·API 응답 변경 없음. 발송 스레드는 기존 _get_alert_executor(모듈 레벨 ThreadPoolExecutor, max_workers=4)를 재사용하며 자체 SessionLocal로 chat_id를 조회한다
- API 계약: 변경 없음. send_message_async 반환값이 None에서 전송 Future로 바뀌었으나 기존 호출자 6곳은 모두 반환값을 쓰지 않는다
- 문서 반영: 해당 없음 (내부 전달 경로 결함 수정, 공개 계약 불변)
- 회귀 테스트: backend/tests/test_telegram_alert_delivery.py 4건 신규. 사이클이 발송 직후 끝나도 전달 보장, Future 대기 가능, 체결 후속 태스크 완주, 멎은 태스크는 상한 내 포기. 네거티브 컨트롤로 구 구현(loop.create_task)이 같은 시나리오에서 유실함을 확인. verify_harness 전체 통과 (pytest 454건, tsc, eslint, Playwright 8건)
- 미해결 위험: 알림 전량이 4워커 스레드 풀을 통과하므로 텔레그램 지연 시 대량 체결 사이클의 알림이 직렬 지연될 수 있다(유실은 아님). _alert_executor는 stop_telegram_bot에서 종료되지 않아 종료 시 잔여 큐만큼 대기할 수 있다(기존 dispatch_alert부터의 선행 조건). 드레인 상한 10초는 1분 주기 대비 여유가 있으나 후속 태스크가 상습적으로 멎으면 사이클이 밀릴 수 있다
- 인수인계: 2026-09-03 사용자 승인 완료. 재기동(05:36) 후 실측 검증 종료 - 실제 매도 알림 체인으로 chat 1469973957에 HTTP 200 전달 확인, 사용자 텔레그램 수신 확인. 대조군(구 loop.create_task 방식)으로 보낸 메시지는 도착하지 않아 유실 원인이 교차 확증됐다. 실전 체결 3건(User 38·87·14) 모두 체결 +0.28~2.4초에 잔고 스냅샷이 기록됐고 배경 태스크 실패 0건(직전 누적 339건). 커밋은 하지 않았다. 다음 시작 지점은 (a) 레짐 판정 히스테리시스 - 재기동 당일에도 05:31 BEARISH에서 05:49 BULLISH로 18분 만에 반전했다, (b) 미체결 지정가 만료 정책, (c) 시그널 붕괴 청산의 이익 포지션 한정 2회 확정 지연
