# 2026-07-17 Task Board

## 신규 작업
- [x] 이벤트 루프 점거 결함 수정 (Claude 세션, 07-16 QA 결함 1): POST /api/v1/backtest/run(bot/router_backtest.py)이 async 핸들러에서 동기 CPU 백테스트(sim.run)를 직접 실행 + 관리자 토너먼트 BackgroundTask(admin/backtest_runner.py run_dynamic_tournament_task)도 동일 패턴 → 실행 중 전체 API 무응답. 동기 연산을 워커 스레드로 격리해 루프 응답성 확보 — 사용자 지시로 커밋 af9fa90, PR #39 머지 완료(2026-07-17 22:18 KST, harness CI 통과)
- [x] 크로스 이벤트 루프 Semaphore 결함 수정 (Claude 세션, 07-16 QA 결함 2): 모듈 전역 kis_semaphore가 APScheduler 잡의 asyncio.run 별도 루프에서 재사용돼 RuntimeError("bound to a different event loop")로 유저 트레이딩 사이클 중단(재기동 후에도 394회 재발). 루프별 Semaphore 레지스트리로 교체 (backend/app/bot/scheduler.py) — 사용자 지시로 커밋 af9fa90, PR #39 머지 완료
- [x] scanner.get_sentiment_lock 루프 레지스트리 결함 수정 (Claude 세션, QA 결함 2 후속): backend/app/scanner/scanner.py _sentiment_locks가 id(loop) 키 dict로 관리돼 죽은 루프 항목이 무한 증식하고, GC된 루프의 id 재사용 시 다른 루프에 바인딩됐던 asyncio.Lock을 새 루프가 넘겨받아 "bound to a different event loop" 재발 가능. scheduler.get_kis_semaphore와 동일한 WeakKeyDictionary + threading.Lock 이중 검사 패턴으로 교체 — PR #39에 포함 머지 완료
- 인수인계(공통): 로컬 브랜치 claude/inspiring-gates-100503은 원격보다 뒤처짐(원격은 main 갱신 머지 커밋 93b50e2 포함). 워킹트리에는 타 세션 소유 미커밋 변경 잔존 — scheduler.py·router_account.py의 태깅 헝크는 PR #38로 이미 main에 머지돼 중복(해당 세션에서 정리 권장), SSE router·frontend 변경도 각 세션 몫. 로컬에서 기동 중인 백엔드는 수정 코드 반영본 — 후속(07-18): 이 마감 기록 자체가 stash@{0}에 휩쓸려 유실됐다가 stash 전수 점검으로 복원됨. 점검 결과 stash 내용물 중 미반영분은 frontend/next.config.ts의 allowedDevOrigins(["127.0.0.1"]) 1줄뿐(QA 세션 로컬 편의 설정, 필요 시 재적용)이며 나머지는 전부 main 반영 확인 후 stash drop으로 정리 완료
- [x] (실험 과제) 미 의회 매매 공시(PTR) 추종 팩터 백테스트 연구 — 07-18 수행 완료(QQQ 대비 알파 창출 실패로 폐기, 결과 상세 docs/tasks/2026-07-18.md): 유튜브 영상(호두의 AI 분석실, "미 하원의원 주식 거래 복제", 2026-07-17) 검토에서 도출한 연구 과제. 미 하원 사무처(House Clerk) Financial Disclosure 사이트의 STOCK Act 공시(PTR PDF, 거래 후 45일 내 공시·실제 평균 지연 ~35일)를 파싱해 "의원 순매수 상위 종목" 팩터를 만들고, 공시일 기준 진입으로 기존 백테스트 하네스에서 QQQ 벤치마크 대비 알파를 검증한다. 설계 유의점: (1) 진입 시점은 거래일이 아닌 공시일 — 지연 35일이 알파를 죽일 가능성이 큼, (2) 성과 좋은 의원 1명 골라내기(생존 편향) 금지 — 전체 의원·전체 공시 대상, (3) 기존 결론(2,590종 챔피언십, 능동 타이밍 알파 없음)과 별개의 정보 우위자 추종 시그널이므로 검증 실패 시 깔끔히 폐기. 단계 계획: 1단계 타당성 조사 — House Clerk에서 PTR PDF 샘플 확보, 파싱 난이도(양식 일관성·스캔본 비율·OCR 필요 여부)와 수집 범위(연도별 건수) 확인, 파싱 비용 과도 시 조기 중단 또는 기성 데이터셋(QuiverQuant 등) 우회 검토 / 2단계 수집·구조화 파이프라인 — 연도 인덱싱→PDF 다운로드·로컬 캐시→구조화 적재(의원·종목·거래일·공시일·금액 구간, 공시일 필드가 핵심) / 3단계 팩터 생성+백테스트 — "의원 순매수 상위 종목" 팩터를 공시일 진입 기준으로 기존 백테스트 하네스에서 QQQ 벤치마크 대비 알파 검증(전체 의원·전체 공시 대상) / 4단계 판정 — 유의미한 알파 시 스캐너 보조 지표 통합 후속 과제 등록, 아니면 결과 기록 후 폐기. 각 단계 결과에 따라 후속 단계는 재구체화 가능하며, 코드 착수 전 본 항목 [/]로 승격 후 진행

### 변경 영향 기록 (이벤트 루프 점거 수정 — QA 결함 1)
- 변경 분류: 백엔드 안정성 버그 수정 (블로킹 연산 워커 스레드 격리, API 계약 무변경)
- 생산자: backend/app/bot/router_backtest.py run_backtest 핸들러가 prepare_data+run을 _execute_backtest_blocking으로 묶어 asyncio.to_thread에서 실행(워커 스레드 전용 루프 asyncio.run 격리), backend/app/admin/backtest_runner.py run_dynamic_tournament_task를 async→동기 함수로 전환해 Starlette BackgroundTask가 스레드풀에서 실행하도록 변경(내부 코루틴은 asyncio.run으로 격리, 기존 906행 워밍업 경로와 동일 패턴)
- 소비자: POST /api/v1/backtest/run 호출 클라이언트(응답 형식·상태 코드 동일, 실행 중 다른 API가 더 이상 블로킹되지 않음), POST /api/v1/admin/backtest/tournament의 202/캐시/락 흐름 무변경, admin/router.py add_task 호출부는 동기 콜러블을 그대로 수용
- 데이터 경계: 변경 없음 (토너먼트 캐시 파일·락 파일 경로와 형식 동일, DB 미접촉)
- API 계약: 변경 없음 (요청·응답 스키마, 상태 코드, 에러 메시지 동일 — no-data 400 메시지 포함)
- 문서 반영: 영향 없음 - 외부 계약 무변경인 내부 실행 경로 격리로, 재발 방지 불변식은 코드 주석과 AST 가드 테스트로 집행
- 회귀 테스트: backend/tests/test_event_loop_isolation.py 신규 가드 2건(토너먼트 태스크 동기 유지 + run_backtest sim.run/prepare_data 직접 호출 금지 AST) 통과. python scripts/verify_harness.py 전체 통과(2026-07-17, pytest·lint·tsc·Playwright E2E 8건 포함)
- 미해결 위험: 워커 스레드도 GIL을 공유하므로 순수 파이썬 구간에서는 루프 지연이 완전히 0이 되지는 않음(기존 트레이딩 엔진과 동일 특성, pandas/numpy 네이티브 구간은 GIL 해제). 동시 다발 백테스트 요청 시 스레드풀 소진 가능성은 별도 과제(요청 큐/전용 프로세스풀 검토)
- 인수인계: 라이브 검증 완료(2026-07-17 17:10) — 수정 코드로 백엔드 재시작 후 실제 백테스트(QQQ+AAPL 3개월 1d, 62.6초 소요, 200 정상 리포트)가 도는 동안 health 프로브 20회 전부 4~28ms 즉답. 수정 전에는 실행 내내 전체 API 무응답이었음

### 변경 영향 기록 (크로스 루프 Semaphore 수정 — QA 결함 2)
- 변경 분류: 백엔드 안정성 버그 수정 (전역 asyncio 프리미티브의 루프별 발급 전환, 계약 무변경)
- 생산자: backend/app/bot/scheduler.py 전역 kis_semaphore(asyncio.Semaphore(15))를 제거하고 get_kis_semaphore()가 WeakKeyDictionary[루프→Semaphore]에서 루프별 인스턴스를 threading.Lock 이중 검사로 발급, safe_broker_call이 이를 사용. scanner.get_sentiment_lock의 per-loop 패턴과 동일 취지(단, id 재사용 충돌·누수 방지를 위해 weakref 사용)
- 소비자: safe_broker_call을 경유하는 모든 브로커 호출(트레이딩 사이클, 잔고 동기화, 주문 폴링) — 동일 루프 내 동시성 상한 15 유지, APScheduler 잡의 asyncio.run 단명 루프에서 RuntimeError 없이 동작
- 데이터 경계: 변경 없음 (DB·캐시 미접촉, 세마포어는 프로세스 메모리 내 객체)
- API 계약: 변경 없음 (외부 노출 없음. 모듈 심볼 kis_semaphore는 삭제됐으나 저장소 전수 검색 결과 외부 참조 0건)
- 문서 반영: 영향 없음 - 내부 동시성 프리미티브 수명 관리 수정으로, 불변식은 코드 주석과 회귀 테스트로 집행
- 회귀 테스트: backend/tests/test_event_loop_isolation.py 신규 2건(루프별 발급 불변식 + 경합 후 상이한 루프 연속 호출 생존) 통과. python scripts/verify_harness.py 전체 통과(2026-07-17, pytest·lint·tsc·Playwright E2E 8건 포함)
- 미해결 위험: 루프별 발급으로 '프로세스 전체 동시 15' 상한이 '루프당 15'로 완화됨 — 실제 경합은 단일 루프 내 gather에 몰리고 KIS rate limiter가 재차 방어하므로 실효 위험 낮음. scanner.py의 _sentiment_locks(id(loop) 키 dict)는 동일 버그 계열의 id 재사용 충돌·무한 증식 위험이 잠재해 별도 과제로 남김 → 당일 후속 작업으로 해소(아래 기록 참조)
- 인수인계: 라이브 검증 완료(2026-07-17 17:19) — 수정 코드로 재시작한 백엔드를 8분간 관찰(로그 532라인, 트레이딩 사이클 다수 통과), "bound to a different event loop" 0건·run_user_trading_flow Error 0건. 수정 전에는 약 3분당 1회꼴(20시간 394회) 발생했음. 장기 관찰은 다음 세션에서 이어서 확인 권장

### 변경 영향 기록 (scanner sentiment 락 루프 레지스트리 수정 — QA 결함 2 후속)
- 변경 분류: 백엔드 안정성 버그 수정 (per-loop 락 레지스트리의 수명 관리 전환, 계약 무변경)
- 생산자: backend/app/scanner/scanner.py _sentiment_locks를 id(loop) 키 일반 dict에서 WeakKeyDictionary[루프→Lock]로 교체하고, get_sentiment_lock()이 threading.Lock 이중 검사로 루프별 인스턴스를 발급(scheduler.get_kis_semaphore와 동일 패턴). 루프 없는 호출 시 일회용 Lock 반환하는 기존 폴백은 유지. scheduler.py의 관련 주석 1건을 현행화
- 소비자: check_market_sentiment()의 동시 호출 차단 — 동일 루프 내 직렬화 동작 동일, APScheduler 잡의 asyncio.run 단명 루프에서 id 재사용에 의한 "bound to a different event loop" RuntimeError 재발 경로 제거, 죽은 루프 항목은 GC 시 자동 회수되어 무한 증식 차단
- 데이터 경계: 변경 없음 (DB·캐시 미접촉, 락은 프로세스 메모리 내 객체. _sentiment_cache 구조 무변경)
- API 계약: 변경 없음 (외부 노출 없음. get_sentiment_lock 시그니처·반환 타입 동일, 저장소 전수 검색 결과 _sentiment_locks 외부 참조 0건)
- 문서 반영: 영향 없음 - 내부 동시성 프리미티브 수명 관리 수정으로, 불변식은 코드 주석과 회귀 테스트로 집행
- 회귀 테스트: backend/tests/test_event_loop_isolation.py에 신규 2건 추가(루프별 발급 불변식 + 죽은 루프 10개 종료 후 레지스트리 자동 회수 검증) — 파일 전체 6건 통과. python scripts/verify_harness.py 전체 통과(SUCCESS)
- 미해결 위험: WeakKeyDictionary 자동 회수는 CPython 참조 카운팅에 의존 — 루프 객체를 외부에서 강참조로 붙들면 항목이 그만큼 유지되나, 현 코드베이스에 그런 경로 없음. 루프 없는 폴백 경로는 매 호출 새 Lock을 반환하므로 상호 배제가 없다는 기존 특성 유지(테스트 환경 한정, 기존 동작과 동일)
- 인수인계: scanner.py·scheduler.py(주석 1건)·test_event_loop_isolation.py 수정, 하네스 SUCCESS. 실행 중 백엔드에는 재시작 시 반영 — QA 결함 1·2 수정과 함께 재기동하면 됨

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
