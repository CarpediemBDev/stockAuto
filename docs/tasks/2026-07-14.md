# 2026-07-14 Task Board

## SSE 실활성화 — view-trigger 제거 + 플래그 ON (구독 내부 refresh는 안전상 미채택)
- [/] 사용자 지시: 폴링→SSE 전환 마무리(실활성화). 당초 "SSE 구독=보는 중 → 구독 유지 중 주기 갱신(subscription-driven refresh)"을 SSE 스트림에 넣으려 했으나, 스트림 제너레이터 내부 asyncio.create_task+브로커 호출이 이 스택(Windows/asyncio/uvicorn)에서 스트림을 조기 종료시키는 불안정을 로컬 실서버 검증에서 확인 → 원인 불명확한 async 동작을 기본 on으로 main에 올리는 것은 부적절하다 판단, 미채택. 대신 (1) 폴링 기반 view-trigger(_should_trigger_refresh 등)와 단위테스트 제거, (2) 보는 유저 신선도는 기존 1분 스케줄러(admin_balance_cache_sync)+거래 이벤트 스냅샷을 기준으로 하고 SSE가 그 변경을 즉시 push, (3) 프론트 SSE_ENABLED 기본 opt-out(!= "false")로 ON. E2E는 결정성 위해 SSE off 고정. 과다 invalidation 방지: record_equity_snapshot은 유저 채널(balance/holdings)만, 관리자 랭킹은 1분 벌크 동기·이산 이벤트에서만 무효화.
- 실측 검증: verify_harness 전체 통과 목표 + 로컬 실서버 SSE 재검증(전송 전용 스트림 유지·bot 토글 push 도착 확인). 구독 내부 refresh 미채택으로 스트림 안정.

### 변경 영향 기록
- 변경 분류: 기능 활성화·리팩터 - 실시간 전송을 SSE로 실제 전환(폴링 기본 중단), 폴링 view-trigger 제거. 신선도 원천은 1분 스케줄러+거래 이벤트 유지(전송만 SSE)
- 생산자: backend/app/core/sse.py(notify_user_equity 유저채널 한정·notify_admin_users 분리), backend/app/bot/scheduler.py(record_equity_snapshot 발행 정리·1분 벌크 동기 후 admin 1회 무효화), backend/app/trades/router_account.py(view-trigger 전면 제거), frontend/lib/sse.ts(SSE_ENABLED 기본 on), frontend/playwright.config.ts(E2E는 SSE off), frontend/.env.*.example(플래그 문서화). SSE 스트림(app/sse/router.py)은 #27 전송 전용 버전 유지(구독 refresh 미도입)
- 소비자: 전 화면(플래그 on이면 폴링 대신 push 수신). 관리자 사용자 목록은 1분/이산 이벤트로 갱신
- 데이터 경계: account_equity_snapshots는 기존 스케줄러/거래 경로에서만 쓰기(구독 내부 쓰기 없음). Redis pub/sub 채널·DB 스키마 무변경
- API 계약: GET /api/v1/balance 응답 스키마 불변(BackgroundTasks 파라미터 제거는 내부 시그니처). GET /api/v1/events 불변
- 문서 반영: 영향 없음 - API 계약·스키마 불변(SSE_ENABLED 기본값 전환·view-trigger 제거는 내부 동작). 실시간 전송 플래그는 frontend/.env.*.example에만 문서화(docs/ 장기 문서 대상 아님)
- 회귀 테스트: view-trigger 단위테스트(test_balance_stale_revalidate.py) 삭제(대상 로직 제거). test_sse_infra.py 유지. E2E는 SSE off로 기존 플로우 결정적 검증. verify_harness 전체 통과 + 로컬 실서버 스트림/토글 push 재검증
- 미해결 위험: 플래그 기본 on이라 SSE 연결 실패 시 자동 폴백 폴링 없음(EventStreamClient 자동 재연결+SWR revalidateOnFocus/Reconnect가 자가복구, X-Accel-Buffering:no로 nginx 버퍼링 대응). view-trigger 제거로 보는 유저 신선도가 10~45초→최대 1분(스케줄러 주기)으로 완화(사용자 "체감 미미" 확인). 초 단위 신선도가 필요해지면 구독 라이프사이클 기반 갱신을 스트림과 분리된 백그라운드 작업으로 재설계 필요(스트림 내부 create_task는 불안정으로 미채택)
- 인수인계: 구현·검증 후 [R]. 롤백은 NEXT_PUBLIC_SSE_ENABLED=false(폴링 복귀, 신선도는 1분 스케줄러 기준). Redis는 필수 인프라. subscription-driven refresh는 후속 과제로 남김(스트림 밖 스케줄러 잡 방식 권장)

## SSE 검증 후속 수정 — 발행 경로 루프 안전화 + trades push 복원 + 신선도 완충
- [x] 사용자 지시: 세션 검증에서 발견된 5건 수정 + 실서버 검증 중 발견한 6번째 결함(프록시 SSE 버퍼링) 수정. (사용자 "다 완료되면 문서에 완료처리" 지시에 따라 실서버·브라우저 검증 완료 후 완료 처리) (1) `await sse.publish`(async 클라이언트)가 APScheduler 잡의 `asyncio.run` 일회용 루프와 uvicorn 메인 루프에 걸쳐 싱글톤 커넥션 풀을 공유 → 닫힌 루프에 바인딩된 커넥션 재사용으로 발행이 조용히 실패(경고 로그만)할 수 있는 구조. 발행자 5곳(scanner/market/swing/after-hours/admin)을 전부 `publish_sync`(동기 클라이언트, 루프 무관·스레드 안전)로 통일하고 async `publish()` 제거(재발 방지). async 클라이언트는 스트림 구독 전용으로 한정. (2) EVENT_TRADES 발행자 부재 복원: `notify_user_equity(trade_event=)` 재도입, 체결·리셋(force=True) 스냅샷 시 거래 로그 invalidate 발행. (3) 낡은 주석 2곳 정정(router_account.py의 존재하지 않는 구독-주기-refresh 참조, EventStreamProvider.tsx의 "기본 off" 서술) + `notify_admin_users()` 데드 코드를 admin 벌크 동기에서 실사용. (4) 스냅샷 dedup 60s가 1분 잡 주기와 경합해 push 공백이 최대 120s로 벌어지고 90s stale 배지가 정상 상태에서 깜빡이는 문제 → dedup 임계 55s로 완충. (5) SSE on 경로 자동 검증 공백 보강: fake Redis/pubsub 기반 발행→프레임 왕복 테스트 추가(test_sse_infra.py 확장). (6) 로컬 실서버+브라우저 검증에서 발견: Next.js rewrite 프록시(/api/v1→백엔드, 전 환경 공통 경로)가 SSE 응답을 압축 버퍼에 잡아 connected 프레임조차 클라이언트에 미도달(= SSE가 프록시 뒤에서 사실상 무동작) → Cache-Control에 no-transform 추가(app/sse/router.py)로 즉시 통과 확인. 직접(:8000) 스트림은 정상이었으므로 기존 "로컬 push 도착 확인"은 직접 연결 기준이었던 것으로 추정.

### 변경 영향 기록
- 변경 분류: 버그 수정·테스트 보강 - SSE 발행 경로의 이벤트 루프 교차 사용 제거, 거래 로그 push 복원, 신선도 dedup 완충, 주석 정정
- 생산자: backend/app/core/sse.py(async publish 제거·notify_user_equity trade_event 재도입), backend/app/bot/scheduler.py(publish_sync 전환·notify_admin_users 사용·dedup 55s), backend/app/scanner/after_hours_scanner.py·swing_prediction_cache.py, backend/app/trades/market_overview_cache.py(publish_sync 전환), backend/app/sse/router.py(Cache-Control no-transform — 프록시 압축 버퍼링 차단), backend/app/trades/router_account.py(주석 정정), frontend/providers/EventStreamProvider.tsx(주석 정정)
- 소비자: SSE 구독 전 화면(공용 push 안정화 수혜), Dashboard 거래 로그(/trades push 복원), AccountBalance·UserManagement stale 배지(깜빡임 완화)
- 데이터 경계: Redis pub/sub 채널명·이벤트명·DB 스키마 무변경. account_equity_snapshots 기록 주기만 60s→55s 임계로 미세 완화(쓰기 빈도 상한 동일: 1분 잡 주기)
- API 계약: 불변(GET /api/v1/events 프레임 포맷·이벤트명 동일). backend 내부 함수 시그니처만 변경(sse.publish 제거, notify_user_equity에 trade_event 복원)
- 문서 반영: 영향 없음 - 내부 동작 수정(계약 문서 무변경)
- 회귀 테스트: test_sse_infra.py 확장(발행 헬퍼→fake Redis 채널/이벤트 검증, fake pubsub→SSE 프레임 왕복, async publish 제거 확인). verify_harness 전체 통과 목표
- 미해결 위험: publish_sync는 async 함수 안에서 이벤트 루프를 잠깐 블로킹(로컬 Redis publish는 sub-ms, 기존 코드도 동일 패턴로 동기 DB 호출 수행 중이라 일관). 실 Redis 왕복(네트워크 계층)은 여전히 수동 검증 영역. 신선도 근본 재설계(구독 라이프사이클 기반)는 별도 후속 과제 유지
- 인수인계: 구현 완료·verify_harness 전체 통과 + 로컬 실서버/실브라우저 E2E 검증 완료: (a) 직접(:8000) 스트림에서 connected→market→balance/holdings push 도착, (b) no-transform 수정 후 프록시(:3000) 경유 스트림도 프레임 실시간 도달, (c) 로그인된 실화면이 외부 봇 토글 push 2회에 /bot/status 재조회 정확히 2회 수행, 서버 에러 로그·브라우저 콘솔 에러 0. test_sse_infra.py 15개 통과(발행 헬퍼 계약·스트림 왕복 신규 — 스트림 테스트는 TestClient 강제 close 행업으로 fake pubsub 소진 시 is_disconnected=True 자연 종료 방식). 참고: 알렘빅 마이그레이션 fileConfig가 서버 기동 후 파일 로깅을 죽이는 별개 문제 발견(후속 과제). 배포 후 "[SSE] publish" 경고 부재 확인 권장
