# 2026-07-21 Task Board

## 작업 브랜치
- `fix/telegram-bot-linking-order`

## 신규 작업
- [x] 관리자 사용자 관리 및 실시간 아레나 테이블 레이아웃 최적화 (가로 스크롤 제거, 패딩 조정, 폰트 크기 유지)
- [x] DB 내 정지된 봇 실행 상태(UserSettings.is_running)를 일괄 True로 변경
- [x] 텔레그램 봇 연동 로직 개선 (chat_id 우선 검사 -> start username 딥링크 식별 우선 처리 및 기존 chat_id 자동 이전)
- [R] PR #59 하네스 반려 해소 — holdings.rolling_stop_price 모델↔마이그레이션 드리프트 복구: 커밋 1c1de9c(i18n 소비자 코드 병합)가 타 세션의 롤링 박스 작업물인 models.py 컬럼 정의와 docs/SCHEMA.md 명세를 마이그레이션 없이 함께 커밋해 alembic check가 반려된 상태였음. 누락된 마이그레이션 1건만 보충해 드리프트 해소

### 변경 영향 기록 (rolling_stop_price 마이그레이션 누락분 보충)
- 변경 분류: 백엔드 스키마 마이그레이션 누락분 보충 (기존 커밋의 모델 정의에 대응하는 DDL만 추가, 로직 변경 없음)
- 생산자: backend/alembic/versions/78f9aaecb6fc_add_rolling_stop_price_column_to_.py 신규 1건 (revision 78f9aaecb6fc, down_revision 57fac21b1d0f = 본 브랜치 alembic 헤드). 폐기 대상이던 구 리비전 238568cb20f4(down_revision 7888c60cd7a4)는 채택하지 않음 — 7888c60cd7a4의 자식이 둘이 되어 alembic multiple heads를 유발하므로 워킹 트리에만 남기고 커밋에서 제외
- 소비자: alembic upgrade 경로 전반(부트스트랩·CI 하네스 5단계 alembic check). 런타임 코드 소비자는 없음 — 컬럼을 읽고 쓰는 매도 엔진 로직은 본 브랜치에 미포함이며 별도 작업으로 복원 예정
- 데이터 경계: holdings.rolling_stop_price (NUMERIC(20,4), nullable). 기존 행은 NULL = 미시드 상태로 무영향. upgrade/downgrade 양방향에 컬럼 존재 여부 가드를 내장해, 세션 충돌 복구 과정에서 컬럼이 선행 생성된 DB(backend/stockauto.db)에서도 중복 add_column/drop_column 실패가 나지 않음
- API 계약: 변경 없음 - 신규 컬럼은 어떤 응답 스키마에도 노출되지 않음
- 문서 반영: 영향 없음 - docs/SCHEMA.md의 holdings.rolling_stop_price ERD·컬럼 명세가 선행 커밋 1c1de9c에 이미 포함되어 이번 보충분에서 갱신할 장기 문서가 없음
- 회귀 테스트: backend/venv/Scripts/python -m alembic heads 단일 헤드(78f9aaecb6fc) 확인, alembic check "No new upgrade operations detected" 통과(반려 원인 직접 해소 검증), python scripts/verify_harness.py 8단계 전체 통과
- 미해결 위험: 컬럼만 선반영되고 이를 사용하는 롤링 박스 트레일링 스탑 본체(trade_calculations.py의 compute_rolling_box_stop·check_rolling_box_breach, scanner.py의 calculate_rolling_box_low, scheduler.py·backtest_engine.py·backtest_runner.py 소비 경로)는 저장소 어디에도 없고 stash@{0}에만 잔존 — 휴면 컬럼 상태로 main에 들어감. 기능 자체가 전 전략 기본 OFF 설계라 런타임 영향은 없으나, 복원 전까지 backend/tests/test_rolling_box_stop.py는 import 실패로 커밋 불가
- 인수인계: 본 커밋으로 PR #59의 유일한 반려 사유(alembic check)가 해소되어 i18n 6커밋과 텔레그램 수정분이 함께 main에 병합 가능. 후속 과제 3건은 워킹 트리 미커밋 상태로 대기 — ①stash@{0}에서 롤링 박스 본체 복원 후 test_rolling_box_stop.py 동반 커밋 ②equity_gate.py·watchlist_relay.py 휴면 모듈 2종과 테스트 46건 ③docs/tasks/2026-07-20.md 파일 손상 복구(5행 문장 중도 절단, 40~53행이 15~28행 중복). backend/fix_db.py는 alembic_version을 DROP하는 임시 땜빵 스크립트로 커밋 금지

### 변경 영향 기록
- 변경 분류: 텔레그램 연동 로직 수정 및 운영 데이터 갱신
- 생산자: backend/app/core/telegram.py, scripts/enable_all_bots.py
- 소비자: 텔레그램 봇 연동 유저 (/start 명령어), 백엔드 트레이딩 스케줄러
- 데이터 경계: user_settings.telegram_chat_id, user_settings.telegram_enabled, user_settings.is_running
- API 계약: N/A - API 변경 없음
- 문서 반영: 영향 없음 - UI 여백 조정 및 운영 DB 갱신 작업으로 기술 문서 반영 불필요
- 회귀 테스트: python -m py_compile backend/app/core/telegram.py & backend/venv/Scripts/python -m pytest backend/tests/
- 미해결 위험: 없음
- 인수인계: telegram.py _process_global_message에서 /start username 딥링크 식별을 최우선 처리하고 기존 chat_id 소유권 자동 해제/이전 조치 완료 및 is_running 일괄 가동 처리 완료






