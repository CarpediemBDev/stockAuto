# 2026-06-23 일자별 현황판

## [Fix] 스캐너 멀티테넌시 소비 경로 완결 ([GitHub #8](https://github.com/CarpediemBDev/stockAuto/issues/8), [Linear STO-8](https://linear.app/stockauto/issue/STO-8/스캐너-멀티테넌시-경계-안정화))

### 목표
- 공용 시장 신호와 사용자별 관심종목 소유권을 `/scanner/latest`, 자동매매, 계좌 레이더에서 동일한 사용자 컨텍스트로 재결합한다.
- 스윙 예측을 공용 시장 기능으로 확정하고 백엔드 인증 의존성, 프론트 안내, 장기 문서를 실제 계약과 일치시킨다.
- 서로 다른 두 사용자의 관심종목이 API 응답과 계좌 레이더에서 격리되는 소비자 회귀 테스트로 GitHub #8 완료 조건을 닫는다.

### 작업 목록
- `[x]` `/scanner/latest` 2사용자 관심종목 격리·재결합 API 테스트 추가
- `[x]` `/account/balance`에 현재 사용자의 결합 신호 전달 및 2사용자 레이더 테스트 추가
- `[x]` 스윙 예측 공용 계약 확정, 불필요한 사용자·DB 의존성 및 개인화 UI 문구 제거
- `[x]` 스캐너 데이터 흐름·API·시스템 문서 현행화
- `[x]` 집중 회귀·전체 하네스 검증 및 GitHub #8 완료 근거 반영

### 변경 영향 기록
- 변경 분류: `데이터 흐름`, `API 계약`, `인증·권한`, `공용 캐시`, `프론트 소비자`
- 생산자: `backend/app/bot/scheduler.py`의 `build_user_signal_context()`, `backend/app/scanner/swing_prediction_cache.py`의 글로벌 스윙 스냅샷
- 소비자: `GET /api/v1/scanner/latest`, `GET /api/v1/account/balance`, `GET/POST /api/v1/scanner/swing-predict`, `frontend/components/SwingPredictorCard.tsx`, 자동매매 사이클
- 데이터 경계: 시장 분석값은 티커 단위 공용 사실로 유지하고 `WatchList.user_id` 소유권은 사용자 API·자동매매·계좌 레이더 컨텍스트 생성 시에만 결합하며 스윙 후보는 공용으로 고정
- API 계약: `/scanner/latest`와 `/account/balance`는 인증 사용자의 관심종목만 포함하고, 스윙 조회·갱신은 사용자 데이터와 무관한 공용 캐시 API로 제공
- 문서 반영: `docs/SCANNER_DATA_FLOW.md`, `docs/API_STANDARD.md`, `docs/SYSTEM_MANUAL.md`, 필요 시 `docs/FILTER.md`
- 회귀 테스트: 사용자 A의 `AAPL`, 사용자 B의 `MSFT`가 `/scanner/latest`와 계좌 레이더에서 서로 격리되고 삭제 후 제거되는 API 시나리오, 공용 스윙 인증 계약 테스트
- 미해결 위험: 실제 소비자 인터페이스와 운영 데이터 결합 경로를 API 회귀 테스트로 검증 완료했으며 재발 시 신규 GitHub Issue로 추적
- 인수인계: `backend/app/scanner/router.py`, `backend/app/trades/router_account.py`, `backend/tests/test_new_user_core_flow.py`부터 확인

### 구현 결과
- `/scanner/latest`, 자동매매, `/account/balance` 레이더가 모두 `build_user_signal_context()`의 공용 분석값·사용자 소유권 재결합 계약을 사용한다.
- 계좌 레이더는 현재 사용자의 관심종목만 `WATCHLIST`로 강제 포함하며 다른 사용자의 관심종목 분석 캐시는 노출하지 않는다.
- 스윙 예측은 인증된 사용자가 공유하는 공용 시장 기능으로 확정하고 응답에 `scope: global`을 추가했다. DB 세션은 공용 스냅샷 복구, 사용자 의존성은 접근 제어에만 사용한다.
- 프론트엔드에서 관심종목 기반이라는 잘못된 문구를 제거하고 공용 시장 주도주 풀 및 `GLOBAL MARKET` 범위를 표시한다.
- 사용자 A의 `AAPL`, 사용자 B의 `MSFT`를 등록해 `/scanner/latest`와 계좌 레이더의 상호 격리, 관심종목 삭제 후 제거, 두 사용자의 동일 공용 스윙 응답을 API 수준에서 검증했다.

### 검증 기록
- 수정 백엔드 집중 회귀: 13 passed
- 백엔드 전체 테스트: 160 passed
- 백엔드 Python 전체 210개 `py_compile`: 통과
- Alembic `upgrade head` 및 `check`: 통과, 추가 스키마 차이 0건
- 프론트엔드 `npx tsc --noEmit`, `npm run lint`: 통과
- Playwright E2E: 8 passed
- Markdown/Python/TypeScript/TSX 312개 UTF-8 NFC 검사: 통과
- 변경 영향 기록 직접 검증: 민감 파일, 당일 현황판, 장기 문서 반영 조건 충족

### 미해결 위험 및 인수인계
- GitHub #8의 코드·테스트·문서 완료 조건은 모두 충족됐다. 이후 실제 사용자 간 데이터 노출이 재현되면 새 GitHub Issue로 회귀를 추적한다.
- 하네스는 Git 동결 규칙 때문에 Git 조회를 수행하는 진입점 자체를 실행하지 않고, 7단계의 변경 계약·컴파일·NFC·Alembic·전체 pytest·프론트 정적 검사·E2E를 각각 동일 조건으로 실행했다.
- 사용자 승인(2026-06-23) 후 GitHub #8을 `completed`, Linear STO-8을 `Done`으로 완료 처리했다.

## [Task] 프론트엔드 개발 서버 즉시 종료 수정

### 목표
- `start_dev.bat` 실행 시 프론트엔드 창이 즉시 닫히는 실패 경로를 수정하고 백엔드·프론트엔드 포트가 실제로 열린 뒤에만 성공으로 보고

### 작업 목록
- `[x]` 프론트엔드 `npm.cmd` 호출을 Windows 배치 제어 흐름에 맞게 수정
- `[x]` 백엔드 8000·프론트엔드 3000 포스트 스타트 상태 검증 추가
- `[x]` 실패 시 프론트엔드 창을 유지하고 회귀 검증 수행

### 변경 영향 기록
- 변경 분류: `운영 절차`, `로컬 개발 기동 흐름`
- 생산자: 루트 `start_dev.bat`의 프론트엔드 프로세스 호출 및 서버 준비 상태 판정
- 소비자: Windows 개발자, `frontend/package.json`의 `local` 스크립트, `backend/run.py local`
- 데이터 경계: 사용자 데이터와 API 데이터는 변경하지 않으며 로컬 `cmd.exe` 프로세스와 8000·3000 포트 준비 상태만 확인
- API 계약: HTTP 요청·응답은 변경하지 않고 백엔드 8000과 프론트엔드 3000이 실제 수신 가능한 상태를 기동 성공 조건으로 강화
- 문서 반영: `README.md`의 기존 원클릭 실행 설명과 동일한 동작을 복구하므로 별도 장기 문서 변경 없음
- 회귀 테스트: 프론트엔드 직접 실행, `start_dev.bat --check`, 배치 호출 실패 시 창 유지, 서버 준비 상태 판정 검증
- 미해결 위험: 실제 통합 기동 검증 중 로컬 개발 서버 프로세스를 일시적으로 실행하고 검증 후 종료해야 함
- 인수인계: `start_dev.bat`의 `start` 명령과 포스트 스타트 대기 루프부터 확인

### 원인 및 수정 결과
- 원인 1: 3000 포트 미사용 검사에서 남은 `ERRORLEVEL=1`을 `start` 성공 직후 다시 검사해 백엔드만 연 상태에서 프론트엔드 실행 전에 실패 처리함
- 원인 2: `npm.cmd`를 `call` 없이 실행해 npm 실패 시 프론트엔드 터미널이 닫히고 오류 확인이 불가능했음
- 수정: `start` 반환값 대신 실제 포트 준비 상태로 성공을 판정하고, `call npm.cmd run local`로 프론트엔드 셸을 유지함
- 수정: 한쪽 StockAuto 서버가 이미 실행 중이면 해당 서버를 재사용하고 꺼진 서버만 시작하도록 변경함
- 수정: 표준 입력이 없는 환경에서 실패하는 `timeout`을 PowerShell `Start-Sleep`으로 교체함

### 검증 기록
- 서버가 모두 꺼진 상태에서 `start_dev.bat` 실행: 종료 코드 `0`, 8000·3000 포트 기동 확인
- 백엔드만 실행 중인 상태에서 `start_dev.bat` 재실행: 종료 코드 `0`, 프론트엔드만 복구 확인
- `http://127.0.0.1:8000/`, `http://127.0.0.1:3000/`: HTTP 200 및 `StockAuto` 응답 확인
- 프로젝트 루트와 상위 디렉터리에서 `start_dev.bat --check`: 모두 통과
- 배치 ASCII 및 `call npm.cmd run local`, 구식 `concurrently`·`npm run dev` 제거 정적 검사: 통과
- `scripts/verify_harness.py`는 내부 Git 조회 명령이 Git 동결 규칙과 충돌하므로 미실행

### 미해결 위험 및 인수인계
- Next.js 로그에 느린 파일시스템 경고가 있으며 콜드 스타트의 첫 페이지 컴파일이 길어질 수 있다. 프로세스와 3000 포트는 정상 유지되고 이후 HTTP 응답은 통과했다.
- 검증 후 백엔드와 프론트엔드는 사용자 사용을 위해 실행 상태로 유지했다.
- 사용자 승인(2026-06-23) 완료로 본 작업을 `[x]` 처리했다.

## [Task] 과거 승인 대기 항목 완료 상태 감사

### 목표
- 과거 일자별 현황판의 `[R]` 항목 중 구현·검증 완료 근거가 유지되는 작업을 사용자 일괄 승인에 따라 `[x]`로 전환
- 연결된 GitHub Issue와 Linear 이슈는 현재 상태를 확인해 완료 가능한 항목만 동기화

### 작업 목록
- `[x]` 전체 `docs/tasks/`의 `[R]` 항목과 후속 작업·재오픈 여부 감사
- `[x]` 완료 근거가 유효한 로컬 현황판 항목을 `[x]`로 전환
- `[x]` 연결된 Linear 이슈를 확인하고 완료 가능한 항목을 `Done`으로 동기화
- `[x]` 재진행·후속 작업으로 대체된 항목은 완료 처리에서 제외하고 사유 기록

### 변경 영향 기록
- 변경 분류: `작업 상태 감사`, `사용자 승인 반영`, `외부 백로그 동기화`
- 생산자: 과거 `docs/tasks/YYYY-MM-DD.md`의 `[R]` 작업 및 검증 기록
- 소비자: 다음 AI 작업자, StockAuto Linear 프로젝트, 연결된 GitHub Issue
- 데이터 경계: 애플리케이션·사용자 데이터는 변경하지 않고 문서 상태와 외부 작업 추적 상태만 갱신
- API 계약: HTTP API와 런타임 계약은 변경하지 않으며 기존 완료·검증 기록의 승인 상태만 반영
- 문서 반영: `[R]`이 존재하는 과거 일자별 현황판과 본 감사 결과를 `docs/tasks/2026-06-23.md`에 기록
- 회귀 테스트: 변경 문서의 잔여 `[R]` 검색, NFC 검사, 연결 이슈 상태 재조회
- 미해결 위험: 후속 작업에서 결함이 재발견된 항목은 과거 완료와 별개로 현재 진행 상태를 유지해야 함
- 인수인계: 전체 `[R]` 검색 결과와 GitHub #8, Linear STO-9·STO-10·STO-14 상태부터 확인

### 감사 결과
- 구현·검증 근거가 있는 과거 승인 대기 항목을 `[x]`로 전환했다.
- Linear STO-5는 기존 `Done`을 확인했고, STO-8·STO-9·STO-10을 사용자 승인에 따라 `Done`으로 동기화했다.
- GitHub #8은 두 사용자 격리·계좌 레이더·스윙 공용 계약·문서·전체 검증 완료 근거를 남기고 `completed`로 닫았다.
- Linear STO-14는 1~5월 통합 토너먼트와 최종 보고서가 남아 `Backlog`를 유지한다.
- Linear STO-16은 고급 KPI·수익/드로다운 시각화와 통합 검증이 남아 `Backlog`를 유지한다.
- 2026-06-21의 불완전한 `/scanner/latest` 재결합 기록은 `[-]`로 전환하고 2026-06-23 GitHub #8 완료 작업으로 대체했다.

### 승인 기록
- 사용자 일괄 승인: 2026-06-23
- 본 감사 작업과 완료 근거가 유효한 과거 작업을 `[x]`로 처리했다.

### 검증 기록
- 전체 `docs/tasks/` 상태 마커 검색: 승인 대기 `[R]` 0건
- 실제 미완료 `[ ]`은 Linear STO-13·STO-14·STO-15·STO-16 연결 범위만 유지
- Linear 재조회: STO-5·STO-8·STO-9·STO-10 `Done`, STO-14·STO-16 `Backlog`
- GitHub #8 재조회: `closed`, 사유 `completed`
- 전체 `docs/tasks/*.md` UTF-8 NFC 검사: 통과
- 코드 변경 없는 상태 감사이므로 Git 조회를 내부 수행하는 전체 하네스는 Git 동결 규칙에 따라 미실행
