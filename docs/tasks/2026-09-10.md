# 2026-09-10 Task Board

## 신규 작업
- [R] ATR 시간 프레임 불일치 실증 — 5분봉 ATR로 산출한 손절·트레일링·수확 임계가 사실상 상시 하한(3.0/2.0/5.0/15.0)에 눌려 동적 조정이 죽어 있다. 일봉 ATR 대안을 백테스트로 비교한 뒤에만 교체 여부를 판단한다

#### 실행 로그
- 2026-09-09 20:10 UTC (16:10 ET) 예약 세션: 전체 표본 백테스트 시도 → **미실행**. `get_market_session()`이 `AFTER_HOURS` 반환(미국장 애프터마켓 16:00~20:00 ET). 작업 지시가 "CLOSED가 아니면 실행 금지"(yfinance 쿼터를 라이브 스캐너와 공유)이므로 `evaluate_atr_timeframe.py`를 돌리지 않았다.
- 다음 실행 가능 시점: 2026-09-10 00:00 UTC(20:00 ET) 이후 세션이 `CLOSED`로 바뀐 뒤. 그때 `cd backend && venv/Scripts/python.exe -X utf8 scripts/evaluate_atr_timeframe.py` (종목 1,181개 / 50종목 24청크, 10~20분, 타임아웃 600000ms).
- 판독 기준(재확인): 개선분(B-A) **중앙값**. 250건 1차 결과는 중앙값 -1.42% / 평균 +3.20% / 개선 비율 22.2%로 이미 "중앙값 악화, 평균은 아웃라이어 지배" 형태. 전체 표본도 중앙값이 0 근처·음수면 교체 실익 없음.
- 2026-09-13 전체 표본 실행 완료 (2026-09-12 토 18:16 UTC, `get_market_session()` = `CLOSED` 확인 후). 명령: `cd backend && venv/Scripts/python.exe -X utf8 scripts/evaluate_atr_timeframe.py` (기본 `--after` = 실행일-90일 = 2026-06-15). 진입 2,704건 / 고유 1,232종목 → 일봉 확보 1,228종목 → 유효 표본 2,559건 / 1,184종목.
  - 일봉 ATR% 중앙값 4.34% (25% 3.27 / 75% 5.98). 일봉 기준 손절이 하한 3.0%를 넘는 비율 92.4% (5분봉에서는 사실상 0%).
  - A 현행(고정): 중앙값 -3.00% / 평균 +2.19% / 승률 34.1%
  - B 일봉ATR: 중앙값 -1.39% / 평균 +2.67% / 승률 37.9%
  - 개선분(B-A): **중앙값 -0.98%** / 평균 +0.48% / 개선 비율 27.1%. 두 정책 동일 결과 10.0%.
- 결론: **일봉 ATR로 교체하지 않는다.** 사전 판독 기준(개선분 중앙값)이 음수이고, 1차 250건(-1.42%)과 방향이 같다. 진단(5분봉 ATR로는 동적 조정이 죽어 있음)은 사실로 확인됐지만, 조정을 살려도 거래별로는 62.9%가 악화된다. B의 중앙값이 A보다 높아 보이는 것은 판독 근거가 아니다 - 중앙값끼리의 차이와 거래별 차이의 중앙값은 다른 값이고, 절대 수익률은 급등주 표본으로 부풀려져 있다.

### 변경 영향 기록
- 변경 분류: 분석 도구 추가와 실증 결과 기록. 런타임 코드·API·DB 스키마·매매 로직 변경 없음. 교체하지 않기로 결론 났으므로 app/ 코드는 건드리지 않는다.
- 생산자: backend/scripts/evaluate_atr_timeframe.py(신규 - 수동 실행 전용 비교 스크립트). 판정 대상인 app/scanner/scanner.py의 calculate_atr(df_5m, period=14)와 base_strategy의 get_stop_loss_pct·get_trailing_stop_pct는 수정하지 않았다.
- 소비자: 없음. 스케줄러·API·프론트·테스트 어디서도 import 하지 않는다. 사람이 장 마감 후 명시적으로 실행하고 출력 표를 읽는 것이 유일한 사용 경로다.
- 데이터 경계: backend/stockauto.db의 trade_logs를 SELECT로만 읽는다(BUY, price > 0, 티커·일자별 피라미딩 접기). 쓰기 없음. 외부 호출은 yfinance 일봉 다운로드뿐이며 라이브 스캐너와 쿼터를 공유하므로 장 세션이 CLOSED일 때만 실행한다.
- API 계약: 변경 없음.
- 문서 반영: 영향 없음 - 코드 동작이 바뀌지 않았으므로 계약·스키마·전략 문서에 고칠 문장이 없다. 실행 조건과 판독법은 스크립트 모듈 독스트링이 단일 기준이고, 결과와 결론은 이 현황판 실행 로그에 남겼다.
- 회귀 테스트: 런타임 변경이 없어 신규 테스트는 없다. 스크립트는 2026-09-13 전체 표본(2,559건)으로 끝까지 실행돼 exit 0을 확인했다. 커밋은 pre-commit 훅의 python scripts/verify_harness.py 전체 통과(백엔드 컴파일·NFC 포함)를 거쳐 생성됐다 - 훅이 실패하면 커밋 자체가 만들어지지 않는다.
- 미해결 위험: 첫째, 판독 기준인 중앙값은 넓은 손절에 구조적으로 불리하다(작은 추가 손실 다수 + 드문 큰 수익). 평균 +0.48%가 밈주 아웃라이어 효과인지 가르려면 상위 1~5% 제외 평균이나 부트스트랩 신뢰구간이 필요한데 스크립트가 계산하지 않는다. 결과를 본 뒤 기준을 바꾸면 사후 끼워맞추기이므로 결론은 사전 기준대로 두고 위험으로만 남긴다. 둘째, A는 실측이 아니라 하한 고정 근사다. 근거 측정은 10종목×5일인데 이번 표본은 일봉 ATR 중앙값 4.34%의 고변동 소형주 위주라, 5분봉 ATR이 하한을 넘었을 수 있는 종목이 섞여 있다. 셋째, simulate()의 같은 봉 손절 우선 처리는 스크립트 주석처럼 대칭이 아니다. 진입가 위에서 고점이 형성된 뒤에는 트레일링가가 항상 손절가보다 위에 있으므로 갭하락이 아니면 실제로는 트레일링이 먼저 걸리는데 코드는 손절로 청산한다. 이 과소평가는 빈도는 A(좁은 밴드)가 높고 건당 크기는 B(손절-트레일 간격이 넓음)가 커서 순방향이 정량되지 않았다. 넷째, 일봉 시뮬레이션은 봇의 장중 5분봉 트레일링을 재현하지 못하고, 30거래일 보유 상한이 B가 끌고 가는 수익을 자른다. 다섯째, 교체하지 않으므로 ATR 기반 동적 손절·트레일링·수확 임계는 실질적으로 상수 3.0/2.0/5.0/15.0으로 계속 동작한다 - 코드가 약속하는 동작과 실제 동작이 다른 상태가 남는다.
- 인수인계: 결론은 교체 안 함이다. 같은 질문을 다시 열려면 위 첫째(아웃라이어 제외 평균·신뢰구간)와 셋째(봉 내 청산 순서)를 먼저 보강하고, 판독 기준을 실행 전에 다시 고정할 것. 표본은 trade_logs가 늘어날수록 달라지므로 재실행 결과를 이 수치와 직접 비교하지 말고 --after를 2026-06-15로 고정해 대조한다. 다섯째(코드 약속과 실제 동작의 불일치)를 코드·문서를 상수 동작에 맞춰 정리할지는 사용자 결정 사항으로 남긴다.

## 포트폴리오 종목별 매수 이력 조회

- [x] 실시간 포트폴리오 카드에서 종목을 클릭하면 해당 티커의 매수·매도 이력(시각·체결가·수량·전략·실현손익)을 모달로 보여준다. `GET /api/v1/trades`에 `ticker` 필터를 추가하고, 프론트는 슬라이스(전략)별로 현재 포지션 보유 시작일을 계산해 표시한다. <!-- APPROVED: 2026-09-11 사용자 승인 (PR #121 머지 후) -->

### 변경 영향 기록
- 변경 분류: 기능 추가 (화면 표시 + 조회 API 파라미터). 스키마 변경 없음, 매매 로직 변경 없음.
- 생산자: backend/app/trades/router_trades.py(GET /trades에 ticker 쿼리 파라미터·normalize_ticker 신설), frontend/components/HoldingHistory.tsx(신규 - 종목별 체결 이력 뷰와 슬라이스별 보유 시작 계산), frontend/components/PortfolioView.tsx(티커 버튼·푸터 이력 버튼·이력 모달 배선), frontend/components/TradeLogs.tsx(TradeLog 타입에 strategy_type·signal_score·regime_mode 추가), locales/ko.json·locales/en.json(portfolio.history_* 19키 신설), backend/tests/test_trade_log_ticker_filter.py(신규 5건), frontend/e2e/holding-history.spec.ts(신규 1건)
- 소비자: GET /api/v1/trades(기존 소비자는 대시보드 전체 로그·리포트 페이지로 둘 다 ticker 없이 호출해 영향 없음), 프론트 포트폴리오 카드, 모바일 앱(stock-auto-mobile - 선택적 파라미터라 통지 없이도 호환되며, 종목별 이력 화면을 만들 때 쓸 수 있다)
- 데이터 경계: 읽기 전용이다. 테이블·컬럼 추가 없고 쓰기 경로를 건드리지 않는다. 조회는 종전대로 user_id로 먼저 좁힌 뒤 ticker를 걸어 사용자 격리가 필터 유무와 무관하게 유지된다. 티커 표기는 대문자화와 거래소 접두 제거로 정규화하되 저장 정본(접두 없는 대문자 심볼)은 바꾸지 않는다. 프런트는 한 번에 최근 500건까지만 받으며, 보유 시작 시각은 그 창 안에서만 계산되고 창에 닿으면 화면이 그 사실을 명시한다.
- API 계약: GET /api/v1/trades에 선택적 쿼리 파라미터 ticker가 추가된다. 필터는 DB에서 limit보다 먼저 적용되며 skip·limit은 필터된 결과 위에서 동작한다 - 전체를 받아 화면에서 거르는 방식과 결과가 다르고, 그 차이가 이 변경의 핵심이다(거래가 잦은 종목에 밀려 조용한 보유분의 매수 기록이 응답 창 밖으로 나가던 문제). 공백만 전달되면 필터를 걸지 않고 전체를 반환한다. 티커를 주지 않는 기존 호출의 동작은 완전히 동일하다.
- 문서 반영: docs/API_STANDARD.md 7절 "거래 원장 조회 계약" 신설(필터 적용 순서, 티커 정규화, 사용자 격리, 응답 항목, 빈 배열의 의미, 포지션 시작 시각을 서버가 계산하지 않는다는 계약).
- 회귀 테스트: backend/tests/test_trade_log_ticker_filter.py 5건 - 필터가 limit보다 먼저 걸릴 것(활발한 종목 150건에 밀린 매수 1건을 찾아내고, 필터 없이 부르면 그 행이 없음을 네거티브 대조로 확인), 사용자 격리 유지, 표기 정규화(AAPL·nas_aapl·NAS_AAPL·공백 포함), 티커 미지정 시 종전 계약 유지, skip/limit이 필터 위에서 동작할 것. 네거티브 컨트롤로 필터 한 줄을 무력화하자 5건 중 4건이 실패했고(남은 1건은 티커 미지정 계약이라 영향받지 않는 것이 정상) 되돌리니 전부 통과했다. frontend/e2e/holding-history.spec.ts 1건 - 클릭이 ticker=QLD를 실어 보내는지, 체결가·시각이 렌더되는지, 팔았다 다시 산 종목의 보유 시작이 첫 매수가 아니라 재진입 시점인지. python scripts/verify_harness.py 8/8 통과 - pytest 583건, tsc, eslint, Playwright 9건. 중간 2회는 다른 세션이 같은 체크아웃에서 하네스를 동시에 돌려 BLOCKED이었다(1회차 `.next-e2e` 빌드 락 충돌로 웹서버 기동 실패, 2회차 auth-smoke의 refresh 대기 30초 타임아웃). 그 세션 종료를 기다려 직렬화하자 동일 트리에서 전부 통과했다 - 코드 문제가 아니라 실행 경합이다.
- 미해결 위험: 첫째, 실행 중인 백엔드(PID 22256, 2026-09-10 02:47 기동)는 이 변경 이전 코드라 재기동 전까지 ticker 파라미터를 무시하고 전 종목 최신 100건을 돌려준다. FastAPI가 모르는 쿼리 파라미터를 조용히 버리므로 화면은 오류 없이 다른 종목 이력을 섞어 보여준다 - 이 상태가 가장 위험한 오독 경로다. run.py의 reload는 하드코딩 False이고 라이브 A/B 계정이 매매 중이라 재기동 시점은 사용자 판단이 필요하다. 둘째, 보유 시작 계산은 조회 창(500건) 안에서만 유효하다. 창을 넘긴 종목은 "표시 범위 밖"으로 표기되며 잘못된 날짜를 지어내지 않지만, 답을 주지도 못한다. 셋째, 위임(DELEGATED) 전환은 holdings의 strategy_type을 바꾸지만 과거 trade_logs 행의 strategy_type은 그대로다. 위임 전후 체결이 서로 다른 슬라이스로 갈라져 보이며, 이는 원장이 실제로 그렇게 기록된 것이라 화면이 합치지 않는 편이 정직하다. 넷째, 시각 표기가 UI 언어와 무관하게 ko-KR 형식으로 찍힌다 - 기존 TradeLogs와 동일한 동작을 따른 것이라 이 변경의 회귀는 아니지만 영어 UI에서는 어색하다.
- 인수인계: 필터가 DB에서 걸린다는 것이 계약이다. 성능이나 캐시를 이유로 전체를 받아 화면에서 거르는 구조로 되돌리면 조용한 보유분의 매수 기록이 다시 사라지고, 그 실패는 빈 화면이 아니라 "이력 없음"이라는 잘못된 사실로 나타나므로 눈에 띄지 않는다. backend/tests/test_trade_log_ticker_filter.py의 첫 케이스가 이것을 고정하고 있다. 다음 시작 지점은 백엔드 재기동 후 실제 화면 확인이며, 재기동 전에는 모달이 보여주는 이력을 신뢰하면 안 된다.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
