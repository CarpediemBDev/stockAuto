# 2026-08-16 Task Board

## 신규 작업
- [x] 미커밋 변경 전수 검토 및 안전 슬라이스 분리 원격 머지 (사용자 지시 "현재 미커밋된 내용 확인하고 올려도 되는지 확인해" → "문제가 없다면 원격머지까지 진행해"): 한 체크아웃에 5개 세션분 작업이 뒤섞인 상태를 슬라이스별로 감사해, 검증이 깨끗한 백엔드 2종(백테스트 자율 슬롯 / 인증 보안 강화)만 분리 커밋·PR·머지하고, 결함이 확인된 프론트 전략 UI 슬라이스는 워킹트리에 보류한다. <!-- APPROVED: 2026-08-16/유저승인("전부 진행") -->

- [x] 보류 슬라이스 3건 결함 수정 후 재푸시 (사용자 지시 "보류한 3건 수정하고 다시 올려"): (1) 프론트 하드코딩 한글을 실제 다국어화하고 test_i18n_messages ALLOWLIST 파일 전체 면제 4건을 제거해 가드를 원상 복구, (2) institutional_follow를 selection_eligible=False로 정정하고 reason을 프록시·음의 알파 실측으로 교체, (3) AIMarketRegimeWidget의 라이브 전환에 adminSettings 로드 가드와 명시적 확인 단계를 추가하고 카탈로그 조회를 useStrategyCatalog 훅으로 통합. <!-- APPROVED: 2026-08-16/유저승인("전부 진행") -->

- [x] 잔여 부채 일괄 정리 및 현황판 상태 승격 (사용자 지시 "전부 진행하고 전부 원격머지까지 진행해서 깔끔하게"): 무효 상태였던 pytest.mark.real_rate_limiter를 pytest.ini에 등록하고, starlette에서 폐기 예정인 per-request cookies= 사용을 client.cookies 설정+정리 방식으로 교체하며, 선행 고아 키(dashboard.strategy_catalog_title/desc)를 ko/en에서 제거한다. 아울러 08-01·08-05·08-13·08-14·08-15·08-16의 미승격 항목을 승인 상태로 정리한다. <!-- APPROVED: 2026-08-16/유저승인("전부 진행") -->

- [x] 고아 컴포넌트 및 저장 안내 문구 정리 (사용자 지시 "고아 컴포넌트랑 저장 안내 문구도 정리해서 올려"): Dashboard에서 제거돼 어디서도 import되지 않는 CurrentStrategyBadge.tsx를 삭제하고 그로 인해 미사용이 된 dashboard 네임스페이스 키 5개를 ko/en 양쪽에서 제거한다. 설정 화면의 strategy_save_hint 안내는 삭제하지 않고 복원하되, 카탈로그가 카드에서 드롭다운으로 바뀐 현행 UI에 맞게 문구를 갱신한다. <!-- APPROVED: 2026-08-16/유저승인("전부 진행") -->

- [x] 텔레그램 연동 계정 탈취 취약점 수정 (사용자 지시 "우리 프로젝트의 보안 취약점이 있는지 꼼꼼하게 살펴줘" → "텔레그램 연동 취약점부터 수정해줘"): 전역 단일 봇의 `/start <사용자명>` 딥링크가 소유권 증명 없이 임의의 chat_id를 임의 계정에 바인딩해, 사용자명만 알면 피해자의 포트폴리오 조회(/status)와 자동매매 기동·정지(/run,/stop)가 가능하던 High 등급 결함을 제거한다. 사용자명 기반 연동 경로를 완전히 폐기하고 서버가 발급하는 1회용·만료형 링크 토큰(해시 저장) 방식으로 교체하며, 수동 chat_id 입력이 타 계정에 이미 묶인 chat_id를 가로채지 못하도록 중복 바인딩도 함께 차단한다. <!-- APPROVED: 2026-08-30/유저승인("12건 전부 승인") -->

- [x] AIMarketRegimeWidget 브라우저 실기동 검증 및 발견 결함 2건 수정 <!-- APPROVED: 2026-08-17/유저승인("위젯 건도 승인") --> (사용자 지시 "playwright mcp로 localhost:3000 대시보드 전략 위젯 확인해줘" → "둘 다 진행해"): 전 세션이 미검증으로 남긴 확인 토스트·isSettingsReady 가드를 Playwright로 실기동 검증하고, 그 과정에서 드러난 (1) 존재하지 않는 adminSettings.market_regime을 읽어 항상 "BULLISH"로 폴백하던 국면 배지를 실제 SSOT(/market/overview의 market_condition)로 교체, (2) ICU 작은따옴표 인용 때문에 확인 토스트 제목의 {name}이 치환되지 않던 로케일 문자열 3쌍을 수정한다.

### 변경 영향 기록
- 변경 분류: 릴리스 운영 (기존 미커밋 작업의 분리 커밋·머지, 신규 로직 무작성) + 후속 결함 수정 (다국어화, 카탈로그 게이트 정정, 라이브 전환 안전장치) + 브라우저 실기동 검증에서 드러난 표시 결함 수정 (국면 SSOT 교체, ICU 플레이스홀더 이스케이프) + 보안 취약점 수정 (텔레그램 연동 계정 탈취 차단: 사용자명 기반 연동 폐기 → 1회용·만료형 토큰 소유권 증명, chat_id 중복 바인딩 차단)
- 생산자: docs/tasks/2026-08-16.md (본 감사 기록). 1차 머지분은 08-13/08-14/08-15 슬라이스의 기존 파일 (backend/app/backtests/, backend/app/admin/backtest_runner.py, backend/app/strategies/leveraged_regime.py, backend/app/auth/router.py, backend/tests/, scripts/run_leverage_backtest.py, docs/API_STANDARD.md). 2차 수정분은 backend/app/strategies/strategy_catalog.py·institutional_follow.py·strategy_factory.py, backend/tests/test_i18n_messages.py(면제 원복), locales/ko.json·en.json(components.market_regime 신설 및 strategy_catalog·admin.users 키 추가), frontend/components/AIMarketRegimeWidget.tsx·StrategyCatalog.tsx·StrategySelectModal.tsx·Dashboard.tsx·admin/UserManagement.tsx, frontend/app/admin/settings/page.tsx. 5차 수정분은 frontend/hooks/useMarketOverview.ts(신설, 국면 SSOT), frontend/components/AIMarketRegimeWidget.tsx·MarketHeader.tsx, locales/ko.json·en.json(components.market_regime.regime_unknown 신설 및 ICU 따옴표 이스케이프 3쌍), backend/tests/test_i18n_messages.py(ICU 인용 가드 신설). 6차 수정분(텔레그램 보안)은 backend/app/core/telegram.py(토큰 발급·소비 SSOT 및 /start 핸들러 교체), backend/app/core/security.py(_sha256_fingerprint 공용화 + hash_telegram_link_token), backend/app/core/models.py(UserSettings 컬럼 2개), backend/app/admin/router.py(POST /telegram/link-token 신설 + chat_id 중복 바인딩 409 가드), backend/alembic/versions/a4c7e2b91f30_add_telegram_link_token.py(신설), backend/tests/test_telegram_link_security.py(신설 12건), frontend/lib/api.ts, frontend/app/admin/settings/page.tsx, locales/ko.json·en.json
- 소비자: origin/main 및 후속 세션 (보류 슬라이스를 이어받는 다음 작업자). 국면을 읽는 모든 프론트 컴포넌트는 이제 useMarketOverview 훅을 거친다. 텔레그램 연동을 시작하는 모든 경로는 이제 POST /api/v1/admin/telegram/link-token이 발급한 딥링크만 사용한다(사용자명 딥링크는 무효)
- 데이터 경계: 변경 없음 (DB·테이블·스키마·마이그레이션 무관, yfinance 읽기 전용 + Redis/폴백 레이트리밋). 6차는 변경 있음 — user_settings에 telegram_link_token_hash(Index)·telegram_link_token_expires_at 2개 컬럼 추가, 마이그레이션 a4c7e2b91f30(head, down_revision 7c1e4b93af58). 둘 다 nullable이라 기존 telegram_chat_id 연동은 그대로 살아 있고 신규·재연동에만 토큰이 요구된다
- API 계약: /api/v1/auth/signup 429 및 쿠키 인증 요청 403 계약이 docs/API_STANDARD.md 5장으로 신규 명문화, 그 외 응답 스키마 무변동. 6차로 POST /api/v1/admin/telegram/link-token 신설(1회용·만료 10분·IP 60초 10회 429)과 POST /api/v1/admin의 chat_id 중복 바인딩 409가 docs/API_STANDARD.md 5장에 명문화됐다. 응답 스키마 변경 — /api/v1/admin 설정 응답의 global_bot_username은 서버가 딥링크를 완성해 내려주므로 프론트에서 소비하지 않는다(백엔드 필드는 유지)
- 문서 반영: docs/API_STANDARD.md (인증·보안 계약 5장), docs/tasks/2026-08-13.md·2026-08-14.md·2026-08-15.md 동반 커밋, docs/SCHEMA.md(user_settings 컬럼 2개 및 chat_id 배타 소유 명시), docs/API_STANDARD.md 5장(텔레그램 연동 소유권 증명·chat_id 배타 소유 계약 2건 추가)
- 회귀 테스트: python scripts/verify_harness.py 전체 통과 2회 — 1차 머지 전(backend pytest 399 passed / tsc / ESLint / Playwright E2E 8 passed), 2차 수정 후 동일 구성 재통과(2026-08-16). 1차는 PR #91의 CI 하네스(3m17s pass)가 브랜치 내용으로 독립 검증했다. 2차는 test_i18n_messages.py ALLOWLIST 면제를 제거한 상태에서 통과했으므로 하드코딩 한글 가드가 켜진 채로 얻은 결과다(파일이 커밋본과 바이트 동일로 복귀). 미검증 영역 — AIMarketRegimeWidget은 로그인 뒤 대시보드에만 렌더돼 Playwright 스모크 범위 밖이므로, 확인 토스트의 실제 클릭 동작은 tsc 타입체크와 UserManagement의 동일 sonner action/cancel 선례로만 담보된다(브라우저 실기동 미확인). 3차로 5차 수정분 검증을 위해 재통과(2026-08-16, backend pytest 400 passed = 신설 ICU 가드 1건 증가 / tsc / ESLint / Playwright E2E 8 passed, exit 0). 이 회차의 미검증 영역은 해소됐다 — Playwright MCP로 admin 로그인 상태의 대시보드를 실기동해 (a) /admin 응답을 6초 지연시켜 isSettingsReady=false 구간을 결정적으로 관찰(스피너 표시, 적용 버튼 0개, 비교 버튼 disabled → 로드 후 각각 해제), (b) 확인 토스트 제목·설명·action/cancel 렌더와 취소 시 /admin 쓰기 요청 0건·전략 미변경을 네트워크 레벨로 확인, (c) /market/overview를 8초 지연시켜 국면 미확정 구간에 거짓 BULLISH 배지 0건·최적 주장 0건·적용 버튼 0개임을 확인했다. ICU 가드는 네거티브 컨트롤로 검증했다(로케일 1건을 옛 형태로 되돌리면 그 키만 정확히 지목하며 실패, 복구 시 21 passed). 주의 — 이 회차 하네스는 같은 체크아웃에서 동시 진행 중인 텔레그램 연동 취약점 수정 슬라이스의 워킹트리 변경을 포함한 상태로 돌았다. 라이브 전환 커밋 경로('즉시 전환' 클릭 후 저장·성공 토스트)는 봇이 is_running=true로 가동 중이라 의도적으로 실행하지 않았다. 이후 텔레그램 슬라이스가 커진 상태로 다시 돌리면 1/8 CONTRACT가 반려되는데(문구: 계약 영향 변경에는 docs/ 장기 문서 변경이 필요합니다), 원인은 이 슬라이스가 아니다 — verify_harness의 is_contract_sensitive/validate_change_contract를 파일 목록별로 직접 호출해 귀속을 확인했다. 5차 변경 6파일은 계약 민감 파일 0개·오류 0건이고, 텔레그램 슬라이스의 backend/app/core/models.py·backend/app/admin/router.py·frontend/lib/api.ts·backend/alembic/versions/a4c7e2b91f30_add_telegram_link_token.py 4개가 민감 파일로 걸려 docs/ 장기 문서 갱신(또는 '문서 반영: 영향 없음 - 사유') 요구를 발생시킨다. 5차 단독 상태에서 돌린 1회차가 1/8 포함 전 단계 통과한 근거이며, 현재 조합 트리에서도 2~8단계는 전부 통과한다(1/8만 반려) 6차(텔레그램 보안)는 신설 backend/tests/test_telegram_link_security.py 14건(봇 핸들러 12 + 발급 엔드포인트 관통 2)을 네거티브 컨트롤로 검증했다 — telegram.py의 토큰 소비를 옛 사용자명 조회로 되돌리면 정확히 5건이 실패하고(그중 2건이 탈취 직접 재현: 공격자 chat_id가 피해자 계정에 바인딩됨), 원복 시 14 passed. 전체 하네스 8/8 통과(2026-08-17, backend pytest 414 passed / 계약 검사 민감파일 4건 기록 확인 / 컴파일 309파일 / NFC 510파일 / alembic 최신 / tsc / ESLint / Playwright E2E 8 passed, exit 0). 직전 회차에서 test_concurrency_infra_validation.py::test_redis_lock_renewal_backoff_retry 1건이 실패했으나 이 슬라이스와 무관한 선행 결함이다 — 해당 테스트는 mock sleep 3회를 0.05초 실측 대기 안에 관찰하는 wall-clock 경합이고, 격리 재실행 5/5 통과했으며 이 변경은 locks.py·async 경로를 건드리지 않는다(재실행 하네스에서는 통과). 미검증 영역 — 설정 화면 Telegram 탭의 버튼 클릭 실동작은 브라우저로 확인하지 못했다(백엔드 :8000 미기동 + 로그인 필요, 백엔드 기동은 스케줄러·텔레그램 폴링·라이브 관찰 계정을 함께 깨우므로 임의 실행하지 않음). 프론트는 tsc·ESLint·E2E 통과와 발급 엔드포인트 관통 테스트로만 담보된다.
- 미해결 위험: 이전 항목들에서 지목한 부채는 모두 해소됨 — 고아 컴포넌트·미사용 키 5개 삭제와 저장 안내 복원(3번), 선행 고아 키 2개 제거·real_rate_limiter 마커 pytest.ini 등록·per-request cookies= 폐기 API 교체(4번, 해당 경고 2종 0건 확인). 남은 미검증 영역은 하나 — AIMarketRegimeWidget의 확인 토스트 클릭 동작은 로그인 후 대시보드에만 렌더돼 Playwright 스모크 범위 밖이라 tsc 타입체크와 UserManagement의 동일 sonner 선례로만 담보된다. 잔존 경고 5건은 이 작업들과 무관한 선행 PydanticDeprecatedSince20(router_backtest.py의 Field(example=...))이다. 5차에서 그 미검증 영역은 브라우저 실기동으로 해소했고, 대신 새 위험 3건을 남긴다 — (가) 1-Click 추천은 국면과 사실상 무관하게 동작한다. 카탈로그 96종 중 86종이 regime=ALL이고 최상위 gold도 ALL이라 BULLISH/NEUTRAL 어느 쪽으로 계산해도 strategy_c로 같다. 즉 국면 필터가 결과를 못 바꾸는 상태이며, BULLISH 전용 6종·BEARISH 3종·NEUTRAL 1종 중 하나라도 gold로 승격되는 순간부터 국면 정확도가 곧바로 추천 정확도가 된다. (나) 라이브 전환 커밋 경로는 여전히 미검증이다(위 회귀 테스트 사유 참조). (다) 이 회차 변경은 아직 커밋되지 않았고 동시 세션과 워킹트리를 공유한다 [[multi-session-shared-checkout-races]] 6차 관련 잔여 위험 — (라) 이번 수정은 딥링크 연동 경로만 닫았다. 같은 감사에서 확인한 나머지 8건(회원가입 아이디 열거, 백테스트 자원 고갈 DoS 및 스택트레이스 노출, 보안 헤더 전무, 리프레시 토큰·자격증명 평문 폴백 상주, KIS_CREDENTIAL_MASTER_KEY prod 기동 가드 부재, 브로커 토큰 실패 시 print(res.text), 프록시 뒤 rate limit IP 뭉침)은 미조치 상태다. (마) 운영 배포 시 마이그레이션 a4c7e2b91f30이 선반영돼야 하며, 반영 전에는 연동 링크 발급이 실패한다.
- 인수인계: (구현·검증 완료) 1차로 백엔드 안전 슬라이스 2종을 PR #91로 머지(6999d2c), 2차로 보류 슬라이스 3건의 결함을 수정해 재푸시. 수정 내역 — (a) 하드코딩 한글 4파일을 locales 키로 이관하고 ALLOWLIST 면제 4건 제거(가드 원복), (b) institutional_follow를 selection_eligible=False로 정정하고 reason을 생산자 부재·QQQ 대비 -81%p 실측으로 교체하며 미발화 분기에 사유 주석 명시, (c) AIMarketRegimeWidget에 isSettingsReady 가드(미로드 시 액션 버튼 미렌더)와 trade_mode 폴백 제거로 REAL/MOCK 강등 경로 차단, 라이브 전환에 sonner action/cancel 2단계 확인 도입, 중복 useSWR·타입 선언을 useStrategyCatalog 훅으로 통합. 3차로 고아 컴포넌트(CurrentStrategyBadge.tsx)와 미사용 키 5개를 삭제하고 설정 화면 저장 안내를 현행 UI 문구로 복원했다 — 대시보드 위젯은 확인 후 즉시 전환, 설정 화면은 저장 버튼 필요라는 두 경로가 공존하므로 안내를 지우지 않고 살리는 쪽을 택했다. 4차로 잔여 부채 3건(무효 마커 등록, 폐기 예정 쿠키 API 교체, 선행 고아 키 제거)을 정리하고 08-01~08-16 현황판의 미승격 항목 11개를 사용자 승인("전부 진행") 근거로 [x] 승격했다. 5차로 그 브라우저 실기동 확인을 Playwright MCP로 수행했고(로그인은 사용자가 직접 처리, admin/SIMULATED), 그 과정에서 드러난 표시 결함 2건을 고쳤다 — (d) 국면 배지가 응답에 존재하지도 않는 adminSettings.market_regime을 읽어 폴백 "BULLISH"를 상시 표시하고 있었다(같은 화면의 헤더·QQQ 관제탑은 NEUTRAL). 실제 SSOT인 /market/overview의 market_condition을 useMarketOverview 훅으로 뽑아 위젯과 MarketHeader가 공유하게 했고, 폴백을 되살리는 대신 아는 값이 아니면 undefined로 두어 국면 미확정 구간에는 '국면 확인 중' 칩만 띄우고 추천·최적 주장·적용 버튼을 모두 숨긴다. (e) ICU MessageFormat에서 '{name}'은 리터럴 {name}을 뜻해 확인 토스트 제목의 전략 이름이 통째로 치환되지 않고 있었다 — 어느 전략으로 바꾸는지 안 보이는 확인 다이얼로그였다. ko/en 3쌍(confirm_title·change_success·coming_soon_body)을 ''로 이스케이프하고, 기존 파리티 테스트가 이 결함을 구조적으로 못 잡으므로(\{(\w+) 로만 세고 백엔드 str.format에서는 멀쩡히 렌더된다) 프론트 소비 키 한정 ICU 인용 가드를 test_i18n_messages.py에 신설했다. (5차 종결) 이 5차 변경은 이후 동시 세션이 커밋·머지하여 PR #96으로 main에 반영됐고(d8a0cd7), 사용자 승인으로 [x] 종결했다. 다만 슬라이스가 분리되지 않아 커밋 이력과 내용이 어긋난다 — 위젯·훅·MarketHeader는 f262b1c(위젯 제목, 일치)에, locales ICU 이스케이프와 regime_unknown은 a19e3e0(텔레그램 제목)에, ICU 인용 가드 테스트는 36d8d1e(인증 Rate Limit 제목)에 들어갔다. 되돌리기·이력 추적 시 이 분산을 감안해야 하며, main 이력 재작성은 하지 않는다. 이 사례가 2026-08-17 트리거 정지선 복원과 커밋 서명 주체 정정의 실증 근거가 됐다. 배경 [[strategy-catalog-no-alpha]] [[multi-session-shared-checkout-races]] 6차로 텔레그램 연동 계정 탈취(High)를 수정했다 — (f) /start 핸들러가 사용자명 대신 서버 발급 1회용 토큰만 받도록 교체하고(불일치·만료·재사용을 모두 같은 안내로 처리해 계정 존재 여부 비노출), 토큰 발급·소비 로직을 app/core/telegram.py 단일 창구(issue/consume)로 두어 admin 라우터와 봇 핸들러가 같은 TTL·해시 규칙을 공유하게 했다. DB에는 SHA-256 지문만 남기고 재발급 시 직전 토큰을 무효화한다. (g) 수동 chat_id 입력의 중복 바인딩을 409로 막아 이미 다른 계정에 묶인 chat_id 가로채기를 차단했다. (h) 프론트는 사용자명 딥링크를 버리고 버튼 클릭 시 토큰을 발급받아 새 탭을 여는 방식으로 바꿨으며, 그 과정에서 고아가 된 username 상태·startTransition·global_bot_username 프론트 사본을 제거했다. 다음 세션 시작 지점 — 커밋 전이므로 지시가 나오면 커밋·PR부터 진행하고, 그다음 우선순위는 위 (라)의 잔여 8건 중 백테스트 DoS·스택트레이스(Medium 2건)다.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
