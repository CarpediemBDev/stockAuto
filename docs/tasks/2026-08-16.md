# 2026-08-16 Task Board

## 신규 작업
- [/] 미커밋 변경 전수 검토 및 안전 슬라이스 분리 원격 머지 (사용자 지시 "현재 미커밋된 내용 확인하고 올려도 되는지 확인해" → "문제가 없다면 원격머지까지 진행해"): 한 체크아웃에 5개 세션분 작업이 뒤섞인 상태를 슬라이스별로 감사해, 검증이 깨끗한 백엔드 2종(백테스트 자율 슬롯 / 인증 보안 강화)만 분리 커밋·PR·머지하고, 결함이 확인된 프론트 전략 UI 슬라이스는 워킹트리에 보류한다.

### 변경 영향 기록
- 변경 분류: 릴리스 운영 (기존 미커밋 작업의 분리 커밋·머지, 신규 로직 무작성)
- 생산자: docs/tasks/2026-08-16.md (본 감사 기록), 커밋 대상은 08-13/08-14/08-15 슬라이스의 기존 파일 (backend/app/backtests/, backend/app/admin/backtest_runner.py, backend/app/strategies/leveraged_regime.py, backend/app/auth/router.py, backend/tests/, scripts/run_leverage_backtest.py, docs/API_STANDARD.md)
- 소비자: origin/main 및 후속 세션 (보류 슬라이스를 이어받는 다음 작업자)
- 데이터 경계: 변경 없음 (DB·테이블·스키마·마이그레이션 무관, yfinance 읽기 전용 + Redis/폴백 레이트리밋)
- API 계약: /api/v1/auth/signup 429 및 쿠키 인증 요청 403 계약이 docs/API_STANDARD.md 5장으로 신규 명문화, 그 외 응답 스키마 무변동
- 문서 반영: docs/API_STANDARD.md (인증·보안 계약 5장), docs/tasks/2026-08-13.md·2026-08-14.md·2026-08-15.md 동반 커밋
- 회귀 테스트: python scripts/verify_harness.py 전체 통과 (backend pytest 399 passed / tsc / ESLint / Playwright E2E 8 passed, 2026-08-16 실행). 단 이 통과는 워킹트리 기준이며, 보류 슬라이스가 포함한 test_i18n_messages.py ALLOWLIST 면제가 워킹트리에 남은 상태에서의 결과임을 명시한다 — 푸시 대상 브랜치는 해당 면제와 프론트 파일을 포함하지 않으므로 CI 하네스가 독립 검증한다
- 미해결 위험: 보류 슬라이스 3건이 워킹트리에 그대로 남는다 — (1) backend/tests/test_i18n_messages.py의 ALLOWLIST 파일 전체 면제(None) 4건은 직전 커밋 f111342가 세운 하드코딩 한글 가드를 무력화하며 StrategyCatalog.tsx/UserManagement.tsx는 원래 통과하던 파일이다, (2) strategy_catalog.py의 institutional_follow가 OHLCV_PROXY인데 유일하게 selection_eligible=True로 자동선정 후보에 열려 있고 커밋된 2026-08-02 정정 노트의 실측(QQQ +88.95% vs 전략 +7.44%, 약 -81%p 언더퍼폼)과 충돌하며 reason이 주장하는 실시간 수급 필드(organ_net_buy/smart_money_score/institutional_net_buy_days)는 저장소에 생산자가 없어 죽은 분기다, (3) AIMarketRegimeWidget.tsx가 대시보드에서 확인 없이 즉시 라이브 전략 전환을 수행해 직전 커밋 e6434d0의 "즉시 라이브 전환 금지" 설계를 되돌리며, strategies는 로드됐고 adminSettings가 아직 undefined인 창에 1-Click 버튼이 렌더돼 클릭 시 trade_mode 폴백 "SIMULATED"가 전송되어 REAL/MOCK 계정이 조용히 강등될 수 있다. 부수 위험 — pytest.mark.real_rate_limiter가 어디에도 등록되지 않아 무효 마커이고(PytestUnknownMarkWarning 5건), test_auth_cookie_request_blocked_on_cross_site의 per-request cookies= 사용은 starlette 상위 버전에서 깨진다
- 인수인계: (진행중) 안전 슬라이스 2종만 브랜치 분리 커밋 → PR → 하네스 CI 통과 확인 후 머지. 보류 슬라이스는 커밋하지 않고 워킹트리에 남긴다. 다음 세션 시작 지점 — 프론트 전략 UI 슬라이스를 재개하려면 (a) 하드코딩 한글 실제 번역 후 ALLOWLIST 면제 4건 제거, (b) institutional_follow를 selection_eligible=False로 정정하고 reason을 프록시·음의 알파 사실로 교체, (c) AIMarketRegimeWidget에 adminSettings 로드 가드 추가 및 즉시 라이브 전환 여부를 사용자와 재확정. 배경 [[strategy-catalog-no-alpha]] [[multi-session-shared-checkout-races]]

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
