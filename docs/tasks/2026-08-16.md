# 2026-08-16 Task Board

## 신규 작업
- [R] 미커밋 변경 전수 검토 및 안전 슬라이스 분리 원격 머지 (사용자 지시 "현재 미커밋된 내용 확인하고 올려도 되는지 확인해" → "문제가 없다면 원격머지까지 진행해"): 한 체크아웃에 5개 세션분 작업이 뒤섞인 상태를 슬라이스별로 감사해, 검증이 깨끗한 백엔드 2종(백테스트 자율 슬롯 / 인증 보안 강화)만 분리 커밋·PR·머지하고, 결함이 확인된 프론트 전략 UI 슬라이스는 워킹트리에 보류한다.

- [R] 보류 슬라이스 3건 결함 수정 후 재푸시 (사용자 지시 "보류한 3건 수정하고 다시 올려"): (1) 프론트 하드코딩 한글을 실제 다국어화하고 test_i18n_messages ALLOWLIST 파일 전체 면제 4건을 제거해 가드를 원상 복구, (2) institutional_follow를 selection_eligible=False로 정정하고 reason을 프록시·음의 알파 실측으로 교체, (3) AIMarketRegimeWidget의 라이브 전환에 adminSettings 로드 가드와 명시적 확인 단계를 추가하고 카탈로그 조회를 useStrategyCatalog 훅으로 통합.

### 변경 영향 기록
- 변경 분류: 릴리스 운영 (기존 미커밋 작업의 분리 커밋·머지, 신규 로직 무작성) + 후속 결함 수정 (다국어화, 카탈로그 게이트 정정, 라이브 전환 안전장치)
- 생산자: docs/tasks/2026-08-16.md (본 감사 기록). 1차 머지분은 08-13/08-14/08-15 슬라이스의 기존 파일 (backend/app/backtests/, backend/app/admin/backtest_runner.py, backend/app/strategies/leveraged_regime.py, backend/app/auth/router.py, backend/tests/, scripts/run_leverage_backtest.py, docs/API_STANDARD.md). 2차 수정분은 backend/app/strategies/strategy_catalog.py·institutional_follow.py·strategy_factory.py, backend/tests/test_i18n_messages.py(면제 원복), locales/ko.json·en.json(components.market_regime 신설 및 strategy_catalog·admin.users 키 추가), frontend/components/AIMarketRegimeWidget.tsx·StrategyCatalog.tsx·StrategySelectModal.tsx·Dashboard.tsx·admin/UserManagement.tsx, frontend/app/admin/settings/page.tsx
- 소비자: origin/main 및 후속 세션 (보류 슬라이스를 이어받는 다음 작업자)
- 데이터 경계: 변경 없음 (DB·테이블·스키마·마이그레이션 무관, yfinance 읽기 전용 + Redis/폴백 레이트리밋)
- API 계약: /api/v1/auth/signup 429 및 쿠키 인증 요청 403 계약이 docs/API_STANDARD.md 5장으로 신규 명문화, 그 외 응답 스키마 무변동
- 문서 반영: docs/API_STANDARD.md (인증·보안 계약 5장), docs/tasks/2026-08-13.md·2026-08-14.md·2026-08-15.md 동반 커밋
- 회귀 테스트: python scripts/verify_harness.py 전체 통과 2회 — 1차 머지 전(backend pytest 399 passed / tsc / ESLint / Playwright E2E 8 passed), 2차 수정 후 동일 구성 재통과(2026-08-16). 1차는 PR #91의 CI 하네스(3m17s pass)가 브랜치 내용으로 독립 검증했다. 2차는 test_i18n_messages.py ALLOWLIST 면제를 제거한 상태에서 통과했으므로 하드코딩 한글 가드가 켜진 채로 얻은 결과다(파일이 커밋본과 바이트 동일로 복귀). 미검증 영역 — AIMarketRegimeWidget은 로그인 뒤 대시보드에만 렌더돼 Playwright 스모크 범위 밖이므로, 확인 토스트의 실제 클릭 동작은 tsc 타입체크와 UserManagement의 동일 sonner action/cancel 선례로만 담보된다(브라우저 실기동 미확인)
- 미해결 위험: 잔여 정리 대상 2건 — (1) frontend/components/CurrentStrategyBadge.tsx가 Dashboard에서 제거되며 어디서도 import되지 않는 고아 컴포넌트가 됐고 locales의 current_strategy_title 키도 미사용으로 남는다, (2) 설정 화면에서 strategy_save_hint("저장 버튼을 눌러야 실제 적용") 안내가 삭제돼 드롭다운 선택 후 저장이 필요하다는 사실이 화면에서 사라졌다(키도 미사용 잔존) — 원저자의 압축 리디자인 의도일 수 있어 임의 복원하지 않고 사용자 판단 대기. 백엔드 부수 위험 — pytest.mark.real_rate_limiter가 어디에도 등록되지 않아 무효 마커이고(PytestUnknownMarkWarning 5건), test_auth_cookie_request_blocked_on_cross_site의 per-request cookies= 사용은 starlette 상위 버전에서 깨진다
- 인수인계: (구현·검증 완료) 1차로 백엔드 안전 슬라이스 2종을 PR #91로 머지(6999d2c), 2차로 보류 슬라이스 3건의 결함을 수정해 재푸시. 수정 내역 — (a) 하드코딩 한글 4파일을 locales 키로 이관하고 ALLOWLIST 면제 4건 제거(가드 원복), (b) institutional_follow를 selection_eligible=False로 정정하고 reason을 생산자 부재·QQQ 대비 -81%p 실측으로 교체하며 미발화 분기에 사유 주석 명시, (c) AIMarketRegimeWidget에 isSettingsReady 가드(미로드 시 액션 버튼 미렌더)와 trade_mode 폴백 제거로 REAL/MOCK 강등 경로 차단, 라이브 전환에 sonner action/cancel 2단계 확인 도입, 중복 useSWR·타입 선언을 useStrategyCatalog 훅으로 통합. 다음 세션 시작 지점 — 위 미해결 위험 2건(고아 컴포넌트·저장 안내 문구)의 처분 결정. 배경 [[strategy-catalog-no-alpha]] [[multi-session-shared-checkout-races]]

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
