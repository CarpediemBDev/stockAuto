# 2026-07-06 Task Board

## 브랜치 전략 명문화 (AGENTS.md §2-1)
- [/] AGENTS.md에 '브랜치 전략' 절 신설 — 트렁크 기반, 분기 시점 규칙, `<type>/<요약>` 네이밍, 1브랜치=1목적=1PR, merge commit 유지, main 보호(PR+CI 녹색 필수), 병합 후 삭제·현황판 기록. 사용자 합의 확정본

### 변경 영향 기록
- 변경 분류: AI/개발 협업 규칙 보강 — 브랜치 전략을 규칙 SSOT(AGENTS.md §2-1)에 명문화 (기존 §2 Git 동결 삭제·변경 없음, 하위 절 추가)
- 생산자: AGENTS.md (§2-1 '브랜치 전략' 신규 절)
- 소비자: 이 저장소를 여는 모든 AI 세션·개발자, PR/브랜치 운영 전반
- 데이터 경계: 없음 — 런타임 사용자 데이터/DB/캐시와 무관 (협업 규칙 문서)
- API 계약: 변경 없음
- 문서 반영: 영향 없음 - AGENTS.md 자체가 규칙 원장이라 별도 docs/ 장기문서 반영 불필요(§2 Git동결·§3 현황판과 상호 정합)
- 회귀 테스트: 규칙 텍스트만 추가·코드 무변경. python scripts/verify_harness.py 전체 통과로 계약 기록·문서 정합 확인
- 미해결 위험: 없음 — 문서 규칙. GitHub 브랜치 보호(PR+CI 필수) 실제 활성화는 원격 설정 변경이라 사용자 확인 후 gh로 별도 수행
- 인수인계: 확정 브랜치 전략=트렁크 기반+merge commit. 이 AGENTS.md 변경은 규칙상 docs/chore 성격이라 커밋 시 backtest 브랜치가 아닌 별도 브랜치로 분리 권장. 병렬 세션이 아래 P1 트리아지 진행 중

## P1: 전략 카탈로그 전수 트리아지 (무알파·무매매 격리)
- [R] 진단(분석전용): 팩토리 등록 전 전략(76종)을 공유 데이터축(BacktestSimulator base_sim) 위에서 일괄 백테스트하고 `assess_strategy_report`로 채점 → selection_eligible/selection_score/confidence_grade로 랭킹. 하네스=스크래치(triage_harness.py), 리포트=스크래치(TRIAGE_REPORT_2026-07-06.md).
- 실증 결과(유니버스 12대형주·1년·1h·수수료0.10%, QQQ B&H=+29.16%): ①76개 중 QQQ 초과 0개 ②흑자 4개뿐(strategy_c_aggressive +19.4%/exploded_c +16.5%/strategy_c +2.5%/social_buzz +0.6%) ③selection_eligible 게이트 결함 — 47개 통과 중 42개 손실(최악 supertrend −85%), 게이트가 수익성/벤치마크초과 미검사 ④고빈도(거래 수천회) 전량 −60~−85% 수수료·휩쏘 자멸 ⑤무매매 16+준무매매 2=18종 대형주 유니버스 미발화 ⑥토너먼트 7참가자 중 episodic_pivot·premarket_breakout·trend_stabilization 3개 t=0.
- 후속 결정 후보(사용자): A.selection_eligible에 수익성·QQQ초과 하한 추가(P1 코어) B.무매매/자멸 전략 카탈로그 격리 C.근본—프록시 76종 접근 재고, QQQ 단순보유가 전 전략 압도 → 알파원천 재설계.

### 변경 영향 기록
- 변경 분류: 분석 전용 — 전략 성과 전수 트리아지 실증(코드/계약 무변경, 스크래치 하네스로 측정만)
- 생산자: (스크래치) 트리아지 하네스 스크립트 — backend/app/strategies/strategy_factory.get_strategy + app/bot/backtest_engine.BacktestSimulator + app/bot/backtest_metrics.assess_strategy_report 재사용
- 소비자: 전략 카탈로그 유지보수 판단(무알파 제거·strategy_catalog 프로파일 보강), 봇 자동선정 로직(multi_strategy_manager), 사용자 로드맵
- 데이터 경계: 없음 — yfinance 공개 시세만 인메모리로 로드해 측정. 운영 DB/캐시/사용자 데이터 무변경
- API 계약: 변경 없음
- 문서 반영: 트리아지 결과 리포트를 스크래치에 산출. 후속 조치(전략 제거/프로파일 보강) 확정 시 별도 작업으로 계약 문서 반영
- 회귀 테스트: 측정 하네스 자체가 실증 — base_sim 1회 준비 후 전 전략 .run() 완주율·예외율 집계. 코드 변경 없어 verify_harness 대상 아님(후속 코드 변경 시 실행)
- 미해결 위험: yfinance 데이터 품질/기간 의존, 단일 유니버스·단일 기간의 과최적 위험(walk-forward 미적용). 결과는 상대 랭킹·격리 후보 도출용이며 절대 알파 보장 아님
- 인수인계: 실증 랭킹 산출 후 무알파/무매매/미등록 전략 목록을 사용자에 제시 → 제거·프로파일 보강 여부 승인받아 후속 코드 작업 분리

## P1-A/B/C: 트리아지 후속 코드 수정 (게이트 하한·전략 격리·아레나 교체)
- [R] A. `backtest_metrics.assess_strategy_report`에 수익성 하드 게이트 추가 — selection_eligible에 `ret>0 && profit_factor>1.0` 요구(손실·저효율 전략 자동선정 차단), `beats_benchmark`·`is_profitable` 플래그 신설. 벤치마크초과는 하드 배제 대신 플래그+기존 점수 반영(강세장 eligible=0로 walk_forward 굶기는 것 방지). 검증: 저장 트리아지 데이터 오프라인 재채점 eligible 47→3(strategy_c 계열만, 전부 beats_qqq=False).
- [R] B. `strategy_catalog`에 미분류 프록시 5종 프로파일 보강(OHLCV_PROXY+ineligible+사유) — pre_gapper·supernova·panic_dip_buy·pump_run_pull(페니주 is_penny 전용 셋업 프록시, 비페니 유니버스 무발화), vix_hedging(외부 VIX 플래그 is_vix_ok 의존). 나머지 무매매는 실 OHLCV 패턴 감지기라 A 게이트로 동적 배제(오분류 안 함).
- [R] C. `backtest_runner` 토너먼트 아레나 무매매 참가자 교체 — premarket_breakout·trend_stabilization(t=0)→strategy_c_aggressive·exploded_c, multi_slot 앵커 episodic_pivot(t=0)→strategy_c. run_multi_strategy_sim 리포트에 profit_factor 누락 보강. 검증: 라이브 토너먼트 완주(크래시 0), 교체 참가자 매매 확인(0→21회), multi_slot profit_factor 배선 확인(0.39/0.41), 손실 참가자 전원 eligible=False.

### 변경 영향 기록
- 변경 분류: 버그/로직 수정 — 전략 자동선정 게이트 수익성 하한 신설(손실전략 통과 차단) + 카탈로그 데이터출처 오분류 교정 + 토너먼트 무매매 참가자 교체
- 생산자: backend/app/bot/backtest_metrics.py(assess_strategy_report), backend/app/strategies/strategy_catalog.py(_STRATEGY_PROFILES), backend/app/admin/backtest_runner.py(참가자 목록·run_multi_strategy_sim profit_factor), backend/tests/test_backtest_metrics.py(회귀 추가)
- 소비자: backend/app/bot/walk_forward.py(eligible 필터·score 정렬 선정기), admin/router.py 토너먼트 API→프론트 리더보드, 봇 전략 자동선정 경로
- 데이터 경계: 없음 — 채점/분류 로직·인메모리 백테스트. 운영 DB/캐시/사용자 데이터 무변경
- API 계약: 변경 없음(응답 필드 가산 beats_benchmark). 토너먼트 참가자 구성 변경은 데이터 값 변경이며 스키마 무변경
- 문서 반영: 트리아지 리포트(스크래치) 근거. 확정 시 SYSTEM_SETTINGS/전략 카탈로그 문서에 게이트 기준 후속 반영
- 회귀 테스트: test_backtest_metrics 신규 2케이스(손실 고빈도 supertrend ineligible·수익 laggard beats_benchmark=False) pass. ✅ verify_harness 전체 통과(pre-commit 훅 실행): 계약·카오스/롤백·컴파일240·NFC389·alembic·pytest 216 passed·프론트 tsc/lint·Playwright E2E 8 pass. (psutil이 venv에 설치되며 이전 BLOCKED 해소)
- 미해결 위험: 게이트 강화로 walk_forward eligible 후보가 소수(현 유니버스 3종)로 축소 — 강세장에선 정상 신호이나 유니버스/기간 확장 시 재검증 필요. 벤치마크 하드배제 미적용(선정기 굶김 방지).
- 인수인계: A/B/C 구현·검증·커밋·머지 완료 ✅. 커밋 `4a67a22` → PR #15(merge commit `0d258dc`)로 main 병합, CI 하네스 녹색 확인 후 머지, 원격 브랜치 삭제. 로컬 main 동기화됨. (P0 수수료 브랜치는 그 사이 PR #14로 별도 머지되어 순 diff는 내 4파일뿐.) 다음 후보: 근본 과제(알파 원천 재설계).

## P2: 알파 원천 재설계 — 횡단면 모멘텀 전략 신규 도입
- [/] `cross_sectional_momentum` 신규 전략 구현 — 개별 종목 타이밍(롱온리 오버레이)이 76종 전부 QQQ에 패배한 트리아지 결론에 따라, '유니버스에서 가장 센 종목을 골라 담는' 선택형(횡단면) 접근으로 전환. 공유 엔진 무변경 — 엔진이 매 시점 후보를 점수순 정렬해 강한 순서로 매수하는 특성(backtest_engine.py:901) + 사전계산된 relative_strength·change_pct·is_triple_ema_up·is_near_52w_high를 활용한 근사. 승자 방치(스마트익절 비활성·넓은 트레일링·시그널붕괴 완화) + 모멘텀 크래시 가드(BEARISH 레짐 진입점수 0=현금대피). strategy_factory 등록, run_tournament 참가. 검증: 스크래치 러너로 동일 유니버스·기간 백테스트해 QQQ초과·PF·MDD 산출, regime_switching 앵커 대비.
- 1차 실측(유니버스 79종·2026-01-01~05-30): ⚠️인터벌이 결정적. **1h**=ret-40.25%/QQQ+18.80%/excess-59.05pp/trades476/MDD-47.29%(regime앵커 -27.54%보다 나쁨) — 20봉=3거래일짜리 초단기라 고점추격 반전함정. **1d로 전환**=ret+42.44%/QQQ+20.57%/**excess+21.87pp**/trades60/MDD-7.99%/Sharpe3.16/**beats_bench=True**(프로젝트 최초 QQQ 상회). 전략 docstring에 '일봉 전용' 명시. 단 eligible=False(PF 정확히 1.0으로 수익성 하드게이트 pf>1.0 미충족 — 이익 대부분이 미실현 보유분). 미해결: (1)단일 불장 창 과최적·미실현 편중(종료시점 민감)이라 알파 미확정 — 다레짐/워크포워드 필수 (2)시스템 기본 인터벌 1h라 이 전략은 1d 배선 별도 필요.

### 변경 영향 기록
- 변경 분류: 신규 기능 — 알파 원천 재설계용 횡단면 모멘텀 전략 추가(공유 백테스트 엔진·계약 무변경, BaseStrategy 서브클래스 + 팩토리/토너먼트 배선만)
- 생산자: backend/app/strategies/cross_sectional_momentum.py(신규), strategy_factory._create_strategy(분기 추가), run_tournament.TOURNAMENT_STRATEGIES(참가자 추가)
- 소비자: run_tournament·backtest_runner 토너먼트, backtest_metrics.assess_strategy_report 채점, walk_forward 자동선정 후보군
- 데이터 경계: 없음 — yfinance 공개 OHLCV만 인메모리로 사용. 운영 DB/캐시/사용자 데이터 무변경
- API 계약: 변경 없음 — 전략 식별자 문자열 1종 가산(스키마 무변경)
- 문서 반영: 검증 후 성적 확정 시 strategy_map.md·strategy_catalog에 신규 전략 명세 반영(현 단계는 실험 등록)
- 회귀 테스트: 신규 전략은 순수 OHLCV 파생 지표만 사용(strategy_catalog 기본 프로파일 MARKET_DATA+eligible 자동적용). 완료 보고 전 python scripts/verify_harness.py 통과 필요
- 미해결 위험: (1)순수 월간 리밸런싱 아님 — 엔진의 점수순 매수 근사라 정확한 top-N 균등보유는 엔진 훅 필요(후속) (2)5개월 단일 불장 창 과최적 위험 — 다레짐·워크포워드 재검증 전엔 알파 미확정 (3)score_factor가 최강 종목에 비중 집중 유발(엔진 공유 로직이라 불변)
- 인수인계: 구현→스크래치 백테스트로 QQQ초과 확인이 다음 시작점. 흑자·QQQ초과 시에만 카탈로그 정식 등재·문서 반영 진행. 미검증 상태로 커밋 금지(Git 동결)

## 시스템 헬스체크 대시보드 API 및 프리미엄 UI 구축
- [R] 시스템 상태 모니터링 목적별 API 3종 개발 (`/api/v1/system/health/core`, `bot`, `brokers`)
- [R] 프론트엔드 모니터링 UI 및 어드민 대시보드 통합 (Next.js)
- [R] 커밋 전 리뷰 반영 수정 4종: (1) `.gitignore`에 `.claude/` 추가(4.4MB 중첩 워크트리 커밋 오염 차단) (2) `health/bot`의 `is_processing` 스냅샷 버그 수정(모듈 참조로 전환해 실시간 반영) (3) `health/brokers` 하드코딩 가짜 데이터 → `not_implemented` 정직 표기(프론트 StatusBadge `N/A` 중립 배지·`latency_ms` nullable·미구현 서브타이틀 대응) (4) `disk_usage` 대상 경로를 앱 드라이브 루트(`Path.anchor`)로 명시(크로스플랫폼)

### 변경 영향 기록
- 변경 분류: 신규 기능 — 실시간 헬스체크 모니터링 관제 시스템 (백엔드 API 3종 분리 + 프론트 프리미엄 UI)
- 생산자: 백엔드 헬스체크 라우터, 프론트엔드 어드민 UI
- 소비자: 프론트엔드 어드민 헬스체크 관제 사용자
- 데이터 경계: 인프라 상태(Redis, DB, 스케줄러, KIS/Toss 연동) 실시간 읽기 전용
- API 계약: 신규 헬스체크 API 라우트 3종 추가 (분산 구조)
- 문서 반영: 구현 완료 후 API_STANDARD 또는 헬스체크 연동 명세 반영
- 회귀 테스트: 시스템 API 에러가 메인 트레이딩 루프에 영향 없음을 검증. ✅ verify_harness 전체 통과(Memurai 기동 후) — 계약·카오스/롤백·컴파일240·NFC388·alembic·pytest 216 passed·프론트 tsc/lint·Playwright E2E 8 pass
- 미해결 위험: 브로커 응답 지연으로 인한 헬스체크 로딩 타임아웃 방지. `health/brokers`는 실제 핑 미구현(`not_implemented`)이라 실브로커 장애를 감지 못함 — 후속 실측 구현 필요
- 인수인계: 리뷰 반영 수정 4종 완료·verify_harness 녹색. 커밋 준비 완료(Git 동결 준수 — 실제 커밋/분리 지시 대기). 목적 상이로 커밋 분리 권장: ⓐ docs/chore(AGENTS.md 브랜치전략·.gitignore·현황판) ⓑ feat(시스템 헬스 API+UI: main.py·requirements·system/·frontend admin/system·hooks·api.ts·NavBar)
