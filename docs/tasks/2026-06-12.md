- [x] 로그인 불가 회귀 원인 분석 및 기능 복구
  - 로컬 프론트엔드·백엔드 실행 상태 확인
  - 로그인 API 응답 및 HttpOnly refresh 쿠키 발급 검증
  - 로그인 후 새로고침 세션 유지 실브라우저 검증
  - 백엔드·프론트엔드 전체 회귀 테스트
  - 최초 확인 시 프론트엔드·백엔드 서버가 모두 중단된 상태였음
  - Uvicorn 로컬 리로더 감시 범위를 `backend/app`으로 제한하여 `backend/venv` 초기 스캔 제거
  - Windows 리로드 자식 프로세스가 공식 `backend/venv` Python을 사용하도록 실행 파일 고정
  - 직접 로그인 API 및 프록시 응답 `200`, HttpOnly refresh 쿠키 발급 확인
  - 깨끗한 `127.0.0.1:3000` 세션에서 로그인 및 연속 새로고침 2회 유지 확인
  - `localhost:3000` 호스트를 유지한 상태에서 동일 출처 `/api/v1` 프록시로 refresh 쿠키 세션 처리
  - 원인 정정: `localhost`·`127.0.0.1` CORS 허용 목록은 쿠키 장애 원인이 아니며, `127.0.0.1:8000`은 브라우저가 아닌 Next 서버 내부 프록시 대상임
  - 새로고침 로그아웃의 직접 원인은 인증 초기화·401 처리의 동시 refresh 요청으로 회전 토큰이 경합하던 문제였으며 단일 Promise로 직렬화
  - 백엔드 테스트 82건, Playwright E2E 6건, ESLint, TypeScript 검사 통과
  - 중단된 프론트엔드·백엔드 서버 재기동 후 `localhost:3000` 실브라우저 admin 로그인 및 새로고침 세션 유지 재검증
  - API 루트 경로의 trailing-slash 이중 리다이렉트로 인증 헤더가 유실되던 거래로그·관심종목·번역 조회 오류 수정

- [x] 마켓트랩 1분봉 더블 볼린저 밴드 역추세 전략 구현 및 백테스트 통합
  - 더블 볼린저 밴드(2SD, 3SD) 및 RSI, 수축 필터 계산 로직 추가
  - `DoubleBbReversion` 전략 클래스 구현 및 매개변수 설정 가능화
  - `strategy_factory.py` 및 백테스트 시뮬레이터 연동
  - 역사적 백테스트 구동 검증
  - [x] 실시간 스캐너(details 딕셔너리) 지표 누락 및 채점 차단 결함 수정
  - [x] 실제 전략 주입식 pytest 단위 테스트 케이스 보강 및 통과 검증
