# 2026-07-18 Task Board

## 신규 작업
- [R] obs 관찰계정 자산곡선 보존 (Claude 세션, 사용자 지시): 실측 결과 obs_qqq_hold(86)·obs_core(106)·obs_aggressive(107) 3계정이 running=1로 살아있으나, 자산곡선이 계정당 정확히 500건 상한에 걸려 07-09 시딩~07-13 구간이 이미 소실 중. 원인은 record_equity_snapshot(scheduler.py:2137-2148)이 매 스냅샷마다 (user_id, trade_mode)별 500건 초과분을 하드코딩 .offset(500)으로 삭제하는 롤링 컷. 이 삭제는 관리자 자산곡선 차트 경량화용인데 장기 관찰 실험까지 잘라냄. 조치: 사용자 승인(관찰계정만 제외)에 따라 obs_ 프리픽스 계정을 프루닝에서 제외해 무제한 보존, 일반 유저는 기존 500건 유지. 부수: admin/router.py:69의 EQUITY_SNAPSHOT_RETENTION_LIMIT=500은 미사용 죽은 상수 → config SSOT로 이관해 매직넘버 중복 제거. 이미 소실된 구간은 복구 불가(지금부터 유실 방지)
- [/] 전략 카탈로그 화면화·구독 게이팅 설계안 (Claude 세션, 사용자 지시 "1번 설계안 문서화 + 2번 정합성 진단"): 유저↔전략 1:1 매핑(user_settings.strategy_type)을 화면 노출 가능한 1급 자원으로 승격하고 향후 구독료 티어 게이팅까지 확장하는 설계 문서를 docs/plans/에 작성. 코드 미변경(분석·설계 전용). 정합성 진단은 **실제 DB(backend/stockauto.db) 기준으로 재검증**했고 첫 파일기준 진단(18종 누락)은 오판으로 정정함. 정본 결과: 인식집합=팩토리 107키 ∪ 멀티슬롯 특수키 4개=110 vs DB 97행(전부 active). 팬텀(DB엔 있으나 코드가 모름) 0건 — 카탈로그 97종 전부 동작하며 antigravity_surge·regime_sniper·phoenix_bounce도 DB에 존재함(첫 진단이 없다고 한 것은 시드파일과만 비교한 탓). 코드로 동작하나 DB에 없는 13건 중 10건은 무해한 별칭(complex=strategy_c 등), 진짜 누락은 슬롯모드 3종(multi_slot/multi_slot_3/three_slot)뿐 — admin6/7 계정이 실제 사용하나 카탈로그 미등록. 부수 발견: 쓰기 검증(admin/router.py:413)은 존재 여부만 확인하고 is_active 미필터. 재발방지 권고: 실DB↔코드 대조를 verify_harness 상시 가드로 편입. 산출물: 설계 문서만(코드 미변경). 인수인계: 설계 완료. 범위는 사용자 결정으로 "전략 카탈로그 화면(S0~S3)"만으로 확정, 구독·과금(S4)은 현실성 진단(규제·알파·계정성숙 벽, 설계 §7) 결과 보류. 개발은 사용자 지시에 따라 Antigravity로 이관 — 실행 지시서 docs/plans/strategy_catalog_antigravity_handoff.md 작성 완료(원인→명령→기대결과+파일 앵커+수용기준). Claude 역할은 이후 verify_harness 검증(지시 시).
- [/] (실험 과제) 미 의회 매매 공시(PTR) 추종 팩터 백테스트 연구: 유튜브 영상(호두의 AI 분석실, "미 하원의원 주식 거래 복제", 2026-07-17) 검토에서 도출한 연구 과제. 미 하원 사무처(House Clerk) Financial Disclosure 사이트의 STOCK Act 공시(PTR PDF, 거래 후 45일 내 공시·실제 평균 지연 ~35일)를 파싱해 "의원 순매수 상위 종목" 팩터를 만들고, 공시일 기준 진입으로 기존 백테스트 하네스에서 QQQ 벤치마크 대비 알파를 검증한다. (1~3단계 수행)
- [x] (검증 과제) 지역 import → 최상단 이관 순환참조 실증 (Claude 세션, 사용자 지시): 코드검토에서 지적된 "trades↔bot 순환참조로 지역 import가 강제된다"는 주장이 실제 우리 코드에 성립하는지 실증. 사전 정적/동적 추적으로 scheduler는 router_account를 (직간접) import하지 않음을 확인(scheduler의 유일한 app.trades 의존은 market_overview_cache이며 되돌아오지 않음). 또한 scanner/router.py·system/router.py는 이미 scheduler를 최상단 글로벌로 사용 중. 검증: router_account.py의 지역 import 1건(494행 `import app.bot.scheduler as scheduler_mod`)을 최상단으로 이관하고 verify_harness로 순환참조 미발생·회귀 없음을 실증. 결과에 따라 유지/롤백은 사용자 판단.
- [x] (리팩터) 자산 스냅샷 공통 로직 격리 (Claude 세션, 사용자 지시): 코드검토 감사 의견대로 API 컨트롤러(router_account)가 배치 엔진(scheduler) 내부 함수를 역방향 호출하는 결합을 해소. 무거운 Service 클래스 층이 아니라 공통 함수만 제3의 모듈로 격리. record_equity_snapshot과 전용 캐시 _OBSERVATION_USER_IDS를 scheduler.py(bot 도메인)에서 app/trades/equity_snapshot.py(trades 도메인)로 이관 — 이 함수는 core.*(models/db/config/logging/sse)에만 의존해 scheduler 고유 로직 0이라 추출이 깨끗함. 선례: scheduler가 이미 app.trades.market_overview_cache를 import하는 bot→trades 방향과 일치. 효과: router_account는 scheduler_mod 우회 없이 직접 import(동일 도메인), scheduler도 신규 모듈 import. 역방향 결합(controller→scheduler) 제거. 검증: verify_harness 전체.
- [R] (중복제거) AccountEquitySnapshot "최신 스냅샷 조회" 쿼리 중복 통합 (Claude 세션, 사용자 지시로 백로그→착수+PR머지): 동일 쿼리(user_id + trade_mode 필터 → captured_at desc → first())가 3곳에 복붙됨 — app/trades/equity_snapshot.py:38, app/trades/router_account.py:68, app/core/telegram.py:318 (+ app/admin/router.py:666은 같은 개념의 group_by 배치 변형, 통합 대상 제외). 3곳 쿼리가 완전히 동일 형태임을 확인. get_latest_equity_snapshot(db, user_id, trade_mode)을 app/core/equity_repository.py에 신설(세션은 호출부 주입)해 SSOT화. 위치를 app/core/로 잡은 이유: telegram이 core 도메인이라 trades에 두면 core→trades 역방향 의존이 생기므로 양쪽이 하향 의존하도록 core 레벨에 배치. admin 배치 변형은 형태가 달라 제외. 검증: verify_harness 전체 후 PR·머지.

### 변경 영향 기록
- 변경 분류: 백엔드 중복 쿼리 통합(Repository 함수 도입, 동작 불변)
- 생산자: app/core/equity_repository.py 신규 (get_latest_equity_snapshot(db, user_id, trade_mode) — (user_id, trade_mode)의 최신 AccountEquitySnapshot 1건 조회, 세션은 호출부 주입, 없으면 None)
- 소비자: app/trades/equity_snapshot.py(record_equity_snapshot의 dedup 체크), app/trades/router_account.py(대시보드 폴백 조회), app/core/telegram.py(일일 리포트 폴백) — 3곳 인라인 쿼리를 공통 함수 호출로 교체, 각 파일의 중복 AccountEquitySnapshot 로컬 import 정리
- 데이터 경계: 변경 없음 (조회 필터·정렬 조건 동일, 순수 함수 추출)
- API 계약: 변경 없음 (엔드포인트 시그니처·응답 스키마 동일)
- 문서 반영: 영향 없음 - 내부 쿼리 SSOT 통합이라 SCHEMA.md/API_STANDARD.md 등 외부 명세 무관
- 회귀 테스트: python scripts/verify_harness.py 전체 검증(컴파일/pytest/tsc/lint/계약/카오스/롤백) 통과
- 미해결 위험: 없음. equity_repository는 core.models에만 의존하고, core 레벨 배치로 telegram(core)·trades 양쪽이 하향 의존해 core→feature 역방향 없음
- 인수인계: 최신 스냅샷 조회 3중복을 app/core/equity_repository로 통합. 하네스 검증 후 기존 리팩터 커밋과 함께 PR·머지 예정

### 변경 영향 기록
- 변경 분류: 백엔드 모듈 의존성 정리(지역 import → 최상단 이관) 실증 검증
- 생산자: backend/app/trades/router_account.py (최상단에 `import app.bot.scheduler as scheduler_mod` 1건 추가, 494행의 함수 내부 지역 import 1건 제거)
- 소비자: router_account 내 청산 직후 스냅샷 즉시 갱신 경로(record_equity_snapshot 호출부)가 모듈 로드시 이미 바인딩된 scheduler_mod를 사용
- 데이터 경계: 변경 없음 (DB·스냅샷 데이터 경계 무관, 순수 import 위치 변경)
- API 계약: 변경 없음 (엔드포인트 시그니처·응답 스키마 동일)
- 문서 반영: 영향 없음 - 내부 import 구조 실증이라 SCHEMA.md/API_STANDARD.md 등 외부 명세 무관
- 회귀 테스트: python scripts/verify_harness.py 전체 검증(컴파일/pytest/tsc/lint/계약/카오스/롤백) 통과로 순환참조 미발생 및 회귀 없음 실증
- 미해결 위험: 없음. scheduler가 router_account를 되import하지 않아 최상단 이관 시 순환참조 위험 없음을 정적·동적으로 확인함. 나머지 지역 import(59·264·316행)는 이번 범위 밖으로 원상 유지
- 인수인계: 지역 import 1건 최상단 이관 후 하네스 검증 완료. 순환참조 주장이 우리 코드엔 성립하지 않음을 실증. 유지/롤백은 사용자 승인 대기
- 변경 분류: 백엔드 안정성 및 장기 관찰 자산 보존 조치
- 생산자: backend/app/bot/scheduler.py 내 record_equity_snapshot 함수 (obs_ 프리픽스 계정은 500건 프루닝에서 제외하되, N+1 쿼리 방지를 위한 인메모리 _OBSERVATION_USER_IDS 캐시 도입, 대소문자 매칭 휴먼에러 방어 lower() 적용, 고장 난 유저 가드 warning 로그 적재 추가), backend/app/core/config.py (EQUITY_SNAPSHOT_RETENTION_LIMIT = 500 및 OBSERVATION_ACCOUNT_PREFIX = 'obs_' SSOT 설정 추가), backend/app/admin/router.py (미사용 중복 상수 제거 후 app_settings 이관 참조), frontend/components/admin/UserManagement.tsx (obs_ 관찰 계정 옆에 EXEMPT 배지 렌더링하도록 UI 컴포넌트 수정)
- 소비자: record_equity_snapshot을 호출하는 백엔드 스케줄러, 관리자/유저 화면의 자산곡선 차트 API, 관리 대시보드 사용자 목록 화면 (obs_ 관찰 계정에 대해 EXEMPT 마크를 확인 가능)
- 데이터 경계: account_equity_snapshots 테이블 데이터 보존 방식 변경. obs_ 계정들은 롤링 컷 없이 누적되므로 데이터 프루닝 정책의 예외 경계 설정
- API 계약: 변경 없음 (기존 자산곡선 조회 엔드포인트 응답 형식 및 계약 스키마는 동일하게 유지됨)
- 문서 반영: 영향 없음 - 장기 데이터 보존 정책의 내부 구현 및 설정 SSOT 단일화이므로 SCHEMA.md/API_STANDARD.md 등 외부 기술 명세 수정 대상 아님
- 회귀 테스트: backend/tests/test_admin_equity_snapshots.py 내 3개 테스트(test_observation_account_exempt_from_retention_prune, test_observation_account_exempt_from_retention_prune_case_insensitive, test_observation_account_missing_user_defensive_guard) 추가로 관찰 계정의 대소문자 무시 보존 및 유저 부재 시의 안전 가드를 검증함, frontend/e2e/admin-users.spec.ts 내 E2E Mock 데이터 추가 및 테이블 EXEMPT 배지 노출 상태 Assert 검사 추가. python scripts/verify_harness.py 전체 검증 실행
- 미해결 위험: 관찰 계정의 스냅샷 무제한 누적으로 장기 실행 시 DB 용량 및 자산곡선 렌더링 지연이 미미하게 증가할 수 있으나 계정 개수가 3개로 통제되므로 성능 실효 위험은 극히 낮음. 이미 소실된 07-09 ~ 07-13 구간 데이터는 복구 불가
- 인수인계: obs 관찰계정 자산곡선 보존 코딩 및 유닛 테스트 완료. 프론트 UI EXEMPT 배지 추가 및 E2E 테스트 케이스 강화를 포함한 영향 기록을 최종 갱신함. 유저 승인 하에 머지/커밋 대기 상태로 이관

### 변경 영향 기록
- 변경 분류: 백엔드 도메인 결합 해소 리팩터(공통 로직 격리, 동작 불변)
- 생산자: backend/app/trades/equity_snapshot.py 신규 생성 (record_equity_snapshot 함수와 전용 캐시 _OBSERVATION_USER_IDS를 scheduler.py에서 이관, 의존은 core.models/database/config/logging/sse만), backend/app/bot/scheduler.py (해당 함수·캐시 정의 삭제 후 신규 모듈에서 import, 내부 호출부 2곳은 이름 그대로 사용)
- 소비자: backend/app/trades/router_account.py (scheduler_mod.record_equity_snapshot 우회 호출 3곳을 신규 모듈 직접 import로 교체), scheduler.py 내부 벌크 동기 잡 및 refresh_user_equity_snapshot
- 데이터 경계: 변경 없음 (account_equity_snapshots 테이블 읽기/쓰기·프루닝 로직 그대로, 순수 코드 이동)
- API 계약: 변경 없음 (엔드포인트 시그니처·응답 스키마 동일)
- 문서 반영: 영향 없음 - 내부 모듈 배치 변경이라 SCHEMA.md/API_STANDARD.md 등 외부 명세 무관
- 회귀 테스트: python scripts/verify_harness.py 전체 검증(컴파일/pytest/tsc/lint/계약/카오스/롤백) 통과로 동작 불변·순환참조 미발생 실증
- 미해결 위험: 없음. 이관 함수는 core.*에만 의존하고 bot/trades-router를 되import하지 않아 신규 bot→trades import가 사이클을 만들지 않음(기존 market_overview_cache 선례와 동일 방향)
- 인수인계: 자산 스냅샷 공통 로직을 trades/equity_snapshot.py로 격리해 controller→scheduler 역방향 결합 제거. 하네스 검증 후 승인 대기


 

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
