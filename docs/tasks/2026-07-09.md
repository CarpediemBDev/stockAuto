# 2026-07-09 Task Board

## P2-B 승인분: 관찰 전용 계정 3종 재편 — TQQQ 3x 공격형 슬리브 추가 (월 30% 2차 목표 도전)
- [R] 사용자 지시(1차 목표=QQQ 초과·무사고 / 2차 목표=월 30%)에 따라 관찰 세트를 3계정으로 재편. 월별 수익 분포 실측(20년): QLD 2x는 월 30% 0/241(0%)로 2차 목표 구조적 불가 → 3x만 4/197(2%) 가능(대가 −35% 달). 결정: 안전(코어)과 도전(3x)을 오염 없이 병렬 관찰. 임시 계정 admin12/admin13은 목적 표기 계정으로 대체.
- 실측 검증: verify_harness 전체 통과(계약·카오스/롤백·컴파일259·NFC413·alembic 단일헤드 c1f7a3d5e204·pytest 234 passed·프론트 tsc/lint·Playwright E2E 8/8). 회귀 실패 2건 자체 발견·수정(전략 카운트 89→90, 마이그레이터 시드테스트에 leveraged_regime_3x 추가). 계정 정리·시딩 후 라이브 트레이딩 사이클 구동 실증: obs_qqq_hold→QQQ 8주($5,798), obs_core→QLD 48주($4,489), obs_aggressive→TQQQ 84주($6,453) 첫 진입 성사. 실행 중 스케줄러 오류 0건·반복 사이클 중복거래 0건(멱등 정상).
- 구현 범위: ①신규 전략 `leveraged_regime_3x`(자산=TQQQ, 나머지 QQQ SMA200 3일확정 로직 동일) 팩토리·시드 등록 ②관찰 전용 계정 3종 시딩(obs_qqq_hold=benchmark_qqq_hold / obs_core=core_satellite / obs_aggressive=leveraged_regime_3x, 전부 SIMULATED·is_running) ③세션 임시 계정 admin12·admin13(보유·거래·로그 포함) 정리 ④회귀 테스트.

### 변경 영향 기록
- 변경 분류: 신규 기능 — TQQQ 3x 공격형 자율 전략 추가 및 관찰 전용 계정 세트 재편
- 생산자: backend/app/strategies/leveraged_regime.py(LeveragedRegime3x 추가), strategy_factory.py(등록), backend/alembic/versions/(strategies 시드), backend/app/core/migrator.py(관찰 계정 시딩), backend/tests/test_leveraged_regime.py(3x 케이스 추가)
- 소비자: 봇 자동매매 루프, 어드민 자산곡선 대시보드(obs 3계정 병렬 비교), 사용자 성과 판정
- 데이터 경계: 신규 관찰 계정 3종 및 strategies 행 1종 추가. 세션 임시 계정 admin12/13 제거. 기존 경쟁 계정(admin~admin11) 무변경
- API 계약: 변경 없음 — 전략 식별자 문자열 1종 가산(leveraged_regime_3x). 스키마 무변경
- 문서 반영: 채택 확정 시 strategy_map 반영. 현 단계는 본 현황판 기록
- 회귀 테스트: test_leveraged_regime.py에 3x 자산·팩토리 케이스 추가, python scripts/verify_harness.py 전체 통과 필요
- 미해결 위험: (1)3x는 상시 MDD −55%대·월 −35% 달 존재 — 2차 목표는 희귀 이벤트라 미달성이 정상 (2)월 30%는 매달이 아니라 특정 폭발 달에 한함(20년 2%) (3)자율 슬롯 SIMULATED 한정 유지
- 인수인계: 구현→verify_harness→계정 정리·시딩→라이브 관찰. 3계정 자산곡선을 대시보드에서 병렬 비교(obs_qqq_hold 기준선 / obs_core 1차 / obs_aggressive 2차 도전)

## P2 승인분: 코어-새틀라이트 라이브 시뮬레이션 — 지수 레버리지 레짐 전략 봇 배선 + 가상 경쟁 유저 투입
- [R] 사용자 승인(C안: 코어=QLD 2x+SMA200+3일 확정 / 새틀라이트=기존 전략 유지)에 따라, 백테스트로 검증된 레버리지 레짐 전략을 실제 봇 파이프라인에 배선하고 가상 유저를 시딩해 SIMULATED 라이브 페이퍼트레이딩을 개시한다.
- 구현 범위: ①신규 전략 `leveraged_regime`(QQQ 일봉 SMA200 레짐 판정→QLD 보유/현금 대피, 3일 연속 확정 휩쏘 가드, 완결 일봉만 사용해 룩어헤드 차단) + `benchmark_qqq_hold`(QQQ 단순보유 벤치마크) ②MultiStrategyManager에 `core_satellite` 슬롯 모드(leveraged_regime 70% + strategy_c 30%) ③스케줄러에 자율 슬롯 처리 경로 신설(스캐너 시그널 파이프라인 우회, 기존 손절/트레일링 청산 로직에서 자율 슬롯 면제, SIMULATED 모드 한정 집행) ④strategies 테이블 시드 마이그레이션 ⑤경쟁 유저 시딩에 admin12(core_satellite)·admin13(benchmark_qqq_hold) 추가 ⑥회귀 테스트.
- 집행 규율(백테스트와 등가성): 신호=전일까지의 완결 일봉 종가 vs SMA200(3일 연속 확정), 체결=다음 정규장 세션 중(백테스트의 익일 체결과 등가), 레짐 전환 시에만 매매(무전환 시 무매매·멱등), 수수료=SIMULATED 0.10%.
- 실측 검증(라이브 시뮬 구동): ①verify_harness 전체 통과(계약·카오스/롤백·컴파일257·NFC410·alembic 단일헤드·pytest 230 passed·프론트 tsc/lint·Playwright E2E 8/8). ②시딩 후 실제 트레이딩 사이클 구동으로 자율 슬롯이 REGULAR 세션에 레짐=IN 판정→체결 확인: admin12(core_satellite) QLD 49주 @$92.22, admin13(QQQ B&H) QQQ 9주 @$719.47 매수 성사. ③반복 사이클 3회에도 거래 수 2건 유지 = 멱등 가드 정상(과매매 없음). ④구현 중 결함 2건 자체 발견·수정: (a)자율 슬롯의 세션 밖 ORM 속성 접근으로 DetachedInstanceError→보유 재적재+스냅샷 dict 전환으로 사이클 침묵실패 제거, (b)UnfilledOrder 임포트 누락 NameError 수정.

### 변경 영향 기록
- 변경 분류: 신규 기능 — 지수 레버리지 레짐 전략의 봇 실전 배선 및 가상 경쟁 유저 라이브 시뮬레이션 개시
- 생산자: backend/app/strategies/leveraged_regime.py(신규), base_strategy.py(is_autonomous 플래그), strategy_factory.py(등록 2종), backend/app/bot/multi_strategy_manager.py(core_satellite 모드·프리픽스), backend/app/bot/scheduler.py(자율 슬롯 처리 경로), backend/app/core/migrator.py(경쟁 유저 2명 추가), backend/alembic/versions/(strategies 시드 마이그레이션), backend/tests/test_leveraged_regime.py(신규)
- 소비자: 봇 자동매매 루프(async_trading_loop→run_user_trading_flow), 어드민 자산곡선 대시보드(AccountEquitySnapshot으로 admin12/13 vs QQQ 벤치마크 비교), 사용자 전략 채택 의사결정
- 데이터 경계: 신규 가상 유저 2명(users/user_settings)과 strategies 행 3종 추가. 기존 사용자 데이터·보유·거래 기록 무변경. 시세는 yfinance 공개 데이터만 사용
- API 계약: 변경 없음 — 전략 식별자 문자열 가산(leveraged_regime, benchmark_qqq_hold, core_satellite). 스키마 무변경(시드 데이터만 추가)
- 문서 반영: 채택 확정 시 strategy_map/SCHEMA 시드 목록 반영 예정. 현 단계는 본 현황판 기록
- 회귀 테스트: test_leveraged_regime.py(레짐 상태기계·3일 확정·룩어헤드 차단·슬롯 가중치·팩토리 등록) 신규 11케이스 pass. python scripts/verify_harness.py 전체 통과(pytest 230 passed 포함) 확인됨
- 미해결 위험: (1)자율 슬롯은 SIMULATED 모드 한정 — MOCK/REAL은 주문 인텐트 원장 배선이 별도 필요해 의도적으로 차단 (2)병렬 세션의 미커밋 작업(connors_rsi 마이그레이션 d6eba4e1ec31)에 마이그레이션 체인이 의존 — 커밋 시 함께 스테이징 필요 (3)장중이 아니면 포지션 진입은 다음 정규장 개장 후 발생 (4)yfinance 일봉 데이터 장애 시 해당 사이클 무매매(안전측)
- 인수인계: 구현·검증·시딩 완료([R] 승인대기). admin12(id 86, core_satellite)·admin13(id 87, benchmark_qqq_hold) 시딩됨(비번 admin), 각각 QLD/QQQ 첫 포지션 보유 중. 성과 판정은 admin13(QQQ B&H) 대비 admin12 자산곡선 비교(어드민 대시보드 AccountEquitySnapshot). 봇은 백엔드 기동(python run.py local) 중 1분 주기로 레짐 유지·이탈을 자동 집행. Git 동결 규칙에 따라 커밋 미실행 — 승인 시 커밋. 커밋 스테이징 주의: 마이그레이션 체인이 병렬 세션의 connors_rsi(d6eba4e1ec31, 미커밋)에 의존하므로 함께 스테이징 필요. 다음 결정: 라이브 관찰 지속 vs 실계좌(MOCK/REAL) 확장을 위한 주문 인텐트 원장 배선.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)

## P3 승인분: 818개 종목 5대 도메인 다차원 백테스트 토너먼트 배틀
- [R] 5대 도메인(우량주, 잡주, 기술주, 중국주, 이탈리아주) 각 100개씩 총 500개 종목 선발 및 tickers_*.json 파일 생성 완료
- [R] yfinance API 안전 가드(50개 청킹 및 딜레이) 기반 818개 종목 3개년 시세 다운로드 및 Parquet 로컬 캐시 적재
- [ ] run_tournament.py 병렬 CPU 멀티프로세싱 가속화 리팩토링 및 30종 시나리오 백테스트 실행
- [ ] 30종 시나리오 결과 교차 강건성 통계 평가 및 품질 수치(시차, 환율, 수수료) 정밀 감사서 작성

### 변경 영향 기록
- 변경 분류: 성능 개선 및 데이터 테스트 유니버스 대규모 확장
- 생산자: backend/data/tickers_bluechip.json(신규), backend/data/tickers_tech.json(신규), backend/data/tickers_penny.json(신규), backend/data/tickers_china.json(신규), backend/data/tickers_italy.json(신규), backend/run_tournament.py(병렬 리팩토링), backend/app/bot/backtest_engine.py(청킹 수집 보강)
- 소비자: Battle Commander, Data Harvester, Robustness Analyst, Critical Auditor (토너먼트 5인방 워크플로우)
- 데이터 경계: backend/data/cache/ 디렉터리 내 Parquet 캐시 적재 및 backend/data/results/ 결과 CSV 적재. 기존 DB 스키마 무변경.
- API 계약: yfinance 벌크/청크 호출 추가, run_tournament.py CLI 인수 지원(--tickers_file, --start, --end, --interval, --cash)
- 문서 반영: docs/README.md


- 회귀 테스트: python -m py_compile backend/run_tournament.py 문법 무결성 검증, verify_harness.py 통과 검증
- 미해결 위험: (1) yfinance API 429 차단 위험 ➔ 50개 청킹 및 요청 간 0.3초 딜레이를 통해 해결 (2) 멀티프로세스 자원 오버헤드 ➔ CPU 코어수 최적 파티셔닝으로 제어 (3) 이탈리아/중국 주식 시간대 및 환율 오차 ➔ Auditor를 통한 정밀 수치 검증 적용
- 인수인계: tickers_*.json 생성 완료 ➔ run_tournament.py CLI 가동 ➔ csv 취합 후 robustness_analyst/critical_auditor 교차 평가 ➔ 최종 우승 전략 리더보드 등재

