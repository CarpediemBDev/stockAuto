# 2026-08-14 Task Board

## 신규 작업
- [/] 백테스트 아레나(토너먼트)에 자율 슬롯 참가자(지수 레버리지 레짐 / QQQ 벤치마크) 추가: 아레나 UI(BacktestTournament.tsx)는 백엔드 토너먼트가 돌려주는 참가자 리스트를 동적 렌더하므로, 백엔드 _run_dynamic_tournament_internal에 leveraged_regime/benchmark_qqq_hold를 참가자로 병합하면 프론트 무변경으로 화면에 노출된다. 스캐너 참가자(1h)와 달리 자율 전략은 일봉 정의라, 우리가 배선한 엔진 자율 경로(_run_autonomous)를 재사용해 같은 창(start~end)을 interval=1d로 별도 서브런하고 결과를 병합한다.

### 변경 영향 기록
- 변경 분류: 백테스트 토너먼트 참가자 확장 (기존 7개 스캐너 참가자 무변경, 자율 참가자 2종 병합)
- 생산자: backend/app/admin/backtest_runner.py (_run_autonomous_participant/_shape_autonomous_result 신규, _run_dynamic_tournament_internal 참가자 병합, 캐시 버전 v3→v4, 자율 참가자에 low_frequency=True 전달, 동적 종목 상한 MAX_TOURNAMENT_TICKERS=40 + _prioritize_and_cap_tickers 신규 — 1004종목 미완주 방지), backend/app/backtests/backtest_metrics.py (assess_strategy_report에 low_frequency 인자 — 저빈도는 거래횟수 대신 관측기간으로 표본판정+실현PF 대신 총수익률로 수익성판정)
- 소비자: frontend/components/admin/BacktestTournament.tsx (무변경 — 반환 리스트 동적 렌더), 관리자(아레나 화면에서 레짐/벤치마크를 스캐너 전략과 나란히 비교)
- 데이터 경계: 변경 없음 (yfinance 일봉 읽기 전용, DB·스키마 무관)
- API 계약: 변경 없음 (토너먼트 결과 스키마 키 보존 — 자율 참가자도 동일 dict 형태로 병합)
- 문서 반영: docs/tasks/2026-08-14.md 갱신
- 회귀 테스트: test_backtest_tournament_safety.py 유지(참가자 수/명단 미검증) + test_backtest_autonomous.py에 자율 참가자 dict 형태 단위테스트 추가(네트워크 미사용 합성 report) + py_compile + verify_harness
- 미해결 위험: (해소) 저빈도 랭킹 게이트는 low_frequency 예외로 해결(레짐/벤치마크 eligible=True 확인). 화면 검증 중 기존 결함 2건 발견·수정 — (1) _run_dynamic_tournament_internal 종단 캐시 저장이 미정의 cache_path 참조로 항상 NameError 실패 → 아레나가 결과를 못 캐시해 프론트가 무한 재폴링하던 근본 원인(_build_tournament_cache_path로 재계산해 수정), (2) 동적 종목이 전 계정 합산 1004개라 1시간봉 대항전이 미완주 → MAX_TOURNAMENT_TICKERS=40 캡. 라이브 백엔드는 재시작 전까지 수정 전 코드라, 화면 검증은 고쳐진 코드로 캐시를 직접 생성해 프론트가 렌더하도록 함(재시작 시 자동 캐시로 자립).
- 인수인계: (구현·실행·검증 완료) 참가자 병합 + 랭킹 게이트(저빈도 예외) 수정 완료. 라이브 토너먼트 2회 실행(2025-08-14~2026-08-14, 5종목)으로 실측 검증: 게이트 수정 전 레짐+34%가 5위(eligible=False)로 파묻히던 것을 수정 후 benchmark_qqq_hold(+26.5%) 1위·leveraged_regime(+34%) 2위로 정상화, 능동 스캐너 전 종목(-10~-20%)을 상단 압도. 1위가 레짐 아닌 QQQ보유인 건 위험조정(Sharpe 1.33 vs 0.98, MDD -11.7 vs -22.7) 반영 = 게이트 과보정 없이 정직 작동. 테스트: 34개 그린(test_backtest_autonomous 9개[형태+저빈도 게이트 4개] + tournament_safety 3 + leveraged_regime 15 + qa_harness_audit), 기본 low_frequency=False로 스캐너 동작 보존. 참고: verify_harness 전체는 무관한 미커밋 프론트 i18n(별도 칩)으로 여전히 미통과 — 이 작업 슬라이스는 격리 그린. 배경 [[obs-accounts-live-leverage-ab]].

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
