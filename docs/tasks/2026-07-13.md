# 2026-07-13 Task Board

## 계좌 잔고 신선도 개선 — 스냅샷 읽기 경로에 stale-while-revalidate 통일 적용
- [x] 사용자 지시: 대시보드·사용자 관리의 계좌 현황이 1분 주기 스냅샷을 읽어 최대 ~1분 낡을 수 있음. SIMULATED/REAL을 코드로 쪼개지 말고 동일 아키텍처(읽기=스냅샷 캐시, 쓰기=백그라운드 갱신)로 통일하되 모드 차이는 "갱신 임계값" 파라미터 하나로만 흡수. get_balance 응답은 즉시(논블로킹) 반환하고, 스냅샷이 임계값 이상 낡았을 때만 응답 후 background로 refresh_user_equity_snapshot 발화(유저·모드당 디바운스). 프론트 낡음 배지 임계값 강화(5분→90초) 및 관리자 화면 배지 신규 추가. 봇 루프·텔레그램 경로는 무변경.
- 실측 검증: python scripts/verify_harness.py 전체 통과(EXIT=0). 계약·수치/프로세스/카오스/롤백·컴파일275·NFC433·alembic·pytest 245 passed·프론트 tsc/lint·Playwright E2E 8/8. 신규 test_balance_stale_revalidate.py 7케이스로 임계값·디바운스·모드격리 강제.
- 동시 세션 정합: 병렬 세션이 admin/router.py의 list_users를 N+1→단일 서브쿼리로 리팩터하고 응답 필드를 equity_curve→latest_snapshot_at으로 교체함. 그로 인해 stale해진 test_admin_equity_snapshots.py 2건(equity_curve 참조)을 새 계약(latest_snapshot_at·profit_rate)에 맞게 정렬(사용자 승인 하). 관리자 프론트 배지도 latest_snapshot_at 기준으로 정합 확인.

### 변경 영향 기록
- 변경 분류: 성능·신선도 개선 — 잔고 스냅샷 읽기 경로에 stale-while-revalidate 백그라운드 갱신 추가(모드 무관 통일 아키텍처, 임계값만 모드별 파라미터), 프론트 낡음 배지 강화
- 생산자: backend/app/trades/router_account.py(get_balance에 BackgroundTasks 기반 낡음 감지·디바운스 트리거 헬퍼), frontend/components/AccountBalance.tsx(배지 임계값 5분→90초), frontend/components/admin/UserManagement.tsx(낡음 배지 신규)
- 소비자: 대시보드 AccountBalance·BotSignals(둘 다 /account/balance 폴링), 관리자 사용자 관리 화면
- 데이터 경계: account_equity_snapshots 읽기 + refresh_user_equity_snapshot 통한 쓰기(force=True). 신규 컬럼/테이블 없음. 봇 루프·텔레그램 무변경
- API 계약: /account/balance 응답 스키마 불변(captured_at 기존 유지). 신규 필드 없음
- 문서 반영: 영향 없음 - /account/balance 응답 스키마·API 계약 무변경(내부 갱신 트리거·프론트 배지만 추가)이라 docs/ 장기 계약 문서 갱신 불필요. 본 현황판에만 기록
- 회귀 테스트: 신규 test_balance_stale_revalidate.py(임계값·디바운스·모드격리 7케이스) + test_admin_equity_snapshots.py 2건 새 계약 정렬. python scripts/verify_harness.py 전체 통과(EXIT=0, pytest 245 passed)
- 미해결 위험: 인메모리 디바운스라 멀티워커 배포 시 워커당 1회 트리거(과트리거 상한=워커수). REAL 다수 유저 대비 임계값 45초로 rate-limit 방어. background_tasks는 응답 후 실행이라 유저 지연 0. list_users N+1은 병렬 세션이 이미 해결(단일 서브쿼리). SIMULATED realized_pnl 전량 스캔 SQL SUM화는 별도 후속(이번 범위 제외)
- 인수인계: 구현·검증 완료 → [R] 승인대기. 사용자 "승인" 시 [x] 승격. Git 동결 규칙상 커밋은 사용자 지시 시, 커밋 시 별도 브랜치 분리 권장(admin/router.py는 병렬 세션 변경과 함께 있으므로 커밋 범위 조율 필요)

## 실시간 데이터 전송 SSE 전환 — Phase 1~4 (전송 계층 + 채널 발행 + 폴링 대체 + 신선도 훅)
- [R] 사용자 지시: 대시보드·스캐너·사용자관리 등 전 화면이 SWR 15초 폴링으로 동일 데이터를 사용자수×중복 조회 중. 단방향 서버→클라 전송이라 SSE가 정석. 전 Phase 완료: (1) 단일 멀티플렉스 스트림 GET /events + Redis pub/sub 3채널 + fetch 기반 클라이언트(Bearer 헤더, 토큰 URL 미노출) + 프로바이더. (2) 공용 채널 발행 — 스캐너/시장/스윙/에프터장 갱신 완료 시 invalidate 브로드캐스트. (3) 유저 채널 발행 — record_equity_snapshot(잔고·보유·관리자, force=True면 거래로그도)·봇 토글·관심종목 변경 시 invalidate. (4) useFreshness 훅(단일 공유 클럭·clock-skew 보정·3-상태 fresh/stale/disconnected) + 연결상태 컨텍스트. 각 화면 refreshInterval을 pollInterval()로 감싸 SSE on일 때만 폴링 중단.
- 실측 검증: python scripts/verify_harness.py 전체 통과(SUCCESS). pytest 252 passed(신규 test_sse_infra.py 7케이스 포함), 프론트 tsc --noEmit 클린, eslint 클린, Playwright E2E 8/8. NEXT_PUBLIC_SSE_ENABLED 기본 off → 폴링 경로 그대로라 기존 화면·E2E 무영향 확인.
- 활성화: NEXT_PUBLIC_SSE_ENABLED=true 한 줄이면 전 화면이 폴링→push로 전환(Redis 가동 전제). 롤백은 플래그 제거만.
- 후속 반영 완료: AccountBalance·UserManagement 낡음 배지를 useFreshness(공유 클럭·skew 보정)로 교체 완료. NavBar도 pollInterval 적용(키 '/bot/status' 정상, 오타 아니었음). 전 화면 refreshInterval→pollInterval 일관 적용.
- 의도적 보류(별도 PR): view-trigger 제거는 이번 머지에서 제외. SSE off 상태에서 지우면 보는 유저 신선도가 1분으로 후퇴하고, 깨끗한 제거는 "SSE 구독=보는 중 → 구독 중 주기 갱신"(subscription-driven refresh) 구현 + 플래그 on이 선행돼야 함(브로커 부하 프로파일 변경이라 별도 리뷰 필요). 회귀를 main에 올리지 않기 위해 전용 PR로 분리.

### 변경 영향 기록
- 변경 분류: 아키텍처 기반 구축 - 실시간 데이터 전송을 폴링에서 SSE로 전환하기 위한 Phase 1 전송 계층 신설(채널 부착·폴링 제거는 후속 Phase)
- 생산자: backend/app/core/sse.py(Redis pub/sub publish·채널 상수·async 클라이언트), backend/app/sse/router.py(GET /events 멀티플렉스 스트림·Bearer 단명세션 인증·keep-alive·connected 이벤트), backend/app/main.py(라우터 배선·async redis 종료), frontend/lib/sse.ts(fetch 기반 SSE 클라이언트·Authorization 헤더·지터 백오프 재연결·401 refresh), frontend/providers/EventStreamProvider.tsx(수신→SWR mutate 디스패치·플래그 게이트), frontend/app/layout.tsx(프로바이더 마운트)
- 소비자: Phase 1은 없음 - 인프라만. 프로바이더가 플래그 off 기본이라 기존 화면 무영향. connected/heartbeat로 파이프 검증만
- 데이터 경계: Redis pub/sub 채널 sse:public / sse:user:{id} / sse:admin. DB 스키마 무변경. 기존 주문 락 Redis 재사용(별도 async 커넥션 풀). 스트리밍 응답은 DB 세션 미보유(단명 세션 인증)
- API 계약: 신규 GET /api/v1/events (text/event-stream). 기존 REST 엔드포인트 계약 전부 불변
- 문서 반영: docs/API_STANDARD.md에 SSE 스트림 규약 추가는 후속 Phase. Phase 1은 본 현황판 기록
- 회귀 테스트: 프론트 플래그 off 기본 → 기존 Playwright E2E 8/8 무영향. 신규 test_sse_infra.py 7케이스. verify_harness 전체 통과(pytest 252 passed, tsc/lint 클린, E2E 8/8, SUCCESS)
- 미해결 위험: native EventSource는 Bearer 헤더 불가 → fetch 기반 클라이언트로 우회(토큰 URL 미노출). 멀티워커는 Redis publish로 워커 간 전달 보장. 스트리밍이 커넥션 풀 고갈시키지 않도록 get_current_user 대신 단명 세션 인증. publish는 전부 best-effort(Redis 장애 시 예외 삼킴, 폴백 폴링/다음 이벤트로 복구). invalidate 방식이라 per-user-파생(scanner_latest·balance)은 변경 시 재조회(폴링 대비 조회수 급감이지 0은 아님) — 순수 공용은 후속에 full-payload push로 최적화 여지
- 인수인계: Phase 1~4 구현·검증 완료 → [R] 승인대기. 활성화는 NEXT_PUBLIC_SSE_ENABLED=true(Redis 전제). view-trigger(이전 [R] 항목)는 SSE on 시 스냅샷 write publish가 대체하므로 활성화 후 제거 대상. 두 배지 useFreshness 교체·NavBar bot_status 키 오타는 저위험 후속. captured_at 필드·정렬·낡음 배지는 전 Phase 유지
