# 2026-08-13 Task Board

## 신규 작업
- [/] 백테스트 엔진에 자율 슬롯(지수 레버리지 레짐) 시뮬레이션 경로 배선: 백테스트 엔진(BacktestSimulator)이 종목 채점 파이프라인(calculate_score)만 시뮬할 수 있어 자율 슬롯 전략(leveraged_regime/benchmark_qqq_hold/leveraged_regime_3x)은 항상 0점으로 아무 매매도 못 하는 공백을 해소한다. LeveragedRegime의 상태기계(compute_target_state, QQQ 일봉 200SMA + 3일 확정 휩쏘가드)를 SSOT로 재사용해, 신호 지수(QQQ) 완결 일봉으로 목표 상태(IN/OUT)를 판정하고 자산 티커(QLD/TQQQ/QQQ)를 룩어헤드 없이(신호 익일 체결) 전액 매수/현금 대피하는 별도 경로를 엔진에 추가한다. 스캐너 채점 경로는 무수정으로 격리 보존.

### 변경 영향 기록
- 변경 분류: 백테스트 엔진 기능 확장 (자율 슬롯 시뮬 분기 신규, 스캐너 경로 무변경)
- 생산자: backend/app/strategies/leveraged_regime.py (compute_state_series 신규 — 상태 시계열 SSOT, compute_target_state는 마지막값 위임), backend/app/backtests/backtest_engine.py (BacktestSimulator._run_autonomous 신규 및 run() 자율 분기, 일봉 강제, 자율 워밍업 확대), scripts/run_leverage_backtest.py (엔진 자율 경로 호출 러너 — leveraged_regime/benchmark_qqq_hold/3x 나란히), backend/tests/test_backtest_autonomous.py (자율 경로 단위테스트)
- 소비자: backend/app/admin/backtest_runner.py(자율 전략 백테스트 호출 시), 사용자(자율 레버리지 전략의 장기 일봉 백테스트 성적표 — QQQ 초과 여부·MDD·파라미터 강건성)
- 데이터 경계: 변경 없음 (yfinance 일봉 읽기 전용, DB·테이블·스키마·마이그레이션 무관)
- API 계약: 변경 없음 (get_summary_report 반환 스키마 보존 — 자율 경로도 동일 broker/equity_curve/trade_logs 사용)
- 문서 반영: docs/tasks/2026-08-13.md 갱신 (계약 문서 SCHEMA/API_STANDARD 갱신 대상 없음)
- 회귀 테스트: test_leveraged_regime.py 15개 + test_backtest_autonomous.py 4개 그린, py_compile OK, 프론트 TS/ESLint/E2E OK, 백엔드 pytest 389 passed/1 failed. 단 실패 1건은 test_i18n_messages::test_no_unallowlisted_hardcoded_korean 으로 이 작업과 무관한 기존 미커밋 프론트 파일(AIMarketRegimeWidget.tsx/StrategyCatalog.tsx/StrategySelectModal.tsx)의 하드코딩 한글 위반이며 본 백테스트 변경(백엔드+scripts)과 무관 — verify_harness 전체는 이 이유로 미통과(미검증)
- 미해결 위험: (착수중) 자율 상태기계는 일봉 정의라 인터벌을 1d로 강제해야 함(1h 등에서 의미 왜곡). SMA200 워밍업으로 시작일 이전 200+ 거래일 필요. yfinance 조정종가·TQQQ 생존편향(2010~)으로 3x 수치 과대. 전량 몰빵 자산 스위치라 스캐너용 손절/트레일링/피라미딩 미적용 경로 분기 필요.
- 인수인계: (구현 완료·미실행) SSOT 리팩터 + 엔진 _run_autonomous 분기 + 실행 러너 + 단위테스트 4개 완료. 검증: test_backtest_autonomous.py 4개 + test_leveraged_regime.py 15개 그린, py_compile OK, 기존 호출자(엔드포인트 strategy_type 미수신/토너먼트 1h·스캐너)는 자율 가드 무영향 확인. 미실행: 실제 장기 백테스트(yfinance 다운로드+시뮬)는 루프 점거라 사용자 지시 대기 — 실행 명령: `cd backend && venv/Scripts/python.exe ../scripts/run_leverage_backtest.py --start-date 2006-06-21`. 다음: (1) 사용자가 러너 실행해 QQQ 초과/ MDD 절단 판정 산출, (2) verify_harness 통과 후 [R] 승격. 실제 실행 완료(사용자 승인): scripts/run_leverage_backtest.py --start-date 2006-06-21 --include-3x 결과 — 레짐 QLD 2x 총수익 +4185%/CAGR +20.5%/MDD -54.7%/Calmar 0.37/Sharpe 0.74/24거래, QQQ 단순보유 +2133%/CAGR +16.7%/MDD -53.3%/Calmar 0.31/Sharpe 0.81, TQQQ 3x +6012%/CAGR +28.3%/MDD -71.7%. 판정: 총수익·Calmar는 레짐이 QQQ 초과하나 Sharpe는 QQQ가 우위 = 알파 아닌 잘 관리된 레버리지 베타(엔진 내부 재현). 검증 상태: 백테스트 슬라이스는 격리 검증 그린이나 verify_harness 전체는 무관한 미커밋 프론트 i18n 위반으로 미통과 — 사용자 결정으로 이 작업은 격리 검증 상태 유지([/]), 프론트 i18n 처분은 별도 작업으로 분리. 배경: [[obs-accounts-live-leverage-ab]] 라이브 A/B 표본 부족 → 엔진 배선으로 장기 판정 대체.

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
