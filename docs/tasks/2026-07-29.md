# 2026-07-29 Task Board

## 신규 작업 (백엔드 텔레그램 i18n 정리)

> 배경: 07-25 인수인계에서 "프론트엔드 하드코딩 한글 치환 전량 완료"로 보고됐으나, 백엔드 텔레그램 발신 메시지는 대부분 하드코딩으로 남아 UserSettings.language를 무시함을 확인. 실제 i18n 배선은 order_reconciler.py·order_discovery.py 2개 파일뿐. 나머지(매수/매도 체결 알림·봇 명령 응답·연동·일일 리포트·시스템 경고)는 하드코딩(매도=한글, 매수=영어 혼용). 전체 계획은 PR-A(locale 스키마)→PR-B(scheduler 거래 알림)→PR-C(telegram.py 봇/연동/리포트) 3단계.

- [R] PR-A · 텔레그램 i18n locale 스키마 확충 (Claude 세션, 사용자 지시 "선등록하고 PR-A부터 시작"): PR-B/PR-C 배선에 앞서 locales/ko.json·en.json의 telegram 네임스페이스에 신규 키 전량을 선반영하고, 기존 깨진 telegram.regime_exit 키(실현수익 금액 플레이스홀더 누락)를 수정. 코드 발신부 배선은 이 PR 범위 밖(후속 PR-B/PR-C)이라 런타임 동작 변경 없음. 신규 키군: telegram.sell_filled·buy_filled·buy_skipped·network_fault·order_pending_wait, 봇 명령 응답(help·already_running·cannot_start_unresolved·loop_started·already_stopped·loop_stopped·unknown_command·command_error), 연동(link_success·link_user_not_found·not_linked_guide), 일일 리포트(daily_report·daily_report_manual·no_recent_sells), /status 조각(status.*), 그리고 사유·라벨 키군(sell_reason.*·side.*·order_status.*). ko/en 키 집합·플레이스홀더 완전 일치는 test_i18n_messages.py 파리티 테스트로 강제.

### 변경 영향 기록
- 변경 분류: 다국어 딕셔너리 확충 (locale JSON 2개에 telegram 키 추가·기존 키 1개 수정, 코드·계약·스키마 무변경)
- 생산자: locales/ko.json·locales/en.json의 telegram 네임스페이스 — 신규 키 전량을 양쪽 언어 동시 등록하고, 미사용·불완전한 telegram.regime_exit 키(pnl_sign 뒤 실현수익 금액 플레이스홀더 누락)를 realized_pnl 인자를 포함하도록 수정
- 소비자: 후속 PR-B(backend/app/bot/scheduler.py)·PR-C(backend/app/core/telegram.py)의 발신부가 I18n.get_msg(resolve_user_language(...), "telegram.*")로 이 키들을 조회. 이 PR 자체는 소비 코드를 배선하지 않으므로 현재 런타임 발신 경로는 무변경(하드코딩 유지)
- 데이터 경계: 변경 없음 (번역 JSON 키 추가·수정뿐, DB·테이블·컬럼·캐시 무관)
- API 계약: 변경 없음 (엔드포인트·응답 스키마 무변동)
- 문서 반영: 영향 없음 - UI 노출 문구를 딕셔너리에 추가하는 치환 준비 작업이라 docs/SCHEMA.md·docs/API_STANDARD.md에 갱신할 계약이 없음
- 회귀 테스트: test_i18n_messages.py 18건 통과(ko/en 키 집합 515=515 동일·플레이스홀더 일치·빈 네임스페이스 없음). 별도 검증 스크립트로 신규 telegram 키 전량을 더미 인자로 str.format 스모크 실행해 포맷 스펙 오류 0건 확인. verify_harness.py는 8단계 중 1~7단계 통과(계약·컴파일·백엔드 pytest 384건·tsc·lint 그린), 8단계 Playwright E2E만 하네스 내부 180초 제한 타임아웃 — locale JSON 키 추가는 tsc·lint 통과·프론트 next-intl 정적 import라 빌드 무영향이므로 환경성 타임아웃으로 판단
- 미해결 위험: 이 PR은 키만 추가하고 배선하지 않으므로, 배선 전까지 신규 키는 소비처 없는 상태로 존재(테스트가 참조하는 키 제외). 실제 사용자 노출 다국어화는 PR-B/PR-C 병합 후에야 발생. 사유·라벨 키(sell_reason.*·side.*)는 PR-B에서 scheduler의 한글 사유 조립부와 정확히 대응돼야 반쪽 다국어를 피함 — 키 명명을 PR-B 배선 계획과 일치시켜 등록. verify_harness 8단계(E2E)는 미완(180초 타임아웃)이라 엄밀히는 하네스 전체 그린이 아님 — 다만 원인은 E2E 기동 지연(환경)이고 본 변경은 백엔드 locale JSON 한정이라 tsc·lint·pytest로 회귀 부재 확인. 커밋 시 pre-commit 훅이 동일 하네스를 재실행하므로 그 시점에 E2E 재검증됨
- 인수인계: PR-A 완료 시 다음 시작 지점은 PR-B(scheduler.py 6개 발신부 + sell_reason/breach_reason 사유 키化 + L1136·L1219 단일 헬퍼 통합). 미연동 메시지 언어(link_user_not_found·not_linked_guide)는 user 매핑 전이라 language 조회 불가 — 기본 ko 또는 텔레그램 update의 from.language_code 사용 여부를 PR-C에서 결정

- [R] PR-B · scheduler.py 거래 알림 i18n 배선 (Claude 세션, 사용자 지시 "PR-B 착수"): PR-A에서 준비한 telegram.* 키로 scheduler의 하드코딩 발신부 7곳을 배선. 대상: 자동매도 체결(2곳, 중복 → 단일 헬퍼 _send_sell_fill_message로 통합)·레짐 이탈 자동매도(깨진 키 수정본 telegram.regime_exit 배선)·자동매수 체결·자동매수 보류(하드코딩 영어 → 다국어)·미체결 주문 대기(side_kr/status_kr 라벨도 telegram.side.*·order_status.*로 이관)·시스템 접속 장애. 핵심: 매도 사유(sell_reason)를 구조화(SellReason(key,args,confirmed))해 DB/로그에는 기존과 동일한 한국어 canonical 문자열을 유지(데이터/마이그레이션 무영향)하고 텔레그램에만 사용자 언어로 렌더 — 반쪽 다국어 방지. 언어 결정은 발신 직전 _resolve_lang(user_id)=resolve_user_language로 독립 세션 조회. buy_skipped의 reason_title/reason_desc 하드코딩 영어 2종도 telegram.buy_skip_reason.* 키로 신규 추가·이관.

### 변경 영향 기록 (PR-B - scheduler 거래 알림)
- 변경 분류: 백엔드 다국어 배선 (텔레그램 발신 문구를 I18n.get_msg로 치환, 매도 사유 구조화 리팩터, 거래 로직·주문 실행 경로 무변경)
- 생산자: backend/app/bot/scheduler.py — 모듈 헬퍼 신설(_resolve_lang·_send_sell_fill_message·SellReason 데이터클래스·render_sell_reason), 발신부 7곳을 I18n.get_msg(telegram.*)로 치환. locales/ko.json·en.json에 telegram.buy_skip_reason.budget_exceeded·insufficient_cash(title·desc) 4키를 양쪽 언어 추가
- 소비자: 텔레그램으로 매수/매도 체결·보류·미체결 대기·접속 장애 알림을 받는 사용자 — UserSettings.language에 따라 ko/en으로 발송. DB에 저장되는 order_intent.sell_reason과 ActionLog/logger 메시지는 기존과 동일한 한국어 canonical 유지(감사·재조정 로직 무영향)
- 데이터 경계: 변경 없음 (order_intent.sell_reason 저장값은 render_sell_reason(obj,"ko")로 기존 문자열과 동일하게 유지 — 컬럼·스키마·마이그레이션 무변동, 텔레그램 렌더만 사용자 언어로 분기)
- API 계약: 변경 없음 (엔드포인트·응답 스키마 무변동, 텔레그램 아웃바운드 메시지 문구만 변경)
- 문서 반영: 영향 없음 - 발신 문구 다국어화라 docs/SCHEMA.md·docs/API_STANDARD.md에 갱신할 계약이 없음
- 회귀 테스트: JSON 파리티 519=519 키 동일·플레이스홀더 일치, scheduler py_compile OK, 백엔드 pytest 384건 통과(베이스라인 동일=회귀 0), test_i18n_messages.py 18건, 프론트 tsc 0건·lint 0건. 추가 런타임 스모크: SellReason 5종×ko/en render + telegram.sell_filled·buy_filled·buy_skipped·regime_exit·order_pending_wait·network_fault를 실인자로 get_msg 렌더해 날 키/미치환 중괄호 0건 확인. verify_harness 8단계 중 E2E(8단계)는 PR-A와 동일한 환경성 180초 타임아웃이라 미실행, 그 외 단계는 위 개별 검증으로 커버
- 미해결 위험: _resolve_lang이 발신마다 독립 DB 세션을 열어 조회(빈도 낮은 알림 경로라 부하 미미, 실패 시 ko 폴백). sell_reason 구조화로 Part A→Part B 튜플에 sell_reason_obj 1필드 추가 — _execute_single_sell(*args) 위치 언패킹 순서를 시그니처와 정확히 일치시켜야 함(불일치 시 즉시 런타임 오류로 검출). PR-C(telegram.py 봇/연동/리포트)는 후속
- 인수인계: PR-B 완료 시 다음 시작 지점은 PR-C(telegram.py의 봇 명령 응답·연동·일일 리포트·/status). 미연동 메시지 언어(link_user_not_found·not_linked_guide) 결정은 PR-C에서

- [R] PR-C · telegram.py 봇/연동/리포트 i18n 배선 (Claude 세션, 사용자 지시 "B로 PR-C 착수"): PR-A 키로 telegram.py 하드코딩 발신부 전량 배선. 대상: 봇 명령 응답(/start help·/run·/stop·/status 상태 본문)·연동 성공/실패·미연동 안내·일일 리포트 2종(전체/개별)·매도 없음·오류. 언어 결정 이원화 — 연동/명령 처리는 resolve_user_language(db,user_id), 미연동(user 매핑 전) 메시지(link_user_not_found·not_linked_guide)는 옵션 B로 텔레그램 update의 from.language_code를 _lang_from_telegram_code로 매핑(en*→en, 그 외 ko). 이를 위해 롱폴링 루프에서 language_code를 추출해 _process_global_message로 전달. /status 본문은 telegram.status.* 조각(header·running/stopped·fallback 2종·no_holdings·holding_line·broker_na_simulated/none)으로 조립. 일일 리포트 전체 발송은 루프 내 per-user 언어 해석.

### 변경 영향 기록 (PR-C - telegram.py 봇/연동/리포트)
- 변경 분류: 백엔드 다국어 배선 (텔레그램 봇 응답·연동·리포트 문구를 I18n.get_msg로 치환, 로직 무변경)
- 생산자: backend/app/core/telegram.py — _lang_from_telegram_code 헬퍼 신설, _process_global_message에 tg_lang_code 인자 추가 및 롱폴링 루프가 from.language_code 전달, _process_command·send_daily_report_to_all_users_sync·send_daily_report_to_user_sync의 하드코딩 문구를 telegram.* 키로 치환. backend/tests/test_concurrency_infra_validation.py — 폴링 루프 시그니처 변경에 맞춰 executor.submit 단언 갱신
- 소비자: 텔레그램 봇과 상호작용하는 사용자 — 연동 완료 사용자는 UserSettings.language, 미연동 사용자는 텔레그램 클라이언트 언어(from.language_code) 기준으로 ko/en 응답 수신. 일일 리포트는 수신자별 언어로 발송
- 데이터 경계: 변경 없음 (DB 조회만, 쓰기·스키마·마이그레이션 무관. language_code는 텔레그램 update에서 읽기만)
- API 계약: 변경 없음 (텔레그램 아웃바운드 문구만 변경)
- 문서 반영: 영향 없음 - 발신 문구 다국어화라 계약 문서 갱신 대상 없음
- 회귀 테스트: telegram.py py_compile OK, 백엔드 pytest 384건 통과. 폴링 루프 시그니처 변경(from.language_code 전달)에 맞춰 test_concurrency_infra_validation.py의 executor.submit 단언을 갱신(mock update에 from.language_code=en-GB 추가 → _process_global_message에 "en-GB" 전달 검증). 런타임 렌더 스모크 23키(help·run/stop·status.header·holding_line·fallback 2종·broker_*·daily_report·daily_report_manual·no_recent_sells·link_success·link_user_not_found·not_linked_guide·command_error) ko/en 날 키/미치환 중괄호 0건, _lang_from_telegram_code(en-US→en, ko-KR/fr/None→ko) 검증. test_i18n_messages.py 18건. verify_harness E2E(8단계)는 PR-A/B와 동일한 환경성 타임아웃이라 미실행, 그 외 단계는 개별 검증으로 커버
- 미해결 위험: 미연동 메시지 언어는 텔레그램 클라이언트 언어에만 의존(앱 설정과 다를 수 있으나 매핑 전이라 불가피, 연동 후에는 UserSettings.language가 SSOT). from.language_code 부재 시 ko 폴백. /status의 total_asset_usd=total_asset/fx_rate는 기존과 동일하게 fx_rate 0 시 ZeroDivision 위험이 남으나 본 PR이 신규 도입한 위험 아님(기존 동작 보존)
- 인수인계: PR-C 완료로 백엔드 텔레그램 발신 전량 다국어화 종결(order_reconciler·order_discovery는 기존 배선). PR-A/B/C 3건 [R] 상태로 커밋·PR 대기. 사용자 원지적 후속: 프론트 UI/토스트 잔여 누락 감사(미착수)

- [R] PR-D · 프론트 UserManagement + BotSignals i18n (Claude 세션, 사용자 지시 "PR-D부터 진행"): 프론트 i18n 잔여 감사에서 High로 분류된 2건. components/admin/UserManagement.tsx는 useTranslations('admin.users')를 선언만 하고 본문 대부분(테이블 헤더 8개·사용자 상세 프로필 패널 4섹션·토스트 4종·영구삭제 확인 다이얼로그·페이지네이션·버튼 title)이 하드코딩 한글 → admin.users 네임스페이스 확장으로 전량 치환. components/BotSignals.tsx는 useTranslations 미사용 → bot_signals 네임스페이스 신설 후 레이더 제목·빈 상태 2문구 치환(map 콜백 인자 t가 번역 훅과 충돌하므로 ticker로 개명). 스테이지 8이 관리자 6종을 치환하면서 UserManagement를 목록에서 누락한 것이 근본 원인이며, 기존 회귀 테스트는 소비된 키만 검증해 하드코딩 컴포넌트를 못 잡았음.

### 변경 영향 기록 (PR-D - UserManagement + BotSignals)
- 변경 분류: 프론트엔드 다국어 치환 (UI 문구만 이동, 로직·API·스키마 무변경)
- 생산자: locales/ko.json·en.json의 admin.users 네임스페이스 확장(테이블 헤더·상세 패널·토스트·다이얼로그·페이지네이션·버튼 키 신규) + bot_signals 네임스페이스 신설(radar_title·radar_empty). 양쪽 언어 동시 등록으로 파리티 유지
- 소비자: frontend/components/admin/UserManagement.tsx(하드코딩 한글 전량 → t('admin.users.*'), 스냅샷 신선도 라벨·페이지네이션 요약은 플레이스홀더 포함), frontend/components/BotSignals.tsx(useTranslations('bot_signals') 추가, radar map 인자 t→ticker 개명)
- 데이터 경계: 변경 없음 (번역 JSON 키 추가뿐, DB·API·상태 무관)
- API 계약: 변경 없음
- 문서 반영: 영향 없음 - UI 문구 치환이라 계약 문서 갱신 대상 없음
- 회귀 테스트: test_i18n_messages.py 18건 통과(TestFrontendConsumedKeysExist가 신규 admin.users.*·bot_signals.* 소비 키의 ko.json 실재를 자동 대조 + ko/en 파리티 567=567·플레이스홀더 일치). npx tsc --noEmit 0건, npm run lint 0건. 편집 파일 잔여 사용자노출 한글 0(주석 제외). 브라우저: :3000 앱 정상 렌더(로그인 페이지 로드·런타임 오류 없음) 확인. 관리자 UserManagement 실물 캡처는 프리뷰 페인 비합성(스크린샷 불가)+관리자 자격 부재로 미수행 — 정적 검증으로 키 해석 정확성 커버
- 미해결 위험: 기존 회귀 테스트는 useTranslations를 쓰는 컴포넌트의 소비 키만 검증하므로, t()를 아예 안 쓰는 하드코딩 컴포넌트는 여전히 못 잡음(UserManagement가 그 사각지대였음) — 감사에서 제안한 'JSX 한글 리터럴 스캔' 방어 테스트는 별도 후속(PR-F 이후). 날짜 포맷 toLocaleString('ko-KR')은 텍스트가 아닌 로케일 포맷 인자라 이번 범위서 제외(영어에서도 숫자 위주로 판독 가능)
- 인수인계: PR-D 후 남은 프론트 잔여는 PR-E(useWatchlistActions·error.tsx 토스트/에러바운더리)·PR-F(timezoneStore·useFreshness·utils·api 공유 유틸). 백엔드 PR-A/B/C와 프론트 PR-D는 커밋·PR 대기

- [R] PR-E · 프론트 토스트/에러바운더리 i18n (Claude 세션, 사용자 지시 "PR-E로 진행"): 감사 Medium 2건. hooks/useWatchlistActions.ts(관심종목 추가/삭제 토스트 4종, .ts 훅이라 기존 리포트 정규식에도 안 잡혔던 사각지대) → watchlist 네임스페이스 신설, 커스텀 훅 내부에서 useTranslations 사용. app/error.tsx(세그먼트 에러 바운더리 5문구, global-error와 달리 NextIntlClientProvider 안이라 useTranslations 사용 가능) → error_boundary 네임스페이스 신설.

### 변경 영향 기록 (PR-E - 토스트/에러바운더리)
- 변경 분류: 프론트엔드 다국어 치환 (UI 문구만 이동, 로직·API·스키마 무변경)
- 생산자: locales/ko.json·en.json에 watchlist 네임스페이스(toast_added·toast_add_failed·toast_removed·toast_delete_failed) + error_boundary 네임스페이스(title·body·error_code·retry·dashboard) 신설, 양쪽 언어 동시
- 소비자: frontend/hooks/useWatchlistActions.ts(useTranslations('watchlist') 추가, 토스트 4종 치환. 추가 성공은 options.successMessage 우선, 없으면 t 폴백), frontend/app/error.tsx(useTranslations('error_boundary') 추가, 5문구 치환)
- 데이터 경계: 변경 없음 (번역 JSON 키 추가뿐)
- API 계약: 변경 없음
- 문서 반영: 영향 없음 - UI 문구 치환
- 회귀 테스트: test_i18n_messages.py 18건 통과(TestFrontendConsumedKeysExist가 신규 watchlist.*·error_boundary.* 소비 키의 ko.json 실재 대조 + ko/en 파리티 576=576·플레이스홀더 일치). npx tsc --noEmit 0건, npm run lint 0건(useCallback deps에 t 추가로 exhaustive-deps 무경고). 편집 파일 사용자노출 한글 0(주석 제외). 브라우저 실물: error.tsx는 런타임 오류 시에만·watchlist 토스트는 로그인 후 액션 시에만 노출돼 캡처 미수행, 정적 검증으로 커버
- 미해결 위험: error.tsx는 세그먼트(페이지/중첩 레이아웃) 오류만 잡고 Provider는 유지되므로 useTranslations 안전(루트 레이아웃 오류는 global-error.tsx가 인라인 딕셔너리로 별도 담당 — 이미 처리됨). useWatchlistActions는 클라이언트 훅이라 useTranslations 사용 가능(호출부 scanner/page·ManualWatchList 모두 클라이언트)
- 인수인계: PR-E 후 남은 프론트 잔여는 PR-F(timezoneStore·useFreshness·utils·api 공유 유틸, 컴포넌트 밖이라 locale 주입/키 반환 방식 설계 필요) + 재발 방어 테스트

- [R] PR-F · 프론트 공유 유틸/스토어 i18n (Claude 세션, 사용자 지시 "PR-F로 진행"): 감사 Medium/Low의 공유 유틸 4파일. 훅인 hooks/useFreshness.ts(신선도 라벨 분/초)와 store/timezoneStore.ts(useTimezone 훅의 타임존 라벨)는 useTranslations로 정공법 치환(freshness·timezone 네임스페이스 신설). 순수 함수 lib/utils.ts formatKrw는 훅 불가라 원 접미사→₩ 접두사(로케일 중립)로 한글 제거. lib/utils.ts getErrorMessage 폴백과 lib/api.ts 인증응답 에러는 순수 모듈 함수라 호출부(~15곳) 리팩터 없이는 로케일 주입 불가하고 opaque-error/기술적 malformed-response 엣지케이스라 PR-F 범위서 보류(근거는 미해결 위험에 명시).

### 변경 영향 기록 (PR-F - 공유 유틸/스토어)
- 변경 분류: 프론트엔드 다국어 치환 (훅 기반 치환 + 순수 함수 로케일 중립화, 로직·API·스키마 무변경)
- 생산자: locales/ko.json·en.json에 freshness(min·sec)·timezone(local·local_with_abbr·seoul·new_york·utc) 네임스페이스 신설. frontend/hooks/useFreshness.ts(useTranslations('freshness')로 label 산출, 모듈 함수 ageLabel 제거), frontend/store/timezoneStore.ts(useTimezone 훅에서 useTranslations('timezone')로 timezoneOptions·resolvedTimezone label을 id 기준 재산출 — 영속된 라벨 대신 항상 재번역, 정적 TIMEZONE_OPTIONS label은 영어 폴백으로 변경), frontend/lib/utils.ts(formatKrw 원→₩ 접두사)
- 소비자: 신선도 배지(AccountBalance는 balance.stale_warning 템플릿에 label 주입)·타임존 셀렉터(NavBar 등 7개 컴포넌트가 useTimezone 소비)·원화 표기(PortfolioView 5곳 formatKrw). 표시 언어가 UserSettings 로케일(next-intl) 따라 ko/en 전환. 영속 localStorage(stockauto-timezone-storage)의 기존 라벨은 useTimezone이 항상 재번역하므로 무시됨(마이그레이션 불필요)
- 데이터 경계: 변경 없음 (번역 JSON 키 추가 + 순수 함수 반환 형식 조정, DB·API 무관. persist 스키마 미변경)
- API 계약: 변경 없음
- 문서 반영: 영향 없음 - UI 문구/포맷 치환
- 회귀 테스트: test_i18n_messages.py 18건 통과(파리티 583=583·플레이스홀더 일치, TestFrontendConsumedKeysExist가 freshness.*·timezone.* 소비 키 대조). npx tsc --noEmit 0건, npm run lint 0건. 편집 3파일(useFreshness·timezoneStore·utils.formatKrw) 사용자노출 한글 0. 잔존 한글은 utils.ts:17 getErrorMessage 폴백뿐(의도적 보류, 위 미해결 위험 참조)
- 미해결 위험: lib/utils.ts getErrorMessage의 폴백 "알 수 없는 오류가 발생했습니다."와 lib/api.ts의 "인증 갱신 응답 형식이 올바르지 않습니다."는 순수 모듈 함수라 useTranslations 불가 → 이번 범위서 보류. 전자는 error 객체에 message가 전혀 없는 opaque 케이스에서만 노출(대부분은 서버/Error.message가 그대로 표시), 후자는 인증 응답이 깨진 기술적 엣지케이스. 완전 치환하려면 호출부(~15곳 toast)가 t() 폴백을 주입하는 별도 패스 필요. formatKrw ₩ 접두사는 한국 사용자 표기가 "1,234원"→"₩1,234"로 바뀌나 ₩는 표준 기호라 수용 가능
- 인수인계: PR-F로 프론트 사용자노출 하드코딩 한글의 훅 치환 가능분은 종결. 잔여는 getErrorMessage/api.ts 순수함수 2건(엣지)과 재발 방어 테스트(t 미사용 컴포넌트의 JSX 한글 리터럴 스캔). 백엔드 A/B/C + 프론트 D/E/F 총 6 PR 커밋·PR 대기

- [R] PR-G · 하드코딩 한글 재발 방어 테스트 (Claude 세션, 사용자 지시 "재발 방어 테스트 추가"): 기존 TestFrontendConsumedKeysExist는 useTranslations로 '소비된 키'만 검증해, t()를 아예 안 쓰는 하드코딩 컴포넌트(UserManagement가 스테이지 8을 빠져나간 사각지대)를 못 잡았음. 이를 막기 위해 frontend .ts/.tsx의 주석 제거 후 문자열·JSX 텍스트에 남은 한글 리터럴을 스캔해 실패시키는 테스트를 test_i18n_messages.py에 신설. 의도적으로 한글을 남긴 곳(global-error 인라인 딕셔너리·layout 메타데이터·NavBar 이중언어 토글/언어 자기명명·utils/api 순수함수 보류분)은 파일/부분문자열 allowlist로 명시 관리해, 신규 하드코딩만 실패로 검출.

### 변경 영향 기록 (PR-G - 재발 방어 테스트)
- 변경 분류: 테스트 인프라 추가 (프로덕션 코드·계약·스키마 무변경)
- 생산자: backend/tests/test_i18n_messages.py에 TestNoHardcodedKoreanInFrontend 클래스 신설 — frontend .ts/.tsx를 순회하며 블록/라인 주석 제거 후 한글([가-힣]) 리터럴을 검출, ALLOWLIST(의도적 잔존분) 외 발견 시 실패. 신설 즉시 실제 누락 1건 검출: components/ManualWatchList.tsx의 수동추가 도움말(* 첫 단어는 티커로…)이 하드코딩이었음 — 감사 초기 스캐너가 '*'로 시작하는 JSX 텍스트 줄을 주석으로 오인해 놓친 케이스. scanner.manual_add_hint 키 신설(임베드 마크업이라 t.rich <example> 태그) 후 치환
- 소비자: CI harness(pytest)·pre-commit 훅. 앞으로 t() 없이 한글 UI 문구를 넣은 컴포넌트가 병합되면 이 테스트가 즉시 실패해 조기 차단
- 데이터 경계: 변경 없음 (테스트 전용, 파일 정적 스캔만 수행)
- API 계약: 변경 없음
- 문서 반영: 영향 없음 - 테스트 추가
- 회귀 테스트: test_i18n_messages.py 19건 통과(신규 TestNoHardcodedKoreanInFrontend 포함, 파리티 585=585). 신규 테스트가 ManualWatchList 누락을 실검출→치환 후 그린(네거티브 컨트롤: 실제 하드코딩에 실패함을 실증). npx tsc --noEmit 0건, npm run lint 0건(t.rich 렌더 포함)
- 미해결 위험: 주석 판별은 정규식 휴리스틱((?<!:)//.*$ + 블록주석 제거)이라 문자열 내부 //를 포함한 극단 케이스에서 오탐 가능(현 트리엔 해당 없음). allowlist는 의도적 잔존분을 명시 관리하므로, getErrorMessage/api.ts 등을 향후 실제 치환하면 allowlist에서 제거해야 함(방치 시 새 하드코딩을 가릴 수 있음)
- 인수인계: 이로써 감사에서 드러난 근본 사각지대(소비 키만 검증)를 정적 스캔으로 보완. i18n 전체 작업(백엔드 A/B/C + 프론트 D/E/F + 방어 G) 종료 → PR #88로 병합 완료(863e09e)

- [/] PR-H · 하드코딩 placeholder 치환 + 방어 테스트 확장 (Claude 세션, 사용자 지시 "placeholder 등 하드코딩없이 확인"): PR #88 병합 후 재점검에서, 방어 테스트가 한글만 잡아 놓친 하드코딩 영어 placeholder를 발견(label은 t()인데 placeholder만 영어). 대상: login/signup(Username·Password·Confirm Password)·admin settings(Enter APP KEY·APP SECRET·Account No)·ManualWatchList(e.g. AAPL...). 백엔드 텔레그램 발신은 전부 I18n.get_msg 경유로 하드코딩 0 재확인(log·exception은 사용자 요청대로 다국어 대상 제외). 재발 방지로 placeholder="literal" 하드코딩을 검출하는 테스트를 방어 클래스에 추가.

### 변경 영향 기록 (PR-H - placeholder i18n + 방어 확장)
- 변경 분류: 프론트엔드 다국어 치환 + 테스트 강화 (UI 속성값 치환, 로직·API·스키마 무변경)
- 생산자: locales에 settings.placeholder_app_key·placeholder_app_secret·placeholder_account_no, scanner.ticker_input_placeholder 신설(ko/en). test_i18n_messages.py의 TestNoHardcodedKoreanInFrontend에 test_no_hardcoded_placeholder_attributes 추가(placeholder="문자열" 리터럴 검출, 알파벳 없는 숫자 예시는 자동 허용)
- 소비자: app/login/page.tsx·app/signup/page.tsx(placeholder를 기존 label 키 재사용 t()), app/admin/settings/page.tsx(신규 settings 키), components/ManualWatchList.tsx(scanner 신규 키). placeholder="987654321"은 숫자 예시라 언어 중립으로 유지. 부수 발견: E2E 스펙(auth-smoke·scanner-complex)이 getByPlaceholder("Username"/"Password"/"Confirm Password")로 로그인 입력을 찾아, placeholder 로케일화 시 ko 빌드에서 셀렉터가 깨짐 → login/signup 입력에 name 속성(username·password·passwordConfirm) 추가하고 스펙을 input[name=...] 로케일 불변 셀렉터로 전환
- 데이터 경계: 변경 없음
- API 계약: 변경 없음
- 문서 반영: 영향 없음 - UI 속성 치환
- 회귀 테스트: test_i18n_messages.py 20건(파리티 + 신규 placeholder 하드코딩 검사 + 소비 키). placeholder 로케일화가 E2E 셀렉터를 깨뜨린 것을 클린 실행으로 실검출(auth 5건 실패)→name 셀렉터 전환 후 E2E 8/8 그린(59s). npx tsc --noEmit·npm run lint 통과. 커밋 시 pre-commit 훅 전체 하네스 재검증
- 미해결 위험: placeholder 검사는 placeholder="literal"만 잡고 placeholder={t(...)}·placeholder={expr}는 통과. 알파벳 없는 리터럴(숫자 예시)은 허용해 987654321 유지. aria-label/title/alt는 현재 전부 t() 사용 확인됐으나 검사 대상엔 미포함(향후 필요 시 확장)
- 인수인계: 백엔드 텔레그램 + 프론트(라벨·메시지·placeholder) 하드코딩 전량 종결. 잔여는 getErrorMessage/api.ts 순수함수 2건(엣지, ALLOWLIST)뿐

> ⚠️ 위 9개 필드를 모두 채워야 verify_harness의 계약 검사를 통과합니다. (형식 고정: `- 필드명: 내용`, 별표/볼드 금지)
