[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = -q
markers =
    real_rate_limiter: 목 없이 실제 RateLimiter 의존성을 통과시키는 테스트(가짜 Redis만 주입). 미등록 상태로 두면 PytestUnknownMarkWarning만 나고 선택 실행(-m)도 되지 않아 마커가 사실상 무효였다.
